(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: tendermint/types/validator.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Gogoproto_gogo = Gogoproto_gogo
  module Tendermint_crypto_keys = Tendermint_crypto_keys
end

(**/**)

module rec Tendermint : sig
  module rec Types : sig
    (** {%html:
<p>BlockIdFlag indicates which BlockID the signature is for</p>
%}
    *)
    module rec BlockIDFlag : sig
      type t =
        | BLOCK_ID_FLAG_UNKNOWN
            (** {%html:
<p>indicates an error condition</p>
%} *)
        | BLOCK_ID_FLAG_ABSENT
            (** {%html:
<p>the vote was not received</p>
%} *)
        | BLOCK_ID_FLAG_COMMIT
            (** {%html:
<p>voted for the block that received the majority</p>
%}
            *)
        | BLOCK_ID_FLAG_NIL  (** {%html:
<p>voted for nil</p>
%} *)

      val name : unit -> string
      (** Fully qualified protobuf name of this enum *)

      (**/**)

      val to_int : t -> int
      val from_int : int -> t Runtime'.Result.t
      val from_int_exn : int -> t
      val to_string : t -> string
      val from_string_exn : string -> t

      (**/**)
    end

    and ValidatorSet : sig
      type t = {
        validators : Validator.t list;
        proposer : Validator.t option;
        total_voting_power : int64;
      }

      val make :
        ?validators:Validator.t list ->
        ?proposer:Validator.t ->
        ?total_voting_power:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?validators:Validator.t list ->
        ?proposer:Validator.t ->
        ?total_voting_power:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and Validator : sig
      type t = {
        address : bytes;
        pub_key :
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t
          option;
        voting_power : int64;
        proposer_priority : int64;
      }

      val make :
        ?address:bytes ->
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        ?proposer_priority:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?address:bytes ->
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        ?proposer_priority:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and SimpleValidator : sig
      type t = {
        pub_key :
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t
          option;
        voting_power : int64;
      }

      val make :
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end
  end
end = struct
  module rec Types : sig
    (** {%html:
<p>BlockIdFlag indicates which BlockID the signature is for</p>
%}
    *)
    module rec BlockIDFlag : sig
      type t =
        | BLOCK_ID_FLAG_UNKNOWN
            (** {%html:
<p>indicates an error condition</p>
%} *)
        | BLOCK_ID_FLAG_ABSENT
            (** {%html:
<p>the vote was not received</p>
%} *)
        | BLOCK_ID_FLAG_COMMIT
            (** {%html:
<p>voted for the block that received the majority</p>
%}
            *)
        | BLOCK_ID_FLAG_NIL  (** {%html:
<p>voted for nil</p>
%} *)

      val name : unit -> string
      (** Fully qualified protobuf name of this enum *)

      (**/**)

      val to_int : t -> int
      val from_int : int -> t Runtime'.Result.t
      val from_int_exn : int -> t
      val to_string : t -> string
      val from_string_exn : string -> t

      (**/**)
    end

    and ValidatorSet : sig
      type t = {
        validators : Validator.t list;
        proposer : Validator.t option;
        total_voting_power : int64;
      }

      val make :
        ?validators:Validator.t list ->
        ?proposer:Validator.t ->
        ?total_voting_power:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?validators:Validator.t list ->
        ?proposer:Validator.t ->
        ?total_voting_power:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and Validator : sig
      type t = {
        address : bytes;
        pub_key :
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t
          option;
        voting_power : int64;
        proposer_priority : int64;
      }

      val make :
        ?address:bytes ->
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        ?proposer_priority:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?address:bytes ->
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        ?proposer_priority:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and SimpleValidator : sig
      type t = {
        pub_key :
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t
          option;
        voting_power : int64;
      }

      val make :
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end
  end = struct
    module rec BlockIDFlag : sig
      type t =
        | BLOCK_ID_FLAG_UNKNOWN
            (** {%html:
<p>indicates an error condition</p>
%} *)
        | BLOCK_ID_FLAG_ABSENT
            (** {%html:
<p>the vote was not received</p>
%} *)
        | BLOCK_ID_FLAG_COMMIT
            (** {%html:
<p>voted for the block that received the majority</p>
%}
            *)
        | BLOCK_ID_FLAG_NIL  (** {%html:
<p>voted for nil</p>
%} *)

      val name : unit -> string
      (** Fully qualified protobuf name of this enum *)

      (**/**)

      val to_int : t -> int
      val from_int : int -> t Runtime'.Result.t
      val from_int_exn : int -> t
      val to_string : t -> string
      val from_string_exn : string -> t

      (**/**)
    end = struct
      module This'_ = BlockIDFlag

      type t =
        | BLOCK_ID_FLAG_UNKNOWN
            (** {%html:
<p>indicates an error condition</p>
%} *)
        | BLOCK_ID_FLAG_ABSENT
            (** {%html:
<p>the vote was not received</p>
%} *)
        | BLOCK_ID_FLAG_COMMIT
            (** {%html:
<p>voted for the block that received the majority</p>
%}
            *)
        | BLOCK_ID_FLAG_NIL  (** {%html:
<p>voted for nil</p>
%} *)

      let name () = ".tendermint.types.BlockIDFlag"

      let to_int = function
        | BLOCK_ID_FLAG_UNKNOWN -> 0
        | BLOCK_ID_FLAG_ABSENT -> 1
        | BLOCK_ID_FLAG_COMMIT -> 2
        | BLOCK_ID_FLAG_NIL -> 3

      let from_int_exn = function
        | 0 -> BLOCK_ID_FLAG_UNKNOWN
        | 1 -> BLOCK_ID_FLAG_ABSENT
        | 2 -> BLOCK_ID_FLAG_COMMIT
        | 3 -> BLOCK_ID_FLAG_NIL
        | n -> Runtime'.Result.raise (`Unknown_enum_value n)

      let from_int e = Runtime'.Result.catch (fun () -> from_int_exn e)

      let to_string = function
        | BLOCK_ID_FLAG_UNKNOWN -> "BLOCK_ID_FLAG_UNKNOWN"
        | BLOCK_ID_FLAG_ABSENT -> "BLOCK_ID_FLAG_ABSENT"
        | BLOCK_ID_FLAG_COMMIT -> "BLOCK_ID_FLAG_COMMIT"
        | BLOCK_ID_FLAG_NIL -> "BLOCK_ID_FLAG_NIL"

      let from_string_exn = function
        | "BLOCK_ID_FLAG_UNKNOWN" -> BLOCK_ID_FLAG_UNKNOWN
        | "BLOCK_ID_FLAG_ABSENT" -> BLOCK_ID_FLAG_ABSENT
        | "BLOCK_ID_FLAG_COMMIT" -> BLOCK_ID_FLAG_COMMIT
        | "BLOCK_ID_FLAG_NIL" -> BLOCK_ID_FLAG_NIL
        | s -> Runtime'.Result.raise (`Unknown_enum_name s)
    end

    and ValidatorSet : sig
      type t = {
        validators : Validator.t list;
        proposer : Validator.t option;
        total_voting_power : int64;
      }

      val make :
        ?validators:Validator.t list ->
        ?proposer:Validator.t ->
        ?total_voting_power:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?validators:Validator.t list ->
        ?proposer:Validator.t ->
        ?total_voting_power:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = ValidatorSet

      let name () = ".tendermint.types.ValidatorSet"

      type t = {
        validators : Validator.t list;
        proposer : Validator.t option;
        total_voting_power : int64;
      }

      type make_t =
        ?validators:Validator.t list ->
        ?proposer:Validator.t ->
        ?total_voting_power:int64 ->
        unit ->
        t

      let make ?(validators = []) ?proposer ?(total_voting_power = 0L) () =
        { validators; proposer; total_voting_power }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_validators =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   repeated
                     ( (1, "validators", "validators"),
                       message (module Validator),
                       not_packed ))
             in
             let merge_proposer =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ((2, "proposer", "proposer"), message (module Validator)))
             in
             let merge_total_voting_power =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((3, "total_voting_power", "totalVotingPower"), int64, 0L))
             in
             fun t1 t2 ->
               {
                 validators = merge_validators t1.validators t2.validators;
                 proposer = merge_proposer t1.proposer t2.proposer;
                 total_voting_power =
                   merge_total_voting_power t1.total_voting_power
                     t2.total_voting_power;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          repeated
            ( (1, "validators", "validators"),
              message (module Validator),
              not_packed )
          ^:: basic_opt ((2, "proposer", "proposer"), message (module Validator))
          ^:: basic ((3, "total_voting_power", "totalVotingPower"), int64, 0L)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { validators; proposer; total_voting_power } ->
          serialize writer validators proposer total_voting_power

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor validators proposer total_voting_power =
          { validators; proposer; total_voting_power }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { validators; proposer; total_voting_power } ->
          serialize validators proposer total_voting_power

      let from_json_exn =
        let constructor validators proposer total_voting_power =
          { validators; proposer; total_voting_power }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and Validator : sig
      type t = {
        address : bytes;
        pub_key :
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t
          option;
        voting_power : int64;
        proposer_priority : int64;
      }

      val make :
        ?address:bytes ->
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        ?proposer_priority:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?address:bytes ->
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        ?proposer_priority:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = Validator

      let name () = ".tendermint.types.Validator"

      type t = {
        address : bytes;
        pub_key :
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t
          option;
        voting_power : int64;
        proposer_priority : int64;
      }

      type make_t =
        ?address:bytes ->
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        ?proposer_priority:int64 ->
        unit ->
        t

      let make ?(address = Bytes.of_string {||}) ?pub_key ?(voting_power = 0L)
          ?(proposer_priority = 0L) () =
        { address; pub_key; voting_power; proposer_priority }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_address =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((1, "address", "address"), bytes, Bytes.of_string {||}))
             in
             let merge_pub_key =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (2, "pub_key", "pubKey"),
                       message
                         (module Imported'modules.Tendermint_crypto_keys
                                 .Tendermint
                                 .Crypto
                                 .PublicKey) ))
             in
             let merge_voting_power =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((3, "voting_power", "votingPower"), int64, 0L))
             in
             let merge_proposer_priority =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((4, "proposer_priority", "proposerPriority"), int64, 0L))
             in
             fun t1 t2 ->
               {
                 address = merge_address t1.address t2.address;
                 pub_key = merge_pub_key t1.pub_key t2.pub_key;
                 voting_power =
                   merge_voting_power t1.voting_power t2.voting_power;
                 proposer_priority =
                   merge_proposer_priority t1.proposer_priority
                     t2.proposer_priority;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "address", "address"), bytes, Bytes.of_string {||})
          ^:: basic_opt
                ( (2, "pub_key", "pubKey"),
                  message
                    (module Imported'modules.Tendermint_crypto_keys.Tendermint
                            .Crypto
                            .PublicKey) )
          ^:: basic ((3, "voting_power", "votingPower"), int64, 0L)
          ^:: basic ((4, "proposer_priority", "proposerPriority"), int64, 0L)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { address; pub_key; voting_power; proposer_priority } ->
          serialize writer address pub_key voting_power proposer_priority

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor address pub_key voting_power proposer_priority =
          { address; pub_key; voting_power; proposer_priority }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { address; pub_key; voting_power; proposer_priority } ->
          serialize address pub_key voting_power proposer_priority

      let from_json_exn =
        let constructor address pub_key voting_power proposer_priority =
          { address; pub_key; voting_power; proposer_priority }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and SimpleValidator : sig
      type t = {
        pub_key :
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t
          option;
        voting_power : int64;
      }

      val make :
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = SimpleValidator

      let name () = ".tendermint.types.SimpleValidator"

      type t = {
        pub_key :
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t
          option;
        voting_power : int64;
      }

      type make_t =
        ?pub_key:
          Imported'modules.Tendermint_crypto_keys.Tendermint.Crypto.PublicKey.t ->
        ?voting_power:int64 ->
        unit ->
        t

      let make ?pub_key ?(voting_power = 0L) () = { pub_key; voting_power }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_pub_key =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (1, "pub_key", "pubKey"),
                       message
                         (module Imported'modules.Tendermint_crypto_keys
                                 .Tendermint
                                 .Crypto
                                 .PublicKey) ))
             in
             let merge_voting_power =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((2, "voting_power", "votingPower"), int64, 0L))
             in
             fun t1 t2 ->
               {
                 pub_key = merge_pub_key t1.pub_key t2.pub_key;
                 voting_power =
                   merge_voting_power t1.voting_power t2.voting_power;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic_opt
            ( (1, "pub_key", "pubKey"),
              message
                (module Imported'modules.Tendermint_crypto_keys.Tendermint
                        .Crypto
                        .PublicKey) )
          ^:: basic ((2, "voting_power", "votingPower"), int64, 0L)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { pub_key; voting_power } ->
          serialize writer pub_key voting_power

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor pub_key voting_power = { pub_key; voting_power } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { pub_key; voting_power } -> serialize pub_key voting_power

      let from_json_exn =
        let constructor pub_key voting_power = { pub_key; voting_power } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end
  end
end

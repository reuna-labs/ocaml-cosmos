(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: ibc/applications/transfer/v1/tx.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Amino_amino = Amino_amino
  module Gogoproto_gogo = Gogoproto_gogo
  module Cosmos_msg_v1_msg = Cosmos_msg_v1_msg
  module Cosmos_base_v1beta1_coin = Cosmos_base_v1beta1_coin
  module Ibc_core_client_v1_client = Ibc_core_client_v1_client

  module Ibc_applications_transfer_v1_transfer =
    Ibc_applications_transfer_v1_transfer
end

(**/**)

module rec Ibc : sig
  module rec Applications : sig
    module rec Transfer : sig
      module rec V1 : sig
        (** {%html:
<p>MsgTransfer defines a msg to transfer fungible tokens (i.e Coins) between
ICS20 enabled chains. See ICS Spec here:
https://github.com/cosmos/ibc/tree/master/spec/app/ics-020-fungible-token-transfer#data-structures</p>
%}
        *)
        module rec MsgTransfer : sig
          type t = {
            source_port : string;
                (** {%html:
<p>the port on which the packet will be sent</p>
%}
                *)
            source_channel : string;
                (** {%html:
<p>the channel by which the packet will be sent</p>
%}
                *)
            token :
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t
              option;
                (** {%html:
<p>token to be transferred</p>
%} *)
            sender : string;  (** {%html:
<p>the sender address</p>
%} *)
            receiver : string;
                (** {%html:
<p>the recipient address on the destination chain</p>
%}
                *)
            timeout_height :
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t
              option;
                (** {%html:
<p>Timeout height relative to the current block height.
If you are sending with IBC v1 protocol, either timeout_height or timeout_timestamp must be set.
If you are sending with IBC v2 protocol, timeout_timestamp must be set, and timeout_height must be omitted.</p>
%}
                *)
            timeout_timestamp : int64;
                (** {%html:
<p>Timeout timestamp in absolute nanoseconds since unix epoch.
If you are sending with IBC v1 protocol, either timeout_height or timeout_timestamp must be set.
If you are sending with IBC v2 protocol, timeout_timestamp must be set.</p>
%}
                *)
            memo : string;  (** {%html:
<p>optional memo</p>
%} *)
            encoding : string;  (** {%html:
<p>optional encoding</p>
%} *)
          }

          val make :
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgTransferResponse defines the Msg/Transfer response type.</p>
%}
        *)
        and MsgTransferResponse : sig
          type t = int64
          (** {%html:
<p>sequence number of the transfer packet sent</p>
%} *)

          val make : ?sequence:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?sequence:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgUpdateParams is the Msg/UpdateParams request type.</p>
%}
        *)
        and MsgUpdateParams : sig
          type t = {
            signer : string;  (** {%html:
<p>signer address</p>
%} *)
            params :
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t
              option;
                (** {%html:
<p>params defines the transfer parameters to update.</p>
<p>NOTE: All parameters must be supplied.</p>
%}
                *)
          }

          val make :
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgUpdateParamsResponse defines the response structure for executing a
MsgUpdateParams message.</p>
%}
        *)
        and MsgUpdateParamsResponse : sig
          type t = unit

          val make : unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        module Msg : sig
          module Transfer : sig
            include
              Runtime'.Service.Rpc
                with type Request.t = MsgTransfer.t
                 and type Response.t = MsgTransferResponse.t

            (** Module alias for the request message for this method call *)
            module Request :
              Runtime'.Spec.Message
                with type t = MsgTransfer.t
                 and type make_t = MsgTransfer.make_t

            (** Module alias for the response message for this method call *)
            module Response :
              Runtime'.Spec.Message
                with type t = MsgTransferResponse.t
                 and type make_t = MsgTransferResponse.make_t
          end

          val transfer :
            (module Runtime'.Spec.Message with type t = MsgTransfer.t)
            * (module Runtime'.Spec.Message with type t = MsgTransferResponse.t)

          module UpdateParams : sig
            include
              Runtime'.Service.Rpc
                with type Request.t = MsgUpdateParams.t
                 and type Response.t = MsgUpdateParamsResponse.t

            (** Module alias for the request message for this method call *)
            module Request :
              Runtime'.Spec.Message
                with type t = MsgUpdateParams.t
                 and type make_t = MsgUpdateParams.make_t

            (** Module alias for the response message for this method call *)
            module Response :
              Runtime'.Spec.Message
                with type t = MsgUpdateParamsResponse.t
                 and type make_t = MsgUpdateParamsResponse.make_t
          end

          val updateParams :
            (module Runtime'.Spec.Message with type t = MsgUpdateParams.t)
            * (module Runtime'.Spec.Message
                 with type t = MsgUpdateParamsResponse.t)
        end
      end
    end
  end
end = struct
  module rec Applications : sig
    module rec Transfer : sig
      module rec V1 : sig
        (** {%html:
<p>MsgTransfer defines a msg to transfer fungible tokens (i.e Coins) between
ICS20 enabled chains. See ICS Spec here:
https://github.com/cosmos/ibc/tree/master/spec/app/ics-020-fungible-token-transfer#data-structures</p>
%}
        *)
        module rec MsgTransfer : sig
          type t = {
            source_port : string;
                (** {%html:
<p>the port on which the packet will be sent</p>
%}
                *)
            source_channel : string;
                (** {%html:
<p>the channel by which the packet will be sent</p>
%}
                *)
            token :
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t
              option;
                (** {%html:
<p>token to be transferred</p>
%} *)
            sender : string;  (** {%html:
<p>the sender address</p>
%} *)
            receiver : string;
                (** {%html:
<p>the recipient address on the destination chain</p>
%}
                *)
            timeout_height :
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t
              option;
                (** {%html:
<p>Timeout height relative to the current block height.
If you are sending with IBC v1 protocol, either timeout_height or timeout_timestamp must be set.
If you are sending with IBC v2 protocol, timeout_timestamp must be set, and timeout_height must be omitted.</p>
%}
                *)
            timeout_timestamp : int64;
                (** {%html:
<p>Timeout timestamp in absolute nanoseconds since unix epoch.
If you are sending with IBC v1 protocol, either timeout_height or timeout_timestamp must be set.
If you are sending with IBC v2 protocol, timeout_timestamp must be set.</p>
%}
                *)
            memo : string;  (** {%html:
<p>optional memo</p>
%} *)
            encoding : string;  (** {%html:
<p>optional encoding</p>
%} *)
          }

          val make :
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgTransferResponse defines the Msg/Transfer response type.</p>
%}
        *)
        and MsgTransferResponse : sig
          type t = int64
          (** {%html:
<p>sequence number of the transfer packet sent</p>
%} *)

          val make : ?sequence:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?sequence:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgUpdateParams is the Msg/UpdateParams request type.</p>
%}
        *)
        and MsgUpdateParams : sig
          type t = {
            signer : string;  (** {%html:
<p>signer address</p>
%} *)
            params :
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t
              option;
                (** {%html:
<p>params defines the transfer parameters to update.</p>
<p>NOTE: All parameters must be supplied.</p>
%}
                *)
          }

          val make :
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgUpdateParamsResponse defines the response structure for executing a
MsgUpdateParams message.</p>
%}
        *)
        and MsgUpdateParamsResponse : sig
          type t = unit

          val make : unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        module Msg : sig
          module Transfer : sig
            include
              Runtime'.Service.Rpc
                with type Request.t = MsgTransfer.t
                 and type Response.t = MsgTransferResponse.t

            (** Module alias for the request message for this method call *)
            module Request :
              Runtime'.Spec.Message
                with type t = MsgTransfer.t
                 and type make_t = MsgTransfer.make_t

            (** Module alias for the response message for this method call *)
            module Response :
              Runtime'.Spec.Message
                with type t = MsgTransferResponse.t
                 and type make_t = MsgTransferResponse.make_t
          end

          val transfer :
            (module Runtime'.Spec.Message with type t = MsgTransfer.t)
            * (module Runtime'.Spec.Message with type t = MsgTransferResponse.t)

          module UpdateParams : sig
            include
              Runtime'.Service.Rpc
                with type Request.t = MsgUpdateParams.t
                 and type Response.t = MsgUpdateParamsResponse.t

            (** Module alias for the request message for this method call *)
            module Request :
              Runtime'.Spec.Message
                with type t = MsgUpdateParams.t
                 and type make_t = MsgUpdateParams.make_t

            (** Module alias for the response message for this method call *)
            module Response :
              Runtime'.Spec.Message
                with type t = MsgUpdateParamsResponse.t
                 and type make_t = MsgUpdateParamsResponse.make_t
          end

          val updateParams :
            (module Runtime'.Spec.Message with type t = MsgUpdateParams.t)
            * (module Runtime'.Spec.Message
                 with type t = MsgUpdateParamsResponse.t)
        end
      end
    end
  end = struct
    module rec Transfer : sig
      module rec V1 : sig
        (** {%html:
<p>MsgTransfer defines a msg to transfer fungible tokens (i.e Coins) between
ICS20 enabled chains. See ICS Spec here:
https://github.com/cosmos/ibc/tree/master/spec/app/ics-020-fungible-token-transfer#data-structures</p>
%}
        *)
        module rec MsgTransfer : sig
          type t = {
            source_port : string;
                (** {%html:
<p>the port on which the packet will be sent</p>
%}
                *)
            source_channel : string;
                (** {%html:
<p>the channel by which the packet will be sent</p>
%}
                *)
            token :
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t
              option;
                (** {%html:
<p>token to be transferred</p>
%} *)
            sender : string;  (** {%html:
<p>the sender address</p>
%} *)
            receiver : string;
                (** {%html:
<p>the recipient address on the destination chain</p>
%}
                *)
            timeout_height :
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t
              option;
                (** {%html:
<p>Timeout height relative to the current block height.
If you are sending with IBC v1 protocol, either timeout_height or timeout_timestamp must be set.
If you are sending with IBC v2 protocol, timeout_timestamp must be set, and timeout_height must be omitted.</p>
%}
                *)
            timeout_timestamp : int64;
                (** {%html:
<p>Timeout timestamp in absolute nanoseconds since unix epoch.
If you are sending with IBC v1 protocol, either timeout_height or timeout_timestamp must be set.
If you are sending with IBC v2 protocol, timeout_timestamp must be set.</p>
%}
                *)
            memo : string;  (** {%html:
<p>optional memo</p>
%} *)
            encoding : string;  (** {%html:
<p>optional encoding</p>
%} *)
          }

          val make :
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgTransferResponse defines the Msg/Transfer response type.</p>
%}
        *)
        and MsgTransferResponse : sig
          type t = int64
          (** {%html:
<p>sequence number of the transfer packet sent</p>
%} *)

          val make : ?sequence:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?sequence:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgUpdateParams is the Msg/UpdateParams request type.</p>
%}
        *)
        and MsgUpdateParams : sig
          type t = {
            signer : string;  (** {%html:
<p>signer address</p>
%} *)
            params :
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t
              option;
                (** {%html:
<p>params defines the transfer parameters to update.</p>
<p>NOTE: All parameters must be supplied.</p>
%}
                *)
          }

          val make :
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgUpdateParamsResponse defines the response structure for executing a
MsgUpdateParams message.</p>
%}
        *)
        and MsgUpdateParamsResponse : sig
          type t = unit

          val make : unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        module Msg : sig
          module Transfer : sig
            include
              Runtime'.Service.Rpc
                with type Request.t = MsgTransfer.t
                 and type Response.t = MsgTransferResponse.t

            (** Module alias for the request message for this method call *)
            module Request :
              Runtime'.Spec.Message
                with type t = MsgTransfer.t
                 and type make_t = MsgTransfer.make_t

            (** Module alias for the response message for this method call *)
            module Response :
              Runtime'.Spec.Message
                with type t = MsgTransferResponse.t
                 and type make_t = MsgTransferResponse.make_t
          end

          val transfer :
            (module Runtime'.Spec.Message with type t = MsgTransfer.t)
            * (module Runtime'.Spec.Message with type t = MsgTransferResponse.t)

          module UpdateParams : sig
            include
              Runtime'.Service.Rpc
                with type Request.t = MsgUpdateParams.t
                 and type Response.t = MsgUpdateParamsResponse.t

            (** Module alias for the request message for this method call *)
            module Request :
              Runtime'.Spec.Message
                with type t = MsgUpdateParams.t
                 and type make_t = MsgUpdateParams.make_t

            (** Module alias for the response message for this method call *)
            module Response :
              Runtime'.Spec.Message
                with type t = MsgUpdateParamsResponse.t
                 and type make_t = MsgUpdateParamsResponse.make_t
          end

          val updateParams :
            (module Runtime'.Spec.Message with type t = MsgUpdateParams.t)
            * (module Runtime'.Spec.Message
                 with type t = MsgUpdateParamsResponse.t)
        end
      end
    end = struct
      module rec V1 : sig
        (** {%html:
<p>MsgTransfer defines a msg to transfer fungible tokens (i.e Coins) between
ICS20 enabled chains. See ICS Spec here:
https://github.com/cosmos/ibc/tree/master/spec/app/ics-020-fungible-token-transfer#data-structures</p>
%}
        *)
        module rec MsgTransfer : sig
          type t = {
            source_port : string;
                (** {%html:
<p>the port on which the packet will be sent</p>
%}
                *)
            source_channel : string;
                (** {%html:
<p>the channel by which the packet will be sent</p>
%}
                *)
            token :
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t
              option;
                (** {%html:
<p>token to be transferred</p>
%} *)
            sender : string;  (** {%html:
<p>the sender address</p>
%} *)
            receiver : string;
                (** {%html:
<p>the recipient address on the destination chain</p>
%}
                *)
            timeout_height :
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t
              option;
                (** {%html:
<p>Timeout height relative to the current block height.
If you are sending with IBC v1 protocol, either timeout_height or timeout_timestamp must be set.
If you are sending with IBC v2 protocol, timeout_timestamp must be set, and timeout_height must be omitted.</p>
%}
                *)
            timeout_timestamp : int64;
                (** {%html:
<p>Timeout timestamp in absolute nanoseconds since unix epoch.
If you are sending with IBC v1 protocol, either timeout_height or timeout_timestamp must be set.
If you are sending with IBC v2 protocol, timeout_timestamp must be set.</p>
%}
                *)
            memo : string;  (** {%html:
<p>optional memo</p>
%} *)
            encoding : string;  (** {%html:
<p>optional encoding</p>
%} *)
          }

          val make :
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgTransferResponse defines the Msg/Transfer response type.</p>
%}
        *)
        and MsgTransferResponse : sig
          type t = int64
          (** {%html:
<p>sequence number of the transfer packet sent</p>
%} *)

          val make : ?sequence:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?sequence:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgUpdateParams is the Msg/UpdateParams request type.</p>
%}
        *)
        and MsgUpdateParams : sig
          type t = {
            signer : string;  (** {%html:
<p>signer address</p>
%} *)
            params :
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t
              option;
                (** {%html:
<p>params defines the transfer parameters to update.</p>
<p>NOTE: All parameters must be supplied.</p>
%}
                *)
          }

          val make :
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgUpdateParamsResponse defines the response structure for executing a
MsgUpdateParams message.</p>
%}
        *)
        and MsgUpdateParamsResponse : sig
          type t = unit

          val make : unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        module Msg : sig
          module Transfer : sig
            include
              Runtime'.Service.Rpc
                with type Request.t = MsgTransfer.t
                 and type Response.t = MsgTransferResponse.t

            (** Module alias for the request message for this method call *)
            module Request :
              Runtime'.Spec.Message
                with type t = MsgTransfer.t
                 and type make_t = MsgTransfer.make_t

            (** Module alias for the response message for this method call *)
            module Response :
              Runtime'.Spec.Message
                with type t = MsgTransferResponse.t
                 and type make_t = MsgTransferResponse.make_t
          end

          val transfer :
            (module Runtime'.Spec.Message with type t = MsgTransfer.t)
            * (module Runtime'.Spec.Message with type t = MsgTransferResponse.t)

          module UpdateParams : sig
            include
              Runtime'.Service.Rpc
                with type Request.t = MsgUpdateParams.t
                 and type Response.t = MsgUpdateParamsResponse.t

            (** Module alias for the request message for this method call *)
            module Request :
              Runtime'.Spec.Message
                with type t = MsgUpdateParams.t
                 and type make_t = MsgUpdateParams.make_t

            (** Module alias for the response message for this method call *)
            module Response :
              Runtime'.Spec.Message
                with type t = MsgUpdateParamsResponse.t
                 and type make_t = MsgUpdateParamsResponse.make_t
          end

          val updateParams :
            (module Runtime'.Spec.Message with type t = MsgUpdateParams.t)
            * (module Runtime'.Spec.Message
                 with type t = MsgUpdateParamsResponse.t)
        end
      end = struct
        module rec MsgTransfer : sig
          type t = {
            source_port : string;
                (** {%html:
<p>the port on which the packet will be sent</p>
%}
                *)
            source_channel : string;
                (** {%html:
<p>the channel by which the packet will be sent</p>
%}
                *)
            token :
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t
              option;
                (** {%html:
<p>token to be transferred</p>
%} *)
            sender : string;  (** {%html:
<p>the sender address</p>
%} *)
            receiver : string;
                (** {%html:
<p>the recipient address on the destination chain</p>
%}
                *)
            timeout_height :
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t
              option;
                (** {%html:
<p>Timeout height relative to the current block height.
If you are sending with IBC v1 protocol, either timeout_height or timeout_timestamp must be set.
If you are sending with IBC v2 protocol, timeout_timestamp must be set, and timeout_height must be omitted.</p>
%}
                *)
            timeout_timestamp : int64;
                (** {%html:
<p>Timeout timestamp in absolute nanoseconds since unix epoch.
If you are sending with IBC v1 protocol, either timeout_height or timeout_timestamp must be set.
If you are sending with IBC v2 protocol, timeout_timestamp must be set.</p>
%}
                *)
            memo : string;  (** {%html:
<p>optional memo</p>
%} *)
            encoding : string;  (** {%html:
<p>optional encoding</p>
%} *)
          }

          val make :
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = MsgTransfer

          let name () = ".ibc.applications.transfer.v1.MsgTransfer"

          type t = {
            source_port : string;
            source_channel : string;
            token :
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t
              option;
            sender : string;
            receiver : string;
            timeout_height :
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t
              option;
            timeout_timestamp : int64;
            memo : string;
            encoding : string;
          }

          type make_t =
            ?source_port:string ->
            ?source_channel:string ->
            ?token:
              Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin
              .t ->
            ?sender:string ->
            ?receiver:string ->
            ?timeout_height:
              Imported'modules.Ibc_core_client_v1_client.Ibc.Core.Client.V1
              .Height
              .t ->
            ?timeout_timestamp:int64 ->
            ?memo:string ->
            ?encoding:string ->
            unit ->
            t

          let make ?(source_port = {||}) ?(source_channel = {||}) ?token
              ?(sender = {||}) ?(receiver = {||}) ?timeout_height
              ?(timeout_timestamp = 0L) ?(memo = {||}) ?(encoding = {||}) () =
            {
              source_port;
              source_channel;
              token;
              sender;
              receiver;
              timeout_height;
              timeout_timestamp;
              memo;
              encoding;
            }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_source_port =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "source_port", "sourcePort"), string, {||}))
                 in
                 let merge_source_channel =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic
                         ((2, "source_channel", "sourceChannel"), string, {||}))
                 in
                 let merge_token =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt
                         ( (3, "token", "token"),
                           message
                             (module Imported'modules.Cosmos_base_v1beta1_coin
                                     .Cosmos
                                     .Base
                                     .V1beta1
                                     .Coin) ))
                 in
                 let merge_sender =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((4, "sender", "sender"), string, {||}))
                 in
                 let merge_receiver =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((5, "receiver", "receiver"), string, {||}))
                 in
                 let merge_timeout_height =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt
                         ( (6, "timeout_height", "timeoutHeight"),
                           message
                             (module Imported'modules.Ibc_core_client_v1_client
                                     .Ibc
                                     .Core
                                     .Client
                                     .V1
                                     .Height) ))
                 in
                 let merge_timeout_timestamp =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic
                         ( (7, "timeout_timestamp", "timeoutTimestamp"),
                           uint64,
                           0L ))
                 in
                 let merge_memo =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((8, "memo", "memo"), string, {||}))
                 in
                 let merge_encoding =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((9, "encoding", "encoding"), string, {||}))
                 in
                 fun t1 t2 ->
                   {
                     source_port =
                       merge_source_port t1.source_port t2.source_port;
                     source_channel =
                       merge_source_channel t1.source_channel t2.source_channel;
                     token = merge_token t1.token t2.token;
                     sender = merge_sender t1.sender t2.sender;
                     receiver = merge_receiver t1.receiver t2.receiver;
                     timeout_height =
                       merge_timeout_height t1.timeout_height t2.timeout_height;
                     timeout_timestamp =
                       merge_timeout_timestamp t1.timeout_timestamp
                         t2.timeout_timestamp;
                     memo = merge_memo t1.memo t2.memo;
                     encoding = merge_encoding t1.encoding t2.encoding;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "source_port", "sourcePort"), string, {||})
              ^:: basic ((2, "source_channel", "sourceChannel"), string, {||})
              ^:: basic_opt
                    ( (3, "token", "token"),
                      message
                        (module Imported'modules.Cosmos_base_v1beta1_coin.Cosmos
                                .Base
                                .V1beta1
                                .Coin) )
              ^:: basic ((4, "sender", "sender"), string, {||})
              ^:: basic ((5, "receiver", "receiver"), string, {||})
              ^:: basic_opt
                    ( (6, "timeout_height", "timeoutHeight"),
                      message
                        (module Imported'modules.Ibc_core_client_v1_client.Ibc
                                .Core
                                .Client
                                .V1
                                .Height) )
              ^:: basic
                    ((7, "timeout_timestamp", "timeoutTimestamp"), uint64, 0L)
              ^:: basic ((8, "memo", "memo"), string, {||})
              ^:: basic ((9, "encoding", "encoding"), string, {||})
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer
              {
                source_port;
                source_channel;
                token;
                sender;
                receiver;
                timeout_height;
                timeout_timestamp;
                memo;
                encoding;
              }
            ->
              serialize writer source_port source_channel token sender receiver
                timeout_height timeout_timestamp memo encoding

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor source_port source_channel token sender receiver
                timeout_height timeout_timestamp memo encoding =
              {
                source_port;
                source_channel;
                token;
                sender;
                receiver;
                timeout_height;
                timeout_timestamp;
                memo;
                encoding;
              }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun {
                  source_port;
                  source_channel;
                  token;
                  sender;
                  receiver;
                  timeout_height;
                  timeout_timestamp;
                  memo;
                  encoding;
                }
            ->
              serialize source_port source_channel token sender receiver
                timeout_height timeout_timestamp memo encoding

          let from_json_exn =
            let constructor source_port source_channel token sender receiver
                timeout_height timeout_timestamp memo encoding =
              {
                source_port;
                source_channel;
                token;
                sender;
                receiver;
                timeout_height;
                timeout_timestamp;
                memo;
                encoding;
              }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and MsgTransferResponse : sig
          type t = int64
          (** {%html:
<p>sequence number of the transfer packet sent</p>
%} *)

          val make : ?sequence:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?sequence:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = MsgTransferResponse

          let name () = ".ibc.applications.transfer.v1.MsgTransferResponse"

          type t = int64
          type make_t = ?sequence:int64 -> unit -> t

          let make ?(sequence = 0L) () = sequence

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_sequence =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "sequence", "sequence"), uint64, 0L))
                 in
                 fun t1_sequence t2_sequence ->
                   merge_sequence t1_sequence t2_sequence)
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "sequence", "sequence"), uint64, 0L) ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer sequence -> serialize writer sequence

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor sequence = sequence in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun sequence -> serialize sequence

          let from_json_exn =
            let constructor sequence = sequence in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and MsgUpdateParams : sig
          type t = {
            signer : string;  (** {%html:
<p>signer address</p>
%} *)
            params :
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t
              option;
                (** {%html:
<p>params defines the transfer parameters to update.</p>
<p>NOTE: All parameters must be supplied.</p>
%}
                *)
          }

          val make :
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = MsgUpdateParams

          let name () = ".ibc.applications.transfer.v1.MsgUpdateParams"

          type t = {
            signer : string;
            params :
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t
              option;
          }

          type make_t =
            ?signer:string ->
            ?params:
              Imported'modules.Ibc_applications_transfer_v1_transfer.Ibc
              .Applications
              .Transfer
              .V1
              .Params
              .t ->
            unit ->
            t

          let make ?(signer = {||}) ?params () = { signer; params }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_signer =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "signer", "signer"), string, {||}))
                 in
                 let merge_params =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt
                         ( (2, "params", "params"),
                           message
                             (module Imported'modules
                                     .Ibc_applications_transfer_v1_transfer
                                     .Ibc
                                     .Applications
                                     .Transfer
                                     .V1
                                     .Params) ))
                 in
                 fun t1 t2 ->
                   {
                     signer = merge_signer t1.signer t2.signer;
                     params = merge_params t1.params t2.params;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "signer", "signer"), string, {||})
              ^:: basic_opt
                    ( (2, "params", "params"),
                      message
                        (module Imported'modules
                                .Ibc_applications_transfer_v1_transfer
                                .Ibc
                                .Applications
                                .Transfer
                                .V1
                                .Params) )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { signer; params } -> serialize writer signer params

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor signer params = { signer; params } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { signer; params } -> serialize signer params

          let from_json_exn =
            let constructor signer params = { signer; params } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and MsgUpdateParamsResponse : sig
          type t = unit

          val make : unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = MsgUpdateParamsResponse

          let name () = ".ibc.applications.transfer.v1.MsgUpdateParamsResponse"

          type t = unit
          type make_t = unit -> t

          let make () = ()

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' = lazy (fun () () -> ()) in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () = Runtime'.Spec.(nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer () -> serialize writer

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor = () in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun () -> serialize

          let from_json_exn =
            let constructor = () in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        module Msg = struct
          module Transfer = struct
            let package_name = Some "ibc.applications.transfer.v1"
            let service_name = "Msg"
            let method_name = "Transfer"
            let name = "/ibc.applications.transfer.v1.Msg/Transfer"

            module Request = MsgTransfer
            module Response = MsgTransferResponse
          end

          let transfer =
            ( (module MsgTransfer : Runtime'.Spec.Message
                with type t = MsgTransfer.t),
              (module MsgTransferResponse : Runtime'.Spec.Message
                with type t = MsgTransferResponse.t) )

          module UpdateParams = struct
            let package_name = Some "ibc.applications.transfer.v1"
            let service_name = "Msg"
            let method_name = "UpdateParams"
            let name = "/ibc.applications.transfer.v1.Msg/UpdateParams"

            module Request = MsgUpdateParams
            module Response = MsgUpdateParamsResponse
          end

          let updateParams =
            ( (module MsgUpdateParams : Runtime'.Spec.Message
                with type t = MsgUpdateParams.t),
              (module MsgUpdateParamsResponse : Runtime'.Spec.Message
                with type t = MsgUpdateParamsResponse.t) )
        end
      end
    end
  end
end

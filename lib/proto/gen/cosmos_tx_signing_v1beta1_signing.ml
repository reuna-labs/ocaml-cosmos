(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: cosmos/tx/signing/v1beta1/signing.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Cosmos_crypto_multisig_v1beta1_multisig =
    Cosmos_crypto_multisig_v1beta1_multisig

  module Google_protobuf_any = Google_protobuf_any
end

(**/**)

module rec Cosmos : sig
  module rec Tx : sig
    module rec Signing : sig
      module rec V1beta1 : sig
        (** {%html:
<p>SignMode represents a signing mode with its own security guarantees.</p>
<p>This enum should be considered a registry of all known sign modes
in the Cosmos ecosystem. Apps are not expected to support all known
sign modes. Apps that would like to support custom  sign modes are
encouraged to open a small PR against this file to add a new case
to this SignMode enum describing their sign mode so that different
apps have a consistent version of this enum.</p>
%}
        *)
        module rec SignMode : sig
          type t =
            | SIGN_MODE_UNSPECIFIED
                (** {%html:
<p>SIGN_MODE_UNSPECIFIED specifies an unknown signing mode and will be
rejected.</p>
%}
                *)
            | SIGN_MODE_DIRECT
                (** {%html:
<p>SIGN_MODE_DIRECT specifies a signing mode which uses SignDoc and is
verified with raw bytes from Tx.</p>
%}
                *)
            | SIGN_MODE_DIRECT_AUX
                (** {%html:
<p>SIGN_MODE_DIRECT_AUX specifies a signing mode which uses
SignDocDirectAux. As opposed to SIGN_MODE_DIRECT, this sign mode does not
require signers signing over other signers' <code>signer_info</code>.</p>
<p>Since: cosmos-sdk 0.46</p>
%}
                *)
            | SIGN_MODE_LEGACY_AMINO_JSON
                (** {%html:
<p>SIGN_MODE_LEGACY_AMINO_JSON is a backwards compatibility mode which uses
Amino JSON and will be removed in the future.</p>
%}
                *)
            | SIGN_MODE_EIP_191
                (** {%html:
<p>SIGN_MODE_EIP_191 specifies the sign mode for EIP 191 signing on the Cosmos
SDK. Ref: https://eips.ethereum.org/EIPS/eip-191</p>
<p>Currently, SIGN_MODE_EIP_191 is registered as a SignMode enum variant,
but is not implemented on the SDK by default. To enable EIP-191, you need
to pass a custom <code>TxConfig</code> that has an implementation of
<code>SignModeHandler</code> for EIP-191. The SDK may decide to fully support
EIP-191 in the future.</p>
<p>Since: cosmos-sdk 0.45.2</p>
%}
                *)

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        (** {%html:
<p>SignatureDescriptors wraps multiple SignatureDescriptor's.</p>
%}
        *)
        and SignatureDescriptors : sig
          type t = SignatureDescriptor.t list
          (** {%html:
<p>signatures are the signature descriptors</p>
%} *)

          val make : ?signatures:SignatureDescriptor.t list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?signatures:SignatureDescriptor.t list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SignatureDescriptor is a convenience type which represents the full data for
a signature including the public key of the signer, signing modes and the
signature itself. It is primarily used for coordinating signatures between
clients.</p>
%}
        *)
        and SignatureDescriptor : sig
          (** {%html:
<p>Data represents signature data</p>
%} *)
          module rec Data : sig
            (** {%html:
<p>Single is the signature data for a single signer</p>
%}
            *)
            module rec Single : sig
              type t = {
                mode : SignMode.t;
                    (** {%html:
<p>mode is the signing mode of the single signer</p>
%}
                    *)
                signature : bytes;
                    (** {%html:
<p>signature is the raw signature bytes</p>
%}
                    *)
              }

              val make : ?mode:SignMode.t -> ?signature:bytes -> unit -> t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t = ?mode:SignMode.t -> ?signature:bytes -> unit -> t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            (** {%html:
<p>Multi is the signature data for a multisig public key</p>
%}
            *)
            and Multi : sig
              type t = {
                bitarray :
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t
                  option;
                    (** {%html:
<p>bitarray specifies which keys within the multisig are signing</p>
%}
                    *)
                signatures : Data.t list;
                    (** {%html:
<p>signatures is the signatures of the multi-signature</p>
%}
                    *)
              }

              val make :
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t =
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            type t = [ `not_set | `Single of Single.t | `Multi of Multi.t ]
            (** {%html:
<p>sum is the oneof that specifies whether this represents single or multi-signature data</p>
%}

                [`Single] {%html:
<p>single represents a single signer</p>
%}

                [`Multi] {%html:
<p>multi represents a multisig signer</p>
%} *)

            val make :
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t
            (** Helper function to generate a message using default values *)

            val to_proto : t -> Runtime'.Writer.t
            (** Serialize the message to binary format *)

            val from_proto :
              Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from binary format *)

            val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
            (** Serialize to Json (compatible with Yojson.Basic.t) *)

            val from_json :
              Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from Json (compatible with Yojson.Basic.t) *)

            val name : unit -> string
            (** Fully qualified protobuf name of this message *)

            (**/**)

            type make_t =
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t

            val merge : t -> t -> t
            val to_proto' : Runtime'.Writer.t -> t -> unit
            val from_proto_exn : Runtime'.Reader.t -> t
            val from_json_exn : Runtime'.Json.t -> t

            (**/**)
          end

          type t = {
            public_key :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>public_key is the public key of the signer</p>
%}
                *)
            data : Data.t option;
            sequence : int64;
                (** {%html:
<p>sequence is the sequence of the account, which describes the
number of committed transactions signed by a given address. It is used to prevent
replay attacks.</p>
%}
                *)
          }

          val make :
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end
  end
end = struct
  module rec Tx : sig
    module rec Signing : sig
      module rec V1beta1 : sig
        (** {%html:
<p>SignMode represents a signing mode with its own security guarantees.</p>
<p>This enum should be considered a registry of all known sign modes
in the Cosmos ecosystem. Apps are not expected to support all known
sign modes. Apps that would like to support custom  sign modes are
encouraged to open a small PR against this file to add a new case
to this SignMode enum describing their sign mode so that different
apps have a consistent version of this enum.</p>
%}
        *)
        module rec SignMode : sig
          type t =
            | SIGN_MODE_UNSPECIFIED
                (** {%html:
<p>SIGN_MODE_UNSPECIFIED specifies an unknown signing mode and will be
rejected.</p>
%}
                *)
            | SIGN_MODE_DIRECT
                (** {%html:
<p>SIGN_MODE_DIRECT specifies a signing mode which uses SignDoc and is
verified with raw bytes from Tx.</p>
%}
                *)
            | SIGN_MODE_DIRECT_AUX
                (** {%html:
<p>SIGN_MODE_DIRECT_AUX specifies a signing mode which uses
SignDocDirectAux. As opposed to SIGN_MODE_DIRECT, this sign mode does not
require signers signing over other signers' <code>signer_info</code>.</p>
<p>Since: cosmos-sdk 0.46</p>
%}
                *)
            | SIGN_MODE_LEGACY_AMINO_JSON
                (** {%html:
<p>SIGN_MODE_LEGACY_AMINO_JSON is a backwards compatibility mode which uses
Amino JSON and will be removed in the future.</p>
%}
                *)
            | SIGN_MODE_EIP_191
                (** {%html:
<p>SIGN_MODE_EIP_191 specifies the sign mode for EIP 191 signing on the Cosmos
SDK. Ref: https://eips.ethereum.org/EIPS/eip-191</p>
<p>Currently, SIGN_MODE_EIP_191 is registered as a SignMode enum variant,
but is not implemented on the SDK by default. To enable EIP-191, you need
to pass a custom <code>TxConfig</code> that has an implementation of
<code>SignModeHandler</code> for EIP-191. The SDK may decide to fully support
EIP-191 in the future.</p>
<p>Since: cosmos-sdk 0.45.2</p>
%}
                *)

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        (** {%html:
<p>SignatureDescriptors wraps multiple SignatureDescriptor's.</p>
%}
        *)
        and SignatureDescriptors : sig
          type t = SignatureDescriptor.t list
          (** {%html:
<p>signatures are the signature descriptors</p>
%} *)

          val make : ?signatures:SignatureDescriptor.t list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?signatures:SignatureDescriptor.t list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SignatureDescriptor is a convenience type which represents the full data for
a signature including the public key of the signer, signing modes and the
signature itself. It is primarily used for coordinating signatures between
clients.</p>
%}
        *)
        and SignatureDescriptor : sig
          (** {%html:
<p>Data represents signature data</p>
%} *)
          module rec Data : sig
            (** {%html:
<p>Single is the signature data for a single signer</p>
%}
            *)
            module rec Single : sig
              type t = {
                mode : SignMode.t;
                    (** {%html:
<p>mode is the signing mode of the single signer</p>
%}
                    *)
                signature : bytes;
                    (** {%html:
<p>signature is the raw signature bytes</p>
%}
                    *)
              }

              val make : ?mode:SignMode.t -> ?signature:bytes -> unit -> t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t = ?mode:SignMode.t -> ?signature:bytes -> unit -> t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            (** {%html:
<p>Multi is the signature data for a multisig public key</p>
%}
            *)
            and Multi : sig
              type t = {
                bitarray :
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t
                  option;
                    (** {%html:
<p>bitarray specifies which keys within the multisig are signing</p>
%}
                    *)
                signatures : Data.t list;
                    (** {%html:
<p>signatures is the signatures of the multi-signature</p>
%}
                    *)
              }

              val make :
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t =
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            type t = [ `not_set | `Single of Single.t | `Multi of Multi.t ]
            (** {%html:
<p>sum is the oneof that specifies whether this represents single or multi-signature data</p>
%}

                [`Single] {%html:
<p>single represents a single signer</p>
%}

                [`Multi] {%html:
<p>multi represents a multisig signer</p>
%} *)

            val make :
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t
            (** Helper function to generate a message using default values *)

            val to_proto : t -> Runtime'.Writer.t
            (** Serialize the message to binary format *)

            val from_proto :
              Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from binary format *)

            val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
            (** Serialize to Json (compatible with Yojson.Basic.t) *)

            val from_json :
              Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from Json (compatible with Yojson.Basic.t) *)

            val name : unit -> string
            (** Fully qualified protobuf name of this message *)

            (**/**)

            type make_t =
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t

            val merge : t -> t -> t
            val to_proto' : Runtime'.Writer.t -> t -> unit
            val from_proto_exn : Runtime'.Reader.t -> t
            val from_json_exn : Runtime'.Json.t -> t

            (**/**)
          end

          type t = {
            public_key :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>public_key is the public key of the signer</p>
%}
                *)
            data : Data.t option;
            sequence : int64;
                (** {%html:
<p>sequence is the sequence of the account, which describes the
number of committed transactions signed by a given address. It is used to prevent
replay attacks.</p>
%}
                *)
          }

          val make :
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end
  end = struct
    module rec Signing : sig
      module rec V1beta1 : sig
        (** {%html:
<p>SignMode represents a signing mode with its own security guarantees.</p>
<p>This enum should be considered a registry of all known sign modes
in the Cosmos ecosystem. Apps are not expected to support all known
sign modes. Apps that would like to support custom  sign modes are
encouraged to open a small PR against this file to add a new case
to this SignMode enum describing their sign mode so that different
apps have a consistent version of this enum.</p>
%}
        *)
        module rec SignMode : sig
          type t =
            | SIGN_MODE_UNSPECIFIED
                (** {%html:
<p>SIGN_MODE_UNSPECIFIED specifies an unknown signing mode and will be
rejected.</p>
%}
                *)
            | SIGN_MODE_DIRECT
                (** {%html:
<p>SIGN_MODE_DIRECT specifies a signing mode which uses SignDoc and is
verified with raw bytes from Tx.</p>
%}
                *)
            | SIGN_MODE_DIRECT_AUX
                (** {%html:
<p>SIGN_MODE_DIRECT_AUX specifies a signing mode which uses
SignDocDirectAux. As opposed to SIGN_MODE_DIRECT, this sign mode does not
require signers signing over other signers' <code>signer_info</code>.</p>
<p>Since: cosmos-sdk 0.46</p>
%}
                *)
            | SIGN_MODE_LEGACY_AMINO_JSON
                (** {%html:
<p>SIGN_MODE_LEGACY_AMINO_JSON is a backwards compatibility mode which uses
Amino JSON and will be removed in the future.</p>
%}
                *)
            | SIGN_MODE_EIP_191
                (** {%html:
<p>SIGN_MODE_EIP_191 specifies the sign mode for EIP 191 signing on the Cosmos
SDK. Ref: https://eips.ethereum.org/EIPS/eip-191</p>
<p>Currently, SIGN_MODE_EIP_191 is registered as a SignMode enum variant,
but is not implemented on the SDK by default. To enable EIP-191, you need
to pass a custom <code>TxConfig</code> that has an implementation of
<code>SignModeHandler</code> for EIP-191. The SDK may decide to fully support
EIP-191 in the future.</p>
<p>Since: cosmos-sdk 0.45.2</p>
%}
                *)

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        (** {%html:
<p>SignatureDescriptors wraps multiple SignatureDescriptor's.</p>
%}
        *)
        and SignatureDescriptors : sig
          type t = SignatureDescriptor.t list
          (** {%html:
<p>signatures are the signature descriptors</p>
%} *)

          val make : ?signatures:SignatureDescriptor.t list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?signatures:SignatureDescriptor.t list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SignatureDescriptor is a convenience type which represents the full data for
a signature including the public key of the signer, signing modes and the
signature itself. It is primarily used for coordinating signatures between
clients.</p>
%}
        *)
        and SignatureDescriptor : sig
          (** {%html:
<p>Data represents signature data</p>
%} *)
          module rec Data : sig
            (** {%html:
<p>Single is the signature data for a single signer</p>
%}
            *)
            module rec Single : sig
              type t = {
                mode : SignMode.t;
                    (** {%html:
<p>mode is the signing mode of the single signer</p>
%}
                    *)
                signature : bytes;
                    (** {%html:
<p>signature is the raw signature bytes</p>
%}
                    *)
              }

              val make : ?mode:SignMode.t -> ?signature:bytes -> unit -> t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t = ?mode:SignMode.t -> ?signature:bytes -> unit -> t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            (** {%html:
<p>Multi is the signature data for a multisig public key</p>
%}
            *)
            and Multi : sig
              type t = {
                bitarray :
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t
                  option;
                    (** {%html:
<p>bitarray specifies which keys within the multisig are signing</p>
%}
                    *)
                signatures : Data.t list;
                    (** {%html:
<p>signatures is the signatures of the multi-signature</p>
%}
                    *)
              }

              val make :
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t =
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            type t = [ `not_set | `Single of Single.t | `Multi of Multi.t ]
            (** {%html:
<p>sum is the oneof that specifies whether this represents single or multi-signature data</p>
%}

                [`Single] {%html:
<p>single represents a single signer</p>
%}

                [`Multi] {%html:
<p>multi represents a multisig signer</p>
%} *)

            val make :
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t
            (** Helper function to generate a message using default values *)

            val to_proto : t -> Runtime'.Writer.t
            (** Serialize the message to binary format *)

            val from_proto :
              Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from binary format *)

            val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
            (** Serialize to Json (compatible with Yojson.Basic.t) *)

            val from_json :
              Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from Json (compatible with Yojson.Basic.t) *)

            val name : unit -> string
            (** Fully qualified protobuf name of this message *)

            (**/**)

            type make_t =
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t

            val merge : t -> t -> t
            val to_proto' : Runtime'.Writer.t -> t -> unit
            val from_proto_exn : Runtime'.Reader.t -> t
            val from_json_exn : Runtime'.Json.t -> t

            (**/**)
          end

          type t = {
            public_key :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>public_key is the public key of the signer</p>
%}
                *)
            data : Data.t option;
            sequence : int64;
                (** {%html:
<p>sequence is the sequence of the account, which describes the
number of committed transactions signed by a given address. It is used to prevent
replay attacks.</p>
%}
                *)
          }

          val make :
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end = struct
      module rec V1beta1 : sig
        (** {%html:
<p>SignMode represents a signing mode with its own security guarantees.</p>
<p>This enum should be considered a registry of all known sign modes
in the Cosmos ecosystem. Apps are not expected to support all known
sign modes. Apps that would like to support custom  sign modes are
encouraged to open a small PR against this file to add a new case
to this SignMode enum describing their sign mode so that different
apps have a consistent version of this enum.</p>
%}
        *)
        module rec SignMode : sig
          type t =
            | SIGN_MODE_UNSPECIFIED
                (** {%html:
<p>SIGN_MODE_UNSPECIFIED specifies an unknown signing mode and will be
rejected.</p>
%}
                *)
            | SIGN_MODE_DIRECT
                (** {%html:
<p>SIGN_MODE_DIRECT specifies a signing mode which uses SignDoc and is
verified with raw bytes from Tx.</p>
%}
                *)
            | SIGN_MODE_DIRECT_AUX
                (** {%html:
<p>SIGN_MODE_DIRECT_AUX specifies a signing mode which uses
SignDocDirectAux. As opposed to SIGN_MODE_DIRECT, this sign mode does not
require signers signing over other signers' <code>signer_info</code>.</p>
<p>Since: cosmos-sdk 0.46</p>
%}
                *)
            | SIGN_MODE_LEGACY_AMINO_JSON
                (** {%html:
<p>SIGN_MODE_LEGACY_AMINO_JSON is a backwards compatibility mode which uses
Amino JSON and will be removed in the future.</p>
%}
                *)
            | SIGN_MODE_EIP_191
                (** {%html:
<p>SIGN_MODE_EIP_191 specifies the sign mode for EIP 191 signing on the Cosmos
SDK. Ref: https://eips.ethereum.org/EIPS/eip-191</p>
<p>Currently, SIGN_MODE_EIP_191 is registered as a SignMode enum variant,
but is not implemented on the SDK by default. To enable EIP-191, you need
to pass a custom <code>TxConfig</code> that has an implementation of
<code>SignModeHandler</code> for EIP-191. The SDK may decide to fully support
EIP-191 in the future.</p>
<p>Since: cosmos-sdk 0.45.2</p>
%}
                *)

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end

        (** {%html:
<p>SignatureDescriptors wraps multiple SignatureDescriptor's.</p>
%}
        *)
        and SignatureDescriptors : sig
          type t = SignatureDescriptor.t list
          (** {%html:
<p>signatures are the signature descriptors</p>
%} *)

          val make : ?signatures:SignatureDescriptor.t list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?signatures:SignatureDescriptor.t list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SignatureDescriptor is a convenience type which represents the full data for
a signature including the public key of the signer, signing modes and the
signature itself. It is primarily used for coordinating signatures between
clients.</p>
%}
        *)
        and SignatureDescriptor : sig
          (** {%html:
<p>Data represents signature data</p>
%} *)
          module rec Data : sig
            (** {%html:
<p>Single is the signature data for a single signer</p>
%}
            *)
            module rec Single : sig
              type t = {
                mode : SignMode.t;
                    (** {%html:
<p>mode is the signing mode of the single signer</p>
%}
                    *)
                signature : bytes;
                    (** {%html:
<p>signature is the raw signature bytes</p>
%}
                    *)
              }

              val make : ?mode:SignMode.t -> ?signature:bytes -> unit -> t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t = ?mode:SignMode.t -> ?signature:bytes -> unit -> t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            (** {%html:
<p>Multi is the signature data for a multisig public key</p>
%}
            *)
            and Multi : sig
              type t = {
                bitarray :
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t
                  option;
                    (** {%html:
<p>bitarray specifies which keys within the multisig are signing</p>
%}
                    *)
                signatures : Data.t list;
                    (** {%html:
<p>signatures is the signatures of the multi-signature</p>
%}
                    *)
              }

              val make :
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t =
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            type t = [ `not_set | `Single of Single.t | `Multi of Multi.t ]
            (** {%html:
<p>sum is the oneof that specifies whether this represents single or multi-signature data</p>
%}

                [`Single] {%html:
<p>single represents a single signer</p>
%}

                [`Multi] {%html:
<p>multi represents a multisig signer</p>
%} *)

            val make :
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t
            (** Helper function to generate a message using default values *)

            val to_proto : t -> Runtime'.Writer.t
            (** Serialize the message to binary format *)

            val from_proto :
              Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from binary format *)

            val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
            (** Serialize to Json (compatible with Yojson.Basic.t) *)

            val from_json :
              Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from Json (compatible with Yojson.Basic.t) *)

            val name : unit -> string
            (** Fully qualified protobuf name of this message *)

            (**/**)

            type make_t =
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t

            val merge : t -> t -> t
            val to_proto' : Runtime'.Writer.t -> t -> unit
            val from_proto_exn : Runtime'.Reader.t -> t
            val from_json_exn : Runtime'.Json.t -> t

            (**/**)
          end

          type t = {
            public_key :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>public_key is the public key of the signer</p>
%}
                *)
            data : Data.t option;
            sequence : int64;
                (** {%html:
<p>sequence is the sequence of the account, which describes the
number of committed transactions signed by a given address. It is used to prevent
replay attacks.</p>
%}
                *)
          }

          val make :
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end = struct
        module rec SignMode : sig
          type t =
            | SIGN_MODE_UNSPECIFIED
                (** {%html:
<p>SIGN_MODE_UNSPECIFIED specifies an unknown signing mode and will be
rejected.</p>
%}
                *)
            | SIGN_MODE_DIRECT
                (** {%html:
<p>SIGN_MODE_DIRECT specifies a signing mode which uses SignDoc and is
verified with raw bytes from Tx.</p>
%}
                *)
            | SIGN_MODE_DIRECT_AUX
                (** {%html:
<p>SIGN_MODE_DIRECT_AUX specifies a signing mode which uses
SignDocDirectAux. As opposed to SIGN_MODE_DIRECT, this sign mode does not
require signers signing over other signers' <code>signer_info</code>.</p>
<p>Since: cosmos-sdk 0.46</p>
%}
                *)
            | SIGN_MODE_LEGACY_AMINO_JSON
                (** {%html:
<p>SIGN_MODE_LEGACY_AMINO_JSON is a backwards compatibility mode which uses
Amino JSON and will be removed in the future.</p>
%}
                *)
            | SIGN_MODE_EIP_191
                (** {%html:
<p>SIGN_MODE_EIP_191 specifies the sign mode for EIP 191 signing on the Cosmos
SDK. Ref: https://eips.ethereum.org/EIPS/eip-191</p>
<p>Currently, SIGN_MODE_EIP_191 is registered as a SignMode enum variant,
but is not implemented on the SDK by default. To enable EIP-191, you need
to pass a custom <code>TxConfig</code> that has an implementation of
<code>SignModeHandler</code> for EIP-191. The SDK may decide to fully support
EIP-191 in the future.</p>
<p>Since: cosmos-sdk 0.45.2</p>
%}
                *)

          val name : unit -> string
          (** Fully qualified protobuf name of this enum *)

          (**/**)

          val to_int : t -> int
          val from_int : int -> t Runtime'.Result.t
          val from_int_exn : int -> t
          val to_string : t -> string
          val from_string_exn : string -> t

          (**/**)
        end = struct
          module This'_ = SignMode

          type t =
            | SIGN_MODE_UNSPECIFIED
                (** {%html:
<p>SIGN_MODE_UNSPECIFIED specifies an unknown signing mode and will be
rejected.</p>
%}
                *)
            | SIGN_MODE_DIRECT
                (** {%html:
<p>SIGN_MODE_DIRECT specifies a signing mode which uses SignDoc and is
verified with raw bytes from Tx.</p>
%}
                *)
            | SIGN_MODE_DIRECT_AUX
                (** {%html:
<p>SIGN_MODE_DIRECT_AUX specifies a signing mode which uses
SignDocDirectAux. As opposed to SIGN_MODE_DIRECT, this sign mode does not
require signers signing over other signers' <code>signer_info</code>.</p>
<p>Since: cosmos-sdk 0.46</p>
%}
                *)
            | SIGN_MODE_LEGACY_AMINO_JSON
                (** {%html:
<p>SIGN_MODE_LEGACY_AMINO_JSON is a backwards compatibility mode which uses
Amino JSON and will be removed in the future.</p>
%}
                *)
            | SIGN_MODE_EIP_191
                (** {%html:
<p>SIGN_MODE_EIP_191 specifies the sign mode for EIP 191 signing on the Cosmos
SDK. Ref: https://eips.ethereum.org/EIPS/eip-191</p>
<p>Currently, SIGN_MODE_EIP_191 is registered as a SignMode enum variant,
but is not implemented on the SDK by default. To enable EIP-191, you need
to pass a custom <code>TxConfig</code> that has an implementation of
<code>SignModeHandler</code> for EIP-191. The SDK may decide to fully support
EIP-191 in the future.</p>
<p>Since: cosmos-sdk 0.45.2</p>
%}
                *)

          let name () = ".cosmos.tx.signing.v1beta1.SignMode"

          let to_int = function
            | SIGN_MODE_UNSPECIFIED -> 0
            | SIGN_MODE_DIRECT -> 1
            | SIGN_MODE_DIRECT_AUX -> 3
            | SIGN_MODE_LEGACY_AMINO_JSON -> 127
            | SIGN_MODE_EIP_191 -> 191

          let from_int_exn = function
            | 0 -> SIGN_MODE_UNSPECIFIED
            | 1 -> SIGN_MODE_DIRECT
            | 3 -> SIGN_MODE_DIRECT_AUX
            | 127 -> SIGN_MODE_LEGACY_AMINO_JSON
            | 191 -> SIGN_MODE_EIP_191
            | n -> Runtime'.Result.raise (`Unknown_enum_value n)

          let from_int e = Runtime'.Result.catch (fun () -> from_int_exn e)

          let to_string = function
            | SIGN_MODE_UNSPECIFIED -> "SIGN_MODE_UNSPECIFIED"
            | SIGN_MODE_DIRECT -> "SIGN_MODE_DIRECT"
            | SIGN_MODE_DIRECT_AUX -> "SIGN_MODE_DIRECT_AUX"
            | SIGN_MODE_LEGACY_AMINO_JSON -> "SIGN_MODE_LEGACY_AMINO_JSON"
            | SIGN_MODE_EIP_191 -> "SIGN_MODE_EIP_191"

          let from_string_exn = function
            | "SIGN_MODE_UNSPECIFIED" -> SIGN_MODE_UNSPECIFIED
            | "SIGN_MODE_DIRECT" -> SIGN_MODE_DIRECT
            | "SIGN_MODE_DIRECT_AUX" -> SIGN_MODE_DIRECT_AUX
            | "SIGN_MODE_LEGACY_AMINO_JSON" -> SIGN_MODE_LEGACY_AMINO_JSON
            | "SIGN_MODE_EIP_191" -> SIGN_MODE_EIP_191
            | s -> Runtime'.Result.raise (`Unknown_enum_name s)
        end

        and SignatureDescriptors : sig
          type t = SignatureDescriptor.t list
          (** {%html:
<p>signatures are the signature descriptors</p>
%} *)

          val make : ?signatures:SignatureDescriptor.t list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?signatures:SignatureDescriptor.t list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = SignatureDescriptors

          let name () = ".cosmos.tx.signing.v1beta1.SignatureDescriptors"

          type t = SignatureDescriptor.t list
          type make_t = ?signatures:SignatureDescriptor.t list -> unit -> t

          let make ?(signatures = []) () = signatures

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_signatures =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (1, "signatures", "signatures"),
                           message (module SignatureDescriptor),
                           not_packed ))
                 in
                 fun t1_signatures t2_signatures ->
                   merge_signatures t1_signatures t2_signatures)
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              repeated
                ( (1, "signatures", "signatures"),
                  message (module SignatureDescriptor),
                  not_packed )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer signatures -> serialize writer signatures

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor signatures = signatures in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun signatures -> serialize signatures

          let from_json_exn =
            let constructor signatures = signatures in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and SignatureDescriptor : sig
          (** {%html:
<p>Data represents signature data</p>
%} *)
          module rec Data : sig
            (** {%html:
<p>Single is the signature data for a single signer</p>
%}
            *)
            module rec Single : sig
              type t = {
                mode : SignMode.t;
                    (** {%html:
<p>mode is the signing mode of the single signer</p>
%}
                    *)
                signature : bytes;
                    (** {%html:
<p>signature is the raw signature bytes</p>
%}
                    *)
              }

              val make : ?mode:SignMode.t -> ?signature:bytes -> unit -> t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t = ?mode:SignMode.t -> ?signature:bytes -> unit -> t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            (** {%html:
<p>Multi is the signature data for a multisig public key</p>
%}
            *)
            and Multi : sig
              type t = {
                bitarray :
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t
                  option;
                    (** {%html:
<p>bitarray specifies which keys within the multisig are signing</p>
%}
                    *)
                signatures : Data.t list;
                    (** {%html:
<p>signatures is the signatures of the multi-signature</p>
%}
                    *)
              }

              val make :
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t =
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            type t = [ `not_set | `Single of Single.t | `Multi of Multi.t ]
            (** {%html:
<p>sum is the oneof that specifies whether this represents single or multi-signature data</p>
%}

                [`Single] {%html:
<p>single represents a single signer</p>
%}

                [`Multi] {%html:
<p>multi represents a multisig signer</p>
%} *)

            val make :
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t
            (** Helper function to generate a message using default values *)

            val to_proto : t -> Runtime'.Writer.t
            (** Serialize the message to binary format *)

            val from_proto :
              Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from binary format *)

            val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
            (** Serialize to Json (compatible with Yojson.Basic.t) *)

            val from_json :
              Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from Json (compatible with Yojson.Basic.t) *)

            val name : unit -> string
            (** Fully qualified protobuf name of this message *)

            (**/**)

            type make_t =
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t

            val merge : t -> t -> t
            val to_proto' : Runtime'.Writer.t -> t -> unit
            val from_proto_exn : Runtime'.Reader.t -> t
            val from_json_exn : Runtime'.Json.t -> t

            (**/**)
          end

          type t = {
            public_key :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>public_key is the public key of the signer</p>
%}
                *)
            data : Data.t option;
            sequence : int64;
                (** {%html:
<p>sequence is the sequence of the account, which describes the
number of committed transactions signed by a given address. It is used to prevent
replay attacks.</p>
%}
                *)
          }

          val make :
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = SignatureDescriptor

          module rec Data : sig
            (** {%html:
<p>Single is the signature data for a single signer</p>
%}
            *)
            module rec Single : sig
              type t = {
                mode : SignMode.t;
                    (** {%html:
<p>mode is the signing mode of the single signer</p>
%}
                    *)
                signature : bytes;
                    (** {%html:
<p>signature is the raw signature bytes</p>
%}
                    *)
              }

              val make : ?mode:SignMode.t -> ?signature:bytes -> unit -> t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t = ?mode:SignMode.t -> ?signature:bytes -> unit -> t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            (** {%html:
<p>Multi is the signature data for a multisig public key</p>
%}
            *)
            and Multi : sig
              type t = {
                bitarray :
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t
                  option;
                    (** {%html:
<p>bitarray specifies which keys within the multisig are signing</p>
%}
                    *)
                signatures : Data.t list;
                    (** {%html:
<p>signatures is the signatures of the multi-signature</p>
%}
                    *)
              }

              val make :
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t =
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end

            type t = [ `not_set | `Single of Single.t | `Multi of Multi.t ]
            (** {%html:
<p>sum is the oneof that specifies whether this represents single or multi-signature data</p>
%}

                [`Single] {%html:
<p>single represents a single signer</p>
%}

                [`Multi] {%html:
<p>multi represents a multisig signer</p>
%} *)

            val make :
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t
            (** Helper function to generate a message using default values *)

            val to_proto : t -> Runtime'.Writer.t
            (** Serialize the message to binary format *)

            val from_proto :
              Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from binary format *)

            val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
            (** Serialize to Json (compatible with Yojson.Basic.t) *)

            val from_json :
              Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
            (** Deserialize from Json (compatible with Yojson.Basic.t) *)

            val name : unit -> string
            (** Fully qualified protobuf name of this message *)

            (**/**)

            type make_t =
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t

            val merge : t -> t -> t
            val to_proto' : Runtime'.Writer.t -> t -> unit
            val from_proto_exn : Runtime'.Reader.t -> t
            val from_json_exn : Runtime'.Json.t -> t

            (**/**)
          end = struct
            module This'_ = Data

            module rec Single : sig
              type t = {
                mode : SignMode.t;
                    (** {%html:
<p>mode is the signing mode of the single signer</p>
%}
                    *)
                signature : bytes;
                    (** {%html:
<p>signature is the raw signature bytes</p>
%}
                    *)
              }

              val make : ?mode:SignMode.t -> ?signature:bytes -> unit -> t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t = ?mode:SignMode.t -> ?signature:bytes -> unit -> t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end = struct
              module This'_ = Single

              let name () =
                ".cosmos.tx.signing.v1beta1.SignatureDescriptor.Data.Single"

              type t = { mode : SignMode.t; signature : bytes }
              type make_t = ?mode:SignMode.t -> ?signature:bytes -> unit -> t

              let make ?(mode = SignMode.from_int_exn 0)
                  ?(signature = Bytes.of_string {||}) () =
                { mode; signature }

              let merge =
                (* deferred by tools/lazify-merge.py; see that file *)
                let merge' =
                  lazy
                    (let merge_mode =
                       Runtime'.Merge.merge
                         Runtime'.Spec.(
                           basic
                             ( (1, "mode", "mode"),
                               enum (module SignMode),
                               SignMode.from_int_exn 0 ))
                     in
                     let merge_signature =
                       Runtime'.Merge.merge
                         Runtime'.Spec.(
                           basic
                             ( (2, "signature", "signature"),
                               bytes,
                               Bytes.of_string {||} ))
                     in
                     fun t1 t2 ->
                       {
                         mode = merge_mode t1.mode t2.mode;
                         signature = merge_signature t1.signature t2.signature;
                       })
                in
                fun t1' t2' -> (Lazy.force merge') t1' t2'

              let spec () =
                Runtime'.Spec.(
                  basic
                    ( (1, "mode", "mode"),
                      enum (module SignMode),
                      SignMode.from_int_exn 0 )
                  ^:: basic
                        ( (2, "signature", "signature"),
                          bytes,
                          Bytes.of_string {||} )
                  ^:: nil)

              let to_proto' =
                let serialize =
                  Runtime'.apply_lazy (fun () ->
                      Runtime'.Serialize.serialize (spec ()))
                in
                fun writer { mode; signature } ->
                  serialize writer mode signature

              let to_proto t =
                let writer = Runtime'.Writer.init () in
                to_proto' writer t;
                writer

              let from_proto_exn =
                let constructor mode signature = { mode; signature } in
                Runtime'.apply_lazy (fun () ->
                    Runtime'.Deserialize.deserialize (spec ()) constructor)

              let from_proto writer =
                Runtime'.Result.catch (fun () -> from_proto_exn writer)

              let to_json options =
                let serialize =
                  Runtime'.Serialize_json.serialize ~message_name:(name ())
                    (spec ()) options
                in
                fun { mode; signature } -> serialize mode signature

              let from_json_exn =
                let constructor mode signature = { mode; signature } in
                Runtime'.apply_lazy (fun () ->
                    Runtime'.Deserialize_json.deserialize
                      ~message_name:(name ()) (spec ()) constructor)

              let from_json json =
                Runtime'.Result.catch (fun () -> from_json_exn json)
            end

            and Multi : sig
              type t = {
                bitarray :
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t
                  option;
                    (** {%html:
<p>bitarray specifies which keys within the multisig are signing</p>
%}
                    *)
                signatures : Data.t list;
                    (** {%html:
<p>signatures is the signatures of the multi-signature</p>
%}
                    *)
              }

              val make :
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t
              (** Helper function to generate a message using default values *)

              val to_proto : t -> Runtime'.Writer.t
              (** Serialize the message to binary format *)

              val from_proto :
                Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from binary format *)

              val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
              (** Serialize to Json (compatible with Yojson.Basic.t) *)

              val from_json :
                Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
              (** Deserialize from Json (compatible with Yojson.Basic.t) *)

              val name : unit -> string
              (** Fully qualified protobuf name of this message *)

              (**/**)

              type make_t =
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t

              val merge : t -> t -> t
              val to_proto' : Runtime'.Writer.t -> t -> unit
              val from_proto_exn : Runtime'.Reader.t -> t
              val from_json_exn : Runtime'.Json.t -> t

              (**/**)
            end = struct
              module This'_ = Multi

              let name () =
                ".cosmos.tx.signing.v1beta1.SignatureDescriptor.Data.Multi"

              type t = {
                bitarray :
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t
                  option;
                signatures : Data.t list;
              }

              type make_t =
                ?bitarray:
                  Imported'modules.Cosmos_crypto_multisig_v1beta1_multisig
                  .Cosmos
                  .Crypto
                  .Multisig
                  .V1beta1
                  .CompactBitArray
                  .t ->
                ?signatures:Data.t list ->
                unit ->
                t

              let make ?bitarray ?(signatures = []) () =
                { bitarray; signatures }

              let merge =
                (* deferred by tools/lazify-merge.py; see that file *)
                let merge' =
                  lazy
                    (let merge_bitarray =
                       Runtime'.Merge.merge
                         Runtime'.Spec.(
                           basic_opt
                             ( (1, "bitarray", "bitarray"),
                               message
                                 (module Imported'modules
                                         .Cosmos_crypto_multisig_v1beta1_multisig
                                         .Cosmos
                                         .Crypto
                                         .Multisig
                                         .V1beta1
                                         .CompactBitArray) ))
                     in
                     let merge_signatures =
                       Runtime'.Merge.merge
                         Runtime'.Spec.(
                           repeated
                             ( (2, "signatures", "signatures"),
                               message (module Data),
                               not_packed ))
                     in
                     fun t1 t2 ->
                       {
                         bitarray = merge_bitarray t1.bitarray t2.bitarray;
                         signatures =
                           merge_signatures t1.signatures t2.signatures;
                       })
                in
                fun t1' t2' -> (Lazy.force merge') t1' t2'

              let spec () =
                Runtime'.Spec.(
                  basic_opt
                    ( (1, "bitarray", "bitarray"),
                      message
                        (module Imported'modules
                                .Cosmos_crypto_multisig_v1beta1_multisig
                                .Cosmos
                                .Crypto
                                .Multisig
                                .V1beta1
                                .CompactBitArray) )
                  ^:: repeated
                        ( (2, "signatures", "signatures"),
                          message (module Data),
                          not_packed )
                  ^:: nil)

              let to_proto' =
                let serialize =
                  Runtime'.apply_lazy (fun () ->
                      Runtime'.Serialize.serialize (spec ()))
                in
                fun writer { bitarray; signatures } ->
                  serialize writer bitarray signatures

              let to_proto t =
                let writer = Runtime'.Writer.init () in
                to_proto' writer t;
                writer

              let from_proto_exn =
                let constructor bitarray signatures =
                  { bitarray; signatures }
                in
                Runtime'.apply_lazy (fun () ->
                    Runtime'.Deserialize.deserialize (spec ()) constructor)

              let from_proto writer =
                Runtime'.Result.catch (fun () -> from_proto_exn writer)

              let to_json options =
                let serialize =
                  Runtime'.Serialize_json.serialize ~message_name:(name ())
                    (spec ()) options
                in
                fun { bitarray; signatures } -> serialize bitarray signatures

              let from_json_exn =
                let constructor bitarray signatures =
                  { bitarray; signatures }
                in
                Runtime'.apply_lazy (fun () ->
                    Runtime'.Deserialize_json.deserialize
                      ~message_name:(name ()) (spec ()) constructor)

              let from_json json =
                Runtime'.Result.catch (fun () -> from_json_exn json)
            end

            let name () = ".cosmos.tx.signing.v1beta1.SignatureDescriptor.Data"

            type t = [ `not_set | `Single of Single.t | `Multi of Multi.t ]

            type make_t =
              ?sum:[ `not_set | `Single of Single.t | `Multi of Multi.t ] ->
              unit ->
              t

            let make ?(sum = `not_set) () = sum

            let merge =
              (* deferred by tools/lazify-merge.py; see that file *)
              let merge' =
                lazy
                  (let merge_oneof_sum__Single =
                     Runtime'.Merge.merge
                       Runtime'.Spec.(
                         basic_req ((0, "", ""), message (module Single)))
                   in
                   let merge_oneof_sum__Multi =
                     Runtime'.Merge.merge
                       Runtime'.Spec.(
                         basic_req ((0, "", ""), message (module Multi)))
                   in
                   fun t1_sum t2_sum ->
                     match (t1_sum, t2_sum) with
                     | `Single v1, `Single v2 ->
                         `Single (merge_oneof_sum__Single v1 v2)
                     | `Multi v1, `Multi v2 ->
                         `Multi (merge_oneof_sum__Multi v1 v2)
                     | v1, `not_set -> v1
                     | _, v2 -> v2)
              in
              fun t1' t2' -> (Lazy.force merge') t1' t2'

            let spec () =
              Runtime'.Spec.(
                oneof
                  ( [
                      oneof_elem
                        ( (1, "single", "single"),
                          message (module Single),
                          ( (fun v -> `Single v),
                            function
                            | `Single v -> v
                            | _ ->
                                raise
                                  (Invalid_argument
                                     "Cannot destruct given oneof") ) );
                      oneof_elem
                        ( (2, "multi", "multi"),
                          message (module Multi),
                          ( (fun v -> `Multi v),
                            function
                            | `Multi v -> v
                            | _ ->
                                raise
                                  (Invalid_argument
                                     "Cannot destruct given oneof") ) );
                    ],
                    function
                    | `not_set -> failwith "Impossible case"
                    | `Single _ -> 0
                    | `Multi _ -> 1 )
                ^:: nil)

            let to_proto' =
              let serialize =
                Runtime'.apply_lazy (fun () ->
                    Runtime'.Serialize.serialize (spec ()))
              in
              fun writer sum -> serialize writer sum

            let to_proto t =
              let writer = Runtime'.Writer.init () in
              to_proto' writer t;
              writer

            let from_proto_exn =
              let constructor sum = sum in
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Deserialize.deserialize (spec ()) constructor)

            let from_proto writer =
              Runtime'.Result.catch (fun () -> from_proto_exn writer)

            let to_json options =
              let serialize =
                Runtime'.Serialize_json.serialize ~message_name:(name ())
                  (spec ()) options
              in
              fun sum -> serialize sum

            let from_json_exn =
              let constructor sum = sum in
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                    (spec ()) constructor)

            let from_json json =
              Runtime'.Result.catch (fun () -> from_json_exn json)
          end

          let name () = ".cosmos.tx.signing.v1beta1.SignatureDescriptor"

          type t = {
            public_key :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
            data : Data.t option;
            sequence : int64;
          }

          type make_t =
            ?public_key:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?data:Data.t ->
            ?sequence:int64 ->
            unit ->
            t

          let make ?public_key ?data ?(sequence = 0L) () =
            { public_key; data; sequence }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_public_key =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt
                         ( (1, "public_key", "publicKey"),
                           message
                             (module Imported'modules.Google_protobuf_any.Google
                                     .Protobuf
                                     .Any) ))
                 in
                 let merge_data =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt ((2, "data", "data"), message (module Data)))
                 in
                 let merge_sequence =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((3, "sequence", "sequence"), uint64, 0L))
                 in
                 fun t1 t2 ->
                   {
                     public_key = merge_public_key t1.public_key t2.public_key;
                     data = merge_data t1.data t2.data;
                     sequence = merge_sequence t1.sequence t2.sequence;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic_opt
                ( (1, "public_key", "publicKey"),
                  message
                    (module Imported'modules.Google_protobuf_any.Google.Protobuf
                            .Any) )
              ^:: basic_opt ((2, "data", "data"), message (module Data))
              ^:: basic ((3, "sequence", "sequence"), uint64, 0L)
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { public_key; data; sequence } ->
              serialize writer public_key data sequence

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor public_key data sequence =
              { public_key; data; sequence }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { public_key; data; sequence } ->
              serialize public_key data sequence

          let from_json_exn =
            let constructor public_key data sequence =
              { public_key; data; sequence }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end
      end
    end
  end
end

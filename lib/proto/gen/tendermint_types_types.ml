(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: tendermint/types/types.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Gogoproto_gogo = Gogoproto_gogo
  module Google_protobuf_timestamp = Google_protobuf_timestamp
  module Tendermint_crypto_proof = Tendermint_crypto_proof
  module Tendermint_version_types = Tendermint_version_types
  module Tendermint_types_validator = Tendermint_types_validator
end

(**/**)

module rec Tendermint : sig
  module rec Types : sig
    (** {%html:
<p>SignedMsgType is a type of signed message in the consensus.</p>
%}
    *)
    module rec SignedMsgType : sig
      type t =
        | SIGNED_MSG_TYPE_UNKNOWN
        | SIGNED_MSG_TYPE_PREVOTE  (** {%html:
<p>Votes</p>
%} *)
        | SIGNED_MSG_TYPE_PRECOMMIT
        | SIGNED_MSG_TYPE_PROPOSAL  (** {%html:
<p>Proposals</p>
%} *)

      val name : unit -> string
      (** Fully qualified protobuf name of this enum *)

      (**/**)

      val to_int : t -> int
      val from_int : int -> t Runtime'.Result.t
      val from_int_exn : int -> t
      val to_string : t -> string
      val from_string_exn : string -> t

      (**/**)
    end

    (** {%html:
<p>PartsetHeader</p>
%} *)
    and PartSetHeader : sig
      type t = { total : int; hash : bytes }

      val make : ?total:int -> ?hash:bytes -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?total:int -> ?hash:bytes -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and Part : sig
      type t = {
        index : int;
        bytes : bytes;
        proof :
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t
          option;
      }

      val make :
        ?index:int ->
        ?bytes:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?index:int ->
        ?bytes:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>BlockID</p>
%} *)
    and BlockID : sig
      type t = { hash : bytes; part_set_header : PartSetHeader.t option }

      val make : ?hash:bytes -> ?part_set_header:PartSetHeader.t -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?hash:bytes -> ?part_set_header:PartSetHeader.t -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>Header defines the structure of a block header.</p>
<hr />
%}
    *)
    and Header : sig
      type t = {
        version :
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t
          option;
            (** {%html:
<p>basic block info</p>
%} *)
        chain_id : string;
        height : int64;
        time :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        last_block_id : BlockID.t option;
            (** {%html:
<p>prev block info</p>
%} *)
        last_commit_hash : bytes;
            (** {%html:
<p>hashes of block data</p>
<p>commit from validators from the last block</p>
%}
            *)
        data_hash : bytes;  (** {%html:
<p>transactions</p>
%} *)
        validators_hash : bytes;
            (** {%html:
<p>hashes from the app output from the prev block</p>
<p>validators for the current block</p>
%}
            *)
        next_validators_hash : bytes;
            (** {%html:
<p>validators for the next block</p>
%} *)
        consensus_hash : bytes;
            (** {%html:
<p>consensus params for current block</p>
%} *)
        app_hash : bytes;
            (** {%html:
<p>state after txs from the previous block</p>
%} *)
        last_results_hash : bytes;
            (** {%html:
<p>root hash of all results from the txs from the previous block</p>
%}
            *)
        evidence_hash : bytes;
            (** {%html:
<p>consensus info</p>
<p>evidence included in the block</p>
%}
            *)
        proposer_address : bytes;
            (** {%html:
<p>original proposer of the block</p>
%} *)
      }

      val make :
        ?version:
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t ->
        ?chain_id:string ->
        ?height:int64 ->
        ?time:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?last_block_id:BlockID.t ->
        ?last_commit_hash:bytes ->
        ?data_hash:bytes ->
        ?validators_hash:bytes ->
        ?next_validators_hash:bytes ->
        ?consensus_hash:bytes ->
        ?app_hash:bytes ->
        ?last_results_hash:bytes ->
        ?evidence_hash:bytes ->
        ?proposer_address:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?version:
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t ->
        ?chain_id:string ->
        ?height:int64 ->
        ?time:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?last_block_id:BlockID.t ->
        ?last_commit_hash:bytes ->
        ?data_hash:bytes ->
        ?validators_hash:bytes ->
        ?next_validators_hash:bytes ->
        ?consensus_hash:bytes ->
        ?app_hash:bytes ->
        ?last_results_hash:bytes ->
        ?evidence_hash:bytes ->
        ?proposer_address:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>Data contains the set of transactions included in the block</p>
%}
    *)
    and Data : sig
      type t = bytes list
      (** {%html:
<p>Txs that will be applied by state @ block.Height+1.
NOTE: not all txs here are valid.  We're just agreeing on the order first.
This means that block.AppHash does not include these txs.</p>
%}
      *)

      val make : ?txs:bytes list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?txs:bytes list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>Vote represents a prevote or precommit vote from validators for
consensus.</p>
%}
    *)
    and Vote : sig
      type t = {
        type' : SignedMsgType.t;
        height : int64;
        round : int;
        block_id : BlockID.t option;
            (** {%html:
<p>zero if vote is nil.</p>
%} *)
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        validator_address : bytes;
        validator_index : int;
        signature : bytes;
            (** {%html:
<p>Vote signature by the validator if they participated in consensus for the
associated block.</p>
%}
            *)
        extension : bytes;
            (** {%html:
<p>Vote extension provided by the application. Only valid for precommit
messages.</p>
%}
            *)
        extension_signature : bytes;
            (** {%html:
<p>Vote extension signature by the validator if they participated in
consensus for the associated block.
Only valid for precommit messages.</p>
%}
            *)
      }

      val make :
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?validator_address:bytes ->
        ?validator_index:int ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?validator_address:bytes ->
        ?validator_index:int ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>Commit contains the evidence that a block was committed by a set of validators.</p>
%}
    *)
    and Commit : sig
      type t = {
        height : int64;
        round : int;
        block_id : BlockID.t option;
        signatures : CommitSig.t list;
      }

      val make :
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?signatures:CommitSig.t list ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?signatures:CommitSig.t list ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>CommitSig is a part of the Vote included in a Commit.</p>
%}
    *)
    and CommitSig : sig
      type t = {
        block_id_flag :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t;
        validator_address : bytes;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
      }

      val make :
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and ExtendedCommit : sig
      type t = {
        height : int64;
        round : int;
        block_id : BlockID.t option;
        extended_signatures : ExtendedCommitSig.t list;
      }

      val make :
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?extended_signatures:ExtendedCommitSig.t list ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?extended_signatures:ExtendedCommitSig.t list ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>ExtendedCommitSig retains all the same fields as CommitSig but adds vote
extension-related fields. We use two signatures to ensure backwards compatibility.
That is the digest of the original signature is still the same in prior versions</p>
%}
    *)
    and ExtendedCommitSig : sig
      type t = {
        block_id_flag :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t;
        validator_address : bytes;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
        extension : bytes;  (** {%html:
<p>Vote extension data</p>
%} *)
        extension_signature : bytes;
            (** {%html:
<p>Vote extension signature</p>
%} *)
      }

      val make :
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and Proposal : sig
      type t = {
        type' : SignedMsgType.t;
        height : int64;
        round : int;
        pol_round : int;
        block_id : BlockID.t option;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
      }

      val make :
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?pol_round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?pol_round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and SignedHeader : sig
      type t = { header : Header.t option; commit : Commit.t option }

      val make : ?header:Header.t -> ?commit:Commit.t -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?header:Header.t -> ?commit:Commit.t -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and LightBlock : sig
      type t = {
        signed_header : SignedHeader.t option;
        validator_set :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t
          option;
      }

      val make :
        ?signed_header:SignedHeader.t ->
        ?validator_set:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?signed_header:SignedHeader.t ->
        ?validator_set:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and BlockMeta : sig
      type t = {
        block_id : BlockID.t option;
        block_size : int64;
        header : Header.t option;
        num_txs : int64;
      }

      val make :
        ?block_id:BlockID.t ->
        ?block_size:int64 ->
        ?header:Header.t ->
        ?num_txs:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block_id:BlockID.t ->
        ?block_size:int64 ->
        ?header:Header.t ->
        ?num_txs:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>TxProof represents a Merkle proof of the presence of a transaction in the Merkle tree.</p>
%}
    *)
    and TxProof : sig
      type t = {
        root_hash : bytes;
        data : bytes;
        proof :
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t
          option;
      }

      val make :
        ?root_hash:bytes ->
        ?data:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?root_hash:bytes ->
        ?data:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end
  end
end = struct
  module rec Types : sig
    (** {%html:
<p>SignedMsgType is a type of signed message in the consensus.</p>
%}
    *)
    module rec SignedMsgType : sig
      type t =
        | SIGNED_MSG_TYPE_UNKNOWN
        | SIGNED_MSG_TYPE_PREVOTE  (** {%html:
<p>Votes</p>
%} *)
        | SIGNED_MSG_TYPE_PRECOMMIT
        | SIGNED_MSG_TYPE_PROPOSAL  (** {%html:
<p>Proposals</p>
%} *)

      val name : unit -> string
      (** Fully qualified protobuf name of this enum *)

      (**/**)

      val to_int : t -> int
      val from_int : int -> t Runtime'.Result.t
      val from_int_exn : int -> t
      val to_string : t -> string
      val from_string_exn : string -> t

      (**/**)
    end

    (** {%html:
<p>PartsetHeader</p>
%} *)
    and PartSetHeader : sig
      type t = { total : int; hash : bytes }

      val make : ?total:int -> ?hash:bytes -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?total:int -> ?hash:bytes -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and Part : sig
      type t = {
        index : int;
        bytes : bytes;
        proof :
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t
          option;
      }

      val make :
        ?index:int ->
        ?bytes:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?index:int ->
        ?bytes:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>BlockID</p>
%} *)
    and BlockID : sig
      type t = { hash : bytes; part_set_header : PartSetHeader.t option }

      val make : ?hash:bytes -> ?part_set_header:PartSetHeader.t -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?hash:bytes -> ?part_set_header:PartSetHeader.t -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>Header defines the structure of a block header.</p>
<hr />
%}
    *)
    and Header : sig
      type t = {
        version :
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t
          option;
            (** {%html:
<p>basic block info</p>
%} *)
        chain_id : string;
        height : int64;
        time :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        last_block_id : BlockID.t option;
            (** {%html:
<p>prev block info</p>
%} *)
        last_commit_hash : bytes;
            (** {%html:
<p>hashes of block data</p>
<p>commit from validators from the last block</p>
%}
            *)
        data_hash : bytes;  (** {%html:
<p>transactions</p>
%} *)
        validators_hash : bytes;
            (** {%html:
<p>hashes from the app output from the prev block</p>
<p>validators for the current block</p>
%}
            *)
        next_validators_hash : bytes;
            (** {%html:
<p>validators for the next block</p>
%} *)
        consensus_hash : bytes;
            (** {%html:
<p>consensus params for current block</p>
%} *)
        app_hash : bytes;
            (** {%html:
<p>state after txs from the previous block</p>
%} *)
        last_results_hash : bytes;
            (** {%html:
<p>root hash of all results from the txs from the previous block</p>
%}
            *)
        evidence_hash : bytes;
            (** {%html:
<p>consensus info</p>
<p>evidence included in the block</p>
%}
            *)
        proposer_address : bytes;
            (** {%html:
<p>original proposer of the block</p>
%} *)
      }

      val make :
        ?version:
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t ->
        ?chain_id:string ->
        ?height:int64 ->
        ?time:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?last_block_id:BlockID.t ->
        ?last_commit_hash:bytes ->
        ?data_hash:bytes ->
        ?validators_hash:bytes ->
        ?next_validators_hash:bytes ->
        ?consensus_hash:bytes ->
        ?app_hash:bytes ->
        ?last_results_hash:bytes ->
        ?evidence_hash:bytes ->
        ?proposer_address:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?version:
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t ->
        ?chain_id:string ->
        ?height:int64 ->
        ?time:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?last_block_id:BlockID.t ->
        ?last_commit_hash:bytes ->
        ?data_hash:bytes ->
        ?validators_hash:bytes ->
        ?next_validators_hash:bytes ->
        ?consensus_hash:bytes ->
        ?app_hash:bytes ->
        ?last_results_hash:bytes ->
        ?evidence_hash:bytes ->
        ?proposer_address:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>Data contains the set of transactions included in the block</p>
%}
    *)
    and Data : sig
      type t = bytes list
      (** {%html:
<p>Txs that will be applied by state @ block.Height+1.
NOTE: not all txs here are valid.  We're just agreeing on the order first.
This means that block.AppHash does not include these txs.</p>
%}
      *)

      val make : ?txs:bytes list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?txs:bytes list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>Vote represents a prevote or precommit vote from validators for
consensus.</p>
%}
    *)
    and Vote : sig
      type t = {
        type' : SignedMsgType.t;
        height : int64;
        round : int;
        block_id : BlockID.t option;
            (** {%html:
<p>zero if vote is nil.</p>
%} *)
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        validator_address : bytes;
        validator_index : int;
        signature : bytes;
            (** {%html:
<p>Vote signature by the validator if they participated in consensus for the
associated block.</p>
%}
            *)
        extension : bytes;
            (** {%html:
<p>Vote extension provided by the application. Only valid for precommit
messages.</p>
%}
            *)
        extension_signature : bytes;
            (** {%html:
<p>Vote extension signature by the validator if they participated in
consensus for the associated block.
Only valid for precommit messages.</p>
%}
            *)
      }

      val make :
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?validator_address:bytes ->
        ?validator_index:int ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?validator_address:bytes ->
        ?validator_index:int ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>Commit contains the evidence that a block was committed by a set of validators.</p>
%}
    *)
    and Commit : sig
      type t = {
        height : int64;
        round : int;
        block_id : BlockID.t option;
        signatures : CommitSig.t list;
      }

      val make :
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?signatures:CommitSig.t list ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?signatures:CommitSig.t list ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>CommitSig is a part of the Vote included in a Commit.</p>
%}
    *)
    and CommitSig : sig
      type t = {
        block_id_flag :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t;
        validator_address : bytes;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
      }

      val make :
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and ExtendedCommit : sig
      type t = {
        height : int64;
        round : int;
        block_id : BlockID.t option;
        extended_signatures : ExtendedCommitSig.t list;
      }

      val make :
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?extended_signatures:ExtendedCommitSig.t list ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?extended_signatures:ExtendedCommitSig.t list ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>ExtendedCommitSig retains all the same fields as CommitSig but adds vote
extension-related fields. We use two signatures to ensure backwards compatibility.
That is the digest of the original signature is still the same in prior versions</p>
%}
    *)
    and ExtendedCommitSig : sig
      type t = {
        block_id_flag :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t;
        validator_address : bytes;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
        extension : bytes;  (** {%html:
<p>Vote extension data</p>
%} *)
        extension_signature : bytes;
            (** {%html:
<p>Vote extension signature</p>
%} *)
      }

      val make :
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and Proposal : sig
      type t = {
        type' : SignedMsgType.t;
        height : int64;
        round : int;
        pol_round : int;
        block_id : BlockID.t option;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
      }

      val make :
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?pol_round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?pol_round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and SignedHeader : sig
      type t = { header : Header.t option; commit : Commit.t option }

      val make : ?header:Header.t -> ?commit:Commit.t -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?header:Header.t -> ?commit:Commit.t -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and LightBlock : sig
      type t = {
        signed_header : SignedHeader.t option;
        validator_set :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t
          option;
      }

      val make :
        ?signed_header:SignedHeader.t ->
        ?validator_set:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?signed_header:SignedHeader.t ->
        ?validator_set:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and BlockMeta : sig
      type t = {
        block_id : BlockID.t option;
        block_size : int64;
        header : Header.t option;
        num_txs : int64;
      }

      val make :
        ?block_id:BlockID.t ->
        ?block_size:int64 ->
        ?header:Header.t ->
        ?num_txs:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block_id:BlockID.t ->
        ?block_size:int64 ->
        ?header:Header.t ->
        ?num_txs:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>TxProof represents a Merkle proof of the presence of a transaction in the Merkle tree.</p>
%}
    *)
    and TxProof : sig
      type t = {
        root_hash : bytes;
        data : bytes;
        proof :
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t
          option;
      }

      val make :
        ?root_hash:bytes ->
        ?data:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?root_hash:bytes ->
        ?data:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end
  end = struct
    module rec SignedMsgType : sig
      type t =
        | SIGNED_MSG_TYPE_UNKNOWN
        | SIGNED_MSG_TYPE_PREVOTE  (** {%html:
<p>Votes</p>
%} *)
        | SIGNED_MSG_TYPE_PRECOMMIT
        | SIGNED_MSG_TYPE_PROPOSAL  (** {%html:
<p>Proposals</p>
%} *)

      val name : unit -> string
      (** Fully qualified protobuf name of this enum *)

      (**/**)

      val to_int : t -> int
      val from_int : int -> t Runtime'.Result.t
      val from_int_exn : int -> t
      val to_string : t -> string
      val from_string_exn : string -> t

      (**/**)
    end = struct
      module This'_ = SignedMsgType

      type t =
        | SIGNED_MSG_TYPE_UNKNOWN
        | SIGNED_MSG_TYPE_PREVOTE  (** {%html:
<p>Votes</p>
%} *)
        | SIGNED_MSG_TYPE_PRECOMMIT
        | SIGNED_MSG_TYPE_PROPOSAL  (** {%html:
<p>Proposals</p>
%} *)

      let name () = ".tendermint.types.SignedMsgType"

      let to_int = function
        | SIGNED_MSG_TYPE_UNKNOWN -> 0
        | SIGNED_MSG_TYPE_PREVOTE -> 1
        | SIGNED_MSG_TYPE_PRECOMMIT -> 2
        | SIGNED_MSG_TYPE_PROPOSAL -> 32

      let from_int_exn = function
        | 0 -> SIGNED_MSG_TYPE_UNKNOWN
        | 1 -> SIGNED_MSG_TYPE_PREVOTE
        | 2 -> SIGNED_MSG_TYPE_PRECOMMIT
        | 32 -> SIGNED_MSG_TYPE_PROPOSAL
        | n -> Runtime'.Result.raise (`Unknown_enum_value n)

      let from_int e = Runtime'.Result.catch (fun () -> from_int_exn e)

      let to_string = function
        | SIGNED_MSG_TYPE_UNKNOWN -> "SIGNED_MSG_TYPE_UNKNOWN"
        | SIGNED_MSG_TYPE_PREVOTE -> "SIGNED_MSG_TYPE_PREVOTE"
        | SIGNED_MSG_TYPE_PRECOMMIT -> "SIGNED_MSG_TYPE_PRECOMMIT"
        | SIGNED_MSG_TYPE_PROPOSAL -> "SIGNED_MSG_TYPE_PROPOSAL"

      let from_string_exn = function
        | "SIGNED_MSG_TYPE_UNKNOWN" -> SIGNED_MSG_TYPE_UNKNOWN
        | "SIGNED_MSG_TYPE_PREVOTE" -> SIGNED_MSG_TYPE_PREVOTE
        | "SIGNED_MSG_TYPE_PRECOMMIT" -> SIGNED_MSG_TYPE_PRECOMMIT
        | "SIGNED_MSG_TYPE_PROPOSAL" -> SIGNED_MSG_TYPE_PROPOSAL
        | s -> Runtime'.Result.raise (`Unknown_enum_name s)
    end

    and PartSetHeader : sig
      type t = { total : int; hash : bytes }

      val make : ?total:int -> ?hash:bytes -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?total:int -> ?hash:bytes -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = PartSetHeader

      let name () = ".tendermint.types.PartSetHeader"

      type t = { total : int; hash : bytes }
      type make_t = ?total:int -> ?hash:bytes -> unit -> t

      let make ?(total = 0) ?(hash = Bytes.of_string {||}) () = { total; hash }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_total =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((1, "total", "total"), uint32_int, 0))
             in
             let merge_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((2, "hash", "hash"), bytes, Bytes.of_string {||}))
             in
             fun t1 t2 ->
               {
                 total = merge_total t1.total t2.total;
                 hash = merge_hash t1.hash t2.hash;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "total", "total"), uint32_int, 0)
          ^:: basic ((2, "hash", "hash"), bytes, Bytes.of_string {||})
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { total; hash } -> serialize writer total hash

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor total hash = { total; hash } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { total; hash } -> serialize total hash

      let from_json_exn =
        let constructor total hash = { total; hash } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and Part : sig
      type t = {
        index : int;
        bytes : bytes;
        proof :
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t
          option;
      }

      val make :
        ?index:int ->
        ?bytes:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?index:int ->
        ?bytes:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = Part

      let name () = ".tendermint.types.Part"

      type t = {
        index : int;
        bytes : bytes;
        proof :
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t
          option;
      }

      type make_t =
        ?index:int ->
        ?bytes:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t

      let make ?(index = 0) ?(bytes = Bytes.of_string {||}) ?proof () =
        { index; bytes; proof }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_index =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((1, "index", "index"), uint32_int, 0))
             in
             let merge_bytes =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((2, "bytes", "bytes"), bytes, Bytes.of_string {||}))
             in
             let merge_proof =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (3, "proof", "proof"),
                       message
                         (module Imported'modules.Tendermint_crypto_proof
                                 .Tendermint
                                 .Crypto
                                 .Proof) ))
             in
             fun t1 t2 ->
               {
                 index = merge_index t1.index t2.index;
                 bytes = merge_bytes t1.bytes t2.bytes;
                 proof = merge_proof t1.proof t2.proof;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "index", "index"), uint32_int, 0)
          ^:: basic ((2, "bytes", "bytes"), bytes, Bytes.of_string {||})
          ^:: basic_opt
                ( (3, "proof", "proof"),
                  message
                    (module Imported'modules.Tendermint_crypto_proof.Tendermint
                            .Crypto
                            .Proof) )
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { index; bytes; proof } -> serialize writer index bytes proof

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor index bytes proof = { index; bytes; proof } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { index; bytes; proof } -> serialize index bytes proof

      let from_json_exn =
        let constructor index bytes proof = { index; bytes; proof } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and BlockID : sig
      type t = { hash : bytes; part_set_header : PartSetHeader.t option }

      val make : ?hash:bytes -> ?part_set_header:PartSetHeader.t -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?hash:bytes -> ?part_set_header:PartSetHeader.t -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = BlockID

      let name () = ".tendermint.types.BlockID"

      type t = { hash : bytes; part_set_header : PartSetHeader.t option }
      type make_t = ?hash:bytes -> ?part_set_header:PartSetHeader.t -> unit -> t

      let make ?(hash = Bytes.of_string {||}) ?part_set_header () =
        { hash; part_set_header }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((1, "hash", "hash"), bytes, Bytes.of_string {||}))
             in
             let merge_part_set_header =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (2, "part_set_header", "partSetHeader"),
                       message (module PartSetHeader) ))
             in
             fun t1 t2 ->
               {
                 hash = merge_hash t1.hash t2.hash;
                 part_set_header =
                   merge_part_set_header t1.part_set_header t2.part_set_header;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "hash", "hash"), bytes, Bytes.of_string {||})
          ^:: basic_opt
                ( (2, "part_set_header", "partSetHeader"),
                  message (module PartSetHeader) )
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { hash; part_set_header } ->
          serialize writer hash part_set_header

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor hash part_set_header = { hash; part_set_header } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { hash; part_set_header } -> serialize hash part_set_header

      let from_json_exn =
        let constructor hash part_set_header = { hash; part_set_header } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and Header : sig
      type t = {
        version :
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t
          option;
            (** {%html:
<p>basic block info</p>
%} *)
        chain_id : string;
        height : int64;
        time :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        last_block_id : BlockID.t option;
            (** {%html:
<p>prev block info</p>
%} *)
        last_commit_hash : bytes;
            (** {%html:
<p>hashes of block data</p>
<p>commit from validators from the last block</p>
%}
            *)
        data_hash : bytes;  (** {%html:
<p>transactions</p>
%} *)
        validators_hash : bytes;
            (** {%html:
<p>hashes from the app output from the prev block</p>
<p>validators for the current block</p>
%}
            *)
        next_validators_hash : bytes;
            (** {%html:
<p>validators for the next block</p>
%} *)
        consensus_hash : bytes;
            (** {%html:
<p>consensus params for current block</p>
%} *)
        app_hash : bytes;
            (** {%html:
<p>state after txs from the previous block</p>
%} *)
        last_results_hash : bytes;
            (** {%html:
<p>root hash of all results from the txs from the previous block</p>
%}
            *)
        evidence_hash : bytes;
            (** {%html:
<p>consensus info</p>
<p>evidence included in the block</p>
%}
            *)
        proposer_address : bytes;
            (** {%html:
<p>original proposer of the block</p>
%} *)
      }

      val make :
        ?version:
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t ->
        ?chain_id:string ->
        ?height:int64 ->
        ?time:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?last_block_id:BlockID.t ->
        ?last_commit_hash:bytes ->
        ?data_hash:bytes ->
        ?validators_hash:bytes ->
        ?next_validators_hash:bytes ->
        ?consensus_hash:bytes ->
        ?app_hash:bytes ->
        ?last_results_hash:bytes ->
        ?evidence_hash:bytes ->
        ?proposer_address:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?version:
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t ->
        ?chain_id:string ->
        ?height:int64 ->
        ?time:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?last_block_id:BlockID.t ->
        ?last_commit_hash:bytes ->
        ?data_hash:bytes ->
        ?validators_hash:bytes ->
        ?next_validators_hash:bytes ->
        ?consensus_hash:bytes ->
        ?app_hash:bytes ->
        ?last_results_hash:bytes ->
        ?evidence_hash:bytes ->
        ?proposer_address:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = Header

      let name () = ".tendermint.types.Header"

      type t = {
        version :
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t
          option;
        chain_id : string;
        height : int64;
        time :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        last_block_id : BlockID.t option;
        last_commit_hash : bytes;
        data_hash : bytes;
        validators_hash : bytes;
        next_validators_hash : bytes;
        consensus_hash : bytes;
        app_hash : bytes;
        last_results_hash : bytes;
        evidence_hash : bytes;
        proposer_address : bytes;
      }

      type make_t =
        ?version:
          Imported'modules.Tendermint_version_types.Tendermint.Version.Consensus
          .t ->
        ?chain_id:string ->
        ?height:int64 ->
        ?time:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?last_block_id:BlockID.t ->
        ?last_commit_hash:bytes ->
        ?data_hash:bytes ->
        ?validators_hash:bytes ->
        ?next_validators_hash:bytes ->
        ?consensus_hash:bytes ->
        ?app_hash:bytes ->
        ?last_results_hash:bytes ->
        ?evidence_hash:bytes ->
        ?proposer_address:bytes ->
        unit ->
        t

      let make ?version ?(chain_id = {||}) ?(height = 0L) ?time ?last_block_id
          ?(last_commit_hash = Bytes.of_string {||})
          ?(data_hash = Bytes.of_string {||})
          ?(validators_hash = Bytes.of_string {||})
          ?(next_validators_hash = Bytes.of_string {||})
          ?(consensus_hash = Bytes.of_string {||})
          ?(app_hash = Bytes.of_string {||})
          ?(last_results_hash = Bytes.of_string {||})
          ?(evidence_hash = Bytes.of_string {||})
          ?(proposer_address = Bytes.of_string {||}) () =
        {
          version;
          chain_id;
          height;
          time;
          last_block_id;
          last_commit_hash;
          data_hash;
          validators_hash;
          next_validators_hash;
          consensus_hash;
          app_hash;
          last_results_hash;
          evidence_hash;
          proposer_address;
        }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_version =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (1, "version", "version"),
                       message
                         (module Imported'modules.Tendermint_version_types
                                 .Tendermint
                                 .Version
                                 .Consensus) ))
             in
             let merge_chain_id =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((2, "chain_id", "chainId"), string, {||}))
             in
             let merge_height =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((3, "height", "height"), int64, 0L))
             in
             let merge_time =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (4, "time", "time"),
                       message
                         (module Imported'modules.Google_protobuf_timestamp
                                 .Google
                                 .Protobuf
                                 .Timestamp) ))
             in
             let merge_last_block_id =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (5, "last_block_id", "lastBlockId"),
                       message (module BlockID) ))
             in
             let merge_last_commit_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (6, "last_commit_hash", "lastCommitHash"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             let merge_data_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((7, "data_hash", "dataHash"), bytes, Bytes.of_string {||}))
             in
             let merge_validators_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (8, "validators_hash", "validatorsHash"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             let merge_next_validators_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (9, "next_validators_hash", "nextValidatorsHash"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             let merge_consensus_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (10, "consensus_hash", "consensusHash"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             let merge_app_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((11, "app_hash", "appHash"), bytes, Bytes.of_string {||}))
             in
             let merge_last_results_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (12, "last_results_hash", "lastResultsHash"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             let merge_evidence_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (13, "evidence_hash", "evidenceHash"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             let merge_proposer_address =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (14, "proposer_address", "proposerAddress"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             fun t1 t2 ->
               {
                 version = merge_version t1.version t2.version;
                 chain_id = merge_chain_id t1.chain_id t2.chain_id;
                 height = merge_height t1.height t2.height;
                 time = merge_time t1.time t2.time;
                 last_block_id =
                   merge_last_block_id t1.last_block_id t2.last_block_id;
                 last_commit_hash =
                   merge_last_commit_hash t1.last_commit_hash
                     t2.last_commit_hash;
                 data_hash = merge_data_hash t1.data_hash t2.data_hash;
                 validators_hash =
                   merge_validators_hash t1.validators_hash t2.validators_hash;
                 next_validators_hash =
                   merge_next_validators_hash t1.next_validators_hash
                     t2.next_validators_hash;
                 consensus_hash =
                   merge_consensus_hash t1.consensus_hash t2.consensus_hash;
                 app_hash = merge_app_hash t1.app_hash t2.app_hash;
                 last_results_hash =
                   merge_last_results_hash t1.last_results_hash
                     t2.last_results_hash;
                 evidence_hash =
                   merge_evidence_hash t1.evidence_hash t2.evidence_hash;
                 proposer_address =
                   merge_proposer_address t1.proposer_address
                     t2.proposer_address;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic_opt
            ( (1, "version", "version"),
              message
                (module Imported'modules.Tendermint_version_types.Tendermint
                        .Version
                        .Consensus) )
          ^:: basic ((2, "chain_id", "chainId"), string, {||})
          ^:: basic ((3, "height", "height"), int64, 0L)
          ^:: basic_opt
                ( (4, "time", "time"),
                  message
                    (module Imported'modules.Google_protobuf_timestamp.Google
                            .Protobuf
                            .Timestamp) )
          ^:: basic_opt
                ((5, "last_block_id", "lastBlockId"), message (module BlockID))
          ^:: basic
                ( (6, "last_commit_hash", "lastCommitHash"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: basic ((7, "data_hash", "dataHash"), bytes, Bytes.of_string {||})
          ^:: basic
                ( (8, "validators_hash", "validatorsHash"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: basic
                ( (9, "next_validators_hash", "nextValidatorsHash"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: basic
                ( (10, "consensus_hash", "consensusHash"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: basic ((11, "app_hash", "appHash"), bytes, Bytes.of_string {||})
          ^:: basic
                ( (12, "last_results_hash", "lastResultsHash"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: basic
                ( (13, "evidence_hash", "evidenceHash"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: basic
                ( (14, "proposer_address", "proposerAddress"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer
          {
            version;
            chain_id;
            height;
            time;
            last_block_id;
            last_commit_hash;
            data_hash;
            validators_hash;
            next_validators_hash;
            consensus_hash;
            app_hash;
            last_results_hash;
            evidence_hash;
            proposer_address;
          }
        ->
          serialize writer version chain_id height time last_block_id
            last_commit_hash data_hash validators_hash next_validators_hash
            consensus_hash app_hash last_results_hash evidence_hash
            proposer_address

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor version chain_id height time last_block_id
            last_commit_hash data_hash validators_hash next_validators_hash
            consensus_hash app_hash last_results_hash evidence_hash
            proposer_address =
          {
            version;
            chain_id;
            height;
            time;
            last_block_id;
            last_commit_hash;
            data_hash;
            validators_hash;
            next_validators_hash;
            consensus_hash;
            app_hash;
            last_results_hash;
            evidence_hash;
            proposer_address;
          }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun {
              version;
              chain_id;
              height;
              time;
              last_block_id;
              last_commit_hash;
              data_hash;
              validators_hash;
              next_validators_hash;
              consensus_hash;
              app_hash;
              last_results_hash;
              evidence_hash;
              proposer_address;
            }
        ->
          serialize version chain_id height time last_block_id last_commit_hash
            data_hash validators_hash next_validators_hash consensus_hash
            app_hash last_results_hash evidence_hash proposer_address

      let from_json_exn =
        let constructor version chain_id height time last_block_id
            last_commit_hash data_hash validators_hash next_validators_hash
            consensus_hash app_hash last_results_hash evidence_hash
            proposer_address =
          {
            version;
            chain_id;
            height;
            time;
            last_block_id;
            last_commit_hash;
            data_hash;
            validators_hash;
            next_validators_hash;
            consensus_hash;
            app_hash;
            last_results_hash;
            evidence_hash;
            proposer_address;
          }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and Data : sig
      type t = bytes list
      (** {%html:
<p>Txs that will be applied by state @ block.Height+1.
NOTE: not all txs here are valid.  We're just agreeing on the order first.
This means that block.AppHash does not include these txs.</p>
%}
      *)

      val make : ?txs:bytes list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?txs:bytes list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = Data

      let name () = ".tendermint.types.Data"

      type t = bytes list
      type make_t = ?txs:bytes list -> unit -> t

      let make ?(txs = []) () = txs

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_txs =
               Runtime'.Merge.merge
                 Runtime'.Spec.(repeated ((1, "txs", "txs"), bytes, not_packed))
             in
             fun t1_txs t2_txs -> merge_txs t1_txs t2_txs)
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(repeated ((1, "txs", "txs"), bytes, not_packed) ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer txs -> serialize writer txs

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor txs = txs in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun txs -> serialize txs

      let from_json_exn =
        let constructor txs = txs in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and Vote : sig
      type t = {
        type' : SignedMsgType.t;
        height : int64;
        round : int;
        block_id : BlockID.t option;
            (** {%html:
<p>zero if vote is nil.</p>
%} *)
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        validator_address : bytes;
        validator_index : int;
        signature : bytes;
            (** {%html:
<p>Vote signature by the validator if they participated in consensus for the
associated block.</p>
%}
            *)
        extension : bytes;
            (** {%html:
<p>Vote extension provided by the application. Only valid for precommit
messages.</p>
%}
            *)
        extension_signature : bytes;
            (** {%html:
<p>Vote extension signature by the validator if they participated in
consensus for the associated block.
Only valid for precommit messages.</p>
%}
            *)
      }

      val make :
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?validator_address:bytes ->
        ?validator_index:int ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?validator_address:bytes ->
        ?validator_index:int ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = Vote

      let name () = ".tendermint.types.Vote"

      type t = {
        type' : SignedMsgType.t;
        height : int64;
        round : int;
        block_id : BlockID.t option;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        validator_address : bytes;
        validator_index : int;
        signature : bytes;
        extension : bytes;
        extension_signature : bytes;
      }

      type make_t =
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?validator_address:bytes ->
        ?validator_index:int ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t

      let make ?(type' = SignedMsgType.from_int_exn 0) ?(height = 0L)
          ?(round = 0) ?block_id ?timestamp
          ?(validator_address = Bytes.of_string {||}) ?(validator_index = 0)
          ?(signature = Bytes.of_string {||})
          ?(extension = Bytes.of_string {||})
          ?(extension_signature = Bytes.of_string {||}) () =
        {
          type';
          height;
          round;
          block_id;
          timestamp;
          validator_address;
          validator_index;
          signature;
          extension;
          extension_signature;
        }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_type' =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (1, "type", "type"),
                       enum (module SignedMsgType),
                       SignedMsgType.from_int_exn 0 ))
             in
             let merge_height =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((2, "height", "height"), int64, 0L))
             in
             let merge_round =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((3, "round", "round"), int32_int, 0))
             in
             let merge_block_id =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ((4, "block_id", "blockId"), message (module BlockID)))
             in
             let merge_timestamp =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (5, "timestamp", "timestamp"),
                       message
                         (module Imported'modules.Google_protobuf_timestamp
                                 .Google
                                 .Protobuf
                                 .Timestamp) ))
             in
             let merge_validator_address =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (6, "validator_address", "validatorAddress"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             let merge_validator_index =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((7, "validator_index", "validatorIndex"), int32_int, 0))
             in
             let merge_signature =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((8, "signature", "signature"), bytes, Bytes.of_string {||}))
             in
             let merge_extension =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((9, "extension", "extension"), bytes, Bytes.of_string {||}))
             in
             let merge_extension_signature =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (10, "extension_signature", "extensionSignature"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             fun t1 t2 ->
               {
                 type' = merge_type' t1.type' t2.type';
                 height = merge_height t1.height t2.height;
                 round = merge_round t1.round t2.round;
                 block_id = merge_block_id t1.block_id t2.block_id;
                 timestamp = merge_timestamp t1.timestamp t2.timestamp;
                 validator_address =
                   merge_validator_address t1.validator_address
                     t2.validator_address;
                 validator_index =
                   merge_validator_index t1.validator_index t2.validator_index;
                 signature = merge_signature t1.signature t2.signature;
                 extension = merge_extension t1.extension t2.extension;
                 extension_signature =
                   merge_extension_signature t1.extension_signature
                     t2.extension_signature;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic
            ( (1, "type", "type"),
              enum (module SignedMsgType),
              SignedMsgType.from_int_exn 0 )
          ^:: basic ((2, "height", "height"), int64, 0L)
          ^:: basic ((3, "round", "round"), int32_int, 0)
          ^:: basic_opt ((4, "block_id", "blockId"), message (module BlockID))
          ^:: basic_opt
                ( (5, "timestamp", "timestamp"),
                  message
                    (module Imported'modules.Google_protobuf_timestamp.Google
                            .Protobuf
                            .Timestamp) )
          ^:: basic
                ( (6, "validator_address", "validatorAddress"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: basic ((7, "validator_index", "validatorIndex"), int32_int, 0)
          ^:: basic ((8, "signature", "signature"), bytes, Bytes.of_string {||})
          ^:: basic ((9, "extension", "extension"), bytes, Bytes.of_string {||})
          ^:: basic
                ( (10, "extension_signature", "extensionSignature"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer
          {
            type';
            height;
            round;
            block_id;
            timestamp;
            validator_address;
            validator_index;
            signature;
            extension;
            extension_signature;
          }
        ->
          serialize writer type' height round block_id timestamp
            validator_address validator_index signature extension
            extension_signature

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor type' height round block_id timestamp validator_address
            validator_index signature extension extension_signature =
          {
            type';
            height;
            round;
            block_id;
            timestamp;
            validator_address;
            validator_index;
            signature;
            extension;
            extension_signature;
          }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun {
              type';
              height;
              round;
              block_id;
              timestamp;
              validator_address;
              validator_index;
              signature;
              extension;
              extension_signature;
            }
        ->
          serialize type' height round block_id timestamp validator_address
            validator_index signature extension extension_signature

      let from_json_exn =
        let constructor type' height round block_id timestamp validator_address
            validator_index signature extension extension_signature =
          {
            type';
            height;
            round;
            block_id;
            timestamp;
            validator_address;
            validator_index;
            signature;
            extension;
            extension_signature;
          }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and Commit : sig
      type t = {
        height : int64;
        round : int;
        block_id : BlockID.t option;
        signatures : CommitSig.t list;
      }

      val make :
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?signatures:CommitSig.t list ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?signatures:CommitSig.t list ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = Commit

      let name () = ".tendermint.types.Commit"

      type t = {
        height : int64;
        round : int;
        block_id : BlockID.t option;
        signatures : CommitSig.t list;
      }

      type make_t =
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?signatures:CommitSig.t list ->
        unit ->
        t

      let make ?(height = 0L) ?(round = 0) ?block_id ?(signatures = []) () =
        { height; round; block_id; signatures }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_height =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((1, "height", "height"), int64, 0L))
             in
             let merge_round =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((2, "round", "round"), int32_int, 0))
             in
             let merge_block_id =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ((3, "block_id", "blockId"), message (module BlockID)))
             in
             let merge_signatures =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   repeated
                     ( (4, "signatures", "signatures"),
                       message (module CommitSig),
                       not_packed ))
             in
             fun t1 t2 ->
               {
                 height = merge_height t1.height t2.height;
                 round = merge_round t1.round t2.round;
                 block_id = merge_block_id t1.block_id t2.block_id;
                 signatures = merge_signatures t1.signatures t2.signatures;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "height", "height"), int64, 0L)
          ^:: basic ((2, "round", "round"), int32_int, 0)
          ^:: basic_opt ((3, "block_id", "blockId"), message (module BlockID))
          ^:: repeated
                ( (4, "signatures", "signatures"),
                  message (module CommitSig),
                  not_packed )
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { height; round; block_id; signatures } ->
          serialize writer height round block_id signatures

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor height round block_id signatures =
          { height; round; block_id; signatures }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { height; round; block_id; signatures } ->
          serialize height round block_id signatures

      let from_json_exn =
        let constructor height round block_id signatures =
          { height; round; block_id; signatures }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and CommitSig : sig
      type t = {
        block_id_flag :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t;
        validator_address : bytes;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
      }

      val make :
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = CommitSig

      let name () = ".tendermint.types.CommitSig"

      type t = {
        block_id_flag :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t;
        validator_address : bytes;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
      }

      type make_t =
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t

      let make
          ?(block_id_flag =
            Imported'modules.Tendermint_types_validator.Tendermint.Types
            .BlockIDFlag
            .from_int_exn 0) ?(validator_address = Bytes.of_string {||})
          ?timestamp ?(signature = Bytes.of_string {||}) () =
        { block_id_flag; validator_address; timestamp; signature }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_block_id_flag =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (1, "block_id_flag", "blockIdFlag"),
                       enum
                         (module Imported'modules.Tendermint_types_validator
                                 .Tendermint
                                 .Types
                                 .BlockIDFlag),
                       Imported'modules.Tendermint_types_validator.Tendermint
                       .Types
                       .BlockIDFlag
                       .from_int_exn 0 ))
             in
             let merge_validator_address =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (2, "validator_address", "validatorAddress"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             let merge_timestamp =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (3, "timestamp", "timestamp"),
                       message
                         (module Imported'modules.Google_protobuf_timestamp
                                 .Google
                                 .Protobuf
                                 .Timestamp) ))
             in
             let merge_signature =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((4, "signature", "signature"), bytes, Bytes.of_string {||}))
             in
             fun t1 t2 ->
               {
                 block_id_flag =
                   merge_block_id_flag t1.block_id_flag t2.block_id_flag;
                 validator_address =
                   merge_validator_address t1.validator_address
                     t2.validator_address;
                 timestamp = merge_timestamp t1.timestamp t2.timestamp;
                 signature = merge_signature t1.signature t2.signature;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic
            ( (1, "block_id_flag", "blockIdFlag"),
              enum
                (module Imported'modules.Tendermint_types_validator.Tendermint
                        .Types
                        .BlockIDFlag),
              Imported'modules.Tendermint_types_validator.Tendermint.Types
              .BlockIDFlag
              .from_int_exn 0 )
          ^:: basic
                ( (2, "validator_address", "validatorAddress"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: basic_opt
                ( (3, "timestamp", "timestamp"),
                  message
                    (module Imported'modules.Google_protobuf_timestamp.Google
                            .Protobuf
                            .Timestamp) )
          ^:: basic ((4, "signature", "signature"), bytes, Bytes.of_string {||})
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { block_id_flag; validator_address; timestamp; signature } ->
          serialize writer block_id_flag validator_address timestamp signature

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor block_id_flag validator_address timestamp signature =
          { block_id_flag; validator_address; timestamp; signature }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { block_id_flag; validator_address; timestamp; signature } ->
          serialize block_id_flag validator_address timestamp signature

      let from_json_exn =
        let constructor block_id_flag validator_address timestamp signature =
          { block_id_flag; validator_address; timestamp; signature }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and ExtendedCommit : sig
      type t = {
        height : int64;
        round : int;
        block_id : BlockID.t option;
        extended_signatures : ExtendedCommitSig.t list;
      }

      val make :
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?extended_signatures:ExtendedCommitSig.t list ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?extended_signatures:ExtendedCommitSig.t list ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = ExtendedCommit

      let name () = ".tendermint.types.ExtendedCommit"

      type t = {
        height : int64;
        round : int;
        block_id : BlockID.t option;
        extended_signatures : ExtendedCommitSig.t list;
      }

      type make_t =
        ?height:int64 ->
        ?round:int ->
        ?block_id:BlockID.t ->
        ?extended_signatures:ExtendedCommitSig.t list ->
        unit ->
        t

      let make ?(height = 0L) ?(round = 0) ?block_id ?(extended_signatures = [])
          () =
        { height; round; block_id; extended_signatures }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_height =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((1, "height", "height"), int64, 0L))
             in
             let merge_round =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((2, "round", "round"), int32_int, 0))
             in
             let merge_block_id =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ((3, "block_id", "blockId"), message (module BlockID)))
             in
             let merge_extended_signatures =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   repeated
                     ( (4, "extended_signatures", "extendedSignatures"),
                       message (module ExtendedCommitSig),
                       not_packed ))
             in
             fun t1 t2 ->
               {
                 height = merge_height t1.height t2.height;
                 round = merge_round t1.round t2.round;
                 block_id = merge_block_id t1.block_id t2.block_id;
                 extended_signatures =
                   merge_extended_signatures t1.extended_signatures
                     t2.extended_signatures;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "height", "height"), int64, 0L)
          ^:: basic ((2, "round", "round"), int32_int, 0)
          ^:: basic_opt ((3, "block_id", "blockId"), message (module BlockID))
          ^:: repeated
                ( (4, "extended_signatures", "extendedSignatures"),
                  message (module ExtendedCommitSig),
                  not_packed )
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { height; round; block_id; extended_signatures } ->
          serialize writer height round block_id extended_signatures

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor height round block_id extended_signatures =
          { height; round; block_id; extended_signatures }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { height; round; block_id; extended_signatures } ->
          serialize height round block_id extended_signatures

      let from_json_exn =
        let constructor height round block_id extended_signatures =
          { height; round; block_id; extended_signatures }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and ExtendedCommitSig : sig
      type t = {
        block_id_flag :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t;
        validator_address : bytes;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
        extension : bytes;  (** {%html:
<p>Vote extension data</p>
%} *)
        extension_signature : bytes;
            (** {%html:
<p>Vote extension signature</p>
%} *)
      }

      val make :
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = ExtendedCommitSig

      let name () = ".tendermint.types.ExtendedCommitSig"

      type t = {
        block_id_flag :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t;
        validator_address : bytes;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
        extension : bytes;
        extension_signature : bytes;
      }

      type make_t =
        ?block_id_flag:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .BlockIDFlag
          .t ->
        ?validator_address:bytes ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        ?extension:bytes ->
        ?extension_signature:bytes ->
        unit ->
        t

      let make
          ?(block_id_flag =
            Imported'modules.Tendermint_types_validator.Tendermint.Types
            .BlockIDFlag
            .from_int_exn 0) ?(validator_address = Bytes.of_string {||})
          ?timestamp ?(signature = Bytes.of_string {||})
          ?(extension = Bytes.of_string {||})
          ?(extension_signature = Bytes.of_string {||}) () =
        {
          block_id_flag;
          validator_address;
          timestamp;
          signature;
          extension;
          extension_signature;
        }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_block_id_flag =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (1, "block_id_flag", "blockIdFlag"),
                       enum
                         (module Imported'modules.Tendermint_types_validator
                                 .Tendermint
                                 .Types
                                 .BlockIDFlag),
                       Imported'modules.Tendermint_types_validator.Tendermint
                       .Types
                       .BlockIDFlag
                       .from_int_exn 0 ))
             in
             let merge_validator_address =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (2, "validator_address", "validatorAddress"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             let merge_timestamp =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (3, "timestamp", "timestamp"),
                       message
                         (module Imported'modules.Google_protobuf_timestamp
                                 .Google
                                 .Protobuf
                                 .Timestamp) ))
             in
             let merge_signature =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((4, "signature", "signature"), bytes, Bytes.of_string {||}))
             in
             let merge_extension =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((5, "extension", "extension"), bytes, Bytes.of_string {||}))
             in
             let merge_extension_signature =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (6, "extension_signature", "extensionSignature"),
                       bytes,
                       Bytes.of_string {||} ))
             in
             fun t1 t2 ->
               {
                 block_id_flag =
                   merge_block_id_flag t1.block_id_flag t2.block_id_flag;
                 validator_address =
                   merge_validator_address t1.validator_address
                     t2.validator_address;
                 timestamp = merge_timestamp t1.timestamp t2.timestamp;
                 signature = merge_signature t1.signature t2.signature;
                 extension = merge_extension t1.extension t2.extension;
                 extension_signature =
                   merge_extension_signature t1.extension_signature
                     t2.extension_signature;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic
            ( (1, "block_id_flag", "blockIdFlag"),
              enum
                (module Imported'modules.Tendermint_types_validator.Tendermint
                        .Types
                        .BlockIDFlag),
              Imported'modules.Tendermint_types_validator.Tendermint.Types
              .BlockIDFlag
              .from_int_exn 0 )
          ^:: basic
                ( (2, "validator_address", "validatorAddress"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: basic_opt
                ( (3, "timestamp", "timestamp"),
                  message
                    (module Imported'modules.Google_protobuf_timestamp.Google
                            .Protobuf
                            .Timestamp) )
          ^:: basic ((4, "signature", "signature"), bytes, Bytes.of_string {||})
          ^:: basic ((5, "extension", "extension"), bytes, Bytes.of_string {||})
          ^:: basic
                ( (6, "extension_signature", "extensionSignature"),
                  bytes,
                  Bytes.of_string {||} )
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer
          {
            block_id_flag;
            validator_address;
            timestamp;
            signature;
            extension;
            extension_signature;
          }
        ->
          serialize writer block_id_flag validator_address timestamp signature
            extension extension_signature

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor block_id_flag validator_address timestamp signature
            extension extension_signature =
          {
            block_id_flag;
            validator_address;
            timestamp;
            signature;
            extension;
            extension_signature;
          }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun {
              block_id_flag;
              validator_address;
              timestamp;
              signature;
              extension;
              extension_signature;
            }
        ->
          serialize block_id_flag validator_address timestamp signature
            extension extension_signature

      let from_json_exn =
        let constructor block_id_flag validator_address timestamp signature
            extension extension_signature =
          {
            block_id_flag;
            validator_address;
            timestamp;
            signature;
            extension;
            extension_signature;
          }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and Proposal : sig
      type t = {
        type' : SignedMsgType.t;
        height : int64;
        round : int;
        pol_round : int;
        block_id : BlockID.t option;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
      }

      val make :
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?pol_round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?pol_round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = Proposal

      let name () = ".tendermint.types.Proposal"

      type t = {
        type' : SignedMsgType.t;
        height : int64;
        round : int;
        pol_round : int;
        block_id : BlockID.t option;
        timestamp :
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t
          option;
        signature : bytes;
      }

      type make_t =
        ?type':SignedMsgType.t ->
        ?height:int64 ->
        ?round:int ->
        ?pol_round:int ->
        ?block_id:BlockID.t ->
        ?timestamp:
          Imported'modules.Google_protobuf_timestamp.Google.Protobuf.Timestamp.t ->
        ?signature:bytes ->
        unit ->
        t

      let make ?(type' = SignedMsgType.from_int_exn 0) ?(height = 0L)
          ?(round = 0) ?(pol_round = 0) ?block_id ?timestamp
          ?(signature = Bytes.of_string {||}) () =
        { type'; height; round; pol_round; block_id; timestamp; signature }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_type' =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( (1, "type", "type"),
                       enum (module SignedMsgType),
                       SignedMsgType.from_int_exn 0 ))
             in
             let merge_height =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((2, "height", "height"), int64, 0L))
             in
             let merge_round =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((3, "round", "round"), int32_int, 0))
             in
             let merge_pol_round =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((4, "pol_round", "polRound"), int32_int, 0))
             in
             let merge_block_id =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ((5, "block_id", "blockId"), message (module BlockID)))
             in
             let merge_timestamp =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (6, "timestamp", "timestamp"),
                       message
                         (module Imported'modules.Google_protobuf_timestamp
                                 .Google
                                 .Protobuf
                                 .Timestamp) ))
             in
             let merge_signature =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((7, "signature", "signature"), bytes, Bytes.of_string {||}))
             in
             fun t1 t2 ->
               {
                 type' = merge_type' t1.type' t2.type';
                 height = merge_height t1.height t2.height;
                 round = merge_round t1.round t2.round;
                 pol_round = merge_pol_round t1.pol_round t2.pol_round;
                 block_id = merge_block_id t1.block_id t2.block_id;
                 timestamp = merge_timestamp t1.timestamp t2.timestamp;
                 signature = merge_signature t1.signature t2.signature;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic
            ( (1, "type", "type"),
              enum (module SignedMsgType),
              SignedMsgType.from_int_exn 0 )
          ^:: basic ((2, "height", "height"), int64, 0L)
          ^:: basic ((3, "round", "round"), int32_int, 0)
          ^:: basic ((4, "pol_round", "polRound"), int32_int, 0)
          ^:: basic_opt ((5, "block_id", "blockId"), message (module BlockID))
          ^:: basic_opt
                ( (6, "timestamp", "timestamp"),
                  message
                    (module Imported'modules.Google_protobuf_timestamp.Google
                            .Protobuf
                            .Timestamp) )
          ^:: basic ((7, "signature", "signature"), bytes, Bytes.of_string {||})
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer
          { type'; height; round; pol_round; block_id; timestamp; signature }
        ->
          serialize writer type' height round pol_round block_id timestamp
            signature

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor type' height round pol_round block_id timestamp
            signature =
          { type'; height; round; pol_round; block_id; timestamp; signature }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { type'; height; round; pol_round; block_id; timestamp; signature }
        -> serialize type' height round pol_round block_id timestamp signature

      let from_json_exn =
        let constructor type' height round pol_round block_id timestamp
            signature =
          { type'; height; round; pol_round; block_id; timestamp; signature }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and SignedHeader : sig
      type t = { header : Header.t option; commit : Commit.t option }

      val make : ?header:Header.t -> ?commit:Commit.t -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?header:Header.t -> ?commit:Commit.t -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = SignedHeader

      let name () = ".tendermint.types.SignedHeader"

      type t = { header : Header.t option; commit : Commit.t option }
      type make_t = ?header:Header.t -> ?commit:Commit.t -> unit -> t

      let make ?header ?commit () = { header; commit }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_header =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt ((1, "header", "header"), message (module Header)))
             in
             let merge_commit =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt ((2, "commit", "commit"), message (module Commit)))
             in
             fun t1 t2 ->
               {
                 header = merge_header t1.header t2.header;
                 commit = merge_commit t1.commit t2.commit;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic_opt ((1, "header", "header"), message (module Header))
          ^:: basic_opt ((2, "commit", "commit"), message (module Commit))
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { header; commit } -> serialize writer header commit

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor header commit = { header; commit } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { header; commit } -> serialize header commit

      let from_json_exn =
        let constructor header commit = { header; commit } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and LightBlock : sig
      type t = {
        signed_header : SignedHeader.t option;
        validator_set :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t
          option;
      }

      val make :
        ?signed_header:SignedHeader.t ->
        ?validator_set:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?signed_header:SignedHeader.t ->
        ?validator_set:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = LightBlock

      let name () = ".tendermint.types.LightBlock"

      type t = {
        signed_header : SignedHeader.t option;
        validator_set :
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t
          option;
      }

      type make_t =
        ?signed_header:SignedHeader.t ->
        ?validator_set:
          Imported'modules.Tendermint_types_validator.Tendermint.Types
          .ValidatorSet
          .t ->
        unit ->
        t

      let make ?signed_header ?validator_set () =
        { signed_header; validator_set }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_signed_header =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (1, "signed_header", "signedHeader"),
                       message (module SignedHeader) ))
             in
             let merge_validator_set =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (2, "validator_set", "validatorSet"),
                       message
                         (module Imported'modules.Tendermint_types_validator
                                 .Tendermint
                                 .Types
                                 .ValidatorSet) ))
             in
             fun t1 t2 ->
               {
                 signed_header =
                   merge_signed_header t1.signed_header t2.signed_header;
                 validator_set =
                   merge_validator_set t1.validator_set t2.validator_set;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic_opt
            ((1, "signed_header", "signedHeader"), message (module SignedHeader))
          ^:: basic_opt
                ( (2, "validator_set", "validatorSet"),
                  message
                    (module Imported'modules.Tendermint_types_validator
                            .Tendermint
                            .Types
                            .ValidatorSet) )
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { signed_header; validator_set } ->
          serialize writer signed_header validator_set

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor signed_header validator_set =
          { signed_header; validator_set }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { signed_header; validator_set } ->
          serialize signed_header validator_set

      let from_json_exn =
        let constructor signed_header validator_set =
          { signed_header; validator_set }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and BlockMeta : sig
      type t = {
        block_id : BlockID.t option;
        block_size : int64;
        header : Header.t option;
        num_txs : int64;
      }

      val make :
        ?block_id:BlockID.t ->
        ?block_size:int64 ->
        ?header:Header.t ->
        ?num_txs:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block_id:BlockID.t ->
        ?block_size:int64 ->
        ?header:Header.t ->
        ?num_txs:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = BlockMeta

      let name () = ".tendermint.types.BlockMeta"

      type t = {
        block_id : BlockID.t option;
        block_size : int64;
        header : Header.t option;
        num_txs : int64;
      }

      type make_t =
        ?block_id:BlockID.t ->
        ?block_size:int64 ->
        ?header:Header.t ->
        ?num_txs:int64 ->
        unit ->
        t

      let make ?block_id ?(block_size = 0L) ?header ?(num_txs = 0L) () =
        { block_id; block_size; header; num_txs }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_block_id =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ((1, "block_id", "blockId"), message (module BlockID)))
             in
             let merge_block_size =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((2, "block_size", "blockSize"), int64, 0L))
             in
             let merge_header =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt ((3, "header", "header"), message (module Header)))
             in
             let merge_num_txs =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((4, "num_txs", "numTxs"), int64, 0L))
             in
             fun t1 t2 ->
               {
                 block_id = merge_block_id t1.block_id t2.block_id;
                 block_size = merge_block_size t1.block_size t2.block_size;
                 header = merge_header t1.header t2.header;
                 num_txs = merge_num_txs t1.num_txs t2.num_txs;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic_opt ((1, "block_id", "blockId"), message (module BlockID))
          ^:: basic ((2, "block_size", "blockSize"), int64, 0L)
          ^:: basic_opt ((3, "header", "header"), message (module Header))
          ^:: basic ((4, "num_txs", "numTxs"), int64, 0L)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { block_id; block_size; header; num_txs } ->
          serialize writer block_id block_size header num_txs

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor block_id block_size header num_txs =
          { block_id; block_size; header; num_txs }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { block_id; block_size; header; num_txs } ->
          serialize block_id block_size header num_txs

      let from_json_exn =
        let constructor block_id block_size header num_txs =
          { block_id; block_size; header; num_txs }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and TxProof : sig
      type t = {
        root_hash : bytes;
        data : bytes;
        proof :
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t
          option;
      }

      val make :
        ?root_hash:bytes ->
        ?data:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?root_hash:bytes ->
        ?data:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = TxProof

      let name () = ".tendermint.types.TxProof"

      type t = {
        root_hash : bytes;
        data : bytes;
        proof :
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t
          option;
      }

      type make_t =
        ?root_hash:bytes ->
        ?data:bytes ->
        ?proof:
          Imported'modules.Tendermint_crypto_proof.Tendermint.Crypto.Proof.t ->
        unit ->
        t

      let make ?(root_hash = Bytes.of_string {||})
          ?(data = Bytes.of_string {||}) ?proof () =
        { root_hash; data; proof }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_root_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((1, "root_hash", "rootHash"), bytes, Bytes.of_string {||}))
             in
             let merge_data =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((2, "data", "data"), bytes, Bytes.of_string {||}))
             in
             let merge_proof =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (3, "proof", "proof"),
                       message
                         (module Imported'modules.Tendermint_crypto_proof
                                 .Tendermint
                                 .Crypto
                                 .Proof) ))
             in
             fun t1 t2 ->
               {
                 root_hash = merge_root_hash t1.root_hash t2.root_hash;
                 data = merge_data t1.data t2.data;
                 proof = merge_proof t1.proof t2.proof;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "root_hash", "rootHash"), bytes, Bytes.of_string {||})
          ^:: basic ((2, "data", "data"), bytes, Bytes.of_string {||})
          ^:: basic_opt
                ( (3, "proof", "proof"),
                  message
                    (module Imported'modules.Tendermint_crypto_proof.Tendermint
                            .Crypto
                            .Proof) )
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { root_hash; data; proof } ->
          serialize writer root_hash data proof

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor root_hash data proof = { root_hash; data; proof } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { root_hash; data; proof } -> serialize root_hash data proof

      let from_json_exn =
        let constructor root_hash data proof = { root_hash; data; proof } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end
  end
end

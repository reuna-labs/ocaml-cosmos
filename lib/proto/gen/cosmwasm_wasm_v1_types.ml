(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: cosmwasm/wasm/v1/types.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Cosmos_proto_cosmos = Cosmos_proto_cosmos
  module Gogoproto_gogo = Gogoproto_gogo
  module Google_protobuf_any = Google_protobuf_any
  module Amino_amino = Amino_amino
end

(**/**)

module rec Cosmwasm : sig
  module rec Wasm : sig
    module rec V1 : sig
      (** {%html:
<p>AccessType permission types</p>
%} *)
      module rec AccessType : sig
        type t =
          | ACCESS_TYPE_UNSPECIFIED
              (** {%html:
<p>AccessTypeUnspecified placeholder for empty value</p>
%}
              *)
          | ACCESS_TYPE_NOBODY
              (** {%html:
<p>AccessTypeNobody forbidden</p>
%} *)
          | ACCESS_TYPE_EVERYBODY
              (** {%html:
<p>AccessTypeEverybody unrestricted</p>
%} *)
          | ACCESS_TYPE_ANY_OF_ADDRESSES
              (** {%html:
<p>AccessTypeAnyOfAddresses allow any of the addresses</p>
%}
              *)

        val name : unit -> string
        (** Fully qualified protobuf name of this enum *)

        (**/**)

        val to_int : t -> int
        val from_int : int -> t Runtime'.Result.t
        val from_int_exn : int -> t
        val to_string : t -> string
        val from_string_exn : string -> t

        (**/**)
      end

      (** {%html:
<p>ContractCodeHistoryOperationType actions that caused a code change</p>
%}
      *)
      and ContractCodeHistoryOperationType : sig
        type t =
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED
              (** {%html:
<p>ContractCodeHistoryOperationTypeUnspecified placeholder for empty value</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT
              (** {%html:
<p>ContractCodeHistoryOperationTypeInit on chain contract instantiation</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE
              (** {%html:
<p>ContractCodeHistoryOperationTypeMigrate code migration</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS
              (** {%html:
<p>ContractCodeHistoryOperationTypeGenesis based on genesis data</p>
%}
              *)

        val name : unit -> string
        (** Fully qualified protobuf name of this enum *)

        (**/**)

        val to_int : t -> int
        val from_int : int -> t Runtime'.Result.t
        val from_int_exn : int -> t
        val to_string : t -> string
        val from_string_exn : string -> t

        (**/**)
      end

      (** {%html:
<p>AccessTypeParam</p>
%} *)
      and AccessTypeParam : sig
        type t = AccessType.t

        val make : ?value:AccessType.t -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?value:AccessType.t -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>AccessConfig access control type.</p>
%} *)
      and AccessConfig : sig
        type t = { permission : AccessType.t; addresses : string list }

        val make :
          ?permission:AccessType.t -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?permission:AccessType.t -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Params defines the set of wasm parameters.</p>
%} *)
      and Params : sig
        type t = {
          code_upload_access : AccessConfig.t option;
          instantiate_default_permission : AccessType.t;
        }

        val make :
          ?code_upload_access:AccessConfig.t ->
          ?instantiate_default_permission:AccessType.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_upload_access:AccessConfig.t ->
          ?instantiate_default_permission:AccessType.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>CodeInfo is data for the uploaded contract WASM code</p>
%}
      *)
      and CodeInfo : sig
        type t = {
          code_hash : bytes;
              (** {%html:
<p>CodeHash is the unique identifier created by wasmvm</p>
%}
              *)
          creator : string;
              (** {%html:
<p>Creator address who initially stored the code</p>
%}
              *)
          instantiate_config : AccessConfig.t option;
              (** {%html:
<p>InstantiateConfig access control to apply on contract creation, optional</p>
%}
              *)
        }

        val make :
          ?code_hash:bytes ->
          ?creator:string ->
          ?instantiate_config:AccessConfig.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_hash:bytes ->
          ?creator:string ->
          ?instantiate_config:AccessConfig.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ContractInfo stores a WASM contract instance</p>
%} *)
      and ContractInfo : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored Wasm code</p>
%}
              *)
          creator : string;
              (** {%html:
<p>Creator address who initially instantiated the contract</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          created : AbsoluteTxPosition.t option;
              (** {%html:
<p>Created Tx position when the contract was instantiated.</p>
%}
              *)
          ibc_port_id : string;
          extension :
            Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
              (** {%html:
<p>Extension is an extension point to store custom metadata within the
persistence model.</p>
%}
              *)
          ibc2_port_id : string;
        }

        val make :
          ?code_id:int64 ->
          ?creator:string ->
          ?admin:string ->
          ?label:string ->
          ?created:AbsoluteTxPosition.t ->
          ?ibc_port_id:string ->
          ?extension:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?ibc2_port_id:string ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_id:int64 ->
          ?creator:string ->
          ?admin:string ->
          ?label:string ->
          ?created:AbsoluteTxPosition.t ->
          ?ibc_port_id:string ->
          ?extension:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?ibc2_port_id:string ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ContractCodeHistoryEntry metadata to a contract.</p>
%} *)
      and ContractCodeHistoryEntry : sig
        type t = {
          operation : ContractCodeHistoryOperationType.t;
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          updated : AbsoluteTxPosition.t option;
              (** {%html:
<p>Updated Tx position when the operation was executed.</p>
%}
              *)
          msg : bytes;
        }

        val make :
          ?operation:ContractCodeHistoryOperationType.t ->
          ?code_id:int64 ->
          ?updated:AbsoluteTxPosition.t ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?operation:ContractCodeHistoryOperationType.t ->
          ?code_id:int64 ->
          ?updated:AbsoluteTxPosition.t ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>AbsoluteTxPosition is a unique transaction position that allows for global
ordering of transactions.</p>
%}
      *)
      and AbsoluteTxPosition : sig
        type t = {
          block_height : int64;
              (** {%html:
<p>BlockHeight is the block the contract was created at</p>
%}
              *)
          tx_index : int64;
              (** {%html:
<p>TxIndex is a monotonic counter within the block (actual transaction index,
or gas consumed)</p>
%}
              *)
        }

        val make : ?block_height:int64 -> ?tx_index:int64 -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?block_height:int64 -> ?tx_index:int64 -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Model is a struct that holds a KV pair</p>
%} *)
      and Model : sig
        type t = {
          key : bytes;
              (** {%html:
<p>hex-encode key to read it better (this is often ascii)</p>
%}
              *)
          value : bytes;  (** {%html:
<p>base64-encode raw value</p>
%} *)
        }

        val make : ?key:bytes -> ?value:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?key:bytes -> ?value:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
    end
  end
end = struct
  module rec Wasm : sig
    module rec V1 : sig
      (** {%html:
<p>AccessType permission types</p>
%} *)
      module rec AccessType : sig
        type t =
          | ACCESS_TYPE_UNSPECIFIED
              (** {%html:
<p>AccessTypeUnspecified placeholder for empty value</p>
%}
              *)
          | ACCESS_TYPE_NOBODY
              (** {%html:
<p>AccessTypeNobody forbidden</p>
%} *)
          | ACCESS_TYPE_EVERYBODY
              (** {%html:
<p>AccessTypeEverybody unrestricted</p>
%} *)
          | ACCESS_TYPE_ANY_OF_ADDRESSES
              (** {%html:
<p>AccessTypeAnyOfAddresses allow any of the addresses</p>
%}
              *)

        val name : unit -> string
        (** Fully qualified protobuf name of this enum *)

        (**/**)

        val to_int : t -> int
        val from_int : int -> t Runtime'.Result.t
        val from_int_exn : int -> t
        val to_string : t -> string
        val from_string_exn : string -> t

        (**/**)
      end

      (** {%html:
<p>ContractCodeHistoryOperationType actions that caused a code change</p>
%}
      *)
      and ContractCodeHistoryOperationType : sig
        type t =
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED
              (** {%html:
<p>ContractCodeHistoryOperationTypeUnspecified placeholder for empty value</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT
              (** {%html:
<p>ContractCodeHistoryOperationTypeInit on chain contract instantiation</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE
              (** {%html:
<p>ContractCodeHistoryOperationTypeMigrate code migration</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS
              (** {%html:
<p>ContractCodeHistoryOperationTypeGenesis based on genesis data</p>
%}
              *)

        val name : unit -> string
        (** Fully qualified protobuf name of this enum *)

        (**/**)

        val to_int : t -> int
        val from_int : int -> t Runtime'.Result.t
        val from_int_exn : int -> t
        val to_string : t -> string
        val from_string_exn : string -> t

        (**/**)
      end

      (** {%html:
<p>AccessTypeParam</p>
%} *)
      and AccessTypeParam : sig
        type t = AccessType.t

        val make : ?value:AccessType.t -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?value:AccessType.t -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>AccessConfig access control type.</p>
%} *)
      and AccessConfig : sig
        type t = { permission : AccessType.t; addresses : string list }

        val make :
          ?permission:AccessType.t -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?permission:AccessType.t -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Params defines the set of wasm parameters.</p>
%} *)
      and Params : sig
        type t = {
          code_upload_access : AccessConfig.t option;
          instantiate_default_permission : AccessType.t;
        }

        val make :
          ?code_upload_access:AccessConfig.t ->
          ?instantiate_default_permission:AccessType.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_upload_access:AccessConfig.t ->
          ?instantiate_default_permission:AccessType.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>CodeInfo is data for the uploaded contract WASM code</p>
%}
      *)
      and CodeInfo : sig
        type t = {
          code_hash : bytes;
              (** {%html:
<p>CodeHash is the unique identifier created by wasmvm</p>
%}
              *)
          creator : string;
              (** {%html:
<p>Creator address who initially stored the code</p>
%}
              *)
          instantiate_config : AccessConfig.t option;
              (** {%html:
<p>InstantiateConfig access control to apply on contract creation, optional</p>
%}
              *)
        }

        val make :
          ?code_hash:bytes ->
          ?creator:string ->
          ?instantiate_config:AccessConfig.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_hash:bytes ->
          ?creator:string ->
          ?instantiate_config:AccessConfig.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ContractInfo stores a WASM contract instance</p>
%} *)
      and ContractInfo : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored Wasm code</p>
%}
              *)
          creator : string;
              (** {%html:
<p>Creator address who initially instantiated the contract</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          created : AbsoluteTxPosition.t option;
              (** {%html:
<p>Created Tx position when the contract was instantiated.</p>
%}
              *)
          ibc_port_id : string;
          extension :
            Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
              (** {%html:
<p>Extension is an extension point to store custom metadata within the
persistence model.</p>
%}
              *)
          ibc2_port_id : string;
        }

        val make :
          ?code_id:int64 ->
          ?creator:string ->
          ?admin:string ->
          ?label:string ->
          ?created:AbsoluteTxPosition.t ->
          ?ibc_port_id:string ->
          ?extension:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?ibc2_port_id:string ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_id:int64 ->
          ?creator:string ->
          ?admin:string ->
          ?label:string ->
          ?created:AbsoluteTxPosition.t ->
          ?ibc_port_id:string ->
          ?extension:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?ibc2_port_id:string ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ContractCodeHistoryEntry metadata to a contract.</p>
%} *)
      and ContractCodeHistoryEntry : sig
        type t = {
          operation : ContractCodeHistoryOperationType.t;
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          updated : AbsoluteTxPosition.t option;
              (** {%html:
<p>Updated Tx position when the operation was executed.</p>
%}
              *)
          msg : bytes;
        }

        val make :
          ?operation:ContractCodeHistoryOperationType.t ->
          ?code_id:int64 ->
          ?updated:AbsoluteTxPosition.t ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?operation:ContractCodeHistoryOperationType.t ->
          ?code_id:int64 ->
          ?updated:AbsoluteTxPosition.t ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>AbsoluteTxPosition is a unique transaction position that allows for global
ordering of transactions.</p>
%}
      *)
      and AbsoluteTxPosition : sig
        type t = {
          block_height : int64;
              (** {%html:
<p>BlockHeight is the block the contract was created at</p>
%}
              *)
          tx_index : int64;
              (** {%html:
<p>TxIndex is a monotonic counter within the block (actual transaction index,
or gas consumed)</p>
%}
              *)
        }

        val make : ?block_height:int64 -> ?tx_index:int64 -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?block_height:int64 -> ?tx_index:int64 -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Model is a struct that holds a KV pair</p>
%} *)
      and Model : sig
        type t = {
          key : bytes;
              (** {%html:
<p>hex-encode key to read it better (this is often ascii)</p>
%}
              *)
          value : bytes;  (** {%html:
<p>base64-encode raw value</p>
%} *)
        }

        val make : ?key:bytes -> ?value:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?key:bytes -> ?value:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
    end
  end = struct
    module rec V1 : sig
      (** {%html:
<p>AccessType permission types</p>
%} *)
      module rec AccessType : sig
        type t =
          | ACCESS_TYPE_UNSPECIFIED
              (** {%html:
<p>AccessTypeUnspecified placeholder for empty value</p>
%}
              *)
          | ACCESS_TYPE_NOBODY
              (** {%html:
<p>AccessTypeNobody forbidden</p>
%} *)
          | ACCESS_TYPE_EVERYBODY
              (** {%html:
<p>AccessTypeEverybody unrestricted</p>
%} *)
          | ACCESS_TYPE_ANY_OF_ADDRESSES
              (** {%html:
<p>AccessTypeAnyOfAddresses allow any of the addresses</p>
%}
              *)

        val name : unit -> string
        (** Fully qualified protobuf name of this enum *)

        (**/**)

        val to_int : t -> int
        val from_int : int -> t Runtime'.Result.t
        val from_int_exn : int -> t
        val to_string : t -> string
        val from_string_exn : string -> t

        (**/**)
      end

      (** {%html:
<p>ContractCodeHistoryOperationType actions that caused a code change</p>
%}
      *)
      and ContractCodeHistoryOperationType : sig
        type t =
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED
              (** {%html:
<p>ContractCodeHistoryOperationTypeUnspecified placeholder for empty value</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT
              (** {%html:
<p>ContractCodeHistoryOperationTypeInit on chain contract instantiation</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE
              (** {%html:
<p>ContractCodeHistoryOperationTypeMigrate code migration</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS
              (** {%html:
<p>ContractCodeHistoryOperationTypeGenesis based on genesis data</p>
%}
              *)

        val name : unit -> string
        (** Fully qualified protobuf name of this enum *)

        (**/**)

        val to_int : t -> int
        val from_int : int -> t Runtime'.Result.t
        val from_int_exn : int -> t
        val to_string : t -> string
        val from_string_exn : string -> t

        (**/**)
      end

      (** {%html:
<p>AccessTypeParam</p>
%} *)
      and AccessTypeParam : sig
        type t = AccessType.t

        val make : ?value:AccessType.t -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?value:AccessType.t -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>AccessConfig access control type.</p>
%} *)
      and AccessConfig : sig
        type t = { permission : AccessType.t; addresses : string list }

        val make :
          ?permission:AccessType.t -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?permission:AccessType.t -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Params defines the set of wasm parameters.</p>
%} *)
      and Params : sig
        type t = {
          code_upload_access : AccessConfig.t option;
          instantiate_default_permission : AccessType.t;
        }

        val make :
          ?code_upload_access:AccessConfig.t ->
          ?instantiate_default_permission:AccessType.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_upload_access:AccessConfig.t ->
          ?instantiate_default_permission:AccessType.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>CodeInfo is data for the uploaded contract WASM code</p>
%}
      *)
      and CodeInfo : sig
        type t = {
          code_hash : bytes;
              (** {%html:
<p>CodeHash is the unique identifier created by wasmvm</p>
%}
              *)
          creator : string;
              (** {%html:
<p>Creator address who initially stored the code</p>
%}
              *)
          instantiate_config : AccessConfig.t option;
              (** {%html:
<p>InstantiateConfig access control to apply on contract creation, optional</p>
%}
              *)
        }

        val make :
          ?code_hash:bytes ->
          ?creator:string ->
          ?instantiate_config:AccessConfig.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_hash:bytes ->
          ?creator:string ->
          ?instantiate_config:AccessConfig.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ContractInfo stores a WASM contract instance</p>
%} *)
      and ContractInfo : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored Wasm code</p>
%}
              *)
          creator : string;
              (** {%html:
<p>Creator address who initially instantiated the contract</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          created : AbsoluteTxPosition.t option;
              (** {%html:
<p>Created Tx position when the contract was instantiated.</p>
%}
              *)
          ibc_port_id : string;
          extension :
            Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
              (** {%html:
<p>Extension is an extension point to store custom metadata within the
persistence model.</p>
%}
              *)
          ibc2_port_id : string;
        }

        val make :
          ?code_id:int64 ->
          ?creator:string ->
          ?admin:string ->
          ?label:string ->
          ?created:AbsoluteTxPosition.t ->
          ?ibc_port_id:string ->
          ?extension:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?ibc2_port_id:string ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_id:int64 ->
          ?creator:string ->
          ?admin:string ->
          ?label:string ->
          ?created:AbsoluteTxPosition.t ->
          ?ibc_port_id:string ->
          ?extension:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?ibc2_port_id:string ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ContractCodeHistoryEntry metadata to a contract.</p>
%} *)
      and ContractCodeHistoryEntry : sig
        type t = {
          operation : ContractCodeHistoryOperationType.t;
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          updated : AbsoluteTxPosition.t option;
              (** {%html:
<p>Updated Tx position when the operation was executed.</p>
%}
              *)
          msg : bytes;
        }

        val make :
          ?operation:ContractCodeHistoryOperationType.t ->
          ?code_id:int64 ->
          ?updated:AbsoluteTxPosition.t ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?operation:ContractCodeHistoryOperationType.t ->
          ?code_id:int64 ->
          ?updated:AbsoluteTxPosition.t ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>AbsoluteTxPosition is a unique transaction position that allows for global
ordering of transactions.</p>
%}
      *)
      and AbsoluteTxPosition : sig
        type t = {
          block_height : int64;
              (** {%html:
<p>BlockHeight is the block the contract was created at</p>
%}
              *)
          tx_index : int64;
              (** {%html:
<p>TxIndex is a monotonic counter within the block (actual transaction index,
or gas consumed)</p>
%}
              *)
        }

        val make : ?block_height:int64 -> ?tx_index:int64 -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?block_height:int64 -> ?tx_index:int64 -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Model is a struct that holds a KV pair</p>
%} *)
      and Model : sig
        type t = {
          key : bytes;
              (** {%html:
<p>hex-encode key to read it better (this is often ascii)</p>
%}
              *)
          value : bytes;  (** {%html:
<p>base64-encode raw value</p>
%} *)
        }

        val make : ?key:bytes -> ?value:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?key:bytes -> ?value:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
    end = struct
      module rec AccessType : sig
        type t =
          | ACCESS_TYPE_UNSPECIFIED
              (** {%html:
<p>AccessTypeUnspecified placeholder for empty value</p>
%}
              *)
          | ACCESS_TYPE_NOBODY
              (** {%html:
<p>AccessTypeNobody forbidden</p>
%} *)
          | ACCESS_TYPE_EVERYBODY
              (** {%html:
<p>AccessTypeEverybody unrestricted</p>
%} *)
          | ACCESS_TYPE_ANY_OF_ADDRESSES
              (** {%html:
<p>AccessTypeAnyOfAddresses allow any of the addresses</p>
%}
              *)

        val name : unit -> string
        (** Fully qualified protobuf name of this enum *)

        (**/**)

        val to_int : t -> int
        val from_int : int -> t Runtime'.Result.t
        val from_int_exn : int -> t
        val to_string : t -> string
        val from_string_exn : string -> t

        (**/**)
      end = struct
        module This'_ = AccessType

        type t =
          | ACCESS_TYPE_UNSPECIFIED
              (** {%html:
<p>AccessTypeUnspecified placeholder for empty value</p>
%}
              *)
          | ACCESS_TYPE_NOBODY
              (** {%html:
<p>AccessTypeNobody forbidden</p>
%} *)
          | ACCESS_TYPE_EVERYBODY
              (** {%html:
<p>AccessTypeEverybody unrestricted</p>
%} *)
          | ACCESS_TYPE_ANY_OF_ADDRESSES
              (** {%html:
<p>AccessTypeAnyOfAddresses allow any of the addresses</p>
%}
              *)

        let name () = ".cosmwasm.wasm.v1.AccessType"

        let to_int = function
          | ACCESS_TYPE_UNSPECIFIED -> 0
          | ACCESS_TYPE_NOBODY -> 1
          | ACCESS_TYPE_EVERYBODY -> 3
          | ACCESS_TYPE_ANY_OF_ADDRESSES -> 4

        let from_int_exn = function
          | 0 -> ACCESS_TYPE_UNSPECIFIED
          | 1 -> ACCESS_TYPE_NOBODY
          | 3 -> ACCESS_TYPE_EVERYBODY
          | 4 -> ACCESS_TYPE_ANY_OF_ADDRESSES
          | n -> Runtime'.Result.raise (`Unknown_enum_value n)

        let from_int e = Runtime'.Result.catch (fun () -> from_int_exn e)

        let to_string = function
          | ACCESS_TYPE_UNSPECIFIED -> "ACCESS_TYPE_UNSPECIFIED"
          | ACCESS_TYPE_NOBODY -> "ACCESS_TYPE_NOBODY"
          | ACCESS_TYPE_EVERYBODY -> "ACCESS_TYPE_EVERYBODY"
          | ACCESS_TYPE_ANY_OF_ADDRESSES -> "ACCESS_TYPE_ANY_OF_ADDRESSES"

        let from_string_exn = function
          | "ACCESS_TYPE_UNSPECIFIED" -> ACCESS_TYPE_UNSPECIFIED
          | "ACCESS_TYPE_NOBODY" -> ACCESS_TYPE_NOBODY
          | "ACCESS_TYPE_EVERYBODY" -> ACCESS_TYPE_EVERYBODY
          | "ACCESS_TYPE_ANY_OF_ADDRESSES" -> ACCESS_TYPE_ANY_OF_ADDRESSES
          | s -> Runtime'.Result.raise (`Unknown_enum_name s)
      end

      and ContractCodeHistoryOperationType : sig
        type t =
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED
              (** {%html:
<p>ContractCodeHistoryOperationTypeUnspecified placeholder for empty value</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT
              (** {%html:
<p>ContractCodeHistoryOperationTypeInit on chain contract instantiation</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE
              (** {%html:
<p>ContractCodeHistoryOperationTypeMigrate code migration</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS
              (** {%html:
<p>ContractCodeHistoryOperationTypeGenesis based on genesis data</p>
%}
              *)

        val name : unit -> string
        (** Fully qualified protobuf name of this enum *)

        (**/**)

        val to_int : t -> int
        val from_int : int -> t Runtime'.Result.t
        val from_int_exn : int -> t
        val to_string : t -> string
        val from_string_exn : string -> t

        (**/**)
      end = struct
        module This'_ = ContractCodeHistoryOperationType

        type t =
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED
              (** {%html:
<p>ContractCodeHistoryOperationTypeUnspecified placeholder for empty value</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT
              (** {%html:
<p>ContractCodeHistoryOperationTypeInit on chain contract instantiation</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE
              (** {%html:
<p>ContractCodeHistoryOperationTypeMigrate code migration</p>
%}
              *)
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS
              (** {%html:
<p>ContractCodeHistoryOperationTypeGenesis based on genesis data</p>
%}
              *)

        let name () = ".cosmwasm.wasm.v1.ContractCodeHistoryOperationType"

        let to_int = function
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED -> 0
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT -> 1
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE -> 2
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS -> 3

        let from_int_exn = function
          | 0 -> CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED
          | 1 -> CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT
          | 2 -> CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE
          | 3 -> CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS
          | n -> Runtime'.Result.raise (`Unknown_enum_value n)

        let from_int e = Runtime'.Result.catch (fun () -> from_int_exn e)

        let to_string = function
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED ->
              "CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED"
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT ->
              "CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT"
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE ->
              "CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE"
          | CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS ->
              "CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS"

        let from_string_exn = function
          | "CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED" ->
              CONTRACT_CODE_HISTORY_OPERATION_TYPE_UNSPECIFIED
          | "CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT" ->
              CONTRACT_CODE_HISTORY_OPERATION_TYPE_INIT
          | "CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE" ->
              CONTRACT_CODE_HISTORY_OPERATION_TYPE_MIGRATE
          | "CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS" ->
              CONTRACT_CODE_HISTORY_OPERATION_TYPE_GENESIS
          | s -> Runtime'.Result.raise (`Unknown_enum_name s)
      end

      and AccessTypeParam : sig
        type t = AccessType.t

        val make : ?value:AccessType.t -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?value:AccessType.t -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = AccessTypeParam

        let name () = ".cosmwasm.wasm.v1.AccessTypeParam"

        type t = AccessType.t
        type make_t = ?value:AccessType.t -> unit -> t

        let make ?(value = AccessType.from_int_exn 0) () = value

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_value =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (1, "value", "value"),
                         enum (module AccessType),
                         AccessType.from_int_exn 0 ))
               in
               fun t1_value t2_value -> merge_value t1_value t2_value)
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic
              ( (1, "value", "value"),
                enum (module AccessType),
                AccessType.from_int_exn 0 )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer value -> serialize writer value

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor value = value in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun value -> serialize value

        let from_json_exn =
          let constructor value = value in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and AccessConfig : sig
        type t = { permission : AccessType.t; addresses : string list }

        val make :
          ?permission:AccessType.t -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?permission:AccessType.t -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = AccessConfig

        let name () = ".cosmwasm.wasm.v1.AccessConfig"

        type t = { permission : AccessType.t; addresses : string list }

        type make_t =
          ?permission:AccessType.t -> ?addresses:string list -> unit -> t

        let make ?(permission = AccessType.from_int_exn 0) ?(addresses = []) ()
            =
          { permission; addresses }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_permission =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (1, "permission", "permission"),
                         enum (module AccessType),
                         AccessType.from_int_exn 0 ))
               in
               let merge_addresses =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated ((3, "addresses", "addresses"), string, not_packed))
               in
               fun t1 t2 ->
                 {
                   permission = merge_permission t1.permission t2.permission;
                   addresses = merge_addresses t1.addresses t2.addresses;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic
              ( (1, "permission", "permission"),
                enum (module AccessType),
                AccessType.from_int_exn 0 )
            ^:: repeated ((3, "addresses", "addresses"), string, not_packed)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { permission; addresses } ->
            serialize writer permission addresses

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor permission addresses = { permission; addresses } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { permission; addresses } -> serialize permission addresses

        let from_json_exn =
          let constructor permission addresses = { permission; addresses } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and Params : sig
        type t = {
          code_upload_access : AccessConfig.t option;
          instantiate_default_permission : AccessType.t;
        }

        val make :
          ?code_upload_access:AccessConfig.t ->
          ?instantiate_default_permission:AccessType.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_upload_access:AccessConfig.t ->
          ?instantiate_default_permission:AccessType.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = Params

        let name () = ".cosmwasm.wasm.v1.Params"

        type t = {
          code_upload_access : AccessConfig.t option;
          instantiate_default_permission : AccessType.t;
        }

        type make_t =
          ?code_upload_access:AccessConfig.t ->
          ?instantiate_default_permission:AccessType.t ->
          unit ->
          t

        let make ?code_upload_access
            ?(instantiate_default_permission = AccessType.from_int_exn 0) () =
          { code_upload_access; instantiate_default_permission }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_code_upload_access =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (1, "code_upload_access", "codeUploadAccess"),
                         message (module AccessConfig) ))
               in
               let merge_instantiate_default_permission =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( ( 2,
                           "instantiate_default_permission",
                           "instantiateDefaultPermission" ),
                         enum (module AccessType),
                         AccessType.from_int_exn 0 ))
               in
               fun t1 t2 ->
                 {
                   code_upload_access =
                     merge_code_upload_access t1.code_upload_access
                       t2.code_upload_access;
                   instantiate_default_permission =
                     merge_instantiate_default_permission
                       t1.instantiate_default_permission
                       t2.instantiate_default_permission;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic_opt
              ( (1, "code_upload_access", "codeUploadAccess"),
                message (module AccessConfig) )
            ^:: basic
                  ( ( 2,
                      "instantiate_default_permission",
                      "instantiateDefaultPermission" ),
                    enum (module AccessType),
                    AccessType.from_int_exn 0 )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { code_upload_access; instantiate_default_permission } ->
            serialize writer code_upload_access instantiate_default_permission

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor code_upload_access instantiate_default_permission =
            { code_upload_access; instantiate_default_permission }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { code_upload_access; instantiate_default_permission } ->
            serialize code_upload_access instantiate_default_permission

        let from_json_exn =
          let constructor code_upload_access instantiate_default_permission =
            { code_upload_access; instantiate_default_permission }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and CodeInfo : sig
        type t = {
          code_hash : bytes;
              (** {%html:
<p>CodeHash is the unique identifier created by wasmvm</p>
%}
              *)
          creator : string;
              (** {%html:
<p>Creator address who initially stored the code</p>
%}
              *)
          instantiate_config : AccessConfig.t option;
              (** {%html:
<p>InstantiateConfig access control to apply on contract creation, optional</p>
%}
              *)
        }

        val make :
          ?code_hash:bytes ->
          ?creator:string ->
          ?instantiate_config:AccessConfig.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_hash:bytes ->
          ?creator:string ->
          ?instantiate_config:AccessConfig.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = CodeInfo

        let name () = ".cosmwasm.wasm.v1.CodeInfo"

        type t = {
          code_hash : bytes;
          creator : string;
          instantiate_config : AccessConfig.t option;
        }

        type make_t =
          ?code_hash:bytes ->
          ?creator:string ->
          ?instantiate_config:AccessConfig.t ->
          unit ->
          t

        let make ?(code_hash = Bytes.of_string {||}) ?(creator = {||})
            ?instantiate_config () =
          { code_hash; creator; instantiate_config }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_code_hash =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (1, "code_hash", "codeHash"),
                         bytes,
                         Bytes.of_string {||} ))
               in
               let merge_creator =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "creator", "creator"), string, {||}))
               in
               let merge_instantiate_config =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (5, "instantiate_config", "instantiateConfig"),
                         message (module AccessConfig) ))
               in
               fun t1 t2 ->
                 {
                   code_hash = merge_code_hash t1.code_hash t2.code_hash;
                   creator = merge_creator t1.creator t2.creator;
                   instantiate_config =
                     merge_instantiate_config t1.instantiate_config
                       t2.instantiate_config;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "code_hash", "codeHash"), bytes, Bytes.of_string {||})
            ^:: basic ((2, "creator", "creator"), string, {||})
            ^:: basic_opt
                  ( (5, "instantiate_config", "instantiateConfig"),
                    message (module AccessConfig) )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { code_hash; creator; instantiate_config } ->
            serialize writer code_hash creator instantiate_config

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor code_hash creator instantiate_config =
            { code_hash; creator; instantiate_config }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { code_hash; creator; instantiate_config } ->
            serialize code_hash creator instantiate_config

        let from_json_exn =
          let constructor code_hash creator instantiate_config =
            { code_hash; creator; instantiate_config }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and ContractInfo : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored Wasm code</p>
%}
              *)
          creator : string;
              (** {%html:
<p>Creator address who initially instantiated the contract</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          created : AbsoluteTxPosition.t option;
              (** {%html:
<p>Created Tx position when the contract was instantiated.</p>
%}
              *)
          ibc_port_id : string;
          extension :
            Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
              (** {%html:
<p>Extension is an extension point to store custom metadata within the
persistence model.</p>
%}
              *)
          ibc2_port_id : string;
        }

        val make :
          ?code_id:int64 ->
          ?creator:string ->
          ?admin:string ->
          ?label:string ->
          ?created:AbsoluteTxPosition.t ->
          ?ibc_port_id:string ->
          ?extension:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?ibc2_port_id:string ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_id:int64 ->
          ?creator:string ->
          ?admin:string ->
          ?label:string ->
          ?created:AbsoluteTxPosition.t ->
          ?ibc_port_id:string ->
          ?extension:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?ibc2_port_id:string ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = ContractInfo

        let name () = ".cosmwasm.wasm.v1.ContractInfo"

        type t = {
          code_id : int64;
          creator : string;
          admin : string;
          label : string;
          created : AbsoluteTxPosition.t option;
          ibc_port_id : string;
          extension :
            Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
          ibc2_port_id : string;
        }

        type make_t =
          ?code_id:int64 ->
          ?creator:string ->
          ?admin:string ->
          ?label:string ->
          ?created:AbsoluteTxPosition.t ->
          ?ibc_port_id:string ->
          ?extension:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?ibc2_port_id:string ->
          unit ->
          t

        let make ?(code_id = 0L) ?(creator = {||}) ?(admin = {||})
            ?(label = {||}) ?created ?(ibc_port_id = {||}) ?extension
            ?(ibc2_port_id = {||}) () =
          {
            code_id;
            creator;
            admin;
            label;
            created;
            ibc_port_id;
            extension;
            ibc2_port_id;
          }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_code_id =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "code_id", "codeId"), uint64, 0L))
               in
               let merge_creator =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "creator", "creator"), string, {||}))
               in
               let merge_admin =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((3, "admin", "admin"), string, {||}))
               in
               let merge_label =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((4, "label", "label"), string, {||}))
               in
               let merge_created =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (5, "created", "created"),
                         message (module AbsoluteTxPosition) ))
               in
               let merge_ibc_port_id =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((6, "ibc_port_id", "ibcPortId"), string, {||}))
               in
               let merge_extension =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (7, "extension", "extension"),
                         message
                           (module Imported'modules.Google_protobuf_any.Google
                                   .Protobuf
                                   .Any) ))
               in
               let merge_ibc2_port_id =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((8, "ibc2_port_id", "ibc2PortId"), string, {||}))
               in
               fun t1 t2 ->
                 {
                   code_id = merge_code_id t1.code_id t2.code_id;
                   creator = merge_creator t1.creator t2.creator;
                   admin = merge_admin t1.admin t2.admin;
                   label = merge_label t1.label t2.label;
                   created = merge_created t1.created t2.created;
                   ibc_port_id = merge_ibc_port_id t1.ibc_port_id t2.ibc_port_id;
                   extension = merge_extension t1.extension t2.extension;
                   ibc2_port_id =
                     merge_ibc2_port_id t1.ibc2_port_id t2.ibc2_port_id;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "code_id", "codeId"), uint64, 0L)
            ^:: basic ((2, "creator", "creator"), string, {||})
            ^:: basic ((3, "admin", "admin"), string, {||})
            ^:: basic ((4, "label", "label"), string, {||})
            ^:: basic_opt
                  ( (5, "created", "created"),
                    message (module AbsoluteTxPosition) )
            ^:: basic ((6, "ibc_port_id", "ibcPortId"), string, {||})
            ^:: basic_opt
                  ( (7, "extension", "extension"),
                    message
                      (module Imported'modules.Google_protobuf_any.Google
                              .Protobuf
                              .Any) )
            ^:: basic ((8, "ibc2_port_id", "ibc2PortId"), string, {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer
            {
              code_id;
              creator;
              admin;
              label;
              created;
              ibc_port_id;
              extension;
              ibc2_port_id;
            }
          ->
            serialize writer code_id creator admin label created ibc_port_id
              extension ibc2_port_id

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor code_id creator admin label created ibc_port_id
              extension ibc2_port_id =
            {
              code_id;
              creator;
              admin;
              label;
              created;
              ibc_port_id;
              extension;
              ibc2_port_id;
            }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun {
                code_id;
                creator;
                admin;
                label;
                created;
                ibc_port_id;
                extension;
                ibc2_port_id;
              }
          ->
            serialize code_id creator admin label created ibc_port_id extension
              ibc2_port_id

        let from_json_exn =
          let constructor code_id creator admin label created ibc_port_id
              extension ibc2_port_id =
            {
              code_id;
              creator;
              admin;
              label;
              created;
              ibc_port_id;
              extension;
              ibc2_port_id;
            }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and ContractCodeHistoryEntry : sig
        type t = {
          operation : ContractCodeHistoryOperationType.t;
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          updated : AbsoluteTxPosition.t option;
              (** {%html:
<p>Updated Tx position when the operation was executed.</p>
%}
              *)
          msg : bytes;
        }

        val make :
          ?operation:ContractCodeHistoryOperationType.t ->
          ?code_id:int64 ->
          ?updated:AbsoluteTxPosition.t ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?operation:ContractCodeHistoryOperationType.t ->
          ?code_id:int64 ->
          ?updated:AbsoluteTxPosition.t ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = ContractCodeHistoryEntry

        let name () = ".cosmwasm.wasm.v1.ContractCodeHistoryEntry"

        type t = {
          operation : ContractCodeHistoryOperationType.t;
          code_id : int64;
          updated : AbsoluteTxPosition.t option;
          msg : bytes;
        }

        type make_t =
          ?operation:ContractCodeHistoryOperationType.t ->
          ?code_id:int64 ->
          ?updated:AbsoluteTxPosition.t ->
          ?msg:bytes ->
          unit ->
          t

        let make ?(operation = ContractCodeHistoryOperationType.from_int_exn 0)
            ?(code_id = 0L) ?updated ?(msg = Bytes.of_string {||}) () =
          { operation; code_id; updated; msg }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_operation =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (1, "operation", "operation"),
                         enum (module ContractCodeHistoryOperationType),
                         ContractCodeHistoryOperationType.from_int_exn 0 ))
               in
               let merge_code_id =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((2, "code_id", "codeId"), uint64, 0L))
               in
               let merge_updated =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (3, "updated", "updated"),
                         message (module AbsoluteTxPosition) ))
               in
               let merge_msg =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((4, "msg", "msg"), bytes, Bytes.of_string {||}))
               in
               fun t1 t2 ->
                 {
                   operation = merge_operation t1.operation t2.operation;
                   code_id = merge_code_id t1.code_id t2.code_id;
                   updated = merge_updated t1.updated t2.updated;
                   msg = merge_msg t1.msg t2.msg;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic
              ( (1, "operation", "operation"),
                enum (module ContractCodeHistoryOperationType),
                ContractCodeHistoryOperationType.from_int_exn 0 )
            ^:: basic ((2, "code_id", "codeId"), uint64, 0L)
            ^:: basic_opt
                  ( (3, "updated", "updated"),
                    message (module AbsoluteTxPosition) )
            ^:: basic ((4, "msg", "msg"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { operation; code_id; updated; msg } ->
            serialize writer operation code_id updated msg

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor operation code_id updated msg =
            { operation; code_id; updated; msg }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { operation; code_id; updated; msg } ->
            serialize operation code_id updated msg

        let from_json_exn =
          let constructor operation code_id updated msg =
            { operation; code_id; updated; msg }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and AbsoluteTxPosition : sig
        type t = {
          block_height : int64;
              (** {%html:
<p>BlockHeight is the block the contract was created at</p>
%}
              *)
          tx_index : int64;
              (** {%html:
<p>TxIndex is a monotonic counter within the block (actual transaction index,
or gas consumed)</p>
%}
              *)
        }

        val make : ?block_height:int64 -> ?tx_index:int64 -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?block_height:int64 -> ?tx_index:int64 -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = AbsoluteTxPosition

        let name () = ".cosmwasm.wasm.v1.AbsoluteTxPosition"

        type t = { block_height : int64; tx_index : int64 }
        type make_t = ?block_height:int64 -> ?tx_index:int64 -> unit -> t

        let make ?(block_height = 0L) ?(tx_index = 0L) () =
          { block_height; tx_index }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_block_height =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "block_height", "blockHeight"), uint64, 0L))
               in
               let merge_tx_index =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "tx_index", "txIndex"), uint64, 0L))
               in
               fun t1 t2 ->
                 {
                   block_height =
                     merge_block_height t1.block_height t2.block_height;
                   tx_index = merge_tx_index t1.tx_index t2.tx_index;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "block_height", "blockHeight"), uint64, 0L)
            ^:: basic ((2, "tx_index", "txIndex"), uint64, 0L)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { block_height; tx_index } ->
            serialize writer block_height tx_index

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor block_height tx_index = { block_height; tx_index } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { block_height; tx_index } -> serialize block_height tx_index

        let from_json_exn =
          let constructor block_height tx_index = { block_height; tx_index } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and Model : sig
        type t = {
          key : bytes;
              (** {%html:
<p>hex-encode key to read it better (this is often ascii)</p>
%}
              *)
          value : bytes;  (** {%html:
<p>base64-encode raw value</p>
%} *)
        }

        val make : ?key:bytes -> ?value:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?key:bytes -> ?value:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = Model

        let name () = ".cosmwasm.wasm.v1.Model"

        type t = { key : bytes; value : bytes }
        type make_t = ?key:bytes -> ?value:bytes -> unit -> t

        let make ?(key = Bytes.of_string {||}) ?(value = Bytes.of_string {||})
            () =
          { key; value }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_key =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "key", "key"), bytes, Bytes.of_string {||}))
               in
               let merge_value =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "value", "value"), bytes, Bytes.of_string {||}))
               in
               fun t1 t2 ->
                 {
                   key = merge_key t1.key t2.key;
                   value = merge_value t1.value t2.value;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "key", "key"), bytes, Bytes.of_string {||})
            ^:: basic ((2, "value", "value"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { key; value } -> serialize writer key value

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor key value = { key; value } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { key; value } -> serialize key value

        let from_json_exn =
          let constructor key value = { key; value } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end
    end
  end
end

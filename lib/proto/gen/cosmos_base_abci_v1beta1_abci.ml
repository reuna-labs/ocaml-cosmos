(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: cosmos/base/abci/v1beta1/abci.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Gogoproto_gogo = Gogoproto_gogo
  module Tendermint_abci_types = Tendermint_abci_types
  module Tendermint_types_block = Tendermint_types_block
  module Google_protobuf_any = Google_protobuf_any
  module Cosmos_proto_cosmos = Cosmos_proto_cosmos
end

(**/**)

module rec Cosmos : sig
  module rec Base : sig
    module rec Abci : sig
      module rec V1beta1 : sig
        (** {%html:
<p>TxResponse defines a structure containing relevant tx data and metadata. The
tags are stringified and the log is JSON decoded.</p>
%}
        *)
        module rec TxResponse : sig
          type t = {
            height : int64;  (** {%html:
<p>The block height</p>
%} *)
            txhash : string;  (** {%html:
<p>The transaction hash.</p>
%} *)
            codespace : string;  (** {%html:
<p>Namespace for the Code</p>
%} *)
            code : int;  (** {%html:
<p>Response code.</p>
%} *)
            data : string;  (** {%html:
<p>Result bytes, if any.</p>
%} *)
            raw_log : string;
                (** {%html:
<p>The output of the application's logger (raw string). May be
non-deterministic.</p>
%}
                *)
            logs : ABCIMessageLog.t list;
                (** {%html:
<p>The output of the application's logger (typed). May be non-deterministic.</p>
%}
                *)
            info : string;
                (** {%html:
<p>Additional information. May be non-deterministic.</p>
%}
                *)
            gas_wanted : int64;
                (** {%html:
<p>Amount of gas requested for transaction.</p>
%}
                *)
            gas_used : int64;
                (** {%html:
<p>Amount of gas consumed by transaction.</p>
%} *)
            tx :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>The request transaction bytes.</p>
%} *)
            timestamp : string;
                (** {%html:
<p>Time of the previous block. For heights &gt; 1, it's the weighted median of
the timestamps of the valid votes in the block.LastCommit. For height == 1,
it's genesis time.</p>
%}
                *)
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
                (** {%html:
<p>Events defines all the events emitted by processing a transaction. Note,
these events include those emitted by processing all the messages and those
emitted from the ante. Whereas Logs contains the events, with
additional metadata, emitted only by processing the messages.</p>
%}
                *)
          }

          val make :
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ABCIMessageLog defines a structure containing an indexed tx ABCI message log.</p>
%}
        *)
        and ABCIMessageLog : sig
          type t = {
            msg_index : int;
            log : string;
            events : StringEvent.t list;
                (** {%html:
<p>Events contains a slice of Event objects that were emitted during some
execution.</p>
%}
                *)
          }

          val make :
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>StringEvent defines en Event object wrapper where all the attributes
contain key/value pairs that are strings instead of raw bytes.</p>
%}
        *)
        and StringEvent : sig
          type t = { type' : string; attributes : Attribute.t list }

          val make : ?type':string -> ?attributes:Attribute.t list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?type':string -> ?attributes:Attribute.t list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Attribute defines an attribute wrapper where the key and value are
strings instead of raw bytes.</p>
%}
        *)
        and Attribute : sig
          type t = { key : string; value : string }

          val make : ?key:string -> ?value:string -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?key:string -> ?value:string -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>GasInfo defines tx execution gas context.</p>
%} *)
        and GasInfo : sig
          type t = {
            gas_wanted : int64;
                (** {%html:
<p>GasWanted is the maximum units of work we allow this tx to perform.</p>
%}
                *)
            gas_used : int64;
                (** {%html:
<p>GasUsed is the amount of gas actually consumed.</p>
%}
                *)
          }

          val make : ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Result is the union of ResponseFormat and ResponseCheckTx.</p>
%}
        *)
        and Result : sig
          type t = {
            data : bytes;
                [@ocaml.alert
                  protobuf "Marked as deprecated in the .proto file"]
                (** {%html:
<p>Data is any data returned from message or handler execution. It MUST be
length prefixed in order to separate data from multiple message executions.
Deprecated. This field is still populated, but prefer msg_response instead
because it also contains the Msg response typeURL.</p>
%}
                *)
            log : string;
                (** {%html:
<p>Log contains the log information from message or handler execution.</p>
%}
                *)
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
                (** {%html:
<p>Events contains a slice of Event objects that were emitted during message
or handler execution.</p>
%}
                *)
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
                (** {%html:
<p>msg_responses contains the Msg handler responses type packed in Anys.</p>
%}
                *)
          }

          val make :
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SimulationResponse defines the response generated when a transaction is
successfully simulated.</p>
%}
        *)
        and SimulationResponse : sig
          type t = { gas_info : GasInfo.t option; result : Result.t option }

          val make : ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgData defines the data returned in a Result object during message
execution.</p>
%}
        *)
        and MsgData : sig
          type t = { msg_type : string; data : bytes }

          val make : ?msg_type:string -> ?data:bytes -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?msg_type:string -> ?data:bytes -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
        [@@ocaml.alert protobuf "Marked as deprecated in the .proto file"]

        (** {%html:
<p>TxMsgData defines a list of MsgData. A transaction will have a MsgData object
for each message.</p>
%}
        *)
        and TxMsgData : sig
          type t = {
            data : MsgData.t list;
                [@ocaml.alert
                  protobuf "Marked as deprecated in the .proto file"]
                (** {%html:
<p>data field is deprecated and not populated.</p>
%}
                *)
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
                (** {%html:
<p>msg_responses contains the Msg handler responses packed into Anys.</p>
%}
                *)
          }

          val make :
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SearchTxsResult defines a structure for querying txs pageable</p>
%}
        *)
        and SearchTxsResult : sig
          type t = {
            total_count : int64;  (** {%html:
<p>Count of all txs</p>
%} *)
            count : int64;
                (** {%html:
<p>Count of txs in current page</p>
%} *)
            page_number : int64;
                (** {%html:
<p>Index of current page, start from 1</p>
%} *)
            page_total : int64;  (** {%html:
<p>Count of total pages</p>
%} *)
            limit : int64;  (** {%html:
<p>Max count txs per page</p>
%} *)
            txs : TxResponse.t list;
                (** {%html:
<p>List of txs in current page</p>
%} *)
          }

          val make :
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SearchBlocksResult defines a structure for querying blocks pageable</p>
%}
        *)
        and SearchBlocksResult : sig
          type t = {
            total_count : int64;  (** {%html:
<p>Count of all blocks</p>
%} *)
            count : int64;
                (** {%html:
<p>Count of blocks in current page</p>
%} *)
            page_number : int64;
                (** {%html:
<p>Index of current page, start from 1</p>
%} *)
            page_total : int64;  (** {%html:
<p>Count of total pages</p>
%} *)
            limit : int64;  (** {%html:
<p>Max count blocks per page</p>
%} *)
            blocks :
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list;
                (** {%html:
<p>List of blocks in current page</p>
%} *)
          }

          val make :
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end
  end
end = struct
  module rec Base : sig
    module rec Abci : sig
      module rec V1beta1 : sig
        (** {%html:
<p>TxResponse defines a structure containing relevant tx data and metadata. The
tags are stringified and the log is JSON decoded.</p>
%}
        *)
        module rec TxResponse : sig
          type t = {
            height : int64;  (** {%html:
<p>The block height</p>
%} *)
            txhash : string;  (** {%html:
<p>The transaction hash.</p>
%} *)
            codespace : string;  (** {%html:
<p>Namespace for the Code</p>
%} *)
            code : int;  (** {%html:
<p>Response code.</p>
%} *)
            data : string;  (** {%html:
<p>Result bytes, if any.</p>
%} *)
            raw_log : string;
                (** {%html:
<p>The output of the application's logger (raw string). May be
non-deterministic.</p>
%}
                *)
            logs : ABCIMessageLog.t list;
                (** {%html:
<p>The output of the application's logger (typed). May be non-deterministic.</p>
%}
                *)
            info : string;
                (** {%html:
<p>Additional information. May be non-deterministic.</p>
%}
                *)
            gas_wanted : int64;
                (** {%html:
<p>Amount of gas requested for transaction.</p>
%}
                *)
            gas_used : int64;
                (** {%html:
<p>Amount of gas consumed by transaction.</p>
%} *)
            tx :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>The request transaction bytes.</p>
%} *)
            timestamp : string;
                (** {%html:
<p>Time of the previous block. For heights &gt; 1, it's the weighted median of
the timestamps of the valid votes in the block.LastCommit. For height == 1,
it's genesis time.</p>
%}
                *)
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
                (** {%html:
<p>Events defines all the events emitted by processing a transaction. Note,
these events include those emitted by processing all the messages and those
emitted from the ante. Whereas Logs contains the events, with
additional metadata, emitted only by processing the messages.</p>
%}
                *)
          }

          val make :
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ABCIMessageLog defines a structure containing an indexed tx ABCI message log.</p>
%}
        *)
        and ABCIMessageLog : sig
          type t = {
            msg_index : int;
            log : string;
            events : StringEvent.t list;
                (** {%html:
<p>Events contains a slice of Event objects that were emitted during some
execution.</p>
%}
                *)
          }

          val make :
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>StringEvent defines en Event object wrapper where all the attributes
contain key/value pairs that are strings instead of raw bytes.</p>
%}
        *)
        and StringEvent : sig
          type t = { type' : string; attributes : Attribute.t list }

          val make : ?type':string -> ?attributes:Attribute.t list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?type':string -> ?attributes:Attribute.t list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Attribute defines an attribute wrapper where the key and value are
strings instead of raw bytes.</p>
%}
        *)
        and Attribute : sig
          type t = { key : string; value : string }

          val make : ?key:string -> ?value:string -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?key:string -> ?value:string -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>GasInfo defines tx execution gas context.</p>
%} *)
        and GasInfo : sig
          type t = {
            gas_wanted : int64;
                (** {%html:
<p>GasWanted is the maximum units of work we allow this tx to perform.</p>
%}
                *)
            gas_used : int64;
                (** {%html:
<p>GasUsed is the amount of gas actually consumed.</p>
%}
                *)
          }

          val make : ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Result is the union of ResponseFormat and ResponseCheckTx.</p>
%}
        *)
        and Result : sig
          type t = {
            data : bytes;
                [@ocaml.alert
                  protobuf "Marked as deprecated in the .proto file"]
                (** {%html:
<p>Data is any data returned from message or handler execution. It MUST be
length prefixed in order to separate data from multiple message executions.
Deprecated. This field is still populated, but prefer msg_response instead
because it also contains the Msg response typeURL.</p>
%}
                *)
            log : string;
                (** {%html:
<p>Log contains the log information from message or handler execution.</p>
%}
                *)
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
                (** {%html:
<p>Events contains a slice of Event objects that were emitted during message
or handler execution.</p>
%}
                *)
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
                (** {%html:
<p>msg_responses contains the Msg handler responses type packed in Anys.</p>
%}
                *)
          }

          val make :
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SimulationResponse defines the response generated when a transaction is
successfully simulated.</p>
%}
        *)
        and SimulationResponse : sig
          type t = { gas_info : GasInfo.t option; result : Result.t option }

          val make : ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgData defines the data returned in a Result object during message
execution.</p>
%}
        *)
        and MsgData : sig
          type t = { msg_type : string; data : bytes }

          val make : ?msg_type:string -> ?data:bytes -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?msg_type:string -> ?data:bytes -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
        [@@ocaml.alert protobuf "Marked as deprecated in the .proto file"]

        (** {%html:
<p>TxMsgData defines a list of MsgData. A transaction will have a MsgData object
for each message.</p>
%}
        *)
        and TxMsgData : sig
          type t = {
            data : MsgData.t list;
                [@ocaml.alert
                  protobuf "Marked as deprecated in the .proto file"]
                (** {%html:
<p>data field is deprecated and not populated.</p>
%}
                *)
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
                (** {%html:
<p>msg_responses contains the Msg handler responses packed into Anys.</p>
%}
                *)
          }

          val make :
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SearchTxsResult defines a structure for querying txs pageable</p>
%}
        *)
        and SearchTxsResult : sig
          type t = {
            total_count : int64;  (** {%html:
<p>Count of all txs</p>
%} *)
            count : int64;
                (** {%html:
<p>Count of txs in current page</p>
%} *)
            page_number : int64;
                (** {%html:
<p>Index of current page, start from 1</p>
%} *)
            page_total : int64;  (** {%html:
<p>Count of total pages</p>
%} *)
            limit : int64;  (** {%html:
<p>Max count txs per page</p>
%} *)
            txs : TxResponse.t list;
                (** {%html:
<p>List of txs in current page</p>
%} *)
          }

          val make :
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SearchBlocksResult defines a structure for querying blocks pageable</p>
%}
        *)
        and SearchBlocksResult : sig
          type t = {
            total_count : int64;  (** {%html:
<p>Count of all blocks</p>
%} *)
            count : int64;
                (** {%html:
<p>Count of blocks in current page</p>
%} *)
            page_number : int64;
                (** {%html:
<p>Index of current page, start from 1</p>
%} *)
            page_total : int64;  (** {%html:
<p>Count of total pages</p>
%} *)
            limit : int64;  (** {%html:
<p>Max count blocks per page</p>
%} *)
            blocks :
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list;
                (** {%html:
<p>List of blocks in current page</p>
%} *)
          }

          val make :
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end
  end = struct
    module rec Abci : sig
      module rec V1beta1 : sig
        (** {%html:
<p>TxResponse defines a structure containing relevant tx data and metadata. The
tags are stringified and the log is JSON decoded.</p>
%}
        *)
        module rec TxResponse : sig
          type t = {
            height : int64;  (** {%html:
<p>The block height</p>
%} *)
            txhash : string;  (** {%html:
<p>The transaction hash.</p>
%} *)
            codespace : string;  (** {%html:
<p>Namespace for the Code</p>
%} *)
            code : int;  (** {%html:
<p>Response code.</p>
%} *)
            data : string;  (** {%html:
<p>Result bytes, if any.</p>
%} *)
            raw_log : string;
                (** {%html:
<p>The output of the application's logger (raw string). May be
non-deterministic.</p>
%}
                *)
            logs : ABCIMessageLog.t list;
                (** {%html:
<p>The output of the application's logger (typed). May be non-deterministic.</p>
%}
                *)
            info : string;
                (** {%html:
<p>Additional information. May be non-deterministic.</p>
%}
                *)
            gas_wanted : int64;
                (** {%html:
<p>Amount of gas requested for transaction.</p>
%}
                *)
            gas_used : int64;
                (** {%html:
<p>Amount of gas consumed by transaction.</p>
%} *)
            tx :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>The request transaction bytes.</p>
%} *)
            timestamp : string;
                (** {%html:
<p>Time of the previous block. For heights &gt; 1, it's the weighted median of
the timestamps of the valid votes in the block.LastCommit. For height == 1,
it's genesis time.</p>
%}
                *)
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
                (** {%html:
<p>Events defines all the events emitted by processing a transaction. Note,
these events include those emitted by processing all the messages and those
emitted from the ante. Whereas Logs contains the events, with
additional metadata, emitted only by processing the messages.</p>
%}
                *)
          }

          val make :
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ABCIMessageLog defines a structure containing an indexed tx ABCI message log.</p>
%}
        *)
        and ABCIMessageLog : sig
          type t = {
            msg_index : int;
            log : string;
            events : StringEvent.t list;
                (** {%html:
<p>Events contains a slice of Event objects that were emitted during some
execution.</p>
%}
                *)
          }

          val make :
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>StringEvent defines en Event object wrapper where all the attributes
contain key/value pairs that are strings instead of raw bytes.</p>
%}
        *)
        and StringEvent : sig
          type t = { type' : string; attributes : Attribute.t list }

          val make : ?type':string -> ?attributes:Attribute.t list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?type':string -> ?attributes:Attribute.t list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Attribute defines an attribute wrapper where the key and value are
strings instead of raw bytes.</p>
%}
        *)
        and Attribute : sig
          type t = { key : string; value : string }

          val make : ?key:string -> ?value:string -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?key:string -> ?value:string -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>GasInfo defines tx execution gas context.</p>
%} *)
        and GasInfo : sig
          type t = {
            gas_wanted : int64;
                (** {%html:
<p>GasWanted is the maximum units of work we allow this tx to perform.</p>
%}
                *)
            gas_used : int64;
                (** {%html:
<p>GasUsed is the amount of gas actually consumed.</p>
%}
                *)
          }

          val make : ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Result is the union of ResponseFormat and ResponseCheckTx.</p>
%}
        *)
        and Result : sig
          type t = {
            data : bytes;
                [@ocaml.alert
                  protobuf "Marked as deprecated in the .proto file"]
                (** {%html:
<p>Data is any data returned from message or handler execution. It MUST be
length prefixed in order to separate data from multiple message executions.
Deprecated. This field is still populated, but prefer msg_response instead
because it also contains the Msg response typeURL.</p>
%}
                *)
            log : string;
                (** {%html:
<p>Log contains the log information from message or handler execution.</p>
%}
                *)
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
                (** {%html:
<p>Events contains a slice of Event objects that were emitted during message
or handler execution.</p>
%}
                *)
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
                (** {%html:
<p>msg_responses contains the Msg handler responses type packed in Anys.</p>
%}
                *)
          }

          val make :
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SimulationResponse defines the response generated when a transaction is
successfully simulated.</p>
%}
        *)
        and SimulationResponse : sig
          type t = { gas_info : GasInfo.t option; result : Result.t option }

          val make : ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgData defines the data returned in a Result object during message
execution.</p>
%}
        *)
        and MsgData : sig
          type t = { msg_type : string; data : bytes }

          val make : ?msg_type:string -> ?data:bytes -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?msg_type:string -> ?data:bytes -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
        [@@ocaml.alert protobuf "Marked as deprecated in the .proto file"]

        (** {%html:
<p>TxMsgData defines a list of MsgData. A transaction will have a MsgData object
for each message.</p>
%}
        *)
        and TxMsgData : sig
          type t = {
            data : MsgData.t list;
                [@ocaml.alert
                  protobuf "Marked as deprecated in the .proto file"]
                (** {%html:
<p>data field is deprecated and not populated.</p>
%}
                *)
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
                (** {%html:
<p>msg_responses contains the Msg handler responses packed into Anys.</p>
%}
                *)
          }

          val make :
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SearchTxsResult defines a structure for querying txs pageable</p>
%}
        *)
        and SearchTxsResult : sig
          type t = {
            total_count : int64;  (** {%html:
<p>Count of all txs</p>
%} *)
            count : int64;
                (** {%html:
<p>Count of txs in current page</p>
%} *)
            page_number : int64;
                (** {%html:
<p>Index of current page, start from 1</p>
%} *)
            page_total : int64;  (** {%html:
<p>Count of total pages</p>
%} *)
            limit : int64;  (** {%html:
<p>Max count txs per page</p>
%} *)
            txs : TxResponse.t list;
                (** {%html:
<p>List of txs in current page</p>
%} *)
          }

          val make :
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SearchBlocksResult defines a structure for querying blocks pageable</p>
%}
        *)
        and SearchBlocksResult : sig
          type t = {
            total_count : int64;  (** {%html:
<p>Count of all blocks</p>
%} *)
            count : int64;
                (** {%html:
<p>Count of blocks in current page</p>
%} *)
            page_number : int64;
                (** {%html:
<p>Index of current page, start from 1</p>
%} *)
            page_total : int64;  (** {%html:
<p>Count of total pages</p>
%} *)
            limit : int64;  (** {%html:
<p>Max count blocks per page</p>
%} *)
            blocks :
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list;
                (** {%html:
<p>List of blocks in current page</p>
%} *)
          }

          val make :
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end = struct
      module rec V1beta1 : sig
        (** {%html:
<p>TxResponse defines a structure containing relevant tx data and metadata. The
tags are stringified and the log is JSON decoded.</p>
%}
        *)
        module rec TxResponse : sig
          type t = {
            height : int64;  (** {%html:
<p>The block height</p>
%} *)
            txhash : string;  (** {%html:
<p>The transaction hash.</p>
%} *)
            codespace : string;  (** {%html:
<p>Namespace for the Code</p>
%} *)
            code : int;  (** {%html:
<p>Response code.</p>
%} *)
            data : string;  (** {%html:
<p>Result bytes, if any.</p>
%} *)
            raw_log : string;
                (** {%html:
<p>The output of the application's logger (raw string). May be
non-deterministic.</p>
%}
                *)
            logs : ABCIMessageLog.t list;
                (** {%html:
<p>The output of the application's logger (typed). May be non-deterministic.</p>
%}
                *)
            info : string;
                (** {%html:
<p>Additional information. May be non-deterministic.</p>
%}
                *)
            gas_wanted : int64;
                (** {%html:
<p>Amount of gas requested for transaction.</p>
%}
                *)
            gas_used : int64;
                (** {%html:
<p>Amount of gas consumed by transaction.</p>
%} *)
            tx :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>The request transaction bytes.</p>
%} *)
            timestamp : string;
                (** {%html:
<p>Time of the previous block. For heights &gt; 1, it's the weighted median of
the timestamps of the valid votes in the block.LastCommit. For height == 1,
it's genesis time.</p>
%}
                *)
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
                (** {%html:
<p>Events defines all the events emitted by processing a transaction. Note,
these events include those emitted by processing all the messages and those
emitted from the ante. Whereas Logs contains the events, with
additional metadata, emitted only by processing the messages.</p>
%}
                *)
          }

          val make :
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ABCIMessageLog defines a structure containing an indexed tx ABCI message log.</p>
%}
        *)
        and ABCIMessageLog : sig
          type t = {
            msg_index : int;
            log : string;
            events : StringEvent.t list;
                (** {%html:
<p>Events contains a slice of Event objects that were emitted during some
execution.</p>
%}
                *)
          }

          val make :
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>StringEvent defines en Event object wrapper where all the attributes
contain key/value pairs that are strings instead of raw bytes.</p>
%}
        *)
        and StringEvent : sig
          type t = { type' : string; attributes : Attribute.t list }

          val make : ?type':string -> ?attributes:Attribute.t list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?type':string -> ?attributes:Attribute.t list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Attribute defines an attribute wrapper where the key and value are
strings instead of raw bytes.</p>
%}
        *)
        and Attribute : sig
          type t = { key : string; value : string }

          val make : ?key:string -> ?value:string -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?key:string -> ?value:string -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>GasInfo defines tx execution gas context.</p>
%} *)
        and GasInfo : sig
          type t = {
            gas_wanted : int64;
                (** {%html:
<p>GasWanted is the maximum units of work we allow this tx to perform.</p>
%}
                *)
            gas_used : int64;
                (** {%html:
<p>GasUsed is the amount of gas actually consumed.</p>
%}
                *)
          }

          val make : ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Result is the union of ResponseFormat and ResponseCheckTx.</p>
%}
        *)
        and Result : sig
          type t = {
            data : bytes;
                [@ocaml.alert
                  protobuf "Marked as deprecated in the .proto file"]
                (** {%html:
<p>Data is any data returned from message or handler execution. It MUST be
length prefixed in order to separate data from multiple message executions.
Deprecated. This field is still populated, but prefer msg_response instead
because it also contains the Msg response typeURL.</p>
%}
                *)
            log : string;
                (** {%html:
<p>Log contains the log information from message or handler execution.</p>
%}
                *)
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
                (** {%html:
<p>Events contains a slice of Event objects that were emitted during message
or handler execution.</p>
%}
                *)
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
                (** {%html:
<p>msg_responses contains the Msg handler responses type packed in Anys.</p>
%}
                *)
          }

          val make :
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SimulationResponse defines the response generated when a transaction is
successfully simulated.</p>
%}
        *)
        and SimulationResponse : sig
          type t = { gas_info : GasInfo.t option; result : Result.t option }

          val make : ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>MsgData defines the data returned in a Result object during message
execution.</p>
%}
        *)
        and MsgData : sig
          type t = { msg_type : string; data : bytes }

          val make : ?msg_type:string -> ?data:bytes -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?msg_type:string -> ?data:bytes -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
        [@@ocaml.alert protobuf "Marked as deprecated in the .proto file"]

        (** {%html:
<p>TxMsgData defines a list of MsgData. A transaction will have a MsgData object
for each message.</p>
%}
        *)
        and TxMsgData : sig
          type t = {
            data : MsgData.t list;
                [@ocaml.alert
                  protobuf "Marked as deprecated in the .proto file"]
                (** {%html:
<p>data field is deprecated and not populated.</p>
%}
                *)
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
                (** {%html:
<p>msg_responses contains the Msg handler responses packed into Anys.</p>
%}
                *)
          }

          val make :
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SearchTxsResult defines a structure for querying txs pageable</p>
%}
        *)
        and SearchTxsResult : sig
          type t = {
            total_count : int64;  (** {%html:
<p>Count of all txs</p>
%} *)
            count : int64;
                (** {%html:
<p>Count of txs in current page</p>
%} *)
            page_number : int64;
                (** {%html:
<p>Index of current page, start from 1</p>
%} *)
            page_total : int64;  (** {%html:
<p>Count of total pages</p>
%} *)
            limit : int64;  (** {%html:
<p>Max count txs per page</p>
%} *)
            txs : TxResponse.t list;
                (** {%html:
<p>List of txs in current page</p>
%} *)
          }

          val make :
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>SearchBlocksResult defines a structure for querying blocks pageable</p>
%}
        *)
        and SearchBlocksResult : sig
          type t = {
            total_count : int64;  (** {%html:
<p>Count of all blocks</p>
%} *)
            count : int64;
                (** {%html:
<p>Count of blocks in current page</p>
%} *)
            page_number : int64;
                (** {%html:
<p>Index of current page, start from 1</p>
%} *)
            page_total : int64;  (** {%html:
<p>Count of total pages</p>
%} *)
            limit : int64;  (** {%html:
<p>Max count blocks per page</p>
%} *)
            blocks :
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list;
                (** {%html:
<p>List of blocks in current page</p>
%} *)
          }

          val make :
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end = struct
        module rec TxResponse : sig
          type t = {
            height : int64;  (** {%html:
<p>The block height</p>
%} *)
            txhash : string;  (** {%html:
<p>The transaction hash.</p>
%} *)
            codespace : string;  (** {%html:
<p>Namespace for the Code</p>
%} *)
            code : int;  (** {%html:
<p>Response code.</p>
%} *)
            data : string;  (** {%html:
<p>Result bytes, if any.</p>
%} *)
            raw_log : string;
                (** {%html:
<p>The output of the application's logger (raw string). May be
non-deterministic.</p>
%}
                *)
            logs : ABCIMessageLog.t list;
                (** {%html:
<p>The output of the application's logger (typed). May be non-deterministic.</p>
%}
                *)
            info : string;
                (** {%html:
<p>Additional information. May be non-deterministic.</p>
%}
                *)
            gas_wanted : int64;
                (** {%html:
<p>Amount of gas requested for transaction.</p>
%}
                *)
            gas_used : int64;
                (** {%html:
<p>Amount of gas consumed by transaction.</p>
%} *)
            tx :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>The request transaction bytes.</p>
%} *)
            timestamp : string;
                (** {%html:
<p>Time of the previous block. For heights &gt; 1, it's the weighted median of
the timestamps of the valid votes in the block.LastCommit. For height == 1,
it's genesis time.</p>
%}
                *)
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
                (** {%html:
<p>Events defines all the events emitted by processing a transaction. Note,
these events include those emitted by processing all the messages and those
emitted from the ante. Whereas Logs contains the events, with
additional metadata, emitted only by processing the messages.</p>
%}
                *)
          }

          val make :
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = TxResponse

          let name () = ".cosmos.base.abci.v1beta1.TxResponse"

          type t = {
            height : int64;
            txhash : string;
            codespace : string;
            code : int;
            data : string;
            raw_log : string;
            logs : ABCIMessageLog.t list;
            info : string;
            gas_wanted : int64;
            gas_used : int64;
            tx :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
            timestamp : string;
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
          }

          type make_t =
            ?height:int64 ->
            ?txhash:string ->
            ?codespace:string ->
            ?code:int ->
            ?data:string ->
            ?raw_log:string ->
            ?logs:ABCIMessageLog.t list ->
            ?info:string ->
            ?gas_wanted:int64 ->
            ?gas_used:int64 ->
            ?tx:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            ?timestamp:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            unit ->
            t

          let make ?(height = 0L) ?(txhash = {||}) ?(codespace = {||})
              ?(code = 0) ?(data = {||}) ?(raw_log = {||}) ?(logs = [])
              ?(info = {||}) ?(gas_wanted = 0L) ?(gas_used = 0L) ?tx
              ?(timestamp = {||}) ?(events = []) () =
            {
              height;
              txhash;
              codespace;
              code;
              data;
              raw_log;
              logs;
              info;
              gas_wanted;
              gas_used;
              tx;
              timestamp;
              events;
            }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_height =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((1, "height", "height"), int64, 0L))
                 in
                 let merge_txhash =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((2, "txhash", "txhash"), string, {||}))
                 in
                 let merge_codespace =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((3, "codespace", "codespace"), string, {||}))
                 in
                 let merge_code =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((4, "code", "code"), uint32_int, 0))
                 in
                 let merge_data =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((5, "data", "data"), string, {||}))
                 in
                 let merge_raw_log =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((6, "raw_log", "rawLog"), string, {||}))
                 in
                 let merge_logs =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (7, "logs", "logs"),
                           message (module ABCIMessageLog),
                           not_packed ))
                 in
                 let merge_info =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((8, "info", "info"), string, {||}))
                 in
                 let merge_gas_wanted =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((9, "gas_wanted", "gasWanted"), int64, 0L))
                 in
                 let merge_gas_used =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((10, "gas_used", "gasUsed"), int64, 0L))
                 in
                 let merge_tx =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt
                         ( (11, "tx", "tx"),
                           message
                             (module Imported'modules.Google_protobuf_any.Google
                                     .Protobuf
                                     .Any) ))
                 in
                 let merge_timestamp =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((12, "timestamp", "timestamp"), string, {||}))
                 in
                 let merge_events =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (13, "events", "events"),
                           message
                             (module Imported'modules.Tendermint_abci_types
                                     .Tendermint
                                     .Abci
                                     .Event),
                           not_packed ))
                 in
                 fun t1 t2 ->
                   {
                     height = merge_height t1.height t2.height;
                     txhash = merge_txhash t1.txhash t2.txhash;
                     codespace = merge_codespace t1.codespace t2.codespace;
                     code = merge_code t1.code t2.code;
                     data = merge_data t1.data t2.data;
                     raw_log = merge_raw_log t1.raw_log t2.raw_log;
                     logs = merge_logs t1.logs t2.logs;
                     info = merge_info t1.info t2.info;
                     gas_wanted = merge_gas_wanted t1.gas_wanted t2.gas_wanted;
                     gas_used = merge_gas_used t1.gas_used t2.gas_used;
                     tx = merge_tx t1.tx t2.tx;
                     timestamp = merge_timestamp t1.timestamp t2.timestamp;
                     events = merge_events t1.events t2.events;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "height", "height"), int64, 0L)
              ^:: basic ((2, "txhash", "txhash"), string, {||})
              ^:: basic ((3, "codespace", "codespace"), string, {||})
              ^:: basic ((4, "code", "code"), uint32_int, 0)
              ^:: basic ((5, "data", "data"), string, {||})
              ^:: basic ((6, "raw_log", "rawLog"), string, {||})
              ^:: repeated
                    ( (7, "logs", "logs"),
                      message (module ABCIMessageLog),
                      not_packed )
              ^:: basic ((8, "info", "info"), string, {||})
              ^:: basic ((9, "gas_wanted", "gasWanted"), int64, 0L)
              ^:: basic ((10, "gas_used", "gasUsed"), int64, 0L)
              ^:: basic_opt
                    ( (11, "tx", "tx"),
                      message
                        (module Imported'modules.Google_protobuf_any.Google
                                .Protobuf
                                .Any) )
              ^:: basic ((12, "timestamp", "timestamp"), string, {||})
              ^:: repeated
                    ( (13, "events", "events"),
                      message
                        (module Imported'modules.Tendermint_abci_types
                                .Tendermint
                                .Abci
                                .Event),
                      not_packed )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer
              {
                height;
                txhash;
                codespace;
                code;
                data;
                raw_log;
                logs;
                info;
                gas_wanted;
                gas_used;
                tx;
                timestamp;
                events;
              }
            ->
              serialize writer height txhash codespace code data raw_log logs
                info gas_wanted gas_used tx timestamp events

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor height txhash codespace code data raw_log logs info
                gas_wanted gas_used tx timestamp events =
              {
                height;
                txhash;
                codespace;
                code;
                data;
                raw_log;
                logs;
                info;
                gas_wanted;
                gas_used;
                tx;
                timestamp;
                events;
              }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun {
                  height;
                  txhash;
                  codespace;
                  code;
                  data;
                  raw_log;
                  logs;
                  info;
                  gas_wanted;
                  gas_used;
                  tx;
                  timestamp;
                  events;
                }
            ->
              serialize height txhash codespace code data raw_log logs info
                gas_wanted gas_used tx timestamp events

          let from_json_exn =
            let constructor height txhash codespace code data raw_log logs info
                gas_wanted gas_used tx timestamp events =
              {
                height;
                txhash;
                codespace;
                code;
                data;
                raw_log;
                logs;
                info;
                gas_wanted;
                gas_used;
                tx;
                timestamp;
                events;
              }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and ABCIMessageLog : sig
          type t = {
            msg_index : int;
            log : string;
            events : StringEvent.t list;
                (** {%html:
<p>Events contains a slice of Event objects that were emitted during some
execution.</p>
%}
                *)
          }

          val make :
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = ABCIMessageLog

          let name () = ".cosmos.base.abci.v1beta1.ABCIMessageLog"

          type t = {
            msg_index : int;
            log : string;
            events : StringEvent.t list;
          }

          type make_t =
            ?msg_index:int ->
            ?log:string ->
            ?events:StringEvent.t list ->
            unit ->
            t

          let make ?(msg_index = 0) ?(log = {||}) ?(events = []) () =
            { msg_index; log; events }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_msg_index =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "msg_index", "msgIndex"), uint32_int, 0))
                 in
                 let merge_log =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((2, "log", "log"), string, {||}))
                 in
                 let merge_events =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (3, "events", "events"),
                           message (module StringEvent),
                           not_packed ))
                 in
                 fun t1 t2 ->
                   {
                     msg_index = merge_msg_index t1.msg_index t2.msg_index;
                     log = merge_log t1.log t2.log;
                     events = merge_events t1.events t2.events;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "msg_index", "msgIndex"), uint32_int, 0)
              ^:: basic ((2, "log", "log"), string, {||})
              ^:: repeated
                    ( (3, "events", "events"),
                      message (module StringEvent),
                      not_packed )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { msg_index; log; events } ->
              serialize writer msg_index log events

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor msg_index log events = { msg_index; log; events } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { msg_index; log; events } -> serialize msg_index log events

          let from_json_exn =
            let constructor msg_index log events = { msg_index; log; events } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and StringEvent : sig
          type t = { type' : string; attributes : Attribute.t list }

          val make : ?type':string -> ?attributes:Attribute.t list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?type':string -> ?attributes:Attribute.t list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = StringEvent

          let name () = ".cosmos.base.abci.v1beta1.StringEvent"

          type t = { type' : string; attributes : Attribute.t list }

          type make_t =
            ?type':string -> ?attributes:Attribute.t list -> unit -> t

          let make ?(type' = {||}) ?(attributes = []) () = { type'; attributes }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_type' =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((1, "type", "type"), string, {||}))
                 in
                 let merge_attributes =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (2, "attributes", "attributes"),
                           message (module Attribute),
                           not_packed ))
                 in
                 fun t1 t2 ->
                   {
                     type' = merge_type' t1.type' t2.type';
                     attributes = merge_attributes t1.attributes t2.attributes;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "type", "type"), string, {||})
              ^:: repeated
                    ( (2, "attributes", "attributes"),
                      message (module Attribute),
                      not_packed )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { type'; attributes } ->
              serialize writer type' attributes

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor type' attributes = { type'; attributes } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { type'; attributes } -> serialize type' attributes

          let from_json_exn =
            let constructor type' attributes = { type'; attributes } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and Attribute : sig
          type t = { key : string; value : string }

          val make : ?key:string -> ?value:string -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?key:string -> ?value:string -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = Attribute

          let name () = ".cosmos.base.abci.v1beta1.Attribute"

          type t = { key : string; value : string }
          type make_t = ?key:string -> ?value:string -> unit -> t

          let make ?(key = {||}) ?(value = {||}) () = { key; value }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_key =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((1, "key", "key"), string, {||}))
                 in
                 let merge_value =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((2, "value", "value"), string, {||}))
                 in
                 fun t1 t2 ->
                   {
                     key = merge_key t1.key t2.key;
                     value = merge_value t1.value t2.value;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "key", "key"), string, {||})
              ^:: basic ((2, "value", "value"), string, {||})
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { key; value } -> serialize writer key value

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor key value = { key; value } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { key; value } -> serialize key value

          let from_json_exn =
            let constructor key value = { key; value } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and GasInfo : sig
          type t = {
            gas_wanted : int64;
                (** {%html:
<p>GasWanted is the maximum units of work we allow this tx to perform.</p>
%}
                *)
            gas_used : int64;
                (** {%html:
<p>GasUsed is the amount of gas actually consumed.</p>
%}
                *)
          }

          val make : ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = GasInfo

          let name () = ".cosmos.base.abci.v1beta1.GasInfo"

          type t = { gas_wanted : int64; gas_used : int64 }
          type make_t = ?gas_wanted:int64 -> ?gas_used:int64 -> unit -> t

          let make ?(gas_wanted = 0L) ?(gas_used = 0L) () =
            { gas_wanted; gas_used }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_gas_wanted =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "gas_wanted", "gasWanted"), uint64, 0L))
                 in
                 let merge_gas_used =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((2, "gas_used", "gasUsed"), uint64, 0L))
                 in
                 fun t1 t2 ->
                   {
                     gas_wanted = merge_gas_wanted t1.gas_wanted t2.gas_wanted;
                     gas_used = merge_gas_used t1.gas_used t2.gas_used;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "gas_wanted", "gasWanted"), uint64, 0L)
              ^:: basic ((2, "gas_used", "gasUsed"), uint64, 0L)
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { gas_wanted; gas_used } ->
              serialize writer gas_wanted gas_used

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor gas_wanted gas_used = { gas_wanted; gas_used } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { gas_wanted; gas_used } -> serialize gas_wanted gas_used

          let from_json_exn =
            let constructor gas_wanted gas_used = { gas_wanted; gas_used } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and Result : sig
          type t = {
            data : bytes;
                [@ocaml.alert
                  protobuf "Marked as deprecated in the .proto file"]
                (** {%html:
<p>Data is any data returned from message or handler execution. It MUST be
length prefixed in order to separate data from multiple message executions.
Deprecated. This field is still populated, but prefer msg_response instead
because it also contains the Msg response typeURL.</p>
%}
                *)
            log : string;
                (** {%html:
<p>Log contains the log information from message or handler execution.</p>
%}
                *)
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
                (** {%html:
<p>Events contains a slice of Event objects that were emitted during message
or handler execution.</p>
%}
                *)
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
                (** {%html:
<p>msg_responses contains the Msg handler responses type packed in Anys.</p>
%}
                *)
          }

          val make :
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = Result

          let name () = ".cosmos.base.abci.v1beta1.Result"

          type t = {
            data : bytes;
            log : string;
            events :
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list;
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
          }

          type make_t =
            ?data:bytes ->
            ?log:string ->
            ?events:
              Imported'modules.Tendermint_abci_types.Tendermint.Abci.Event.t
              list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          let make ?(data = Bytes.of_string {||}) ?(log = {||}) ?(events = [])
              ?(msg_responses = []) () =
            { data; log; events; msg_responses }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_data =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "data", "data"), bytes, Bytes.of_string {||}))
                 in
                 let merge_log =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((2, "log", "log"), string, {||}))
                 in
                 let merge_events =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (3, "events", "events"),
                           message
                             (module Imported'modules.Tendermint_abci_types
                                     .Tendermint
                                     .Abci
                                     .Event),
                           not_packed ))
                 in
                 let merge_msg_responses =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (4, "msg_responses", "msgResponses"),
                           message
                             (module Imported'modules.Google_protobuf_any.Google
                                     .Protobuf
                                     .Any),
                           not_packed ))
                 in
                 fun t1 t2 ->
                   {
                     data = merge_data t1.data t2.data;
                     log = merge_log t1.log t2.log;
                     events = merge_events t1.events t2.events;
                     msg_responses =
                       merge_msg_responses t1.msg_responses t2.msg_responses;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "data", "data"), bytes, Bytes.of_string {||})
              ^:: basic ((2, "log", "log"), string, {||})
              ^:: repeated
                    ( (3, "events", "events"),
                      message
                        (module Imported'modules.Tendermint_abci_types
                                .Tendermint
                                .Abci
                                .Event),
                      not_packed )
              ^:: repeated
                    ( (4, "msg_responses", "msgResponses"),
                      message
                        (module Imported'modules.Google_protobuf_any.Google
                                .Protobuf
                                .Any),
                      not_packed )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { data; log; events; msg_responses } ->
              serialize writer data log events msg_responses

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor data log events msg_responses =
              { data; log; events; msg_responses }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { data; log; events; msg_responses } ->
              serialize data log events msg_responses

          let from_json_exn =
            let constructor data log events msg_responses =
              { data; log; events; msg_responses }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and SimulationResponse : sig
          type t = { gas_info : GasInfo.t option; result : Result.t option }

          val make : ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = SimulationResponse

          let name () = ".cosmos.base.abci.v1beta1.SimulationResponse"

          type t = { gas_info : GasInfo.t option; result : Result.t option }
          type make_t = ?gas_info:GasInfo.t -> ?result:Result.t -> unit -> t

          let make ?gas_info ?result () = { gas_info; result }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_gas_info =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt
                         ((1, "gas_info", "gasInfo"), message (module GasInfo)))
                 in
                 let merge_result =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt
                         ((2, "result", "result"), message (module Result)))
                 in
                 fun t1 t2 ->
                   {
                     gas_info = merge_gas_info t1.gas_info t2.gas_info;
                     result = merge_result t1.result t2.result;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic_opt ((1, "gas_info", "gasInfo"), message (module GasInfo))
              ^:: basic_opt ((2, "result", "result"), message (module Result))
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { gas_info; result } -> serialize writer gas_info result

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor gas_info result = { gas_info; result } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { gas_info; result } -> serialize gas_info result

          let from_json_exn =
            let constructor gas_info result = { gas_info; result } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and MsgData : sig
          type t = { msg_type : string; data : bytes }

          val make : ?msg_type:string -> ?data:bytes -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?msg_type:string -> ?data:bytes -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = MsgData

          let name () = ".cosmos.base.abci.v1beta1.MsgData"

          type t = { msg_type : string; data : bytes }
          type make_t = ?msg_type:string -> ?data:bytes -> unit -> t

          let make ?(msg_type = {||}) ?(data = Bytes.of_string {||}) () =
            { msg_type; data }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_msg_type =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "msg_type", "msgType"), string, {||}))
                 in
                 let merge_data =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((2, "data", "data"), bytes, Bytes.of_string {||}))
                 in
                 fun t1 t2 ->
                   {
                     msg_type = merge_msg_type t1.msg_type t2.msg_type;
                     data = merge_data t1.data t2.data;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "msg_type", "msgType"), string, {||})
              ^:: basic ((2, "data", "data"), bytes, Bytes.of_string {||})
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { msg_type; data } -> serialize writer msg_type data

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor msg_type data = { msg_type; data } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { msg_type; data } -> serialize msg_type data

          let from_json_exn =
            let constructor msg_type data = { msg_type; data } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end
        [@@ocaml.alert protobuf "Marked as deprecated in the .proto file"]

        and TxMsgData : sig
          type t = {
            data : MsgData.t list;
                [@ocaml.alert
                  protobuf "Marked as deprecated in the .proto file"]
                (** {%html:
<p>data field is deprecated and not populated.</p>
%}
                *)
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
                (** {%html:
<p>msg_responses contains the Msg handler responses packed into Anys.</p>
%}
                *)
          }

          val make :
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = TxMsgData

          let name () = ".cosmos.base.abci.v1beta1.TxMsgData"

          type t = {
            data : MsgData.t list;
            msg_responses :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list;
          }

          type make_t =
            ?data:MsgData.t list ->
            ?msg_responses:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t list ->
            unit ->
            t

          let make ?(data = []) ?(msg_responses = []) () =
            { data; msg_responses }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_data =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (1, "data", "data"),
                           message (module MsgData),
                           not_packed ))
                 in
                 let merge_msg_responses =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (2, "msg_responses", "msgResponses"),
                           message
                             (module Imported'modules.Google_protobuf_any.Google
                                     .Protobuf
                                     .Any),
                           not_packed ))
                 in
                 fun t1 t2 ->
                   {
                     data = merge_data t1.data t2.data;
                     msg_responses =
                       merge_msg_responses t1.msg_responses t2.msg_responses;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              repeated
                ((1, "data", "data"), message (module MsgData), not_packed)
              ^:: repeated
                    ( (2, "msg_responses", "msgResponses"),
                      message
                        (module Imported'modules.Google_protobuf_any.Google
                                .Protobuf
                                .Any),
                      not_packed )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { data; msg_responses } ->
              serialize writer data msg_responses

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor data msg_responses = { data; msg_responses } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { data; msg_responses } -> serialize data msg_responses

          let from_json_exn =
            let constructor data msg_responses = { data; msg_responses } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and SearchTxsResult : sig
          type t = {
            total_count : int64;  (** {%html:
<p>Count of all txs</p>
%} *)
            count : int64;
                (** {%html:
<p>Count of txs in current page</p>
%} *)
            page_number : int64;
                (** {%html:
<p>Index of current page, start from 1</p>
%} *)
            page_total : int64;  (** {%html:
<p>Count of total pages</p>
%} *)
            limit : int64;  (** {%html:
<p>Max count txs per page</p>
%} *)
            txs : TxResponse.t list;
                (** {%html:
<p>List of txs in current page</p>
%} *)
          }

          val make :
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = SearchTxsResult

          let name () = ".cosmos.base.abci.v1beta1.SearchTxsResult"

          type t = {
            total_count : int64;
            count : int64;
            page_number : int64;
            page_total : int64;
            limit : int64;
            txs : TxResponse.t list;
          }

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?txs:TxResponse.t list ->
            unit ->
            t

          let make ?(total_count = 0L) ?(count = 0L) ?(page_number = 0L)
              ?(page_total = 0L) ?(limit = 0L) ?(txs = []) () =
            { total_count; count; page_number; page_total; limit; txs }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_total_count =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "total_count", "totalCount"), uint64, 0L))
                 in
                 let merge_count =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((2, "count", "count"), uint64, 0L))
                 in
                 let merge_page_number =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((3, "page_number", "pageNumber"), uint64, 0L))
                 in
                 let merge_page_total =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((4, "page_total", "pageTotal"), uint64, 0L))
                 in
                 let merge_limit =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((5, "limit", "limit"), uint64, 0L))
                 in
                 let merge_txs =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (6, "txs", "txs"),
                           message (module TxResponse),
                           not_packed ))
                 in
                 fun t1 t2 ->
                   {
                     total_count =
                       merge_total_count t1.total_count t2.total_count;
                     count = merge_count t1.count t2.count;
                     page_number =
                       merge_page_number t1.page_number t2.page_number;
                     page_total = merge_page_total t1.page_total t2.page_total;
                     limit = merge_limit t1.limit t2.limit;
                     txs = merge_txs t1.txs t2.txs;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "total_count", "totalCount"), uint64, 0L)
              ^:: basic ((2, "count", "count"), uint64, 0L)
              ^:: basic ((3, "page_number", "pageNumber"), uint64, 0L)
              ^:: basic ((4, "page_total", "pageTotal"), uint64, 0L)
              ^:: basic ((5, "limit", "limit"), uint64, 0L)
              ^:: repeated
                    ((6, "txs", "txs"), message (module TxResponse), not_packed)
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer
              { total_count; count; page_number; page_total; limit; txs }
            ->
              serialize writer total_count count page_number page_total limit
                txs

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor total_count count page_number page_total limit txs =
              { total_count; count; page_number; page_total; limit; txs }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { total_count; count; page_number; page_total; limit; txs } ->
              serialize total_count count page_number page_total limit txs

          let from_json_exn =
            let constructor total_count count page_number page_total limit txs =
              { total_count; count; page_number; page_total; limit; txs }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and SearchBlocksResult : sig
          type t = {
            total_count : int64;  (** {%html:
<p>Count of all blocks</p>
%} *)
            count : int64;
                (** {%html:
<p>Count of blocks in current page</p>
%} *)
            page_number : int64;
                (** {%html:
<p>Index of current page, start from 1</p>
%} *)
            page_total : int64;  (** {%html:
<p>Count of total pages</p>
%} *)
            limit : int64;  (** {%html:
<p>Max count blocks per page</p>
%} *)
            blocks :
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list;
                (** {%html:
<p>List of blocks in current page</p>
%} *)
          }

          val make :
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = SearchBlocksResult

          let name () = ".cosmos.base.abci.v1beta1.SearchBlocksResult"

          type t = {
            total_count : int64;
            count : int64;
            page_number : int64;
            page_total : int64;
            limit : int64;
            blocks :
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list;
          }

          type make_t =
            ?total_count:int64 ->
            ?count:int64 ->
            ?page_number:int64 ->
            ?page_total:int64 ->
            ?limit:int64 ->
            ?blocks:
              Imported'modules.Tendermint_types_block.Tendermint.Types.Block.t
              list ->
            unit ->
            t

          let make ?(total_count = 0L) ?(count = 0L) ?(page_number = 0L)
              ?(page_total = 0L) ?(limit = 0L) ?(blocks = []) () =
            { total_count; count; page_number; page_total; limit; blocks }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_total_count =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "total_count", "totalCount"), int64, 0L))
                 in
                 let merge_count =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((2, "count", "count"), int64, 0L))
                 in
                 let merge_page_number =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((3, "page_number", "pageNumber"), int64, 0L))
                 in
                 let merge_page_total =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((4, "page_total", "pageTotal"), int64, 0L))
                 in
                 let merge_limit =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((5, "limit", "limit"), int64, 0L))
                 in
                 let merge_blocks =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (6, "blocks", "blocks"),
                           message
                             (module Imported'modules.Tendermint_types_block
                                     .Tendermint
                                     .Types
                                     .Block),
                           not_packed ))
                 in
                 fun t1 t2 ->
                   {
                     total_count =
                       merge_total_count t1.total_count t2.total_count;
                     count = merge_count t1.count t2.count;
                     page_number =
                       merge_page_number t1.page_number t2.page_number;
                     page_total = merge_page_total t1.page_total t2.page_total;
                     limit = merge_limit t1.limit t2.limit;
                     blocks = merge_blocks t1.blocks t2.blocks;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "total_count", "totalCount"), int64, 0L)
              ^:: basic ((2, "count", "count"), int64, 0L)
              ^:: basic ((3, "page_number", "pageNumber"), int64, 0L)
              ^:: basic ((4, "page_total", "pageTotal"), int64, 0L)
              ^:: basic ((5, "limit", "limit"), int64, 0L)
              ^:: repeated
                    ( (6, "blocks", "blocks"),
                      message
                        (module Imported'modules.Tendermint_types_block
                                .Tendermint
                                .Types
                                .Block),
                      not_packed )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer
              { total_count; count; page_number; page_total; limit; blocks }
            ->
              serialize writer total_count count page_number page_total limit
                blocks

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor total_count count page_number page_total limit
                blocks =
              { total_count; count; page_number; page_total; limit; blocks }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { total_count; count; page_number; page_total; limit; blocks }
            -> serialize total_count count page_number page_total limit blocks

          let from_json_exn =
            let constructor total_count count page_number page_total limit
                blocks =
              { total_count; count; page_number; page_total; limit; blocks }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end
      end
    end
  end
end

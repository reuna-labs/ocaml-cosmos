(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: ibc/applications/transfer/v1/transfer.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]
module Imported'modules = struct end

(**/**)

module rec Ibc : sig
  module rec Applications : sig
    module rec Transfer : sig
      module rec V1 : sig
        (** {%html:
<p>Params defines the set of IBC transfer parameters.
NOTE: To prevent a single token from being transferred, set the
TransfersEnabled parameter to true and then set the bank module's SendEnabled
parameter for the denomination to false.</p>
%}
        *)
        module rec Params : sig
          type t = {
            send_enabled : bool;
                (** {%html:
<p>send_enabled enables or disables all cross-chain token transfers from this
chain.</p>
%}
                *)
            receive_enabled : bool;
                (** {%html:
<p>receive_enabled enables or disables all cross-chain token transfers to this
chain.</p>
%}
                *)
          }

          val make : ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end
  end
end = struct
  module rec Applications : sig
    module rec Transfer : sig
      module rec V1 : sig
        (** {%html:
<p>Params defines the set of IBC transfer parameters.
NOTE: To prevent a single token from being transferred, set the
TransfersEnabled parameter to true and then set the bank module's SendEnabled
parameter for the denomination to false.</p>
%}
        *)
        module rec Params : sig
          type t = {
            send_enabled : bool;
                (** {%html:
<p>send_enabled enables or disables all cross-chain token transfers from this
chain.</p>
%}
                *)
            receive_enabled : bool;
                (** {%html:
<p>receive_enabled enables or disables all cross-chain token transfers to this
chain.</p>
%}
                *)
          }

          val make : ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end
  end = struct
    module rec Transfer : sig
      module rec V1 : sig
        (** {%html:
<p>Params defines the set of IBC transfer parameters.
NOTE: To prevent a single token from being transferred, set the
TransfersEnabled parameter to true and then set the bank module's SendEnabled
parameter for the denomination to false.</p>
%}
        *)
        module rec Params : sig
          type t = {
            send_enabled : bool;
                (** {%html:
<p>send_enabled enables or disables all cross-chain token transfers from this
chain.</p>
%}
                *)
            receive_enabled : bool;
                (** {%html:
<p>receive_enabled enables or disables all cross-chain token transfers to this
chain.</p>
%}
                *)
          }

          val make : ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end = struct
      module rec V1 : sig
        (** {%html:
<p>Params defines the set of IBC transfer parameters.
NOTE: To prevent a single token from being transferred, set the
TransfersEnabled parameter to true and then set the bank module's SendEnabled
parameter for the denomination to false.</p>
%}
        *)
        module rec Params : sig
          type t = {
            send_enabled : bool;
                (** {%html:
<p>send_enabled enables or disables all cross-chain token transfers from this
chain.</p>
%}
                *)
            receive_enabled : bool;
                (** {%html:
<p>receive_enabled enables or disables all cross-chain token transfers to this
chain.</p>
%}
                *)
          }

          val make : ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end = struct
        module rec Params : sig
          type t = {
            send_enabled : bool;
                (** {%html:
<p>send_enabled enables or disables all cross-chain token transfers from this
chain.</p>
%}
                *)
            receive_enabled : bool;
                (** {%html:
<p>receive_enabled enables or disables all cross-chain token transfers to this
chain.</p>
%}
                *)
          }

          val make : ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = Params

          let name () = ".ibc.applications.transfer.v1.Params"

          type t = { send_enabled : bool; receive_enabled : bool }
          type make_t = ?send_enabled:bool -> ?receive_enabled:bool -> unit -> t

          let make ?(send_enabled = false) ?(receive_enabled = false) () =
            { send_enabled; receive_enabled }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_send_enabled =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "send_enabled", "sendEnabled"), bool, false))
                 in
                 let merge_receive_enabled =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic
                         ((2, "receive_enabled", "receiveEnabled"), bool, false))
                 in
                 fun t1 t2 ->
                   {
                     send_enabled =
                       merge_send_enabled t1.send_enabled t2.send_enabled;
                     receive_enabled =
                       merge_receive_enabled t1.receive_enabled
                         t2.receive_enabled;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "send_enabled", "sendEnabled"), bool, false)
              ^:: basic ((2, "receive_enabled", "receiveEnabled"), bool, false)
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { send_enabled; receive_enabled } ->
              serialize writer send_enabled receive_enabled

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor send_enabled receive_enabled =
              { send_enabled; receive_enabled }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { send_enabled; receive_enabled } ->
              serialize send_enabled receive_enabled

          let from_json_exn =
            let constructor send_enabled receive_enabled =
              { send_enabled; receive_enabled }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end
      end
    end
  end
end

(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: tendermint/types/params.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Gogoproto_gogo = Gogoproto_gogo
  module Google_protobuf_duration = Google_protobuf_duration
end

(**/**)

module rec Tendermint : sig
  module rec Types : sig
    (** {%html:
<p>ConsensusParams contains consensus critical parameters that determine the
validity of blocks.</p>
%}
    *)
    module rec ConsensusParams : sig
      type t = {
        block : BlockParams.t option;
        evidence : EvidenceParams.t option;
        validator : ValidatorParams.t option;
        version : VersionParams.t option;
        abci : ABCIParams.t option;
      }

      val make :
        ?block:BlockParams.t ->
        ?evidence:EvidenceParams.t ->
        ?validator:ValidatorParams.t ->
        ?version:VersionParams.t ->
        ?abci:ABCIParams.t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block:BlockParams.t ->
        ?evidence:EvidenceParams.t ->
        ?validator:ValidatorParams.t ->
        ?version:VersionParams.t ->
        ?abci:ABCIParams.t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>BlockParams contains limits on the block size.</p>
%} *)
    and BlockParams : sig
      type t = {
        max_bytes : int64;
            (** {%html:
<p>Max block size, in bytes.
Note: must be greater than 0</p>
%}
            *)
        max_gas : int64;
            (** {%html:
<p>Max gas per block.
Note: must be greater or equal to -1</p>
%}
            *)
      }

      val make : ?max_bytes:int64 -> ?max_gas:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?max_bytes:int64 -> ?max_gas:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>EvidenceParams determine how we handle evidence of malfeasance.</p>
%}
    *)
    and EvidenceParams : sig
      type t = {
        max_age_num_blocks : int64;
            (** {%html:
<p>Max age of evidence, in blocks.</p>
<p>The basic formula for calculating this is: MaxAgeDuration / {average block
time}.</p>
%}
            *)
        max_age_duration :
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t
          option;
            (** {%html:
<p>Max age of evidence, in time.</p>
<p>It should correspond with an app's &quot;unbonding period&quot; or other similar
mechanism for handling <a href="https://github.com/ethereum/wiki/wiki/Proof-of-Stake-FAQ#what-is-the-nothing-at-stake-problem-and-how-can-it-be-fixed">Nothing-At-Stake
attacks</a>.</p>
%}
            *)
        max_bytes : int64;
            (** {%html:
<p>This sets the maximum size of total evidence in bytes that can be committed in a single block.
and should fall comfortably under the max block bytes.
Default is 1048576 or 1MB</p>
%}
            *)
      }

      val make :
        ?max_age_num_blocks:int64 ->
        ?max_age_duration:
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t ->
        ?max_bytes:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?max_age_num_blocks:int64 ->
        ?max_age_duration:
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t ->
        ?max_bytes:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>ValidatorParams restrict the public key types validators can use.
NOTE: uses ABCI pubkey naming, not Amino names.</p>
%}
    *)
    and ValidatorParams : sig
      type t = string list

      val make : ?pub_key_types:string list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?pub_key_types:string list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>VersionParams contains the ABCI application version.</p>
%}
    *)
    and VersionParams : sig
      type t = int64

      val make : ?app:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?app:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>HashedParams is a subset of ConsensusParams.</p>
<p>It is hashed into the Header.ConsensusHash.</p>
%}
    *)
    and HashedParams : sig
      type t = { block_max_bytes : int64; block_max_gas : int64 }

      val make : ?block_max_bytes:int64 -> ?block_max_gas:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?block_max_bytes:int64 -> ?block_max_gas:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>ABCIParams configure functionality specific to the Application Blockchain Interface.</p>
%}
    *)
    and ABCIParams : sig
      type t = int64
      (** {%html:
<p>vote_extensions_enable_height configures the first height during which
vote extensions will be enabled. During this specified height, and for all
subsequent heights, precommit messages that do not contain valid extension data
will be considered invalid. Prior to this height, vote extensions will not
be used or accepted by validators on the network.</p>
<p>Once enabled, vote extensions will be created by the application in ExtendVote,
passed to the application for validation in VerifyVoteExtension and given
to the application to use when proposing a block during PrepareProposal.</p>
%}
      *)

      val make : ?vote_extensions_enable_height:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?vote_extensions_enable_height:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end
  end
end = struct
  module rec Types : sig
    (** {%html:
<p>ConsensusParams contains consensus critical parameters that determine the
validity of blocks.</p>
%}
    *)
    module rec ConsensusParams : sig
      type t = {
        block : BlockParams.t option;
        evidence : EvidenceParams.t option;
        validator : ValidatorParams.t option;
        version : VersionParams.t option;
        abci : ABCIParams.t option;
      }

      val make :
        ?block:BlockParams.t ->
        ?evidence:EvidenceParams.t ->
        ?validator:ValidatorParams.t ->
        ?version:VersionParams.t ->
        ?abci:ABCIParams.t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block:BlockParams.t ->
        ?evidence:EvidenceParams.t ->
        ?validator:ValidatorParams.t ->
        ?version:VersionParams.t ->
        ?abci:ABCIParams.t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>BlockParams contains limits on the block size.</p>
%} *)
    and BlockParams : sig
      type t = {
        max_bytes : int64;
            (** {%html:
<p>Max block size, in bytes.
Note: must be greater than 0</p>
%}
            *)
        max_gas : int64;
            (** {%html:
<p>Max gas per block.
Note: must be greater or equal to -1</p>
%}
            *)
      }

      val make : ?max_bytes:int64 -> ?max_gas:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?max_bytes:int64 -> ?max_gas:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>EvidenceParams determine how we handle evidence of malfeasance.</p>
%}
    *)
    and EvidenceParams : sig
      type t = {
        max_age_num_blocks : int64;
            (** {%html:
<p>Max age of evidence, in blocks.</p>
<p>The basic formula for calculating this is: MaxAgeDuration / {average block
time}.</p>
%}
            *)
        max_age_duration :
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t
          option;
            (** {%html:
<p>Max age of evidence, in time.</p>
<p>It should correspond with an app's &quot;unbonding period&quot; or other similar
mechanism for handling <a href="https://github.com/ethereum/wiki/wiki/Proof-of-Stake-FAQ#what-is-the-nothing-at-stake-problem-and-how-can-it-be-fixed">Nothing-At-Stake
attacks</a>.</p>
%}
            *)
        max_bytes : int64;
            (** {%html:
<p>This sets the maximum size of total evidence in bytes that can be committed in a single block.
and should fall comfortably under the max block bytes.
Default is 1048576 or 1MB</p>
%}
            *)
      }

      val make :
        ?max_age_num_blocks:int64 ->
        ?max_age_duration:
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t ->
        ?max_bytes:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?max_age_num_blocks:int64 ->
        ?max_age_duration:
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t ->
        ?max_bytes:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>ValidatorParams restrict the public key types validators can use.
NOTE: uses ABCI pubkey naming, not Amino names.</p>
%}
    *)
    and ValidatorParams : sig
      type t = string list

      val make : ?pub_key_types:string list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?pub_key_types:string list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>VersionParams contains the ABCI application version.</p>
%}
    *)
    and VersionParams : sig
      type t = int64

      val make : ?app:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?app:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>HashedParams is a subset of ConsensusParams.</p>
<p>It is hashed into the Header.ConsensusHash.</p>
%}
    *)
    and HashedParams : sig
      type t = { block_max_bytes : int64; block_max_gas : int64 }

      val make : ?block_max_bytes:int64 -> ?block_max_gas:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?block_max_bytes:int64 -> ?block_max_gas:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>ABCIParams configure functionality specific to the Application Blockchain Interface.</p>
%}
    *)
    and ABCIParams : sig
      type t = int64
      (** {%html:
<p>vote_extensions_enable_height configures the first height during which
vote extensions will be enabled. During this specified height, and for all
subsequent heights, precommit messages that do not contain valid extension data
will be considered invalid. Prior to this height, vote extensions will not
be used or accepted by validators on the network.</p>
<p>Once enabled, vote extensions will be created by the application in ExtendVote,
passed to the application for validation in VerifyVoteExtension and given
to the application to use when proposing a block during PrepareProposal.</p>
%}
      *)

      val make : ?vote_extensions_enable_height:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?vote_extensions_enable_height:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end
  end = struct
    module rec ConsensusParams : sig
      type t = {
        block : BlockParams.t option;
        evidence : EvidenceParams.t option;
        validator : ValidatorParams.t option;
        version : VersionParams.t option;
        abci : ABCIParams.t option;
      }

      val make :
        ?block:BlockParams.t ->
        ?evidence:EvidenceParams.t ->
        ?validator:ValidatorParams.t ->
        ?version:VersionParams.t ->
        ?abci:ABCIParams.t ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?block:BlockParams.t ->
        ?evidence:EvidenceParams.t ->
        ?validator:ValidatorParams.t ->
        ?version:VersionParams.t ->
        ?abci:ABCIParams.t ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = ConsensusParams

      let name () = ".tendermint.types.ConsensusParams"

      type t = {
        block : BlockParams.t option;
        evidence : EvidenceParams.t option;
        validator : ValidatorParams.t option;
        version : VersionParams.t option;
        abci : ABCIParams.t option;
      }

      type make_t =
        ?block:BlockParams.t ->
        ?evidence:EvidenceParams.t ->
        ?validator:ValidatorParams.t ->
        ?version:VersionParams.t ->
        ?abci:ABCIParams.t ->
        unit ->
        t

      let make ?block ?evidence ?validator ?version ?abci () =
        { block; evidence; validator; version; abci }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_block =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ((1, "block", "block"), message (module BlockParams)))
             in
             let merge_evidence =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (2, "evidence", "evidence"),
                       message (module EvidenceParams) ))
             in
             let merge_validator =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (3, "validator", "validator"),
                       message (module ValidatorParams) ))
             in
             let merge_version =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ((4, "version", "version"), message (module VersionParams)))
             in
             let merge_abci =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt ((5, "abci", "abci"), message (module ABCIParams)))
             in
             fun t1 t2 ->
               {
                 block = merge_block t1.block t2.block;
                 evidence = merge_evidence t1.evidence t2.evidence;
                 validator = merge_validator t1.validator t2.validator;
                 version = merge_version t1.version t2.version;
                 abci = merge_abci t1.abci t2.abci;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic_opt ((1, "block", "block"), message (module BlockParams))
          ^:: basic_opt
                ((2, "evidence", "evidence"), message (module EvidenceParams))
          ^:: basic_opt
                ((3, "validator", "validator"), message (module ValidatorParams))
          ^:: basic_opt
                ((4, "version", "version"), message (module VersionParams))
          ^:: basic_opt ((5, "abci", "abci"), message (module ABCIParams))
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { block; evidence; validator; version; abci } ->
          serialize writer block evidence validator version abci

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor block evidence validator version abci =
          { block; evidence; validator; version; abci }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { block; evidence; validator; version; abci } ->
          serialize block evidence validator version abci

      let from_json_exn =
        let constructor block evidence validator version abci =
          { block; evidence; validator; version; abci }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and BlockParams : sig
      type t = {
        max_bytes : int64;
            (** {%html:
<p>Max block size, in bytes.
Note: must be greater than 0</p>
%}
            *)
        max_gas : int64;
            (** {%html:
<p>Max gas per block.
Note: must be greater or equal to -1</p>
%}
            *)
      }

      val make : ?max_bytes:int64 -> ?max_gas:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?max_bytes:int64 -> ?max_gas:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = BlockParams

      let name () = ".tendermint.types.BlockParams"

      type t = { max_bytes : int64; max_gas : int64 }
      type make_t = ?max_bytes:int64 -> ?max_gas:int64 -> unit -> t

      let make ?(max_bytes = 0L) ?(max_gas = 0L) () = { max_bytes; max_gas }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_max_bytes =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((1, "max_bytes", "maxBytes"), int64, 0L))
             in
             let merge_max_gas =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((2, "max_gas", "maxGas"), int64, 0L))
             in
             fun t1 t2 ->
               {
                 max_bytes = merge_max_bytes t1.max_bytes t2.max_bytes;
                 max_gas = merge_max_gas t1.max_gas t2.max_gas;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "max_bytes", "maxBytes"), int64, 0L)
          ^:: basic ((2, "max_gas", "maxGas"), int64, 0L)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { max_bytes; max_gas } -> serialize writer max_bytes max_gas

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor max_bytes max_gas = { max_bytes; max_gas } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { max_bytes; max_gas } -> serialize max_bytes max_gas

      let from_json_exn =
        let constructor max_bytes max_gas = { max_bytes; max_gas } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and EvidenceParams : sig
      type t = {
        max_age_num_blocks : int64;
            (** {%html:
<p>Max age of evidence, in blocks.</p>
<p>The basic formula for calculating this is: MaxAgeDuration / {average block
time}.</p>
%}
            *)
        max_age_duration :
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t
          option;
            (** {%html:
<p>Max age of evidence, in time.</p>
<p>It should correspond with an app's &quot;unbonding period&quot; or other similar
mechanism for handling <a href="https://github.com/ethereum/wiki/wiki/Proof-of-Stake-FAQ#what-is-the-nothing-at-stake-problem-and-how-can-it-be-fixed">Nothing-At-Stake
attacks</a>.</p>
%}
            *)
        max_bytes : int64;
            (** {%html:
<p>This sets the maximum size of total evidence in bytes that can be committed in a single block.
and should fall comfortably under the max block bytes.
Default is 1048576 or 1MB</p>
%}
            *)
      }

      val make :
        ?max_age_num_blocks:int64 ->
        ?max_age_duration:
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t ->
        ?max_bytes:int64 ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?max_age_num_blocks:int64 ->
        ?max_age_duration:
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t ->
        ?max_bytes:int64 ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = EvidenceParams

      let name () = ".tendermint.types.EvidenceParams"

      type t = {
        max_age_num_blocks : int64;
        max_age_duration :
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t
          option;
        max_bytes : int64;
      }

      type make_t =
        ?max_age_num_blocks:int64 ->
        ?max_age_duration:
          Imported'modules.Google_protobuf_duration.Google.Protobuf.Duration.t ->
        ?max_bytes:int64 ->
        unit ->
        t

      let make ?(max_age_num_blocks = 0L) ?max_age_duration ?(max_bytes = 0L) ()
          =
        { max_age_num_blocks; max_age_duration; max_bytes }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_max_age_num_blocks =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((1, "max_age_num_blocks", "maxAgeNumBlocks"), int64, 0L))
             in
             let merge_max_age_duration =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt
                     ( (2, "max_age_duration", "maxAgeDuration"),
                       message
                         (module Imported'modules.Google_protobuf_duration
                                 .Google
                                 .Protobuf
                                 .Duration) ))
             in
             let merge_max_bytes =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((3, "max_bytes", "maxBytes"), int64, 0L))
             in
             fun t1 t2 ->
               {
                 max_age_num_blocks =
                   merge_max_age_num_blocks t1.max_age_num_blocks
                     t2.max_age_num_blocks;
                 max_age_duration =
                   merge_max_age_duration t1.max_age_duration
                     t2.max_age_duration;
                 max_bytes = merge_max_bytes t1.max_bytes t2.max_bytes;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "max_age_num_blocks", "maxAgeNumBlocks"), int64, 0L)
          ^:: basic_opt
                ( (2, "max_age_duration", "maxAgeDuration"),
                  message
                    (module Imported'modules.Google_protobuf_duration.Google
                            .Protobuf
                            .Duration) )
          ^:: basic ((3, "max_bytes", "maxBytes"), int64, 0L)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { max_age_num_blocks; max_age_duration; max_bytes } ->
          serialize writer max_age_num_blocks max_age_duration max_bytes

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor max_age_num_blocks max_age_duration max_bytes =
          { max_age_num_blocks; max_age_duration; max_bytes }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { max_age_num_blocks; max_age_duration; max_bytes } ->
          serialize max_age_num_blocks max_age_duration max_bytes

      let from_json_exn =
        let constructor max_age_num_blocks max_age_duration max_bytes =
          { max_age_num_blocks; max_age_duration; max_bytes }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and ValidatorParams : sig
      type t = string list

      val make : ?pub_key_types:string list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?pub_key_types:string list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = ValidatorParams

      let name () = ".tendermint.types.ValidatorParams"

      type t = string list
      type make_t = ?pub_key_types:string list -> unit -> t

      let make ?(pub_key_types = []) () = pub_key_types

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_pub_key_types =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   repeated
                     ((1, "pub_key_types", "pubKeyTypes"), string, not_packed))
             in
             fun t1_pub_key_types t2_pub_key_types ->
               merge_pub_key_types t1_pub_key_types t2_pub_key_types)
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          repeated ((1, "pub_key_types", "pubKeyTypes"), string, not_packed)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer pub_key_types -> serialize writer pub_key_types

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor pub_key_types = pub_key_types in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun pub_key_types -> serialize pub_key_types

      let from_json_exn =
        let constructor pub_key_types = pub_key_types in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and VersionParams : sig
      type t = int64

      val make : ?app:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?app:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = VersionParams

      let name () = ".tendermint.types.VersionParams"

      type t = int64
      type make_t = ?app:int64 -> unit -> t

      let make ?(app = 0L) () = app

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_app =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((1, "app", "app"), uint64, 0L))
             in
             fun t1_app t2_app -> merge_app t1_app t2_app)
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(basic ((1, "app", "app"), uint64, 0L) ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer app -> serialize writer app

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor app = app in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun app -> serialize app

      let from_json_exn =
        let constructor app = app in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and HashedParams : sig
      type t = { block_max_bytes : int64; block_max_gas : int64 }

      val make : ?block_max_bytes:int64 -> ?block_max_gas:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?block_max_bytes:int64 -> ?block_max_gas:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = HashedParams

      let name () = ".tendermint.types.HashedParams"

      type t = { block_max_bytes : int64; block_max_gas : int64 }
      type make_t = ?block_max_bytes:int64 -> ?block_max_gas:int64 -> unit -> t

      let make ?(block_max_bytes = 0L) ?(block_max_gas = 0L) () =
        { block_max_bytes; block_max_gas }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_block_max_bytes =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((1, "block_max_bytes", "blockMaxBytes"), int64, 0L))
             in
             let merge_block_max_gas =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((2, "block_max_gas", "blockMaxGas"), int64, 0L))
             in
             fun t1 t2 ->
               {
                 block_max_bytes =
                   merge_block_max_bytes t1.block_max_bytes t2.block_max_bytes;
                 block_max_gas =
                   merge_block_max_gas t1.block_max_gas t2.block_max_gas;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "block_max_bytes", "blockMaxBytes"), int64, 0L)
          ^:: basic ((2, "block_max_gas", "blockMaxGas"), int64, 0L)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { block_max_bytes; block_max_gas } ->
          serialize writer block_max_bytes block_max_gas

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor block_max_bytes block_max_gas =
          { block_max_bytes; block_max_gas }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { block_max_bytes; block_max_gas } ->
          serialize block_max_bytes block_max_gas

      let from_json_exn =
        let constructor block_max_bytes block_max_gas =
          { block_max_bytes; block_max_gas }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and ABCIParams : sig
      type t = int64
      (** {%html:
<p>vote_extensions_enable_height configures the first height during which
vote extensions will be enabled. During this specified height, and for all
subsequent heights, precommit messages that do not contain valid extension data
will be considered invalid. Prior to this height, vote extensions will not
be used or accepted by validators on the network.</p>
<p>Once enabled, vote extensions will be created by the application in ExtendVote,
passed to the application for validation in VerifyVoteExtension and given
to the application to use when proposing a block during PrepareProposal.</p>
%}
      *)

      val make : ?vote_extensions_enable_height:int64 -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?vote_extensions_enable_height:int64 -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = ABCIParams

      let name () = ".tendermint.types.ABCIParams"

      type t = int64
      type make_t = ?vote_extensions_enable_height:int64 -> unit -> t

      let make ?(vote_extensions_enable_height = 0L) () =
        vote_extensions_enable_height

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_vote_extensions_enable_height =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ( ( 1,
                         "vote_extensions_enable_height",
                         "voteExtensionsEnableHeight" ),
                       int64,
                       0L ))
             in
             fun t1_vote_extensions_enable_height
               t2_vote_extensions_enable_height
             ->
               merge_vote_extensions_enable_height
                 t1_vote_extensions_enable_height
                 t2_vote_extensions_enable_height)
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic
            ( (1, "vote_extensions_enable_height", "voteExtensionsEnableHeight"),
              int64,
              0L )
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer vote_extensions_enable_height ->
          serialize writer vote_extensions_enable_height

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor vote_extensions_enable_height =
          vote_extensions_enable_height
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun vote_extensions_enable_height ->
          serialize vote_extensions_enable_height

      let from_json_exn =
        let constructor vote_extensions_enable_height =
          vote_extensions_enable_height
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end
  end
end

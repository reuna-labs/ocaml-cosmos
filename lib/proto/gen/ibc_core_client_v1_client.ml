(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: ibc/core/client/v1/client.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Gogoproto_gogo = Gogoproto_gogo
  module Google_protobuf_any = Google_protobuf_any
end

(**/**)

module rec Ibc : sig
  module rec Core : sig
    module rec Client : sig
      module rec V1 : sig
        (** {%html:
<p>IdentifiedClientState defines a client state with an additional client
identifier field.</p>
%}
        *)
        module rec IdentifiedClientState : sig
          type t = {
            client_id : string;  (** {%html:
<p>client identifier</p>
%} *)
            client_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>client state</p>
%} *)
          }

          val make :
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ConsensusStateWithHeight defines a consensus state with an additional height
field.</p>
%}
        *)
        and ConsensusStateWithHeight : sig
          type t = {
            height : Height.t option;
                (** {%html:
<p>consensus state height</p>
%} *)
            consensus_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>consensus state</p>
%} *)
          }

          val make :
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ClientConsensusStates defines all the stored consensus states for a given
client.</p>
%}
        *)
        and ClientConsensusStates : sig
          type t = {
            client_id : string;  (** {%html:
<p>client identifier</p>
%} *)
            consensus_states : ConsensusStateWithHeight.t list;
                (** {%html:
<p>consensus states and their heights associated with the client</p>
%}
                *)
          }

          val make :
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Height is a monotonically increasing data type
that can be compared against another Height for the purposes of updating and
freezing clients</p>
<p>Normally the RevisionHeight is incremented at each height while keeping
RevisionNumber the same. However some consensus algorithms may choose to
reset the height in certain conditions e.g. hard forks, state-machine
breaking changes In these cases, the RevisionNumber is incremented so that
height continues to be monitonically increasing even as the RevisionHeight
gets reset</p>
<p>Please note that json tags for generated Go code are overridden to explicitly exclude the omitempty jsontag.
This enforces the Go json marshaller to always emit zero values for both revision_number and revision_height.</p>
%}
        *)
        and Height : sig
          type t = {
            revision_number : int64;
                (** {%html:
<p>the revision that the client is currently on</p>
%}
                *)
            revision_height : int64;
                (** {%html:
<p>the height within the given revision</p>
%} *)
          }

          val make :
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Params defines the set of IBC light client parameters.</p>
%}
        *)
        and Params : sig
          type t = string list
          (** {%html:
<p>allowed_clients defines the list of allowed client state types which can be created
and interacted with. If a client type is removed from the allowed clients list, usage
of this client will be disabled until it is added again to the list.</p>
%}
          *)

          val make : ?allowed_clients:string list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?allowed_clients:string list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end
  end
end = struct
  module rec Core : sig
    module rec Client : sig
      module rec V1 : sig
        (** {%html:
<p>IdentifiedClientState defines a client state with an additional client
identifier field.</p>
%}
        *)
        module rec IdentifiedClientState : sig
          type t = {
            client_id : string;  (** {%html:
<p>client identifier</p>
%} *)
            client_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>client state</p>
%} *)
          }

          val make :
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ConsensusStateWithHeight defines a consensus state with an additional height
field.</p>
%}
        *)
        and ConsensusStateWithHeight : sig
          type t = {
            height : Height.t option;
                (** {%html:
<p>consensus state height</p>
%} *)
            consensus_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>consensus state</p>
%} *)
          }

          val make :
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ClientConsensusStates defines all the stored consensus states for a given
client.</p>
%}
        *)
        and ClientConsensusStates : sig
          type t = {
            client_id : string;  (** {%html:
<p>client identifier</p>
%} *)
            consensus_states : ConsensusStateWithHeight.t list;
                (** {%html:
<p>consensus states and their heights associated with the client</p>
%}
                *)
          }

          val make :
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Height is a monotonically increasing data type
that can be compared against another Height for the purposes of updating and
freezing clients</p>
<p>Normally the RevisionHeight is incremented at each height while keeping
RevisionNumber the same. However some consensus algorithms may choose to
reset the height in certain conditions e.g. hard forks, state-machine
breaking changes In these cases, the RevisionNumber is incremented so that
height continues to be monitonically increasing even as the RevisionHeight
gets reset</p>
<p>Please note that json tags for generated Go code are overridden to explicitly exclude the omitempty jsontag.
This enforces the Go json marshaller to always emit zero values for both revision_number and revision_height.</p>
%}
        *)
        and Height : sig
          type t = {
            revision_number : int64;
                (** {%html:
<p>the revision that the client is currently on</p>
%}
                *)
            revision_height : int64;
                (** {%html:
<p>the height within the given revision</p>
%} *)
          }

          val make :
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Params defines the set of IBC light client parameters.</p>
%}
        *)
        and Params : sig
          type t = string list
          (** {%html:
<p>allowed_clients defines the list of allowed client state types which can be created
and interacted with. If a client type is removed from the allowed clients list, usage
of this client will be disabled until it is added again to the list.</p>
%}
          *)

          val make : ?allowed_clients:string list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?allowed_clients:string list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end
  end = struct
    module rec Client : sig
      module rec V1 : sig
        (** {%html:
<p>IdentifiedClientState defines a client state with an additional client
identifier field.</p>
%}
        *)
        module rec IdentifiedClientState : sig
          type t = {
            client_id : string;  (** {%html:
<p>client identifier</p>
%} *)
            client_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>client state</p>
%} *)
          }

          val make :
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ConsensusStateWithHeight defines a consensus state with an additional height
field.</p>
%}
        *)
        and ConsensusStateWithHeight : sig
          type t = {
            height : Height.t option;
                (** {%html:
<p>consensus state height</p>
%} *)
            consensus_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>consensus state</p>
%} *)
          }

          val make :
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ClientConsensusStates defines all the stored consensus states for a given
client.</p>
%}
        *)
        and ClientConsensusStates : sig
          type t = {
            client_id : string;  (** {%html:
<p>client identifier</p>
%} *)
            consensus_states : ConsensusStateWithHeight.t list;
                (** {%html:
<p>consensus states and their heights associated with the client</p>
%}
                *)
          }

          val make :
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Height is a monotonically increasing data type
that can be compared against another Height for the purposes of updating and
freezing clients</p>
<p>Normally the RevisionHeight is incremented at each height while keeping
RevisionNumber the same. However some consensus algorithms may choose to
reset the height in certain conditions e.g. hard forks, state-machine
breaking changes In these cases, the RevisionNumber is incremented so that
height continues to be monitonically increasing even as the RevisionHeight
gets reset</p>
<p>Please note that json tags for generated Go code are overridden to explicitly exclude the omitempty jsontag.
This enforces the Go json marshaller to always emit zero values for both revision_number and revision_height.</p>
%}
        *)
        and Height : sig
          type t = {
            revision_number : int64;
                (** {%html:
<p>the revision that the client is currently on</p>
%}
                *)
            revision_height : int64;
                (** {%html:
<p>the height within the given revision</p>
%} *)
          }

          val make :
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Params defines the set of IBC light client parameters.</p>
%}
        *)
        and Params : sig
          type t = string list
          (** {%html:
<p>allowed_clients defines the list of allowed client state types which can be created
and interacted with. If a client type is removed from the allowed clients list, usage
of this client will be disabled until it is added again to the list.</p>
%}
          *)

          val make : ?allowed_clients:string list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?allowed_clients:string list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end = struct
      module rec V1 : sig
        (** {%html:
<p>IdentifiedClientState defines a client state with an additional client
identifier field.</p>
%}
        *)
        module rec IdentifiedClientState : sig
          type t = {
            client_id : string;  (** {%html:
<p>client identifier</p>
%} *)
            client_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>client state</p>
%} *)
          }

          val make :
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ConsensusStateWithHeight defines a consensus state with an additional height
field.</p>
%}
        *)
        and ConsensusStateWithHeight : sig
          type t = {
            height : Height.t option;
                (** {%html:
<p>consensus state height</p>
%} *)
            consensus_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>consensus state</p>
%} *)
          }

          val make :
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>ClientConsensusStates defines all the stored consensus states for a given
client.</p>
%}
        *)
        and ClientConsensusStates : sig
          type t = {
            client_id : string;  (** {%html:
<p>client identifier</p>
%} *)
            consensus_states : ConsensusStateWithHeight.t list;
                (** {%html:
<p>consensus states and their heights associated with the client</p>
%}
                *)
          }

          val make :
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Height is a monotonically increasing data type
that can be compared against another Height for the purposes of updating and
freezing clients</p>
<p>Normally the RevisionHeight is incremented at each height while keeping
RevisionNumber the same. However some consensus algorithms may choose to
reset the height in certain conditions e.g. hard forks, state-machine
breaking changes In these cases, the RevisionNumber is incremented so that
height continues to be monitonically increasing even as the RevisionHeight
gets reset</p>
<p>Please note that json tags for generated Go code are overridden to explicitly exclude the omitempty jsontag.
This enforces the Go json marshaller to always emit zero values for both revision_number and revision_height.</p>
%}
        *)
        and Height : sig
          type t = {
            revision_number : int64;
                (** {%html:
<p>the revision that the client is currently on</p>
%}
                *)
            revision_height : int64;
                (** {%html:
<p>the height within the given revision</p>
%} *)
          }

          val make :
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>Params defines the set of IBC light client parameters.</p>
%}
        *)
        and Params : sig
          type t = string list
          (** {%html:
<p>allowed_clients defines the list of allowed client state types which can be created
and interacted with. If a client type is removed from the allowed clients list, usage
of this client will be disabled until it is added again to the list.</p>
%}
          *)

          val make : ?allowed_clients:string list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?allowed_clients:string list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end = struct
        module rec IdentifiedClientState : sig
          type t = {
            client_id : string;  (** {%html:
<p>client identifier</p>
%} *)
            client_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>client state</p>
%} *)
          }

          val make :
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = IdentifiedClientState

          let name () = ".ibc.core.client.v1.IdentifiedClientState"

          type t = {
            client_id : string;
            client_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
          }

          type make_t =
            ?client_id:string ->
            ?client_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          let make ?(client_id = {||}) ?client_state () =
            { client_id; client_state }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_client_id =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "client_id", "clientId"), string, {||}))
                 in
                 let merge_client_state =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt
                         ( (2, "client_state", "clientState"),
                           message
                             (module Imported'modules.Google_protobuf_any.Google
                                     .Protobuf
                                     .Any) ))
                 in
                 fun t1 t2 ->
                   {
                     client_id = merge_client_id t1.client_id t2.client_id;
                     client_state =
                       merge_client_state t1.client_state t2.client_state;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "client_id", "clientId"), string, {||})
              ^:: basic_opt
                    ( (2, "client_state", "clientState"),
                      message
                        (module Imported'modules.Google_protobuf_any.Google
                                .Protobuf
                                .Any) )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { client_id; client_state } ->
              serialize writer client_id client_state

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor client_id client_state =
              { client_id; client_state }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { client_id; client_state } -> serialize client_id client_state

          let from_json_exn =
            let constructor client_id client_state =
              { client_id; client_state }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and ConsensusStateWithHeight : sig
          type t = {
            height : Height.t option;
                (** {%html:
<p>consensus state height</p>
%} *)
            consensus_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
                (** {%html:
<p>consensus state</p>
%} *)
          }

          val make :
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = ConsensusStateWithHeight

          let name () = ".ibc.core.client.v1.ConsensusStateWithHeight"

          type t = {
            height : Height.t option;
            consensus_state :
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
          }

          type make_t =
            ?height:Height.t ->
            ?consensus_state:
              Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
            unit ->
            t

          let make ?height ?consensus_state () = { height; consensus_state }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_height =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt
                         ((1, "height", "height"), message (module Height)))
                 in
                 let merge_consensus_state =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic_opt
                         ( (2, "consensus_state", "consensusState"),
                           message
                             (module Imported'modules.Google_protobuf_any.Google
                                     .Protobuf
                                     .Any) ))
                 in
                 fun t1 t2 ->
                   {
                     height = merge_height t1.height t2.height;
                     consensus_state =
                       merge_consensus_state t1.consensus_state
                         t2.consensus_state;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic_opt ((1, "height", "height"), message (module Height))
              ^:: basic_opt
                    ( (2, "consensus_state", "consensusState"),
                      message
                        (module Imported'modules.Google_protobuf_any.Google
                                .Protobuf
                                .Any) )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { height; consensus_state } ->
              serialize writer height consensus_state

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor height consensus_state =
              { height; consensus_state }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { height; consensus_state } -> serialize height consensus_state

          let from_json_exn =
            let constructor height consensus_state =
              { height; consensus_state }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and ClientConsensusStates : sig
          type t = {
            client_id : string;  (** {%html:
<p>client identifier</p>
%} *)
            consensus_states : ConsensusStateWithHeight.t list;
                (** {%html:
<p>consensus states and their heights associated with the client</p>
%}
                *)
          }

          val make :
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = ClientConsensusStates

          let name () = ".ibc.core.client.v1.ClientConsensusStates"

          type t = {
            client_id : string;
            consensus_states : ConsensusStateWithHeight.t list;
          }

          type make_t =
            ?client_id:string ->
            ?consensus_states:ConsensusStateWithHeight.t list ->
            unit ->
            t

          let make ?(client_id = {||}) ?(consensus_states = []) () =
            { client_id; consensus_states }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_client_id =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "client_id", "clientId"), string, {||}))
                 in
                 let merge_consensus_states =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (2, "consensus_states", "consensusStates"),
                           message (module ConsensusStateWithHeight),
                           not_packed ))
                 in
                 fun t1 t2 ->
                   {
                     client_id = merge_client_id t1.client_id t2.client_id;
                     consensus_states =
                       merge_consensus_states t1.consensus_states
                         t2.consensus_states;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "client_id", "clientId"), string, {||})
              ^:: repeated
                    ( (2, "consensus_states", "consensusStates"),
                      message (module ConsensusStateWithHeight),
                      not_packed )
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { client_id; consensus_states } ->
              serialize writer client_id consensus_states

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor client_id consensus_states =
              { client_id; consensus_states }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { client_id; consensus_states } ->
              serialize client_id consensus_states

          let from_json_exn =
            let constructor client_id consensus_states =
              { client_id; consensus_states }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and Height : sig
          type t = {
            revision_number : int64;
                (** {%html:
<p>the revision that the client is currently on</p>
%}
                *)
            revision_height : int64;
                (** {%html:
<p>the height within the given revision</p>
%} *)
          }

          val make :
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = Height

          let name () = ".ibc.core.client.v1.Height"

          type t = { revision_number : int64; revision_height : int64 }

          type make_t =
            ?revision_number:int64 -> ?revision_height:int64 -> unit -> t

          let make ?(revision_number = 0L) ?(revision_height = 0L) () =
            { revision_number; revision_height }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_revision_number =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic
                         ((1, "revision_number", "revisionNumber"), uint64, 0L))
                 in
                 let merge_revision_height =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic
                         ((2, "revision_height", "revisionHeight"), uint64, 0L))
                 in
                 fun t1 t2 ->
                   {
                     revision_number =
                       merge_revision_number t1.revision_number
                         t2.revision_number;
                     revision_height =
                       merge_revision_height t1.revision_height
                         t2.revision_height;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "revision_number", "revisionNumber"), uint64, 0L)
              ^:: basic ((2, "revision_height", "revisionHeight"), uint64, 0L)
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { revision_number; revision_height } ->
              serialize writer revision_number revision_height

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor revision_number revision_height =
              { revision_number; revision_height }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { revision_number; revision_height } ->
              serialize revision_number revision_height

          let from_json_exn =
            let constructor revision_number revision_height =
              { revision_number; revision_height }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and Params : sig
          type t = string list
          (** {%html:
<p>allowed_clients defines the list of allowed client state types which can be created
and interacted with. If a client type is removed from the allowed clients list, usage
of this client will be disabled until it is added again to the list.</p>
%}
          *)

          val make : ?allowed_clients:string list -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?allowed_clients:string list -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = Params

          let name () = ".ibc.core.client.v1.Params"

          type t = string list
          type make_t = ?allowed_clients:string list -> unit -> t

          let make ?(allowed_clients = []) () = allowed_clients

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_allowed_clients =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       repeated
                         ( (1, "allowed_clients", "allowedClients"),
                           string,
                           not_packed ))
                 in
                 fun t1_allowed_clients t2_allowed_clients ->
                   merge_allowed_clients t1_allowed_clients t2_allowed_clients)
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              repeated
                ((1, "allowed_clients", "allowedClients"), string, not_packed)
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer allowed_clients -> serialize writer allowed_clients

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor allowed_clients = allowed_clients in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun allowed_clients -> serialize allowed_clients

          let from_json_exn =
            let constructor allowed_clients = allowed_clients in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end
      end
    end
  end
end

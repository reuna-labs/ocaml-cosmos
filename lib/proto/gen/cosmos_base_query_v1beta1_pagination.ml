(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: cosmos/base/query/v1beta1/pagination.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Cosmos_proto_cosmos = Cosmos_proto_cosmos
end

(**/**)

module rec Cosmos : sig
  module rec Base : sig
    module rec Query : sig
      module rec V1beta1 : sig
        (** {%html:
<p>PageRequest is to be embedded in gRPC request messages for efficient
pagination. Ex:</p>
<pre><code>message SomeRequest {
        Foo some_parameter = 1;
        PageRequest pagination = 2;
}
</code></pre>
%}
        *)
        module rec PageRequest : sig
          type t = {
            key : bytes;
                (** {%html:
<p>key is a value returned in PageResponse.next_key to begin
querying the next page most efficiently. Only one of offset or key
should be set.</p>
%}
                *)
            offset : int64;
                (** {%html:
<p>offset is a numeric offset that can be used when key is unavailable.
It is less efficient than using key. Only one of offset or key should
be set.</p>
%}
                *)
            limit : int64;
                (** {%html:
<p>limit is the total number of results to be returned in the result page.
If left empty it will default to a value to be set by each app.</p>
%}
                *)
            count_total : bool;
                (** {%html:
<p>count_total is set to true  to indicate that the result set should include
a count of the total number of items available for pagination in UIs.
count_total is only respected when offset is used. It is ignored when key
is set.</p>
%}
                *)
            reverse : bool;
                (** {%html:
<p>reverse is set to true if results are to be returned in the descending order.</p>
%}
                *)
          }

          val make :
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>PageResponse is to be embedded in gRPC response messages where the
corresponding request message has used PageRequest.</p>
<pre><code>message SomeResponse {
        repeated Bar results = 1;
        PageResponse page = 2;
}
</code></pre>
%}
        *)
        and PageResponse : sig
          type t = {
            next_key : bytes;
                (** {%html:
<p>next_key is the key to be passed to PageRequest.key to
query the next page most efficiently. It will be empty if
there are no more results.</p>
%}
                *)
            total : int64;
                (** {%html:
<p>total is total number of results available if PageRequest.count_total
was set, its value is undefined otherwise</p>
%}
                *)
          }

          val make : ?next_key:bytes -> ?total:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?next_key:bytes -> ?total:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end
  end
end = struct
  module rec Base : sig
    module rec Query : sig
      module rec V1beta1 : sig
        (** {%html:
<p>PageRequest is to be embedded in gRPC request messages for efficient
pagination. Ex:</p>
<pre><code>message SomeRequest {
        Foo some_parameter = 1;
        PageRequest pagination = 2;
}
</code></pre>
%}
        *)
        module rec PageRequest : sig
          type t = {
            key : bytes;
                (** {%html:
<p>key is a value returned in PageResponse.next_key to begin
querying the next page most efficiently. Only one of offset or key
should be set.</p>
%}
                *)
            offset : int64;
                (** {%html:
<p>offset is a numeric offset that can be used when key is unavailable.
It is less efficient than using key. Only one of offset or key should
be set.</p>
%}
                *)
            limit : int64;
                (** {%html:
<p>limit is the total number of results to be returned in the result page.
If left empty it will default to a value to be set by each app.</p>
%}
                *)
            count_total : bool;
                (** {%html:
<p>count_total is set to true  to indicate that the result set should include
a count of the total number of items available for pagination in UIs.
count_total is only respected when offset is used. It is ignored when key
is set.</p>
%}
                *)
            reverse : bool;
                (** {%html:
<p>reverse is set to true if results are to be returned in the descending order.</p>
%}
                *)
          }

          val make :
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>PageResponse is to be embedded in gRPC response messages where the
corresponding request message has used PageRequest.</p>
<pre><code>message SomeResponse {
        repeated Bar results = 1;
        PageResponse page = 2;
}
</code></pre>
%}
        *)
        and PageResponse : sig
          type t = {
            next_key : bytes;
                (** {%html:
<p>next_key is the key to be passed to PageRequest.key to
query the next page most efficiently. It will be empty if
there are no more results.</p>
%}
                *)
            total : int64;
                (** {%html:
<p>total is total number of results available if PageRequest.count_total
was set, its value is undefined otherwise</p>
%}
                *)
          }

          val make : ?next_key:bytes -> ?total:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?next_key:bytes -> ?total:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end
  end = struct
    module rec Query : sig
      module rec V1beta1 : sig
        (** {%html:
<p>PageRequest is to be embedded in gRPC request messages for efficient
pagination. Ex:</p>
<pre><code>message SomeRequest {
        Foo some_parameter = 1;
        PageRequest pagination = 2;
}
</code></pre>
%}
        *)
        module rec PageRequest : sig
          type t = {
            key : bytes;
                (** {%html:
<p>key is a value returned in PageResponse.next_key to begin
querying the next page most efficiently. Only one of offset or key
should be set.</p>
%}
                *)
            offset : int64;
                (** {%html:
<p>offset is a numeric offset that can be used when key is unavailable.
It is less efficient than using key. Only one of offset or key should
be set.</p>
%}
                *)
            limit : int64;
                (** {%html:
<p>limit is the total number of results to be returned in the result page.
If left empty it will default to a value to be set by each app.</p>
%}
                *)
            count_total : bool;
                (** {%html:
<p>count_total is set to true  to indicate that the result set should include
a count of the total number of items available for pagination in UIs.
count_total is only respected when offset is used. It is ignored when key
is set.</p>
%}
                *)
            reverse : bool;
                (** {%html:
<p>reverse is set to true if results are to be returned in the descending order.</p>
%}
                *)
          }

          val make :
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>PageResponse is to be embedded in gRPC response messages where the
corresponding request message has used PageRequest.</p>
<pre><code>message SomeResponse {
        repeated Bar results = 1;
        PageResponse page = 2;
}
</code></pre>
%}
        *)
        and PageResponse : sig
          type t = {
            next_key : bytes;
                (** {%html:
<p>next_key is the key to be passed to PageRequest.key to
query the next page most efficiently. It will be empty if
there are no more results.</p>
%}
                *)
            total : int64;
                (** {%html:
<p>total is total number of results available if PageRequest.count_total
was set, its value is undefined otherwise</p>
%}
                *)
          }

          val make : ?next_key:bytes -> ?total:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?next_key:bytes -> ?total:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end
    end = struct
      module rec V1beta1 : sig
        (** {%html:
<p>PageRequest is to be embedded in gRPC request messages for efficient
pagination. Ex:</p>
<pre><code>message SomeRequest {
        Foo some_parameter = 1;
        PageRequest pagination = 2;
}
</code></pre>
%}
        *)
        module rec PageRequest : sig
          type t = {
            key : bytes;
                (** {%html:
<p>key is a value returned in PageResponse.next_key to begin
querying the next page most efficiently. Only one of offset or key
should be set.</p>
%}
                *)
            offset : int64;
                (** {%html:
<p>offset is a numeric offset that can be used when key is unavailable.
It is less efficient than using key. Only one of offset or key should
be set.</p>
%}
                *)
            limit : int64;
                (** {%html:
<p>limit is the total number of results to be returned in the result page.
If left empty it will default to a value to be set by each app.</p>
%}
                *)
            count_total : bool;
                (** {%html:
<p>count_total is set to true  to indicate that the result set should include
a count of the total number of items available for pagination in UIs.
count_total is only respected when offset is used. It is ignored when key
is set.</p>
%}
                *)
            reverse : bool;
                (** {%html:
<p>reverse is set to true if results are to be returned in the descending order.</p>
%}
                *)
          }

          val make :
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end

        (** {%html:
<p>PageResponse is to be embedded in gRPC response messages where the
corresponding request message has used PageRequest.</p>
<pre><code>message SomeResponse {
        repeated Bar results = 1;
        PageResponse page = 2;
}
</code></pre>
%}
        *)
        and PageResponse : sig
          type t = {
            next_key : bytes;
                (** {%html:
<p>next_key is the key to be passed to PageRequest.key to
query the next page most efficiently. It will be empty if
there are no more results.</p>
%}
                *)
            total : int64;
                (** {%html:
<p>total is total number of results available if PageRequest.count_total
was set, its value is undefined otherwise</p>
%}
                *)
          }

          val make : ?next_key:bytes -> ?total:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?next_key:bytes -> ?total:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end
      end = struct
        module rec PageRequest : sig
          type t = {
            key : bytes;
                (** {%html:
<p>key is a value returned in PageResponse.next_key to begin
querying the next page most efficiently. Only one of offset or key
should be set.</p>
%}
                *)
            offset : int64;
                (** {%html:
<p>offset is a numeric offset that can be used when key is unavailable.
It is less efficient than using key. Only one of offset or key should
be set.</p>
%}
                *)
            limit : int64;
                (** {%html:
<p>limit is the total number of results to be returned in the result page.
If left empty it will default to a value to be set by each app.</p>
%}
                *)
            count_total : bool;
                (** {%html:
<p>count_total is set to true  to indicate that the result set should include
a count of the total number of items available for pagination in UIs.
count_total is only respected when offset is used. It is ignored when key
is set.</p>
%}
                *)
            reverse : bool;
                (** {%html:
<p>reverse is set to true if results are to be returned in the descending order.</p>
%}
                *)
          }

          val make :
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t =
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = PageRequest

          let name () = ".cosmos.base.query.v1beta1.PageRequest"

          type t = {
            key : bytes;
            offset : int64;
            limit : int64;
            count_total : bool;
            reverse : bool;
          }

          type make_t =
            ?key:bytes ->
            ?offset:int64 ->
            ?limit:int64 ->
            ?count_total:bool ->
            ?reverse:bool ->
            unit ->
            t

          let make ?(key = Bytes.of_string {||}) ?(offset = 0L) ?(limit = 0L)
              ?(count_total = false) ?(reverse = false) () =
            { key; offset; limit; count_total; reverse }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_key =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((1, "key", "key"), bytes, Bytes.of_string {||}))
                 in
                 let merge_offset =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((2, "offset", "offset"), uint64, 0L))
                 in
                 let merge_limit =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((3, "limit", "limit"), uint64, 0L))
                 in
                 let merge_count_total =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((4, "count_total", "countTotal"), bool, false))
                 in
                 let merge_reverse =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic ((5, "reverse", "reverse"), bool, false))
                 in
                 fun t1 t2 ->
                   {
                     key = merge_key t1.key t2.key;
                     offset = merge_offset t1.offset t2.offset;
                     limit = merge_limit t1.limit t2.limit;
                     count_total =
                       merge_count_total t1.count_total t2.count_total;
                     reverse = merge_reverse t1.reverse t2.reverse;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "key", "key"), bytes, Bytes.of_string {||})
              ^:: basic ((2, "offset", "offset"), uint64, 0L)
              ^:: basic ((3, "limit", "limit"), uint64, 0L)
              ^:: basic ((4, "count_total", "countTotal"), bool, false)
              ^:: basic ((5, "reverse", "reverse"), bool, false)
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { key; offset; limit; count_total; reverse } ->
              serialize writer key offset limit count_total reverse

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor key offset limit count_total reverse =
              { key; offset; limit; count_total; reverse }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { key; offset; limit; count_total; reverse } ->
              serialize key offset limit count_total reverse

          let from_json_exn =
            let constructor key offset limit count_total reverse =
              { key; offset; limit; count_total; reverse }
            in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end

        and PageResponse : sig
          type t = {
            next_key : bytes;
                (** {%html:
<p>next_key is the key to be passed to PageRequest.key to
query the next page most efficiently. It will be empty if
there are no more results.</p>
%}
                *)
            total : int64;
                (** {%html:
<p>total is total number of results available if PageRequest.count_total
was set, its value is undefined otherwise</p>
%}
                *)
          }

          val make : ?next_key:bytes -> ?total:int64 -> unit -> t
          (** Helper function to generate a message using default values *)

          val to_proto : t -> Runtime'.Writer.t
          (** Serialize the message to binary format *)

          val from_proto :
            Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from binary format *)

          val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
          (** Serialize to Json (compatible with Yojson.Basic.t) *)

          val from_json :
            Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
          (** Deserialize from Json (compatible with Yojson.Basic.t) *)

          val name : unit -> string
          (** Fully qualified protobuf name of this message *)

          (**/**)

          type make_t = ?next_key:bytes -> ?total:int64 -> unit -> t

          val merge : t -> t -> t
          val to_proto' : Runtime'.Writer.t -> t -> unit
          val from_proto_exn : Runtime'.Reader.t -> t
          val from_json_exn : Runtime'.Json.t -> t

          (**/**)
        end = struct
          module This'_ = PageResponse

          let name () = ".cosmos.base.query.v1beta1.PageResponse"

          type t = { next_key : bytes; total : int64 }
          type make_t = ?next_key:bytes -> ?total:int64 -> unit -> t

          let make ?(next_key = Bytes.of_string {||}) ?(total = 0L) () =
            { next_key; total }

          let merge =
            (* deferred by tools/lazify-merge.py; see that file *)
            let merge' =
              lazy
                (let merge_next_key =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(
                       basic
                         ( (1, "next_key", "nextKey"),
                           bytes,
                           Bytes.of_string {||} ))
                 in
                 let merge_total =
                   Runtime'.Merge.merge
                     Runtime'.Spec.(basic ((2, "total", "total"), uint64, 0L))
                 in
                 fun t1 t2 ->
                   {
                     next_key = merge_next_key t1.next_key t2.next_key;
                     total = merge_total t1.total t2.total;
                   })
            in
            fun t1' t2' -> (Lazy.force merge') t1' t2'

          let spec () =
            Runtime'.Spec.(
              basic ((1, "next_key", "nextKey"), bytes, Bytes.of_string {||})
              ^:: basic ((2, "total", "total"), uint64, 0L)
              ^:: nil)

          let to_proto' =
            let serialize =
              Runtime'.apply_lazy (fun () ->
                  Runtime'.Serialize.serialize (spec ()))
            in
            fun writer { next_key; total } -> serialize writer next_key total

          let to_proto t =
            let writer = Runtime'.Writer.init () in
            to_proto' writer t;
            writer

          let from_proto_exn =
            let constructor next_key total = { next_key; total } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize.deserialize (spec ()) constructor)

          let from_proto writer =
            Runtime'.Result.catch (fun () -> from_proto_exn writer)

          let to_json options =
            let serialize =
              Runtime'.Serialize_json.serialize ~message_name:(name ())
                (spec ()) options
            in
            fun { next_key; total } -> serialize next_key total

          let from_json_exn =
            let constructor next_key total = { next_key; total } in
            Runtime'.apply_lazy (fun () ->
                Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                  (spec ()) constructor)

          let from_json json =
            Runtime'.Result.catch (fun () -> from_json_exn json)
        end
      end
    end
  end
end

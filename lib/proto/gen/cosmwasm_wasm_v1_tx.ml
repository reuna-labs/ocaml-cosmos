(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: cosmwasm/wasm/v1/tx.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Cosmos_base_v1beta1_coin = Cosmos_base_v1beta1_coin
  module Cosmos_msg_v1_msg = Cosmos_msg_v1_msg
  module Gogoproto_gogo = Gogoproto_gogo
  module Cosmwasm_wasm_v1_types = Cosmwasm_wasm_v1_types
  module Cosmos_proto_cosmos = Cosmos_proto_cosmos
  module Amino_amino = Amino_amino
end

(**/**)

module rec Cosmwasm : sig
  module rec Wasm : sig
    module rec V1 : sig
      (** {%html:
<p>MsgStoreCode submit Wasm code to the system</p>
%} *)
      module rec MsgStoreCode : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the actor that signed the messages</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission access control to apply on contract creation,
optional</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreCodeResponse returns store result data.</p>
%} *)
      and MsgStoreCodeResponse : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          checksum : bytes;
              (** {%html:
<p>Checksum is the sha256 hash of the stored code</p>
%}
              *)
        }

        val make : ?code_id:int64 -> ?checksum:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?code_id:int64 -> ?checksum:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContract create a new smart contract instance for the given
code id.</p>
%}
      *)
      and MsgInstantiateContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on instantiation</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContractResponse return instantiation result data</p>
%}
      *)
      and MsgInstantiateContractResponse : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContract2 create a new smart contract instance for the given
code id with a predictable address.</p>
%}
      *)
      and MsgInstantiateContract2 : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on instantiation</p>
%}
              *)
          salt : bytes;
              (** {%html:
<p>Salt is an arbitrary value provided by the sender. Size can be 1 to 64.</p>
%}
              *)
          fix_msg : bool;
              (** {%html:
<p>FixMsg include the msg value into the hash for the predictable address.
Default is false</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?salt:bytes ->
          ?fix_msg:bool ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?salt:bytes ->
          ?fix_msg:bool ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContract2Response return instantiation result data</p>
%}
      *)
      and MsgInstantiateContract2Response : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgExecuteContract submits the given message data to a smart contract</p>
%}
      *)
      and MsgExecuteContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on execution</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?contract:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?contract:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgExecuteContractResponse returns execution result data.</p>
%}
      *)
      and MsgExecuteContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgMigrateContract runs a code upgrade/ downgrade for a smart contract</p>
%}
      *)
      and MsgMigrateContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID references the new WASM code</p>
%} *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on migration</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?contract:string ->
          ?code_id:int64 ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?contract:string ->
          ?code_id:int64 ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgMigrateContractResponse returns contract migration result data.</p>
%}
      *)
      and MsgMigrateContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains same raw bytes returned as data from the wasm contract.
(May be empty)</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateAdmin sets a new admin for a smart contract</p>
%}
      *)
      and MsgUpdateAdmin : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          new_admin : string;
              (** {%html:
<p>NewAdmin address to be set</p>
%} *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make :
          ?sender:string -> ?new_admin:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string -> ?new_admin:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateAdminResponse returns empty data</p>
%} *)
      and MsgUpdateAdminResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgClearAdmin removes any admin stored for a smart contract</p>
%}
      *)
      and MsgClearAdmin : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make : ?sender:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?sender:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgClearAdminResponse returns empty data</p>
%} *)
      and MsgClearAdminResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateInstantiateConfig updates instantiate config for a smart contract</p>
%}
      *)
      and MsgUpdateInstantiateConfig : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID references the stored WASM code</p>
%} *)
          new_instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>NewInstantiatePermission is the new access control</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?code_id:int64 ->
          ?new_instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?code_id:int64 ->
          ?new_instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateInstantiateConfigResponse returns empty data</p>
%}
      *)
      and MsgUpdateInstantiateConfigResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateParams is the MsgUpdateParams request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUpdateParams : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          params :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t
            option;
              (** {%html:
<p>params defines the x/wasm parameters to update.</p>
<p>NOTE: All parameters must be supplied.</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?params:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?params:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateParamsResponse defines the response structure for executing a
MsgUpdateParams message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUpdateParamsResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgSudoContract is the MsgSudoContract request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgSudoContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract as sudo</p>
%}
              *)
        }

        val make :
          ?authority:string -> ?contract:string -> ?msg:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string -> ?contract:string -> ?msg:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgSudoContractResponse defines the response structure for executing a
MsgSudoContract message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgSudoContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgPinCodes is the MsgPinCodes request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgPinCodes : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          code_ids : int64 list;
              (** {%html:
<p>CodeIDs references the new WASM codes</p>
%} *)
        }

        val make : ?authority:string -> ?code_ids:int64 list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?code_ids:int64 list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgPinCodesResponse defines the response structure for executing a
MsgPinCodes message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgPinCodesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUnpinCodes is the MsgUnpinCodes request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUnpinCodes : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          code_ids : int64 list;
              (** {%html:
<p>CodeIDs references the WASM codes</p>
%} *)
        }

        val make : ?authority:string -> ?code_ids:int64 list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?code_ids:int64 list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUnpinCodesResponse defines the response structure for executing a
MsgUnpinCodes message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUnpinCodesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndInstantiateContract is the MsgStoreAndInstantiateContract
request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgStoreAndInstantiateContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission to apply on contract creation, optional</p>
%}
              *)
          unpin_code : bool;
              (** {%html:
<p>UnpinCode code on upload, optional. As default the uploaded contract is
pinned to cache.</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred from the authority account to the contract
on instantiation</p>
%}
              *)
          source : string;
              (** {%html:
<p>Source is the URL where the code is hosted</p>
%}
              *)
          builder : string;
              (** {%html:
<p>Builder is the docker image used to build the code deterministically, used
for smart contract verification</p>
%}
              *)
          code_hash : bytes;
              (** {%html:
<p>CodeHash is the SHA256 sum of the code outputted by builder, used for smart
contract verification</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?unpin_code:bool ->
          ?admin:string ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?source:string ->
          ?builder:string ->
          ?code_hash:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?unpin_code:bool ->
          ?admin:string ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?source:string ->
          ?builder:string ->
          ?code_hash:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndInstantiateContractResponse defines the response structure
for executing a MsgStoreAndInstantiateContract message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgStoreAndInstantiateContractResponse : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgAddCodeUploadParamsAddresses is the
MsgAddCodeUploadParamsAddresses request type.</p>
%}
      *)
      and MsgAddCodeUploadParamsAddresses : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          addresses : string list;
        }

        val make : ?authority:string -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgAddCodeUploadParamsAddressesResponse defines the response
structure for executing a MsgAddCodeUploadParamsAddresses message.</p>
%}
      *)
      and MsgAddCodeUploadParamsAddressesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgRemoveCodeUploadParamsAddresses is the
MsgRemoveCodeUploadParamsAddresses request type.</p>
%}
      *)
      and MsgRemoveCodeUploadParamsAddresses : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          addresses : string list;
        }

        val make : ?authority:string -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgRemoveCodeUploadParamsAddressesResponse defines the response
structure for executing a MsgRemoveCodeUploadParamsAddresses message.</p>
%}
      *)
      and MsgRemoveCodeUploadParamsAddressesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndMigrateContract is the MsgStoreAndMigrateContract
request type.</p>
<p>Since: 0.42</p>
%}
      *)
      and MsgStoreAndMigrateContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission to apply on contract creation, optional</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on migration</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?contract:string ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?contract:string ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndMigrateContractResponse defines the response structure
for executing a MsgStoreAndMigrateContract message.</p>
<p>Since: 0.42</p>
%}
      *)
      and MsgStoreAndMigrateContractResponse : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          checksum : bytes;
              (** {%html:
<p>Checksum is the sha256 hash of the stored code</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?code_id:int64 -> ?checksum:bytes -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_id:int64 -> ?checksum:bytes -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateContractLabel sets a new label for a smart contract</p>
%}
      *)
      and MsgUpdateContractLabel : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          new_label : string;
              (** {%html:
<p>NewLabel string to be set</p>
%} *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make :
          ?sender:string -> ?new_label:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string -> ?new_label:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateContractLabelResponse returns empty data</p>
%} *)
      and MsgUpdateContractLabelResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      module Msg : sig
        module StoreCode : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgStoreCode.t
               and type Response.t = MsgStoreCodeResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgStoreCode.t
               and type make_t = MsgStoreCode.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgStoreCodeResponse.t
               and type make_t = MsgStoreCodeResponse.make_t
        end

        val storeCode :
          (module Runtime'.Spec.Message with type t = MsgStoreCode.t)
          * (module Runtime'.Spec.Message with type t = MsgStoreCodeResponse.t)

        module InstantiateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgInstantiateContract.t
               and type Response.t = MsgInstantiateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContract.t
               and type make_t = MsgInstantiateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContractResponse.t
               and type make_t = MsgInstantiateContractResponse.make_t
        end

        val instantiateContract :
          (module Runtime'.Spec.Message with type t = MsgInstantiateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgInstantiateContractResponse.t)

        module InstantiateContract2 : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgInstantiateContract2.t
               and type Response.t = MsgInstantiateContract2Response.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContract2.t
               and type make_t = MsgInstantiateContract2.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContract2Response.t
               and type make_t = MsgInstantiateContract2Response.make_t
        end

        val instantiateContract2 :
          (module Runtime'.Spec.Message with type t = MsgInstantiateContract2.t)
          * (module Runtime'.Spec.Message
               with type t = MsgInstantiateContract2Response.t)

        module ExecuteContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgExecuteContract.t
               and type Response.t = MsgExecuteContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgExecuteContract.t
               and type make_t = MsgExecuteContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgExecuteContractResponse.t
               and type make_t = MsgExecuteContractResponse.make_t
        end

        val executeContract :
          (module Runtime'.Spec.Message with type t = MsgExecuteContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgExecuteContractResponse.t)

        module MigrateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgMigrateContract.t
               and type Response.t = MsgMigrateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgMigrateContract.t
               and type make_t = MsgMigrateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgMigrateContractResponse.t
               and type make_t = MsgMigrateContractResponse.make_t
        end

        val migrateContract :
          (module Runtime'.Spec.Message with type t = MsgMigrateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgMigrateContractResponse.t)

        module UpdateAdmin : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateAdmin.t
               and type Response.t = MsgUpdateAdminResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateAdmin.t
               and type make_t = MsgUpdateAdmin.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateAdminResponse.t
               and type make_t = MsgUpdateAdminResponse.make_t
        end

        val updateAdmin :
          (module Runtime'.Spec.Message with type t = MsgUpdateAdmin.t)
          * (module Runtime'.Spec.Message with type t = MsgUpdateAdminResponse.t)

        module ClearAdmin : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgClearAdmin.t
               and type Response.t = MsgClearAdminResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgClearAdmin.t
               and type make_t = MsgClearAdmin.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgClearAdminResponse.t
               and type make_t = MsgClearAdminResponse.make_t
        end

        val clearAdmin :
          (module Runtime'.Spec.Message with type t = MsgClearAdmin.t)
          * (module Runtime'.Spec.Message with type t = MsgClearAdminResponse.t)

        module UpdateInstantiateConfig : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateInstantiateConfig.t
               and type Response.t = MsgUpdateInstantiateConfigResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateInstantiateConfig.t
               and type make_t = MsgUpdateInstantiateConfig.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateInstantiateConfigResponse.t
               and type make_t = MsgUpdateInstantiateConfigResponse.make_t
        end

        val updateInstantiateConfig :
          (module Runtime'.Spec.Message
             with type t = MsgUpdateInstantiateConfig.t)
          * (module Runtime'.Spec.Message
               with type t = MsgUpdateInstantiateConfigResponse.t)

        module UpdateParams : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateParams.t
               and type Response.t = MsgUpdateParamsResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateParams.t
               and type make_t = MsgUpdateParams.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateParamsResponse.t
               and type make_t = MsgUpdateParamsResponse.make_t
        end

        val updateParams :
          (module Runtime'.Spec.Message with type t = MsgUpdateParams.t)
          * (module Runtime'.Spec.Message
               with type t = MsgUpdateParamsResponse.t)

        module SudoContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgSudoContract.t
               and type Response.t = MsgSudoContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgSudoContract.t
               and type make_t = MsgSudoContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgSudoContractResponse.t
               and type make_t = MsgSudoContractResponse.make_t
        end

        val sudoContract :
          (module Runtime'.Spec.Message with type t = MsgSudoContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgSudoContractResponse.t)

        module PinCodes : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgPinCodes.t
               and type Response.t = MsgPinCodesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgPinCodes.t
               and type make_t = MsgPinCodes.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgPinCodesResponse.t
               and type make_t = MsgPinCodesResponse.make_t
        end

        val pinCodes :
          (module Runtime'.Spec.Message with type t = MsgPinCodes.t)
          * (module Runtime'.Spec.Message with type t = MsgPinCodesResponse.t)

        module UnpinCodes : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUnpinCodes.t
               and type Response.t = MsgUnpinCodesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUnpinCodes.t
               and type make_t = MsgUnpinCodes.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUnpinCodesResponse.t
               and type make_t = MsgUnpinCodesResponse.make_t
        end

        val unpinCodes :
          (module Runtime'.Spec.Message with type t = MsgUnpinCodes.t)
          * (module Runtime'.Spec.Message with type t = MsgUnpinCodesResponse.t)

        module StoreAndInstantiateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgStoreAndInstantiateContract.t
               and type Response.t = MsgStoreAndInstantiateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgStoreAndInstantiateContract.t
               and type make_t = MsgStoreAndInstantiateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgStoreAndInstantiateContractResponse.t
               and type make_t = MsgStoreAndInstantiateContractResponse.make_t
        end

        val storeAndInstantiateContract :
          (module Runtime'.Spec.Message
             with type t = MsgStoreAndInstantiateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgStoreAndInstantiateContractResponse.t)

        module RemoveCodeUploadParamsAddresses : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgRemoveCodeUploadParamsAddresses.t
               and type Response.t =
                MsgRemoveCodeUploadParamsAddressesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgRemoveCodeUploadParamsAddresses.t
               and type make_t = MsgRemoveCodeUploadParamsAddresses.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgRemoveCodeUploadParamsAddressesResponse.t
               and type make_t =
                MsgRemoveCodeUploadParamsAddressesResponse.make_t
        end

        val removeCodeUploadParamsAddresses :
          (module Runtime'.Spec.Message
             with type t = MsgRemoveCodeUploadParamsAddresses.t)
          * (module Runtime'.Spec.Message
               with type t = MsgRemoveCodeUploadParamsAddressesResponse.t)

        module AddCodeUploadParamsAddresses : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgAddCodeUploadParamsAddresses.t
               and type Response.t = MsgAddCodeUploadParamsAddressesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgAddCodeUploadParamsAddresses.t
               and type make_t = MsgAddCodeUploadParamsAddresses.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgAddCodeUploadParamsAddressesResponse.t
               and type make_t = MsgAddCodeUploadParamsAddressesResponse.make_t
        end

        val addCodeUploadParamsAddresses :
          (module Runtime'.Spec.Message
             with type t = MsgAddCodeUploadParamsAddresses.t)
          * (module Runtime'.Spec.Message
               with type t = MsgAddCodeUploadParamsAddressesResponse.t)

        module StoreAndMigrateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgStoreAndMigrateContract.t
               and type Response.t = MsgStoreAndMigrateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgStoreAndMigrateContract.t
               and type make_t = MsgStoreAndMigrateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgStoreAndMigrateContractResponse.t
               and type make_t = MsgStoreAndMigrateContractResponse.make_t
        end

        val storeAndMigrateContract :
          (module Runtime'.Spec.Message
             with type t = MsgStoreAndMigrateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgStoreAndMigrateContractResponse.t)

        module UpdateContractLabel : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateContractLabel.t
               and type Response.t = MsgUpdateContractLabelResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateContractLabel.t
               and type make_t = MsgUpdateContractLabel.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateContractLabelResponse.t
               and type make_t = MsgUpdateContractLabelResponse.make_t
        end

        val updateContractLabel :
          (module Runtime'.Spec.Message with type t = MsgUpdateContractLabel.t)
          * (module Runtime'.Spec.Message
               with type t = MsgUpdateContractLabelResponse.t)
      end
    end
  end
end = struct
  module rec Wasm : sig
    module rec V1 : sig
      (** {%html:
<p>MsgStoreCode submit Wasm code to the system</p>
%} *)
      module rec MsgStoreCode : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the actor that signed the messages</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission access control to apply on contract creation,
optional</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreCodeResponse returns store result data.</p>
%} *)
      and MsgStoreCodeResponse : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          checksum : bytes;
              (** {%html:
<p>Checksum is the sha256 hash of the stored code</p>
%}
              *)
        }

        val make : ?code_id:int64 -> ?checksum:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?code_id:int64 -> ?checksum:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContract create a new smart contract instance for the given
code id.</p>
%}
      *)
      and MsgInstantiateContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on instantiation</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContractResponse return instantiation result data</p>
%}
      *)
      and MsgInstantiateContractResponse : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContract2 create a new smart contract instance for the given
code id with a predictable address.</p>
%}
      *)
      and MsgInstantiateContract2 : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on instantiation</p>
%}
              *)
          salt : bytes;
              (** {%html:
<p>Salt is an arbitrary value provided by the sender. Size can be 1 to 64.</p>
%}
              *)
          fix_msg : bool;
              (** {%html:
<p>FixMsg include the msg value into the hash for the predictable address.
Default is false</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?salt:bytes ->
          ?fix_msg:bool ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?salt:bytes ->
          ?fix_msg:bool ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContract2Response return instantiation result data</p>
%}
      *)
      and MsgInstantiateContract2Response : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgExecuteContract submits the given message data to a smart contract</p>
%}
      *)
      and MsgExecuteContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on execution</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?contract:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?contract:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgExecuteContractResponse returns execution result data.</p>
%}
      *)
      and MsgExecuteContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgMigrateContract runs a code upgrade/ downgrade for a smart contract</p>
%}
      *)
      and MsgMigrateContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID references the new WASM code</p>
%} *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on migration</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?contract:string ->
          ?code_id:int64 ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?contract:string ->
          ?code_id:int64 ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgMigrateContractResponse returns contract migration result data.</p>
%}
      *)
      and MsgMigrateContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains same raw bytes returned as data from the wasm contract.
(May be empty)</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateAdmin sets a new admin for a smart contract</p>
%}
      *)
      and MsgUpdateAdmin : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          new_admin : string;
              (** {%html:
<p>NewAdmin address to be set</p>
%} *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make :
          ?sender:string -> ?new_admin:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string -> ?new_admin:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateAdminResponse returns empty data</p>
%} *)
      and MsgUpdateAdminResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgClearAdmin removes any admin stored for a smart contract</p>
%}
      *)
      and MsgClearAdmin : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make : ?sender:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?sender:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgClearAdminResponse returns empty data</p>
%} *)
      and MsgClearAdminResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateInstantiateConfig updates instantiate config for a smart contract</p>
%}
      *)
      and MsgUpdateInstantiateConfig : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID references the stored WASM code</p>
%} *)
          new_instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>NewInstantiatePermission is the new access control</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?code_id:int64 ->
          ?new_instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?code_id:int64 ->
          ?new_instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateInstantiateConfigResponse returns empty data</p>
%}
      *)
      and MsgUpdateInstantiateConfigResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateParams is the MsgUpdateParams request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUpdateParams : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          params :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t
            option;
              (** {%html:
<p>params defines the x/wasm parameters to update.</p>
<p>NOTE: All parameters must be supplied.</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?params:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?params:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateParamsResponse defines the response structure for executing a
MsgUpdateParams message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUpdateParamsResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgSudoContract is the MsgSudoContract request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgSudoContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract as sudo</p>
%}
              *)
        }

        val make :
          ?authority:string -> ?contract:string -> ?msg:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string -> ?contract:string -> ?msg:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgSudoContractResponse defines the response structure for executing a
MsgSudoContract message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgSudoContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgPinCodes is the MsgPinCodes request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgPinCodes : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          code_ids : int64 list;
              (** {%html:
<p>CodeIDs references the new WASM codes</p>
%} *)
        }

        val make : ?authority:string -> ?code_ids:int64 list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?code_ids:int64 list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgPinCodesResponse defines the response structure for executing a
MsgPinCodes message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgPinCodesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUnpinCodes is the MsgUnpinCodes request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUnpinCodes : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          code_ids : int64 list;
              (** {%html:
<p>CodeIDs references the WASM codes</p>
%} *)
        }

        val make : ?authority:string -> ?code_ids:int64 list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?code_ids:int64 list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUnpinCodesResponse defines the response structure for executing a
MsgUnpinCodes message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUnpinCodesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndInstantiateContract is the MsgStoreAndInstantiateContract
request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgStoreAndInstantiateContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission to apply on contract creation, optional</p>
%}
              *)
          unpin_code : bool;
              (** {%html:
<p>UnpinCode code on upload, optional. As default the uploaded contract is
pinned to cache.</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred from the authority account to the contract
on instantiation</p>
%}
              *)
          source : string;
              (** {%html:
<p>Source is the URL where the code is hosted</p>
%}
              *)
          builder : string;
              (** {%html:
<p>Builder is the docker image used to build the code deterministically, used
for smart contract verification</p>
%}
              *)
          code_hash : bytes;
              (** {%html:
<p>CodeHash is the SHA256 sum of the code outputted by builder, used for smart
contract verification</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?unpin_code:bool ->
          ?admin:string ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?source:string ->
          ?builder:string ->
          ?code_hash:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?unpin_code:bool ->
          ?admin:string ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?source:string ->
          ?builder:string ->
          ?code_hash:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndInstantiateContractResponse defines the response structure
for executing a MsgStoreAndInstantiateContract message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgStoreAndInstantiateContractResponse : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgAddCodeUploadParamsAddresses is the
MsgAddCodeUploadParamsAddresses request type.</p>
%}
      *)
      and MsgAddCodeUploadParamsAddresses : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          addresses : string list;
        }

        val make : ?authority:string -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgAddCodeUploadParamsAddressesResponse defines the response
structure for executing a MsgAddCodeUploadParamsAddresses message.</p>
%}
      *)
      and MsgAddCodeUploadParamsAddressesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgRemoveCodeUploadParamsAddresses is the
MsgRemoveCodeUploadParamsAddresses request type.</p>
%}
      *)
      and MsgRemoveCodeUploadParamsAddresses : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          addresses : string list;
        }

        val make : ?authority:string -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgRemoveCodeUploadParamsAddressesResponse defines the response
structure for executing a MsgRemoveCodeUploadParamsAddresses message.</p>
%}
      *)
      and MsgRemoveCodeUploadParamsAddressesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndMigrateContract is the MsgStoreAndMigrateContract
request type.</p>
<p>Since: 0.42</p>
%}
      *)
      and MsgStoreAndMigrateContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission to apply on contract creation, optional</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on migration</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?contract:string ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?contract:string ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndMigrateContractResponse defines the response structure
for executing a MsgStoreAndMigrateContract message.</p>
<p>Since: 0.42</p>
%}
      *)
      and MsgStoreAndMigrateContractResponse : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          checksum : bytes;
              (** {%html:
<p>Checksum is the sha256 hash of the stored code</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?code_id:int64 -> ?checksum:bytes -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_id:int64 -> ?checksum:bytes -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateContractLabel sets a new label for a smart contract</p>
%}
      *)
      and MsgUpdateContractLabel : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          new_label : string;
              (** {%html:
<p>NewLabel string to be set</p>
%} *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make :
          ?sender:string -> ?new_label:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string -> ?new_label:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateContractLabelResponse returns empty data</p>
%} *)
      and MsgUpdateContractLabelResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      module Msg : sig
        module StoreCode : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgStoreCode.t
               and type Response.t = MsgStoreCodeResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgStoreCode.t
               and type make_t = MsgStoreCode.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgStoreCodeResponse.t
               and type make_t = MsgStoreCodeResponse.make_t
        end

        val storeCode :
          (module Runtime'.Spec.Message with type t = MsgStoreCode.t)
          * (module Runtime'.Spec.Message with type t = MsgStoreCodeResponse.t)

        module InstantiateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgInstantiateContract.t
               and type Response.t = MsgInstantiateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContract.t
               and type make_t = MsgInstantiateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContractResponse.t
               and type make_t = MsgInstantiateContractResponse.make_t
        end

        val instantiateContract :
          (module Runtime'.Spec.Message with type t = MsgInstantiateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgInstantiateContractResponse.t)

        module InstantiateContract2 : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgInstantiateContract2.t
               and type Response.t = MsgInstantiateContract2Response.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContract2.t
               and type make_t = MsgInstantiateContract2.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContract2Response.t
               and type make_t = MsgInstantiateContract2Response.make_t
        end

        val instantiateContract2 :
          (module Runtime'.Spec.Message with type t = MsgInstantiateContract2.t)
          * (module Runtime'.Spec.Message
               with type t = MsgInstantiateContract2Response.t)

        module ExecuteContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgExecuteContract.t
               and type Response.t = MsgExecuteContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgExecuteContract.t
               and type make_t = MsgExecuteContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgExecuteContractResponse.t
               and type make_t = MsgExecuteContractResponse.make_t
        end

        val executeContract :
          (module Runtime'.Spec.Message with type t = MsgExecuteContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgExecuteContractResponse.t)

        module MigrateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgMigrateContract.t
               and type Response.t = MsgMigrateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgMigrateContract.t
               and type make_t = MsgMigrateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgMigrateContractResponse.t
               and type make_t = MsgMigrateContractResponse.make_t
        end

        val migrateContract :
          (module Runtime'.Spec.Message with type t = MsgMigrateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgMigrateContractResponse.t)

        module UpdateAdmin : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateAdmin.t
               and type Response.t = MsgUpdateAdminResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateAdmin.t
               and type make_t = MsgUpdateAdmin.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateAdminResponse.t
               and type make_t = MsgUpdateAdminResponse.make_t
        end

        val updateAdmin :
          (module Runtime'.Spec.Message with type t = MsgUpdateAdmin.t)
          * (module Runtime'.Spec.Message with type t = MsgUpdateAdminResponse.t)

        module ClearAdmin : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgClearAdmin.t
               and type Response.t = MsgClearAdminResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgClearAdmin.t
               and type make_t = MsgClearAdmin.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgClearAdminResponse.t
               and type make_t = MsgClearAdminResponse.make_t
        end

        val clearAdmin :
          (module Runtime'.Spec.Message with type t = MsgClearAdmin.t)
          * (module Runtime'.Spec.Message with type t = MsgClearAdminResponse.t)

        module UpdateInstantiateConfig : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateInstantiateConfig.t
               and type Response.t = MsgUpdateInstantiateConfigResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateInstantiateConfig.t
               and type make_t = MsgUpdateInstantiateConfig.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateInstantiateConfigResponse.t
               and type make_t = MsgUpdateInstantiateConfigResponse.make_t
        end

        val updateInstantiateConfig :
          (module Runtime'.Spec.Message
             with type t = MsgUpdateInstantiateConfig.t)
          * (module Runtime'.Spec.Message
               with type t = MsgUpdateInstantiateConfigResponse.t)

        module UpdateParams : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateParams.t
               and type Response.t = MsgUpdateParamsResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateParams.t
               and type make_t = MsgUpdateParams.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateParamsResponse.t
               and type make_t = MsgUpdateParamsResponse.make_t
        end

        val updateParams :
          (module Runtime'.Spec.Message with type t = MsgUpdateParams.t)
          * (module Runtime'.Spec.Message
               with type t = MsgUpdateParamsResponse.t)

        module SudoContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgSudoContract.t
               and type Response.t = MsgSudoContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgSudoContract.t
               and type make_t = MsgSudoContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgSudoContractResponse.t
               and type make_t = MsgSudoContractResponse.make_t
        end

        val sudoContract :
          (module Runtime'.Spec.Message with type t = MsgSudoContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgSudoContractResponse.t)

        module PinCodes : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgPinCodes.t
               and type Response.t = MsgPinCodesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgPinCodes.t
               and type make_t = MsgPinCodes.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgPinCodesResponse.t
               and type make_t = MsgPinCodesResponse.make_t
        end

        val pinCodes :
          (module Runtime'.Spec.Message with type t = MsgPinCodes.t)
          * (module Runtime'.Spec.Message with type t = MsgPinCodesResponse.t)

        module UnpinCodes : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUnpinCodes.t
               and type Response.t = MsgUnpinCodesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUnpinCodes.t
               and type make_t = MsgUnpinCodes.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUnpinCodesResponse.t
               and type make_t = MsgUnpinCodesResponse.make_t
        end

        val unpinCodes :
          (module Runtime'.Spec.Message with type t = MsgUnpinCodes.t)
          * (module Runtime'.Spec.Message with type t = MsgUnpinCodesResponse.t)

        module StoreAndInstantiateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgStoreAndInstantiateContract.t
               and type Response.t = MsgStoreAndInstantiateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgStoreAndInstantiateContract.t
               and type make_t = MsgStoreAndInstantiateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgStoreAndInstantiateContractResponse.t
               and type make_t = MsgStoreAndInstantiateContractResponse.make_t
        end

        val storeAndInstantiateContract :
          (module Runtime'.Spec.Message
             with type t = MsgStoreAndInstantiateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgStoreAndInstantiateContractResponse.t)

        module RemoveCodeUploadParamsAddresses : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgRemoveCodeUploadParamsAddresses.t
               and type Response.t =
                MsgRemoveCodeUploadParamsAddressesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgRemoveCodeUploadParamsAddresses.t
               and type make_t = MsgRemoveCodeUploadParamsAddresses.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgRemoveCodeUploadParamsAddressesResponse.t
               and type make_t =
                MsgRemoveCodeUploadParamsAddressesResponse.make_t
        end

        val removeCodeUploadParamsAddresses :
          (module Runtime'.Spec.Message
             with type t = MsgRemoveCodeUploadParamsAddresses.t)
          * (module Runtime'.Spec.Message
               with type t = MsgRemoveCodeUploadParamsAddressesResponse.t)

        module AddCodeUploadParamsAddresses : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgAddCodeUploadParamsAddresses.t
               and type Response.t = MsgAddCodeUploadParamsAddressesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgAddCodeUploadParamsAddresses.t
               and type make_t = MsgAddCodeUploadParamsAddresses.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgAddCodeUploadParamsAddressesResponse.t
               and type make_t = MsgAddCodeUploadParamsAddressesResponse.make_t
        end

        val addCodeUploadParamsAddresses :
          (module Runtime'.Spec.Message
             with type t = MsgAddCodeUploadParamsAddresses.t)
          * (module Runtime'.Spec.Message
               with type t = MsgAddCodeUploadParamsAddressesResponse.t)

        module StoreAndMigrateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgStoreAndMigrateContract.t
               and type Response.t = MsgStoreAndMigrateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgStoreAndMigrateContract.t
               and type make_t = MsgStoreAndMigrateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgStoreAndMigrateContractResponse.t
               and type make_t = MsgStoreAndMigrateContractResponse.make_t
        end

        val storeAndMigrateContract :
          (module Runtime'.Spec.Message
             with type t = MsgStoreAndMigrateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgStoreAndMigrateContractResponse.t)

        module UpdateContractLabel : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateContractLabel.t
               and type Response.t = MsgUpdateContractLabelResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateContractLabel.t
               and type make_t = MsgUpdateContractLabel.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateContractLabelResponse.t
               and type make_t = MsgUpdateContractLabelResponse.make_t
        end

        val updateContractLabel :
          (module Runtime'.Spec.Message with type t = MsgUpdateContractLabel.t)
          * (module Runtime'.Spec.Message
               with type t = MsgUpdateContractLabelResponse.t)
      end
    end
  end = struct
    module rec V1 : sig
      (** {%html:
<p>MsgStoreCode submit Wasm code to the system</p>
%} *)
      module rec MsgStoreCode : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the actor that signed the messages</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission access control to apply on contract creation,
optional</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreCodeResponse returns store result data.</p>
%} *)
      and MsgStoreCodeResponse : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          checksum : bytes;
              (** {%html:
<p>Checksum is the sha256 hash of the stored code</p>
%}
              *)
        }

        val make : ?code_id:int64 -> ?checksum:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?code_id:int64 -> ?checksum:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContract create a new smart contract instance for the given
code id.</p>
%}
      *)
      and MsgInstantiateContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on instantiation</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContractResponse return instantiation result data</p>
%}
      *)
      and MsgInstantiateContractResponse : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContract2 create a new smart contract instance for the given
code id with a predictable address.</p>
%}
      *)
      and MsgInstantiateContract2 : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on instantiation</p>
%}
              *)
          salt : bytes;
              (** {%html:
<p>Salt is an arbitrary value provided by the sender. Size can be 1 to 64.</p>
%}
              *)
          fix_msg : bool;
              (** {%html:
<p>FixMsg include the msg value into the hash for the predictable address.
Default is false</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?salt:bytes ->
          ?fix_msg:bool ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?salt:bytes ->
          ?fix_msg:bool ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgInstantiateContract2Response return instantiation result data</p>
%}
      *)
      and MsgInstantiateContract2Response : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgExecuteContract submits the given message data to a smart contract</p>
%}
      *)
      and MsgExecuteContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on execution</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?contract:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?contract:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgExecuteContractResponse returns execution result data.</p>
%}
      *)
      and MsgExecuteContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgMigrateContract runs a code upgrade/ downgrade for a smart contract</p>
%}
      *)
      and MsgMigrateContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID references the new WASM code</p>
%} *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on migration</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?contract:string ->
          ?code_id:int64 ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?contract:string ->
          ?code_id:int64 ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgMigrateContractResponse returns contract migration result data.</p>
%}
      *)
      and MsgMigrateContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains same raw bytes returned as data from the wasm contract.
(May be empty)</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateAdmin sets a new admin for a smart contract</p>
%}
      *)
      and MsgUpdateAdmin : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          new_admin : string;
              (** {%html:
<p>NewAdmin address to be set</p>
%} *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make :
          ?sender:string -> ?new_admin:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string -> ?new_admin:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateAdminResponse returns empty data</p>
%} *)
      and MsgUpdateAdminResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgClearAdmin removes any admin stored for a smart contract</p>
%}
      *)
      and MsgClearAdmin : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make : ?sender:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?sender:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgClearAdminResponse returns empty data</p>
%} *)
      and MsgClearAdminResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateInstantiateConfig updates instantiate config for a smart contract</p>
%}
      *)
      and MsgUpdateInstantiateConfig : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID references the stored WASM code</p>
%} *)
          new_instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>NewInstantiatePermission is the new access control</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?code_id:int64 ->
          ?new_instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?code_id:int64 ->
          ?new_instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateInstantiateConfigResponse returns empty data</p>
%}
      *)
      and MsgUpdateInstantiateConfigResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateParams is the MsgUpdateParams request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUpdateParams : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          params :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t
            option;
              (** {%html:
<p>params defines the x/wasm parameters to update.</p>
<p>NOTE: All parameters must be supplied.</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?params:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?params:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateParamsResponse defines the response structure for executing a
MsgUpdateParams message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUpdateParamsResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgSudoContract is the MsgSudoContract request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgSudoContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract as sudo</p>
%}
              *)
        }

        val make :
          ?authority:string -> ?contract:string -> ?msg:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string -> ?contract:string -> ?msg:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgSudoContractResponse defines the response structure for executing a
MsgSudoContract message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgSudoContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgPinCodes is the MsgPinCodes request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgPinCodes : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          code_ids : int64 list;
              (** {%html:
<p>CodeIDs references the new WASM codes</p>
%} *)
        }

        val make : ?authority:string -> ?code_ids:int64 list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?code_ids:int64 list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgPinCodesResponse defines the response structure for executing a
MsgPinCodes message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgPinCodesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUnpinCodes is the MsgUnpinCodes request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUnpinCodes : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          code_ids : int64 list;
              (** {%html:
<p>CodeIDs references the WASM codes</p>
%} *)
        }

        val make : ?authority:string -> ?code_ids:int64 list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?code_ids:int64 list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUnpinCodesResponse defines the response structure for executing a
MsgUnpinCodes message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgUnpinCodesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndInstantiateContract is the MsgStoreAndInstantiateContract
request type.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgStoreAndInstantiateContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission to apply on contract creation, optional</p>
%}
              *)
          unpin_code : bool;
              (** {%html:
<p>UnpinCode code on upload, optional. As default the uploaded contract is
pinned to cache.</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred from the authority account to the contract
on instantiation</p>
%}
              *)
          source : string;
              (** {%html:
<p>Source is the URL where the code is hosted</p>
%}
              *)
          builder : string;
              (** {%html:
<p>Builder is the docker image used to build the code deterministically, used
for smart contract verification</p>
%}
              *)
          code_hash : bytes;
              (** {%html:
<p>CodeHash is the SHA256 sum of the code outputted by builder, used for smart
contract verification</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?unpin_code:bool ->
          ?admin:string ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?source:string ->
          ?builder:string ->
          ?code_hash:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?unpin_code:bool ->
          ?admin:string ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?source:string ->
          ?builder:string ->
          ?code_hash:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndInstantiateContractResponse defines the response structure
for executing a MsgStoreAndInstantiateContract message.</p>
<p>Since: 0.40</p>
%}
      *)
      and MsgStoreAndInstantiateContractResponse : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgAddCodeUploadParamsAddresses is the
MsgAddCodeUploadParamsAddresses request type.</p>
%}
      *)
      and MsgAddCodeUploadParamsAddresses : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          addresses : string list;
        }

        val make : ?authority:string -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgAddCodeUploadParamsAddressesResponse defines the response
structure for executing a MsgAddCodeUploadParamsAddresses message.</p>
%}
      *)
      and MsgAddCodeUploadParamsAddressesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgRemoveCodeUploadParamsAddresses is the
MsgRemoveCodeUploadParamsAddresses request type.</p>
%}
      *)
      and MsgRemoveCodeUploadParamsAddresses : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          addresses : string list;
        }

        val make : ?authority:string -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgRemoveCodeUploadParamsAddressesResponse defines the response
structure for executing a MsgRemoveCodeUploadParamsAddresses message.</p>
%}
      *)
      and MsgRemoveCodeUploadParamsAddressesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndMigrateContract is the MsgStoreAndMigrateContract
request type.</p>
<p>Since: 0.42</p>
%}
      *)
      and MsgStoreAndMigrateContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission to apply on contract creation, optional</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on migration</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?contract:string ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?contract:string ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgStoreAndMigrateContractResponse defines the response structure
for executing a MsgStoreAndMigrateContract message.</p>
<p>Since: 0.42</p>
%}
      *)
      and MsgStoreAndMigrateContractResponse : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          checksum : bytes;
              (** {%html:
<p>Checksum is the sha256 hash of the stored code</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?code_id:int64 -> ?checksum:bytes -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_id:int64 -> ?checksum:bytes -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateContractLabel sets a new label for a smart contract</p>
%}
      *)
      and MsgUpdateContractLabel : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          new_label : string;
              (** {%html:
<p>NewLabel string to be set</p>
%} *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make :
          ?sender:string -> ?new_label:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string -> ?new_label:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>MsgUpdateContractLabelResponse returns empty data</p>
%} *)
      and MsgUpdateContractLabelResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      module Msg : sig
        module StoreCode : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgStoreCode.t
               and type Response.t = MsgStoreCodeResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgStoreCode.t
               and type make_t = MsgStoreCode.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgStoreCodeResponse.t
               and type make_t = MsgStoreCodeResponse.make_t
        end

        val storeCode :
          (module Runtime'.Spec.Message with type t = MsgStoreCode.t)
          * (module Runtime'.Spec.Message with type t = MsgStoreCodeResponse.t)

        module InstantiateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgInstantiateContract.t
               and type Response.t = MsgInstantiateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContract.t
               and type make_t = MsgInstantiateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContractResponse.t
               and type make_t = MsgInstantiateContractResponse.make_t
        end

        val instantiateContract :
          (module Runtime'.Spec.Message with type t = MsgInstantiateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgInstantiateContractResponse.t)

        module InstantiateContract2 : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgInstantiateContract2.t
               and type Response.t = MsgInstantiateContract2Response.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContract2.t
               and type make_t = MsgInstantiateContract2.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgInstantiateContract2Response.t
               and type make_t = MsgInstantiateContract2Response.make_t
        end

        val instantiateContract2 :
          (module Runtime'.Spec.Message with type t = MsgInstantiateContract2.t)
          * (module Runtime'.Spec.Message
               with type t = MsgInstantiateContract2Response.t)

        module ExecuteContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgExecuteContract.t
               and type Response.t = MsgExecuteContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgExecuteContract.t
               and type make_t = MsgExecuteContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgExecuteContractResponse.t
               and type make_t = MsgExecuteContractResponse.make_t
        end

        val executeContract :
          (module Runtime'.Spec.Message with type t = MsgExecuteContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgExecuteContractResponse.t)

        module MigrateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgMigrateContract.t
               and type Response.t = MsgMigrateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgMigrateContract.t
               and type make_t = MsgMigrateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgMigrateContractResponse.t
               and type make_t = MsgMigrateContractResponse.make_t
        end

        val migrateContract :
          (module Runtime'.Spec.Message with type t = MsgMigrateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgMigrateContractResponse.t)

        module UpdateAdmin : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateAdmin.t
               and type Response.t = MsgUpdateAdminResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateAdmin.t
               and type make_t = MsgUpdateAdmin.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateAdminResponse.t
               and type make_t = MsgUpdateAdminResponse.make_t
        end

        val updateAdmin :
          (module Runtime'.Spec.Message with type t = MsgUpdateAdmin.t)
          * (module Runtime'.Spec.Message with type t = MsgUpdateAdminResponse.t)

        module ClearAdmin : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgClearAdmin.t
               and type Response.t = MsgClearAdminResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgClearAdmin.t
               and type make_t = MsgClearAdmin.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgClearAdminResponse.t
               and type make_t = MsgClearAdminResponse.make_t
        end

        val clearAdmin :
          (module Runtime'.Spec.Message with type t = MsgClearAdmin.t)
          * (module Runtime'.Spec.Message with type t = MsgClearAdminResponse.t)

        module UpdateInstantiateConfig : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateInstantiateConfig.t
               and type Response.t = MsgUpdateInstantiateConfigResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateInstantiateConfig.t
               and type make_t = MsgUpdateInstantiateConfig.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateInstantiateConfigResponse.t
               and type make_t = MsgUpdateInstantiateConfigResponse.make_t
        end

        val updateInstantiateConfig :
          (module Runtime'.Spec.Message
             with type t = MsgUpdateInstantiateConfig.t)
          * (module Runtime'.Spec.Message
               with type t = MsgUpdateInstantiateConfigResponse.t)

        module UpdateParams : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateParams.t
               and type Response.t = MsgUpdateParamsResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateParams.t
               and type make_t = MsgUpdateParams.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateParamsResponse.t
               and type make_t = MsgUpdateParamsResponse.make_t
        end

        val updateParams :
          (module Runtime'.Spec.Message with type t = MsgUpdateParams.t)
          * (module Runtime'.Spec.Message
               with type t = MsgUpdateParamsResponse.t)

        module SudoContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgSudoContract.t
               and type Response.t = MsgSudoContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgSudoContract.t
               and type make_t = MsgSudoContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgSudoContractResponse.t
               and type make_t = MsgSudoContractResponse.make_t
        end

        val sudoContract :
          (module Runtime'.Spec.Message with type t = MsgSudoContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgSudoContractResponse.t)

        module PinCodes : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgPinCodes.t
               and type Response.t = MsgPinCodesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgPinCodes.t
               and type make_t = MsgPinCodes.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgPinCodesResponse.t
               and type make_t = MsgPinCodesResponse.make_t
        end

        val pinCodes :
          (module Runtime'.Spec.Message with type t = MsgPinCodes.t)
          * (module Runtime'.Spec.Message with type t = MsgPinCodesResponse.t)

        module UnpinCodes : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUnpinCodes.t
               and type Response.t = MsgUnpinCodesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUnpinCodes.t
               and type make_t = MsgUnpinCodes.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUnpinCodesResponse.t
               and type make_t = MsgUnpinCodesResponse.make_t
        end

        val unpinCodes :
          (module Runtime'.Spec.Message with type t = MsgUnpinCodes.t)
          * (module Runtime'.Spec.Message with type t = MsgUnpinCodesResponse.t)

        module StoreAndInstantiateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgStoreAndInstantiateContract.t
               and type Response.t = MsgStoreAndInstantiateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgStoreAndInstantiateContract.t
               and type make_t = MsgStoreAndInstantiateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgStoreAndInstantiateContractResponse.t
               and type make_t = MsgStoreAndInstantiateContractResponse.make_t
        end

        val storeAndInstantiateContract :
          (module Runtime'.Spec.Message
             with type t = MsgStoreAndInstantiateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgStoreAndInstantiateContractResponse.t)

        module RemoveCodeUploadParamsAddresses : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgRemoveCodeUploadParamsAddresses.t
               and type Response.t =
                MsgRemoveCodeUploadParamsAddressesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgRemoveCodeUploadParamsAddresses.t
               and type make_t = MsgRemoveCodeUploadParamsAddresses.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgRemoveCodeUploadParamsAddressesResponse.t
               and type make_t =
                MsgRemoveCodeUploadParamsAddressesResponse.make_t
        end

        val removeCodeUploadParamsAddresses :
          (module Runtime'.Spec.Message
             with type t = MsgRemoveCodeUploadParamsAddresses.t)
          * (module Runtime'.Spec.Message
               with type t = MsgRemoveCodeUploadParamsAddressesResponse.t)

        module AddCodeUploadParamsAddresses : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgAddCodeUploadParamsAddresses.t
               and type Response.t = MsgAddCodeUploadParamsAddressesResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgAddCodeUploadParamsAddresses.t
               and type make_t = MsgAddCodeUploadParamsAddresses.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgAddCodeUploadParamsAddressesResponse.t
               and type make_t = MsgAddCodeUploadParamsAddressesResponse.make_t
        end

        val addCodeUploadParamsAddresses :
          (module Runtime'.Spec.Message
             with type t = MsgAddCodeUploadParamsAddresses.t)
          * (module Runtime'.Spec.Message
               with type t = MsgAddCodeUploadParamsAddressesResponse.t)

        module StoreAndMigrateContract : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgStoreAndMigrateContract.t
               and type Response.t = MsgStoreAndMigrateContractResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgStoreAndMigrateContract.t
               and type make_t = MsgStoreAndMigrateContract.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgStoreAndMigrateContractResponse.t
               and type make_t = MsgStoreAndMigrateContractResponse.make_t
        end

        val storeAndMigrateContract :
          (module Runtime'.Spec.Message
             with type t = MsgStoreAndMigrateContract.t)
          * (module Runtime'.Spec.Message
               with type t = MsgStoreAndMigrateContractResponse.t)

        module UpdateContractLabel : sig
          include
            Runtime'.Service.Rpc
              with type Request.t = MsgUpdateContractLabel.t
               and type Response.t = MsgUpdateContractLabelResponse.t

          (** Module alias for the request message for this method call *)
          module Request :
            Runtime'.Spec.Message
              with type t = MsgUpdateContractLabel.t
               and type make_t = MsgUpdateContractLabel.make_t

          (** Module alias for the response message for this method call *)
          module Response :
            Runtime'.Spec.Message
              with type t = MsgUpdateContractLabelResponse.t
               and type make_t = MsgUpdateContractLabelResponse.make_t
        end

        val updateContractLabel :
          (module Runtime'.Spec.Message with type t = MsgUpdateContractLabel.t)
          * (module Runtime'.Spec.Message
               with type t = MsgUpdateContractLabelResponse.t)
      end
    end = struct
      module rec MsgStoreCode : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the actor that signed the messages</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission access control to apply on contract creation,
optional</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgStoreCode

        let name () = ".cosmwasm.wasm.v1.MsgStoreCode"

        type t = {
          sender : string;
          wasm_byte_code : bytes;
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
        }

        type make_t =
          ?sender:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t

        let make ?(sender = {||}) ?(wasm_byte_code = Bytes.of_string {||})
            ?instantiate_permission () =
          { sender; wasm_byte_code; instantiate_permission }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_sender =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "sender", "sender"), string, {||}))
               in
               let merge_wasm_byte_code =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (2, "wasm_byte_code", "wasmByteCode"),
                         bytes,
                         Bytes.of_string {||} ))
               in
               let merge_instantiate_permission =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (5, "instantiate_permission", "instantiatePermission"),
                         message
                           (module Imported'modules.Cosmwasm_wasm_v1_types
                                   .Cosmwasm
                                   .Wasm
                                   .V1
                                   .AccessConfig) ))
               in
               fun t1 t2 ->
                 {
                   sender = merge_sender t1.sender t2.sender;
                   wasm_byte_code =
                     merge_wasm_byte_code t1.wasm_byte_code t2.wasm_byte_code;
                   instantiate_permission =
                     merge_instantiate_permission t1.instantiate_permission
                       t2.instantiate_permission;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "sender", "sender"), string, {||})
            ^:: basic
                  ( (2, "wasm_byte_code", "wasmByteCode"),
                    bytes,
                    Bytes.of_string {||} )
            ^:: basic_opt
                  ( (5, "instantiate_permission", "instantiatePermission"),
                    message
                      (module Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm
                              .Wasm
                              .V1
                              .AccessConfig) )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { sender; wasm_byte_code; instantiate_permission } ->
            serialize writer sender wasm_byte_code instantiate_permission

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor sender wasm_byte_code instantiate_permission =
            { sender; wasm_byte_code; instantiate_permission }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { sender; wasm_byte_code; instantiate_permission } ->
            serialize sender wasm_byte_code instantiate_permission

        let from_json_exn =
          let constructor sender wasm_byte_code instantiate_permission =
            { sender; wasm_byte_code; instantiate_permission }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgStoreCodeResponse : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          checksum : bytes;
              (** {%html:
<p>Checksum is the sha256 hash of the stored code</p>
%}
              *)
        }

        val make : ?code_id:int64 -> ?checksum:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?code_id:int64 -> ?checksum:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgStoreCodeResponse

        let name () = ".cosmwasm.wasm.v1.MsgStoreCodeResponse"

        type t = { code_id : int64; checksum : bytes }
        type make_t = ?code_id:int64 -> ?checksum:bytes -> unit -> t

        let make ?(code_id = 0L) ?(checksum = Bytes.of_string {||}) () =
          { code_id; checksum }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_code_id =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "code_id", "codeId"), uint64, 0L))
               in
               let merge_checksum =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ((2, "checksum", "checksum"), bytes, Bytes.of_string {||}))
               in
               fun t1 t2 ->
                 {
                   code_id = merge_code_id t1.code_id t2.code_id;
                   checksum = merge_checksum t1.checksum t2.checksum;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "code_id", "codeId"), uint64, 0L)
            ^:: basic ((2, "checksum", "checksum"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { code_id; checksum } -> serialize writer code_id checksum

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor code_id checksum = { code_id; checksum } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { code_id; checksum } -> serialize code_id checksum

        let from_json_exn =
          let constructor code_id checksum = { code_id; checksum } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgInstantiateContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on instantiation</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgInstantiateContract

        let name () = ".cosmwasm.wasm.v1.MsgInstantiateContract"

        type t = {
          sender : string;
          admin : string;
          code_id : int64;
          label : string;
          msg : bytes;
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        type make_t =
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        let make ?(sender = {||}) ?(admin = {||}) ?(code_id = 0L)
            ?(label = {||}) ?(msg = Bytes.of_string {||}) ?(funds = []) () =
          { sender; admin; code_id; label; msg; funds }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_sender =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "sender", "sender"), string, {||}))
               in
               let merge_admin =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((2, "admin", "admin"), string, {||}))
               in
               let merge_code_id =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((3, "code_id", "codeId"), uint64, 0L))
               in
               let merge_label =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((4, "label", "label"), string, {||}))
               in
               let merge_msg =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((5, "msg", "msg"), bytes, Bytes.of_string {||}))
               in
               let merge_funds =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (6, "funds", "funds"),
                         message
                           (module Imported'modules.Cosmos_base_v1beta1_coin
                                   .Cosmos
                                   .Base
                                   .V1beta1
                                   .Coin),
                         not_packed ))
               in
               fun t1 t2 ->
                 {
                   sender = merge_sender t1.sender t2.sender;
                   admin = merge_admin t1.admin t2.admin;
                   code_id = merge_code_id t1.code_id t2.code_id;
                   label = merge_label t1.label t2.label;
                   msg = merge_msg t1.msg t2.msg;
                   funds = merge_funds t1.funds t2.funds;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "sender", "sender"), string, {||})
            ^:: basic ((2, "admin", "admin"), string, {||})
            ^:: basic ((3, "code_id", "codeId"), uint64, 0L)
            ^:: basic ((4, "label", "label"), string, {||})
            ^:: basic ((5, "msg", "msg"), bytes, Bytes.of_string {||})
            ^:: repeated
                  ( (6, "funds", "funds"),
                    message
                      (module Imported'modules.Cosmos_base_v1beta1_coin.Cosmos
                              .Base
                              .V1beta1
                              .Coin),
                    not_packed )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { sender; admin; code_id; label; msg; funds } ->
            serialize writer sender admin code_id label msg funds

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor sender admin code_id label msg funds =
            { sender; admin; code_id; label; msg; funds }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { sender; admin; code_id; label; msg; funds } ->
            serialize sender admin code_id label msg funds

        let from_json_exn =
          let constructor sender admin code_id label msg funds =
            { sender; admin; code_id; label; msg; funds }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgInstantiateContractResponse : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgInstantiateContractResponse

        let name () = ".cosmwasm.wasm.v1.MsgInstantiateContractResponse"

        type t = { address : string; data : bytes }
        type make_t = ?address:string -> ?data:bytes -> unit -> t

        let make ?(address = {||}) ?(data = Bytes.of_string {||}) () =
          { address; data }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_address =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "address", "address"), string, {||}))
               in
               let merge_data =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "data", "data"), bytes, Bytes.of_string {||}))
               in
               fun t1 t2 ->
                 {
                   address = merge_address t1.address t2.address;
                   data = merge_data t1.data t2.data;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "address", "address"), string, {||})
            ^:: basic ((2, "data", "data"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { address; data } -> serialize writer address data

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor address data = { address; data } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { address; data } -> serialize address data

        let from_json_exn =
          let constructor address data = { address; data } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgInstantiateContract2 : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on instantiation</p>
%}
              *)
          salt : bytes;
              (** {%html:
<p>Salt is an arbitrary value provided by the sender. Size can be 1 to 64.</p>
%}
              *)
          fix_msg : bool;
              (** {%html:
<p>FixMsg include the msg value into the hash for the predictable address.
Default is false</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?salt:bytes ->
          ?fix_msg:bool ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?salt:bytes ->
          ?fix_msg:bool ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgInstantiateContract2

        let name () = ".cosmwasm.wasm.v1.MsgInstantiateContract2"

        type t = {
          sender : string;
          admin : string;
          code_id : int64;
          label : string;
          msg : bytes;
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
          salt : bytes;
          fix_msg : bool;
        }

        type make_t =
          ?sender:string ->
          ?admin:string ->
          ?code_id:int64 ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?salt:bytes ->
          ?fix_msg:bool ->
          unit ->
          t

        let make ?(sender = {||}) ?(admin = {||}) ?(code_id = 0L)
            ?(label = {||}) ?(msg = Bytes.of_string {||}) ?(funds = [])
            ?(salt = Bytes.of_string {||}) ?(fix_msg = false) () =
          { sender; admin; code_id; label; msg; funds; salt; fix_msg }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_sender =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "sender", "sender"), string, {||}))
               in
               let merge_admin =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((2, "admin", "admin"), string, {||}))
               in
               let merge_code_id =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((3, "code_id", "codeId"), uint64, 0L))
               in
               let merge_label =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((4, "label", "label"), string, {||}))
               in
               let merge_msg =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((5, "msg", "msg"), bytes, Bytes.of_string {||}))
               in
               let merge_funds =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (6, "funds", "funds"),
                         message
                           (module Imported'modules.Cosmos_base_v1beta1_coin
                                   .Cosmos
                                   .Base
                                   .V1beta1
                                   .Coin),
                         not_packed ))
               in
               let merge_salt =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((7, "salt", "salt"), bytes, Bytes.of_string {||}))
               in
               let merge_fix_msg =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((8, "fix_msg", "fixMsg"), bool, false))
               in
               fun t1 t2 ->
                 {
                   sender = merge_sender t1.sender t2.sender;
                   admin = merge_admin t1.admin t2.admin;
                   code_id = merge_code_id t1.code_id t2.code_id;
                   label = merge_label t1.label t2.label;
                   msg = merge_msg t1.msg t2.msg;
                   funds = merge_funds t1.funds t2.funds;
                   salt = merge_salt t1.salt t2.salt;
                   fix_msg = merge_fix_msg t1.fix_msg t2.fix_msg;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "sender", "sender"), string, {||})
            ^:: basic ((2, "admin", "admin"), string, {||})
            ^:: basic ((3, "code_id", "codeId"), uint64, 0L)
            ^:: basic ((4, "label", "label"), string, {||})
            ^:: basic ((5, "msg", "msg"), bytes, Bytes.of_string {||})
            ^:: repeated
                  ( (6, "funds", "funds"),
                    message
                      (module Imported'modules.Cosmos_base_v1beta1_coin.Cosmos
                              .Base
                              .V1beta1
                              .Coin),
                    not_packed )
            ^:: basic ((7, "salt", "salt"), bytes, Bytes.of_string {||})
            ^:: basic ((8, "fix_msg", "fixMsg"), bool, false)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer
            { sender; admin; code_id; label; msg; funds; salt; fix_msg }
          -> serialize writer sender admin code_id label msg funds salt fix_msg

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor sender admin code_id label msg funds salt fix_msg =
            { sender; admin; code_id; label; msg; funds; salt; fix_msg }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { sender; admin; code_id; label; msg; funds; salt; fix_msg } ->
            serialize sender admin code_id label msg funds salt fix_msg

        let from_json_exn =
          let constructor sender admin code_id label msg funds salt fix_msg =
            { sender; admin; code_id; label; msg; funds; salt; fix_msg }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgInstantiateContract2Response : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgInstantiateContract2Response

        let name () = ".cosmwasm.wasm.v1.MsgInstantiateContract2Response"

        type t = { address : string; data : bytes }
        type make_t = ?address:string -> ?data:bytes -> unit -> t

        let make ?(address = {||}) ?(data = Bytes.of_string {||}) () =
          { address; data }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_address =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "address", "address"), string, {||}))
               in
               let merge_data =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "data", "data"), bytes, Bytes.of_string {||}))
               in
               fun t1 t2 ->
                 {
                   address = merge_address t1.address t2.address;
                   data = merge_data t1.data t2.data;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "address", "address"), string, {||})
            ^:: basic ((2, "data", "data"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { address; data } -> serialize writer address data

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor address data = { address; data } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { address; data } -> serialize address data

        let from_json_exn =
          let constructor address data = { address; data } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgExecuteContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred to the contract on execution</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?contract:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?contract:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgExecuteContract

        let name () = ".cosmwasm.wasm.v1.MsgExecuteContract"

        type t = {
          sender : string;
          contract : string;
          msg : bytes;
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        type make_t =
          ?sender:string ->
          ?contract:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        let make ?(sender = {||}) ?(contract = {||})
            ?(msg = Bytes.of_string {||}) ?(funds = []) () =
          { sender; contract; msg; funds }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_sender =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "sender", "sender"), string, {||}))
               in
               let merge_contract =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "contract", "contract"), string, {||}))
               in
               let merge_msg =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((3, "msg", "msg"), bytes, Bytes.of_string {||}))
               in
               let merge_funds =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (5, "funds", "funds"),
                         message
                           (module Imported'modules.Cosmos_base_v1beta1_coin
                                   .Cosmos
                                   .Base
                                   .V1beta1
                                   .Coin),
                         not_packed ))
               in
               fun t1 t2 ->
                 {
                   sender = merge_sender t1.sender t2.sender;
                   contract = merge_contract t1.contract t2.contract;
                   msg = merge_msg t1.msg t2.msg;
                   funds = merge_funds t1.funds t2.funds;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "sender", "sender"), string, {||})
            ^:: basic ((2, "contract", "contract"), string, {||})
            ^:: basic ((3, "msg", "msg"), bytes, Bytes.of_string {||})
            ^:: repeated
                  ( (5, "funds", "funds"),
                    message
                      (module Imported'modules.Cosmos_base_v1beta1_coin.Cosmos
                              .Base
                              .V1beta1
                              .Coin),
                    not_packed )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { sender; contract; msg; funds } ->
            serialize writer sender contract msg funds

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor sender contract msg funds =
            { sender; contract; msg; funds }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { sender; contract; msg; funds } ->
            serialize sender contract msg funds

        let from_json_exn =
          let constructor sender contract msg funds =
            { sender; contract; msg; funds }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgExecuteContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgExecuteContractResponse

        let name () = ".cosmwasm.wasm.v1.MsgExecuteContractResponse"

        type t = bytes
        type make_t = ?data:bytes -> unit -> t

        let make ?(data = Bytes.of_string {||}) () = data

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_data =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "data", "data"), bytes, Bytes.of_string {||}))
               in
               fun t1_data t2_data -> merge_data t1_data t2_data)
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "data", "data"), bytes, Bytes.of_string {||}) ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer data -> serialize writer data

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor data = data in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun data -> serialize data

        let from_json_exn =
          let constructor data = data in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgMigrateContract : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID references the new WASM code</p>
%} *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on migration</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?contract:string ->
          ?code_id:int64 ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?contract:string ->
          ?code_id:int64 ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgMigrateContract

        let name () = ".cosmwasm.wasm.v1.MsgMigrateContract"

        type t = {
          sender : string;
          contract : string;
          code_id : int64;
          msg : bytes;
        }

        type make_t =
          ?sender:string ->
          ?contract:string ->
          ?code_id:int64 ->
          ?msg:bytes ->
          unit ->
          t

        let make ?(sender = {||}) ?(contract = {||}) ?(code_id = 0L)
            ?(msg = Bytes.of_string {||}) () =
          { sender; contract; code_id; msg }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_sender =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "sender", "sender"), string, {||}))
               in
               let merge_contract =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "contract", "contract"), string, {||}))
               in
               let merge_code_id =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((3, "code_id", "codeId"), uint64, 0L))
               in
               let merge_msg =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((4, "msg", "msg"), bytes, Bytes.of_string {||}))
               in
               fun t1 t2 ->
                 {
                   sender = merge_sender t1.sender t2.sender;
                   contract = merge_contract t1.contract t2.contract;
                   code_id = merge_code_id t1.code_id t2.code_id;
                   msg = merge_msg t1.msg t2.msg;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "sender", "sender"), string, {||})
            ^:: basic ((2, "contract", "contract"), string, {||})
            ^:: basic ((3, "code_id", "codeId"), uint64, 0L)
            ^:: basic ((4, "msg", "msg"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { sender; contract; code_id; msg } ->
            serialize writer sender contract code_id msg

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor sender contract code_id msg =
            { sender; contract; code_id; msg }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { sender; contract; code_id; msg } ->
            serialize sender contract code_id msg

        let from_json_exn =
          let constructor sender contract code_id msg =
            { sender; contract; code_id; msg }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgMigrateContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains same raw bytes returned as data from the wasm contract.
(May be empty)</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgMigrateContractResponse

        let name () = ".cosmwasm.wasm.v1.MsgMigrateContractResponse"

        type t = bytes
        type make_t = ?data:bytes -> unit -> t

        let make ?(data = Bytes.of_string {||}) () = data

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_data =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "data", "data"), bytes, Bytes.of_string {||}))
               in
               fun t1_data t2_data -> merge_data t1_data t2_data)
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "data", "data"), bytes, Bytes.of_string {||}) ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer data -> serialize writer data

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor data = data in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun data -> serialize data

        let from_json_exn =
          let constructor data = data in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgUpdateAdmin : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          new_admin : string;
              (** {%html:
<p>NewAdmin address to be set</p>
%} *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make :
          ?sender:string -> ?new_admin:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string -> ?new_admin:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgUpdateAdmin

        let name () = ".cosmwasm.wasm.v1.MsgUpdateAdmin"

        type t = { sender : string; new_admin : string; contract : string }

        type make_t =
          ?sender:string -> ?new_admin:string -> ?contract:string -> unit -> t

        let make ?(sender = {||}) ?(new_admin = {||}) ?(contract = {||}) () =
          { sender; new_admin; contract }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_sender =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "sender", "sender"), string, {||}))
               in
               let merge_new_admin =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "new_admin", "newAdmin"), string, {||}))
               in
               let merge_contract =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((3, "contract", "contract"), string, {||}))
               in
               fun t1 t2 ->
                 {
                   sender = merge_sender t1.sender t2.sender;
                   new_admin = merge_new_admin t1.new_admin t2.new_admin;
                   contract = merge_contract t1.contract t2.contract;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "sender", "sender"), string, {||})
            ^:: basic ((2, "new_admin", "newAdmin"), string, {||})
            ^:: basic ((3, "contract", "contract"), string, {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { sender; new_admin; contract } ->
            serialize writer sender new_admin contract

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor sender new_admin contract =
            { sender; new_admin; contract }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { sender; new_admin; contract } ->
            serialize sender new_admin contract

        let from_json_exn =
          let constructor sender new_admin contract =
            { sender; new_admin; contract }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgUpdateAdminResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgUpdateAdminResponse

        let name () = ".cosmwasm.wasm.v1.MsgUpdateAdminResponse"

        type t = unit
        type make_t = unit -> t

        let make () = ()

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' = lazy (fun () () -> ()) in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () = Runtime'.Spec.(nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer () -> serialize writer

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun () -> serialize

        let from_json_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgClearAdmin : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the actor that signed the messages</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make : ?sender:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?sender:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgClearAdmin

        let name () = ".cosmwasm.wasm.v1.MsgClearAdmin"

        type t = { sender : string; contract : string }
        type make_t = ?sender:string -> ?contract:string -> unit -> t

        let make ?(sender = {||}) ?(contract = {||}) () = { sender; contract }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_sender =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "sender", "sender"), string, {||}))
               in
               let merge_contract =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((3, "contract", "contract"), string, {||}))
               in
               fun t1 t2 ->
                 {
                   sender = merge_sender t1.sender t2.sender;
                   contract = merge_contract t1.contract t2.contract;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "sender", "sender"), string, {||})
            ^:: basic ((3, "contract", "contract"), string, {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { sender; contract } -> serialize writer sender contract

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor sender contract = { sender; contract } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { sender; contract } -> serialize sender contract

        let from_json_exn =
          let constructor sender contract = { sender; contract } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgClearAdminResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgClearAdminResponse

        let name () = ".cosmwasm.wasm.v1.MsgClearAdminResponse"

        type t = unit
        type make_t = unit -> t

        let make () = ()

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' = lazy (fun () () -> ()) in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () = Runtime'.Spec.(nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer () -> serialize writer

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun () -> serialize

        let from_json_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgUpdateInstantiateConfig : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          code_id : int64;
              (** {%html:
<p>CodeID references the stored WASM code</p>
%} *)
          new_instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>NewInstantiatePermission is the new access control</p>
%}
              *)
        }

        val make :
          ?sender:string ->
          ?code_id:int64 ->
          ?new_instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string ->
          ?code_id:int64 ->
          ?new_instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgUpdateInstantiateConfig

        let name () = ".cosmwasm.wasm.v1.MsgUpdateInstantiateConfig"

        type t = {
          sender : string;
          code_id : int64;
          new_instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
        }

        type make_t =
          ?sender:string ->
          ?code_id:int64 ->
          ?new_instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          unit ->
          t

        let make ?(sender = {||}) ?(code_id = 0L) ?new_instantiate_permission ()
            =
          { sender; code_id; new_instantiate_permission }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_sender =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "sender", "sender"), string, {||}))
               in
               let merge_code_id =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((2, "code_id", "codeId"), uint64, 0L))
               in
               let merge_new_instantiate_permission =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( ( 3,
                           "new_instantiate_permission",
                           "newInstantiatePermission" ),
                         message
                           (module Imported'modules.Cosmwasm_wasm_v1_types
                                   .Cosmwasm
                                   .Wasm
                                   .V1
                                   .AccessConfig) ))
               in
               fun t1 t2 ->
                 {
                   sender = merge_sender t1.sender t2.sender;
                   code_id = merge_code_id t1.code_id t2.code_id;
                   new_instantiate_permission =
                     merge_new_instantiate_permission
                       t1.new_instantiate_permission
                       t2.new_instantiate_permission;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "sender", "sender"), string, {||})
            ^:: basic ((2, "code_id", "codeId"), uint64, 0L)
            ^:: basic_opt
                  ( (3, "new_instantiate_permission", "newInstantiatePermission"),
                    message
                      (module Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm
                              .Wasm
                              .V1
                              .AccessConfig) )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { sender; code_id; new_instantiate_permission } ->
            serialize writer sender code_id new_instantiate_permission

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor sender code_id new_instantiate_permission =
            { sender; code_id; new_instantiate_permission }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { sender; code_id; new_instantiate_permission } ->
            serialize sender code_id new_instantiate_permission

        let from_json_exn =
          let constructor sender code_id new_instantiate_permission =
            { sender; code_id; new_instantiate_permission }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgUpdateInstantiateConfigResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgUpdateInstantiateConfigResponse

        let name () = ".cosmwasm.wasm.v1.MsgUpdateInstantiateConfigResponse"

        type t = unit
        type make_t = unit -> t

        let make () = ()

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' = lazy (fun () () -> ()) in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () = Runtime'.Spec.(nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer () -> serialize writer

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun () -> serialize

        let from_json_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgUpdateParams : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          params :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t
            option;
              (** {%html:
<p>params defines the x/wasm parameters to update.</p>
<p>NOTE: All parameters must be supplied.</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?params:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?params:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgUpdateParams

        let name () = ".cosmwasm.wasm.v1.MsgUpdateParams"

        type t = {
          authority : string;
          params :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t
            option;
        }

        type make_t =
          ?authority:string ->
          ?params:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1.Params.t ->
          unit ->
          t

        let make ?(authority = {||}) ?params () = { authority; params }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_authority =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "authority", "authority"), string, {||}))
               in
               let merge_params =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (2, "params", "params"),
                         message
                           (module Imported'modules.Cosmwasm_wasm_v1_types
                                   .Cosmwasm
                                   .Wasm
                                   .V1
                                   .Params) ))
               in
               fun t1 t2 ->
                 {
                   authority = merge_authority t1.authority t2.authority;
                   params = merge_params t1.params t2.params;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "authority", "authority"), string, {||})
            ^:: basic_opt
                  ( (2, "params", "params"),
                    message
                      (module Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm
                              .Wasm
                              .V1
                              .Params) )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { authority; params } -> serialize writer authority params

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor authority params = { authority; params } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { authority; params } -> serialize authority params

        let from_json_exn =
          let constructor authority params = { authority; params } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgUpdateParamsResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgUpdateParamsResponse

        let name () = ".cosmwasm.wasm.v1.MsgUpdateParamsResponse"

        type t = unit
        type make_t = unit -> t

        let make () = ()

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' = lazy (fun () () -> ()) in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () = Runtime'.Spec.(nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer () -> serialize writer

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun () -> serialize

        let from_json_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgSudoContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract as sudo</p>
%}
              *)
        }

        val make :
          ?authority:string -> ?contract:string -> ?msg:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string -> ?contract:string -> ?msg:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgSudoContract

        let name () = ".cosmwasm.wasm.v1.MsgSudoContract"

        type t = { authority : string; contract : string; msg : bytes }

        type make_t =
          ?authority:string -> ?contract:string -> ?msg:bytes -> unit -> t

        let make ?(authority = {||}) ?(contract = {||})
            ?(msg = Bytes.of_string {||}) () =
          { authority; contract; msg }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_authority =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "authority", "authority"), string, {||}))
               in
               let merge_contract =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "contract", "contract"), string, {||}))
               in
               let merge_msg =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((3, "msg", "msg"), bytes, Bytes.of_string {||}))
               in
               fun t1 t2 ->
                 {
                   authority = merge_authority t1.authority t2.authority;
                   contract = merge_contract t1.contract t2.contract;
                   msg = merge_msg t1.msg t2.msg;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "authority", "authority"), string, {||})
            ^:: basic ((2, "contract", "contract"), string, {||})
            ^:: basic ((3, "msg", "msg"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { authority; contract; msg } ->
            serialize writer authority contract msg

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor authority contract msg =
            { authority; contract; msg }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { authority; contract; msg } -> serialize authority contract msg

        let from_json_exn =
          let constructor authority contract msg =
            { authority; contract; msg }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgSudoContractResponse : sig
        type t = bytes
        (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
        *)

        val make : ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgSudoContractResponse

        let name () = ".cosmwasm.wasm.v1.MsgSudoContractResponse"

        type t = bytes
        type make_t = ?data:bytes -> unit -> t

        let make ?(data = Bytes.of_string {||}) () = data

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_data =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "data", "data"), bytes, Bytes.of_string {||}))
               in
               fun t1_data t2_data -> merge_data t1_data t2_data)
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "data", "data"), bytes, Bytes.of_string {||}) ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer data -> serialize writer data

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor data = data in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun data -> serialize data

        let from_json_exn =
          let constructor data = data in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgPinCodes : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          code_ids : int64 list;
              (** {%html:
<p>CodeIDs references the new WASM codes</p>
%} *)
        }

        val make : ?authority:string -> ?code_ids:int64 list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?code_ids:int64 list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgPinCodes

        let name () = ".cosmwasm.wasm.v1.MsgPinCodes"

        type t = { authority : string; code_ids : int64 list }
        type make_t = ?authority:string -> ?code_ids:int64 list -> unit -> t

        let make ?(authority = {||}) ?(code_ids = []) () =
          { authority; code_ids }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_authority =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "authority", "authority"), string, {||}))
               in
               let merge_code_ids =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated ((2, "code_ids", "codeIds"), uint64, packed))
               in
               fun t1 t2 ->
                 {
                   authority = merge_authority t1.authority t2.authority;
                   code_ids = merge_code_ids t1.code_ids t2.code_ids;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "authority", "authority"), string, {||})
            ^:: repeated ((2, "code_ids", "codeIds"), uint64, packed)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { authority; code_ids } ->
            serialize writer authority code_ids

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor authority code_ids = { authority; code_ids } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { authority; code_ids } -> serialize authority code_ids

        let from_json_exn =
          let constructor authority code_ids = { authority; code_ids } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgPinCodesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgPinCodesResponse

        let name () = ".cosmwasm.wasm.v1.MsgPinCodesResponse"

        type t = unit
        type make_t = unit -> t

        let make () = ()

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' = lazy (fun () () -> ()) in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () = Runtime'.Spec.(nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer () -> serialize writer

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun () -> serialize

        let from_json_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgUnpinCodes : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          code_ids : int64 list;
              (** {%html:
<p>CodeIDs references the WASM codes</p>
%} *)
        }

        val make : ?authority:string -> ?code_ids:int64 list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?code_ids:int64 list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgUnpinCodes

        let name () = ".cosmwasm.wasm.v1.MsgUnpinCodes"

        type t = { authority : string; code_ids : int64 list }
        type make_t = ?authority:string -> ?code_ids:int64 list -> unit -> t

        let make ?(authority = {||}) ?(code_ids = []) () =
          { authority; code_ids }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_authority =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "authority", "authority"), string, {||}))
               in
               let merge_code_ids =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated ((2, "code_ids", "codeIds"), uint64, packed))
               in
               fun t1 t2 ->
                 {
                   authority = merge_authority t1.authority t2.authority;
                   code_ids = merge_code_ids t1.code_ids t2.code_ids;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "authority", "authority"), string, {||})
            ^:: repeated ((2, "code_ids", "codeIds"), uint64, packed)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { authority; code_ids } ->
            serialize writer authority code_ids

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor authority code_ids = { authority; code_ids } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { authority; code_ids } -> serialize authority code_ids

        let from_json_exn =
          let constructor authority code_ids = { authority; code_ids } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgUnpinCodesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgUnpinCodesResponse

        let name () = ".cosmwasm.wasm.v1.MsgUnpinCodesResponse"

        type t = unit
        type make_t = unit -> t

        let make () = ()

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' = lazy (fun () () -> ()) in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () = Runtime'.Spec.(nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer () -> serialize writer

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun () -> serialize

        let from_json_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgStoreAndInstantiateContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission to apply on contract creation, optional</p>
%}
              *)
          unpin_code : bool;
              (** {%html:
<p>UnpinCode code on upload, optional. As default the uploaded contract is
pinned to cache.</p>
%}
              *)
          admin : string;
              (** {%html:
<p>Admin is an optional address that can execute migrations</p>
%}
              *)
          label : string;
              (** {%html:
<p>Label is optional metadata to be stored with a contract instance.</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on instantiation</p>
%}
              *)
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
              (** {%html:
<p>Funds coins that are transferred from the authority account to the contract
on instantiation</p>
%}
              *)
          source : string;
              (** {%html:
<p>Source is the URL where the code is hosted</p>
%}
              *)
          builder : string;
              (** {%html:
<p>Builder is the docker image used to build the code deterministically, used
for smart contract verification</p>
%}
              *)
          code_hash : bytes;
              (** {%html:
<p>CodeHash is the SHA256 sum of the code outputted by builder, used for smart
contract verification</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?unpin_code:bool ->
          ?admin:string ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?source:string ->
          ?builder:string ->
          ?code_hash:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?unpin_code:bool ->
          ?admin:string ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?source:string ->
          ?builder:string ->
          ?code_hash:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgStoreAndInstantiateContract

        let name () = ".cosmwasm.wasm.v1.MsgStoreAndInstantiateContract"

        type t = {
          authority : string;
          wasm_byte_code : bytes;
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
          unpin_code : bool;
          admin : string;
          label : string;
          msg : bytes;
          funds :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
          source : string;
          builder : string;
          code_hash : bytes;
        }

        type make_t =
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?unpin_code:bool ->
          ?admin:string ->
          ?label:string ->
          ?msg:bytes ->
          ?funds:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          ?source:string ->
          ?builder:string ->
          ?code_hash:bytes ->
          unit ->
          t

        let make ?(authority = {||}) ?(wasm_byte_code = Bytes.of_string {||})
            ?instantiate_permission ?(unpin_code = false) ?(admin = {||})
            ?(label = {||}) ?(msg = Bytes.of_string {||}) ?(funds = [])
            ?(source = {||}) ?(builder = {||})
            ?(code_hash = Bytes.of_string {||}) () =
          {
            authority;
            wasm_byte_code;
            instantiate_permission;
            unpin_code;
            admin;
            label;
            msg;
            funds;
            source;
            builder;
            code_hash;
          }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_authority =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "authority", "authority"), string, {||}))
               in
               let merge_wasm_byte_code =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (3, "wasm_byte_code", "wasmByteCode"),
                         bytes,
                         Bytes.of_string {||} ))
               in
               let merge_instantiate_permission =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (4, "instantiate_permission", "instantiatePermission"),
                         message
                           (module Imported'modules.Cosmwasm_wasm_v1_types
                                   .Cosmwasm
                                   .Wasm
                                   .V1
                                   .AccessConfig) ))
               in
               let merge_unpin_code =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((5, "unpin_code", "unpinCode"), bool, false))
               in
               let merge_admin =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((6, "admin", "admin"), string, {||}))
               in
               let merge_label =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((7, "label", "label"), string, {||}))
               in
               let merge_msg =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((8, "msg", "msg"), bytes, Bytes.of_string {||}))
               in
               let merge_funds =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (9, "funds", "funds"),
                         message
                           (module Imported'modules.Cosmos_base_v1beta1_coin
                                   .Cosmos
                                   .Base
                                   .V1beta1
                                   .Coin),
                         not_packed ))
               in
               let merge_source =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((10, "source", "source"), string, {||}))
               in
               let merge_builder =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((11, "builder", "builder"), string, {||}))
               in
               let merge_code_hash =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (12, "code_hash", "codeHash"),
                         bytes,
                         Bytes.of_string {||} ))
               in
               fun t1 t2 ->
                 {
                   authority = merge_authority t1.authority t2.authority;
                   wasm_byte_code =
                     merge_wasm_byte_code t1.wasm_byte_code t2.wasm_byte_code;
                   instantiate_permission =
                     merge_instantiate_permission t1.instantiate_permission
                       t2.instantiate_permission;
                   unpin_code = merge_unpin_code t1.unpin_code t2.unpin_code;
                   admin = merge_admin t1.admin t2.admin;
                   label = merge_label t1.label t2.label;
                   msg = merge_msg t1.msg t2.msg;
                   funds = merge_funds t1.funds t2.funds;
                   source = merge_source t1.source t2.source;
                   builder = merge_builder t1.builder t2.builder;
                   code_hash = merge_code_hash t1.code_hash t2.code_hash;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "authority", "authority"), string, {||})
            ^:: basic
                  ( (3, "wasm_byte_code", "wasmByteCode"),
                    bytes,
                    Bytes.of_string {||} )
            ^:: basic_opt
                  ( (4, "instantiate_permission", "instantiatePermission"),
                    message
                      (module Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm
                              .Wasm
                              .V1
                              .AccessConfig) )
            ^:: basic ((5, "unpin_code", "unpinCode"), bool, false)
            ^:: basic ((6, "admin", "admin"), string, {||})
            ^:: basic ((7, "label", "label"), string, {||})
            ^:: basic ((8, "msg", "msg"), bytes, Bytes.of_string {||})
            ^:: repeated
                  ( (9, "funds", "funds"),
                    message
                      (module Imported'modules.Cosmos_base_v1beta1_coin.Cosmos
                              .Base
                              .V1beta1
                              .Coin),
                    not_packed )
            ^:: basic ((10, "source", "source"), string, {||})
            ^:: basic ((11, "builder", "builder"), string, {||})
            ^:: basic
                  ((12, "code_hash", "codeHash"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer
            {
              authority;
              wasm_byte_code;
              instantiate_permission;
              unpin_code;
              admin;
              label;
              msg;
              funds;
              source;
              builder;
              code_hash;
            }
          ->
            serialize writer authority wasm_byte_code instantiate_permission
              unpin_code admin label msg funds source builder code_hash

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor authority wasm_byte_code instantiate_permission
              unpin_code admin label msg funds source builder code_hash =
            {
              authority;
              wasm_byte_code;
              instantiate_permission;
              unpin_code;
              admin;
              label;
              msg;
              funds;
              source;
              builder;
              code_hash;
            }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun {
                authority;
                wasm_byte_code;
                instantiate_permission;
                unpin_code;
                admin;
                label;
                msg;
                funds;
                source;
                builder;
                code_hash;
              }
          ->
            serialize authority wasm_byte_code instantiate_permission unpin_code
              admin label msg funds source builder code_hash

        let from_json_exn =
          let constructor authority wasm_byte_code instantiate_permission
              unpin_code admin label msg funds source builder code_hash =
            {
              authority;
              wasm_byte_code;
              instantiate_permission;
              unpin_code;
              admin;
              label;
              msg;
              funds;
              source;
              builder;
              code_hash;
            }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgStoreAndInstantiateContractResponse : sig
        type t = {
          address : string;
              (** {%html:
<p>Address is the bech32 address of the new contract instance.</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?address:string -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?address:string -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgStoreAndInstantiateContractResponse

        let name () = ".cosmwasm.wasm.v1.MsgStoreAndInstantiateContractResponse"

        type t = { address : string; data : bytes }
        type make_t = ?address:string -> ?data:bytes -> unit -> t

        let make ?(address = {||}) ?(data = Bytes.of_string {||}) () =
          { address; data }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_address =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "address", "address"), string, {||}))
               in
               let merge_data =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "data", "data"), bytes, Bytes.of_string {||}))
               in
               fun t1 t2 ->
                 {
                   address = merge_address t1.address t2.address;
                   data = merge_data t1.data t2.data;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "address", "address"), string, {||})
            ^:: basic ((2, "data", "data"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { address; data } -> serialize writer address data

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor address data = { address; data } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { address; data } -> serialize address data

        let from_json_exn =
          let constructor address data = { address; data } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgAddCodeUploadParamsAddresses : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          addresses : string list;
        }

        val make : ?authority:string -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgAddCodeUploadParamsAddresses

        let name () = ".cosmwasm.wasm.v1.MsgAddCodeUploadParamsAddresses"

        type t = { authority : string; addresses : string list }
        type make_t = ?authority:string -> ?addresses:string list -> unit -> t

        let make ?(authority = {||}) ?(addresses = []) () =
          { authority; addresses }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_authority =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "authority", "authority"), string, {||}))
               in
               let merge_addresses =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated ((2, "addresses", "addresses"), string, not_packed))
               in
               fun t1 t2 ->
                 {
                   authority = merge_authority t1.authority t2.authority;
                   addresses = merge_addresses t1.addresses t2.addresses;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "authority", "authority"), string, {||})
            ^:: repeated ((2, "addresses", "addresses"), string, not_packed)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { authority; addresses } ->
            serialize writer authority addresses

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor authority addresses = { authority; addresses } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { authority; addresses } -> serialize authority addresses

        let from_json_exn =
          let constructor authority addresses = { authority; addresses } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgAddCodeUploadParamsAddressesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgAddCodeUploadParamsAddressesResponse

        let name () =
          ".cosmwasm.wasm.v1.MsgAddCodeUploadParamsAddressesResponse"

        type t = unit
        type make_t = unit -> t

        let make () = ()

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' = lazy (fun () () -> ()) in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () = Runtime'.Spec.(nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer () -> serialize writer

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun () -> serialize

        let from_json_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgRemoveCodeUploadParamsAddresses : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          addresses : string list;
        }

        val make : ?authority:string -> ?addresses:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?authority:string -> ?addresses:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgRemoveCodeUploadParamsAddresses

        let name () = ".cosmwasm.wasm.v1.MsgRemoveCodeUploadParamsAddresses"

        type t = { authority : string; addresses : string list }
        type make_t = ?authority:string -> ?addresses:string list -> unit -> t

        let make ?(authority = {||}) ?(addresses = []) () =
          { authority; addresses }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_authority =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "authority", "authority"), string, {||}))
               in
               let merge_addresses =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated ((2, "addresses", "addresses"), string, not_packed))
               in
               fun t1 t2 ->
                 {
                   authority = merge_authority t1.authority t2.authority;
                   addresses = merge_addresses t1.addresses t2.addresses;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "authority", "authority"), string, {||})
            ^:: repeated ((2, "addresses", "addresses"), string, not_packed)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { authority; addresses } ->
            serialize writer authority addresses

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor authority addresses = { authority; addresses } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { authority; addresses } -> serialize authority addresses

        let from_json_exn =
          let constructor authority addresses = { authority; addresses } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgRemoveCodeUploadParamsAddressesResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgRemoveCodeUploadParamsAddressesResponse

        let name () =
          ".cosmwasm.wasm.v1.MsgRemoveCodeUploadParamsAddressesResponse"

        type t = unit
        type make_t = unit -> t

        let make () = ()

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' = lazy (fun () () -> ()) in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () = Runtime'.Spec.(nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer () -> serialize writer

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun () -> serialize

        let from_json_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgStoreAndMigrateContract : sig
        type t = {
          authority : string;
              (** {%html:
<p>Authority is the address of the governance account.</p>
%}
              *)
          wasm_byte_code : bytes;
              (** {%html:
<p>WASMByteCode can be raw or gzip compressed</p>
%}
              *)
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
              (** {%html:
<p>InstantiatePermission to apply on contract creation, optional</p>
%}
              *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
          msg : bytes;
              (** {%html:
<p>Msg json encoded message to be passed to the contract on migration</p>
%}
              *)
        }

        val make :
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?contract:string ->
          ?msg:bytes ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?contract:string ->
          ?msg:bytes ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgStoreAndMigrateContract

        let name () = ".cosmwasm.wasm.v1.MsgStoreAndMigrateContract"

        type t = {
          authority : string;
          wasm_byte_code : bytes;
          instantiate_permission :
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t
            option;
          contract : string;
          msg : bytes;
        }

        type make_t =
          ?authority:string ->
          ?wasm_byte_code:bytes ->
          ?instantiate_permission:
            Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm.Wasm.V1
            .AccessConfig
            .t ->
          ?contract:string ->
          ?msg:bytes ->
          unit ->
          t

        let make ?(authority = {||}) ?(wasm_byte_code = Bytes.of_string {||})
            ?instantiate_permission ?(contract = {||})
            ?(msg = Bytes.of_string {||}) () =
          { authority; wasm_byte_code; instantiate_permission; contract; msg }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_authority =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "authority", "authority"), string, {||}))
               in
               let merge_wasm_byte_code =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (2, "wasm_byte_code", "wasmByteCode"),
                         bytes,
                         Bytes.of_string {||} ))
               in
               let merge_instantiate_permission =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (3, "instantiate_permission", "instantiatePermission"),
                         message
                           (module Imported'modules.Cosmwasm_wasm_v1_types
                                   .Cosmwasm
                                   .Wasm
                                   .V1
                                   .AccessConfig) ))
               in
               let merge_contract =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((4, "contract", "contract"), string, {||}))
               in
               let merge_msg =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((5, "msg", "msg"), bytes, Bytes.of_string {||}))
               in
               fun t1 t2 ->
                 {
                   authority = merge_authority t1.authority t2.authority;
                   wasm_byte_code =
                     merge_wasm_byte_code t1.wasm_byte_code t2.wasm_byte_code;
                   instantiate_permission =
                     merge_instantiate_permission t1.instantiate_permission
                       t2.instantiate_permission;
                   contract = merge_contract t1.contract t2.contract;
                   msg = merge_msg t1.msg t2.msg;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "authority", "authority"), string, {||})
            ^:: basic
                  ( (2, "wasm_byte_code", "wasmByteCode"),
                    bytes,
                    Bytes.of_string {||} )
            ^:: basic_opt
                  ( (3, "instantiate_permission", "instantiatePermission"),
                    message
                      (module Imported'modules.Cosmwasm_wasm_v1_types.Cosmwasm
                              .Wasm
                              .V1
                              .AccessConfig) )
            ^:: basic ((4, "contract", "contract"), string, {||})
            ^:: basic ((5, "msg", "msg"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer
            { authority; wasm_byte_code; instantiate_permission; contract; msg }
          ->
            serialize writer authority wasm_byte_code instantiate_permission
              contract msg

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor authority wasm_byte_code instantiate_permission
              contract msg =
            { authority; wasm_byte_code; instantiate_permission; contract; msg }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun {
                authority;
                wasm_byte_code;
                instantiate_permission;
                contract;
                msg;
              }
          ->
            serialize authority wasm_byte_code instantiate_permission contract
              msg

        let from_json_exn =
          let constructor authority wasm_byte_code instantiate_permission
              contract msg =
            { authority; wasm_byte_code; instantiate_permission; contract; msg }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgStoreAndMigrateContractResponse : sig
        type t = {
          code_id : int64;
              (** {%html:
<p>CodeID is the reference to the stored WASM code</p>
%}
              *)
          checksum : bytes;
              (** {%html:
<p>Checksum is the sha256 hash of the stored code</p>
%}
              *)
          data : bytes;
              (** {%html:
<p>Data contains bytes to returned from the contract</p>
%}
              *)
        }

        val make : ?code_id:int64 -> ?checksum:bytes -> ?data:bytes -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?code_id:int64 -> ?checksum:bytes -> ?data:bytes -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgStoreAndMigrateContractResponse

        let name () = ".cosmwasm.wasm.v1.MsgStoreAndMigrateContractResponse"

        type t = { code_id : int64; checksum : bytes; data : bytes }

        type make_t =
          ?code_id:int64 -> ?checksum:bytes -> ?data:bytes -> unit -> t

        let make ?(code_id = 0L) ?(checksum = Bytes.of_string {||})
            ?(data = Bytes.of_string {||}) () =
          { code_id; checksum; data }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_code_id =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "code_id", "codeId"), uint64, 0L))
               in
               let merge_checksum =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ((2, "checksum", "checksum"), bytes, Bytes.of_string {||}))
               in
               let merge_data =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((3, "data", "data"), bytes, Bytes.of_string {||}))
               in
               fun t1 t2 ->
                 {
                   code_id = merge_code_id t1.code_id t2.code_id;
                   checksum = merge_checksum t1.checksum t2.checksum;
                   data = merge_data t1.data t2.data;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "code_id", "codeId"), uint64, 0L)
            ^:: basic ((2, "checksum", "checksum"), bytes, Bytes.of_string {||})
            ^:: basic ((3, "data", "data"), bytes, Bytes.of_string {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { code_id; checksum; data } ->
            serialize writer code_id checksum data

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor code_id checksum data = { code_id; checksum; data } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { code_id; checksum; data } -> serialize code_id checksum data

        let from_json_exn =
          let constructor code_id checksum data = { code_id; checksum; data } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgUpdateContractLabel : sig
        type t = {
          sender : string;
              (** {%html:
<p>Sender is the that actor that signed the messages</p>
%}
              *)
          new_label : string;
              (** {%html:
<p>NewLabel string to be set</p>
%} *)
          contract : string;
              (** {%html:
<p>Contract is the address of the smart contract</p>
%}
              *)
        }

        val make :
          ?sender:string -> ?new_label:string -> ?contract:string -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?sender:string -> ?new_label:string -> ?contract:string -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgUpdateContractLabel

        let name () = ".cosmwasm.wasm.v1.MsgUpdateContractLabel"

        type t = { sender : string; new_label : string; contract : string }

        type make_t =
          ?sender:string -> ?new_label:string -> ?contract:string -> unit -> t

        let make ?(sender = {||}) ?(new_label = {||}) ?(contract = {||}) () =
          { sender; new_label; contract }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_sender =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "sender", "sender"), string, {||}))
               in
               let merge_new_label =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "new_label", "newLabel"), string, {||}))
               in
               let merge_contract =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((3, "contract", "contract"), string, {||}))
               in
               fun t1 t2 ->
                 {
                   sender = merge_sender t1.sender t2.sender;
                   new_label = merge_new_label t1.new_label t2.new_label;
                   contract = merge_contract t1.contract t2.contract;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "sender", "sender"), string, {||})
            ^:: basic ((2, "new_label", "newLabel"), string, {||})
            ^:: basic ((3, "contract", "contract"), string, {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { sender; new_label; contract } ->
            serialize writer sender new_label contract

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor sender new_label contract =
            { sender; new_label; contract }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { sender; new_label; contract } ->
            serialize sender new_label contract

        let from_json_exn =
          let constructor sender new_label contract =
            { sender; new_label; contract }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and MsgUpdateContractLabelResponse : sig
        type t = unit

        val make : unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = MsgUpdateContractLabelResponse

        let name () = ".cosmwasm.wasm.v1.MsgUpdateContractLabelResponse"

        type t = unit
        type make_t = unit -> t

        let make () = ()

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' = lazy (fun () () -> ()) in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () = Runtime'.Spec.(nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer () -> serialize writer

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun () -> serialize

        let from_json_exn =
          let constructor = () in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      module Msg = struct
        module StoreCode = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "StoreCode"
          let name = "/cosmwasm.wasm.v1.Msg/StoreCode"

          module Request = MsgStoreCode
          module Response = MsgStoreCodeResponse
        end

        let storeCode =
          ( (module MsgStoreCode : Runtime'.Spec.Message
              with type t = MsgStoreCode.t),
            (module MsgStoreCodeResponse : Runtime'.Spec.Message
              with type t = MsgStoreCodeResponse.t) )

        module InstantiateContract = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "InstantiateContract"
          let name = "/cosmwasm.wasm.v1.Msg/InstantiateContract"

          module Request = MsgInstantiateContract
          module Response = MsgInstantiateContractResponse
        end

        let instantiateContract =
          ( (module MsgInstantiateContract : Runtime'.Spec.Message
              with type t = MsgInstantiateContract.t),
            (module MsgInstantiateContractResponse : Runtime'.Spec.Message
              with type t = MsgInstantiateContractResponse.t) )

        module InstantiateContract2 = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "InstantiateContract2"
          let name = "/cosmwasm.wasm.v1.Msg/InstantiateContract2"

          module Request = MsgInstantiateContract2
          module Response = MsgInstantiateContract2Response
        end

        let instantiateContract2 =
          ( (module MsgInstantiateContract2 : Runtime'.Spec.Message
              with type t = MsgInstantiateContract2.t),
            (module MsgInstantiateContract2Response : Runtime'.Spec.Message
              with type t = MsgInstantiateContract2Response.t) )

        module ExecuteContract = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "ExecuteContract"
          let name = "/cosmwasm.wasm.v1.Msg/ExecuteContract"

          module Request = MsgExecuteContract
          module Response = MsgExecuteContractResponse
        end

        let executeContract =
          ( (module MsgExecuteContract : Runtime'.Spec.Message
              with type t = MsgExecuteContract.t),
            (module MsgExecuteContractResponse : Runtime'.Spec.Message
              with type t = MsgExecuteContractResponse.t) )

        module MigrateContract = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "MigrateContract"
          let name = "/cosmwasm.wasm.v1.Msg/MigrateContract"

          module Request = MsgMigrateContract
          module Response = MsgMigrateContractResponse
        end

        let migrateContract =
          ( (module MsgMigrateContract : Runtime'.Spec.Message
              with type t = MsgMigrateContract.t),
            (module MsgMigrateContractResponse : Runtime'.Spec.Message
              with type t = MsgMigrateContractResponse.t) )

        module UpdateAdmin = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "UpdateAdmin"
          let name = "/cosmwasm.wasm.v1.Msg/UpdateAdmin"

          module Request = MsgUpdateAdmin
          module Response = MsgUpdateAdminResponse
        end

        let updateAdmin =
          ( (module MsgUpdateAdmin : Runtime'.Spec.Message
              with type t = MsgUpdateAdmin.t),
            (module MsgUpdateAdminResponse : Runtime'.Spec.Message
              with type t = MsgUpdateAdminResponse.t) )

        module ClearAdmin = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "ClearAdmin"
          let name = "/cosmwasm.wasm.v1.Msg/ClearAdmin"

          module Request = MsgClearAdmin
          module Response = MsgClearAdminResponse
        end

        let clearAdmin =
          ( (module MsgClearAdmin : Runtime'.Spec.Message
              with type t = MsgClearAdmin.t),
            (module MsgClearAdminResponse : Runtime'.Spec.Message
              with type t = MsgClearAdminResponse.t) )

        module UpdateInstantiateConfig = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "UpdateInstantiateConfig"
          let name = "/cosmwasm.wasm.v1.Msg/UpdateInstantiateConfig"

          module Request = MsgUpdateInstantiateConfig
          module Response = MsgUpdateInstantiateConfigResponse
        end

        let updateInstantiateConfig =
          ( (module MsgUpdateInstantiateConfig : Runtime'.Spec.Message
              with type t = MsgUpdateInstantiateConfig.t),
            (module MsgUpdateInstantiateConfigResponse : Runtime'.Spec.Message
              with type t = MsgUpdateInstantiateConfigResponse.t) )

        module UpdateParams = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "UpdateParams"
          let name = "/cosmwasm.wasm.v1.Msg/UpdateParams"

          module Request = MsgUpdateParams
          module Response = MsgUpdateParamsResponse
        end

        let updateParams =
          ( (module MsgUpdateParams : Runtime'.Spec.Message
              with type t = MsgUpdateParams.t),
            (module MsgUpdateParamsResponse : Runtime'.Spec.Message
              with type t = MsgUpdateParamsResponse.t) )

        module SudoContract = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "SudoContract"
          let name = "/cosmwasm.wasm.v1.Msg/SudoContract"

          module Request = MsgSudoContract
          module Response = MsgSudoContractResponse
        end

        let sudoContract =
          ( (module MsgSudoContract : Runtime'.Spec.Message
              with type t = MsgSudoContract.t),
            (module MsgSudoContractResponse : Runtime'.Spec.Message
              with type t = MsgSudoContractResponse.t) )

        module PinCodes = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "PinCodes"
          let name = "/cosmwasm.wasm.v1.Msg/PinCodes"

          module Request = MsgPinCodes
          module Response = MsgPinCodesResponse
        end

        let pinCodes =
          ( (module MsgPinCodes : Runtime'.Spec.Message
              with type t = MsgPinCodes.t),
            (module MsgPinCodesResponse : Runtime'.Spec.Message
              with type t = MsgPinCodesResponse.t) )

        module UnpinCodes = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "UnpinCodes"
          let name = "/cosmwasm.wasm.v1.Msg/UnpinCodes"

          module Request = MsgUnpinCodes
          module Response = MsgUnpinCodesResponse
        end

        let unpinCodes =
          ( (module MsgUnpinCodes : Runtime'.Spec.Message
              with type t = MsgUnpinCodes.t),
            (module MsgUnpinCodesResponse : Runtime'.Spec.Message
              with type t = MsgUnpinCodesResponse.t) )

        module StoreAndInstantiateContract = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "StoreAndInstantiateContract"
          let name = "/cosmwasm.wasm.v1.Msg/StoreAndInstantiateContract"

          module Request = MsgStoreAndInstantiateContract
          module Response = MsgStoreAndInstantiateContractResponse
        end

        let storeAndInstantiateContract =
          ( (module MsgStoreAndInstantiateContract : Runtime'.Spec.Message
              with type t = MsgStoreAndInstantiateContract.t),
            (module MsgStoreAndInstantiateContractResponse
            : Runtime'.Spec.Message
              with type t = MsgStoreAndInstantiateContractResponse.t) )

        module RemoveCodeUploadParamsAddresses = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "RemoveCodeUploadParamsAddresses"
          let name = "/cosmwasm.wasm.v1.Msg/RemoveCodeUploadParamsAddresses"

          module Request = MsgRemoveCodeUploadParamsAddresses
          module Response = MsgRemoveCodeUploadParamsAddressesResponse
        end

        let removeCodeUploadParamsAddresses =
          ( (module MsgRemoveCodeUploadParamsAddresses : Runtime'.Spec.Message
              with type t = MsgRemoveCodeUploadParamsAddresses.t),
            (module MsgRemoveCodeUploadParamsAddressesResponse
            : Runtime'.Spec.Message
              with type t = MsgRemoveCodeUploadParamsAddressesResponse.t) )

        module AddCodeUploadParamsAddresses = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "AddCodeUploadParamsAddresses"
          let name = "/cosmwasm.wasm.v1.Msg/AddCodeUploadParamsAddresses"

          module Request = MsgAddCodeUploadParamsAddresses
          module Response = MsgAddCodeUploadParamsAddressesResponse
        end

        let addCodeUploadParamsAddresses =
          ( (module MsgAddCodeUploadParamsAddresses : Runtime'.Spec.Message
              with type t = MsgAddCodeUploadParamsAddresses.t),
            (module MsgAddCodeUploadParamsAddressesResponse
            : Runtime'.Spec.Message
              with type t = MsgAddCodeUploadParamsAddressesResponse.t) )

        module StoreAndMigrateContract = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "StoreAndMigrateContract"
          let name = "/cosmwasm.wasm.v1.Msg/StoreAndMigrateContract"

          module Request = MsgStoreAndMigrateContract
          module Response = MsgStoreAndMigrateContractResponse
        end

        let storeAndMigrateContract =
          ( (module MsgStoreAndMigrateContract : Runtime'.Spec.Message
              with type t = MsgStoreAndMigrateContract.t),
            (module MsgStoreAndMigrateContractResponse : Runtime'.Spec.Message
              with type t = MsgStoreAndMigrateContractResponse.t) )

        module UpdateContractLabel = struct
          let package_name = Some "cosmwasm.wasm.v1"
          let service_name = "Msg"
          let method_name = "UpdateContractLabel"
          let name = "/cosmwasm.wasm.v1.Msg/UpdateContractLabel"

          module Request = MsgUpdateContractLabel
          module Response = MsgUpdateContractLabelResponse
        end

        let updateContractLabel =
          ( (module MsgUpdateContractLabel : Runtime'.Spec.Message
              with type t = MsgUpdateContractLabel.t),
            (module MsgUpdateContractLabelResponse : Runtime'.Spec.Message
              with type t = MsgUpdateContractLabelResponse.t) )
      end
    end
  end
end

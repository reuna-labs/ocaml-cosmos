(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: cosmos/auth/v1beta1/auth.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Amino_amino = Amino_amino
  module Cosmos_proto_cosmos = Cosmos_proto_cosmos
  module Gogoproto_gogo = Gogoproto_gogo
  module Google_protobuf_any = Google_protobuf_any
end

(**/**)

module rec Cosmos : sig
  module rec Auth : sig
    module rec V1beta1 : sig
      (** {%html:
<p>BaseAccount defines a base account type. It contains all the necessary fields
for basic account functionality. Any custom account type should extend this
type for additional functionality (e.g. vesting).</p>
%}
      *)
      module rec BaseAccount : sig
        type t = {
          address : string;
          pub_key :
            Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
          account_number : int64;
          sequence : int64;
        }

        val make :
          ?address:string ->
          ?pub_key:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?account_number:int64 ->
          ?sequence:int64 ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?pub_key:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?account_number:int64 ->
          ?sequence:int64 ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ModuleAccount defines an account for modules that holds coins on a pool.</p>
%}
      *)
      and ModuleAccount : sig
        type t = {
          base_account : BaseAccount.t option;
          name : string;
          permissions : string list;
        }

        val make :
          ?base_account:BaseAccount.t ->
          ?name:string ->
          ?permissions:string list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?base_account:BaseAccount.t ->
          ?name:string ->
          ?permissions:string list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ModuleCredential represents a unclaimable pubkey for base accounts controlled by modules.</p>
%}
      *)
      and ModuleCredential : sig
        type t = {
          module_name : string;
              (** {%html:
<p>module_name is the name of the module used for address derivation (passed into address.Module).</p>
%}
              *)
          derivation_keys : bytes list;
              (** {%html:
<p>derivation_keys is for deriving a module account address (passed into address.Module)
adding more keys creates sub-account addresses (passed into address.Derive)</p>
%}
              *)
        }

        val make :
          ?module_name:string -> ?derivation_keys:bytes list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?module_name:string -> ?derivation_keys:bytes list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Params defines the parameters for the auth module.</p>
%}
      *)
      and Params : sig
        type t = {
          max_memo_characters : int64;
          tx_sig_limit : int64;
          tx_size_cost_per_byte : int64;
          sig_verify_cost_ed25519 : int64;
          sig_verify_cost_secp256k1 : int64;
          sig_verify_cost_mldsa65 : int64;
        }

        val make :
          ?max_memo_characters:int64 ->
          ?tx_sig_limit:int64 ->
          ?tx_size_cost_per_byte:int64 ->
          ?sig_verify_cost_ed25519:int64 ->
          ?sig_verify_cost_secp256k1:int64 ->
          ?sig_verify_cost_mldsa65:int64 ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?max_memo_characters:int64 ->
          ?tx_sig_limit:int64 ->
          ?tx_size_cost_per_byte:int64 ->
          ?sig_verify_cost_ed25519:int64 ->
          ?sig_verify_cost_secp256k1:int64 ->
          ?sig_verify_cost_mldsa65:int64 ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
    end
  end
end = struct
  module rec Auth : sig
    module rec V1beta1 : sig
      (** {%html:
<p>BaseAccount defines a base account type. It contains all the necessary fields
for basic account functionality. Any custom account type should extend this
type for additional functionality (e.g. vesting).</p>
%}
      *)
      module rec BaseAccount : sig
        type t = {
          address : string;
          pub_key :
            Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
          account_number : int64;
          sequence : int64;
        }

        val make :
          ?address:string ->
          ?pub_key:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?account_number:int64 ->
          ?sequence:int64 ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?pub_key:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?account_number:int64 ->
          ?sequence:int64 ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ModuleAccount defines an account for modules that holds coins on a pool.</p>
%}
      *)
      and ModuleAccount : sig
        type t = {
          base_account : BaseAccount.t option;
          name : string;
          permissions : string list;
        }

        val make :
          ?base_account:BaseAccount.t ->
          ?name:string ->
          ?permissions:string list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?base_account:BaseAccount.t ->
          ?name:string ->
          ?permissions:string list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ModuleCredential represents a unclaimable pubkey for base accounts controlled by modules.</p>
%}
      *)
      and ModuleCredential : sig
        type t = {
          module_name : string;
              (** {%html:
<p>module_name is the name of the module used for address derivation (passed into address.Module).</p>
%}
              *)
          derivation_keys : bytes list;
              (** {%html:
<p>derivation_keys is for deriving a module account address (passed into address.Module)
adding more keys creates sub-account addresses (passed into address.Derive)</p>
%}
              *)
        }

        val make :
          ?module_name:string -> ?derivation_keys:bytes list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?module_name:string -> ?derivation_keys:bytes list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Params defines the parameters for the auth module.</p>
%}
      *)
      and Params : sig
        type t = {
          max_memo_characters : int64;
          tx_sig_limit : int64;
          tx_size_cost_per_byte : int64;
          sig_verify_cost_ed25519 : int64;
          sig_verify_cost_secp256k1 : int64;
          sig_verify_cost_mldsa65 : int64;
        }

        val make :
          ?max_memo_characters:int64 ->
          ?tx_sig_limit:int64 ->
          ?tx_size_cost_per_byte:int64 ->
          ?sig_verify_cost_ed25519:int64 ->
          ?sig_verify_cost_secp256k1:int64 ->
          ?sig_verify_cost_mldsa65:int64 ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?max_memo_characters:int64 ->
          ?tx_sig_limit:int64 ->
          ?tx_size_cost_per_byte:int64 ->
          ?sig_verify_cost_ed25519:int64 ->
          ?sig_verify_cost_secp256k1:int64 ->
          ?sig_verify_cost_mldsa65:int64 ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
    end
  end = struct
    module rec V1beta1 : sig
      (** {%html:
<p>BaseAccount defines a base account type. It contains all the necessary fields
for basic account functionality. Any custom account type should extend this
type for additional functionality (e.g. vesting).</p>
%}
      *)
      module rec BaseAccount : sig
        type t = {
          address : string;
          pub_key :
            Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
          account_number : int64;
          sequence : int64;
        }

        val make :
          ?address:string ->
          ?pub_key:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?account_number:int64 ->
          ?sequence:int64 ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?pub_key:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?account_number:int64 ->
          ?sequence:int64 ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ModuleAccount defines an account for modules that holds coins on a pool.</p>
%}
      *)
      and ModuleAccount : sig
        type t = {
          base_account : BaseAccount.t option;
          name : string;
          permissions : string list;
        }

        val make :
          ?base_account:BaseAccount.t ->
          ?name:string ->
          ?permissions:string list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?base_account:BaseAccount.t ->
          ?name:string ->
          ?permissions:string list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>ModuleCredential represents a unclaimable pubkey for base accounts controlled by modules.</p>
%}
      *)
      and ModuleCredential : sig
        type t = {
          module_name : string;
              (** {%html:
<p>module_name is the name of the module used for address derivation (passed into address.Module).</p>
%}
              *)
          derivation_keys : bytes list;
              (** {%html:
<p>derivation_keys is for deriving a module account address (passed into address.Module)
adding more keys creates sub-account addresses (passed into address.Derive)</p>
%}
              *)
        }

        val make :
          ?module_name:string -> ?derivation_keys:bytes list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?module_name:string -> ?derivation_keys:bytes list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Params defines the parameters for the auth module.</p>
%}
      *)
      and Params : sig
        type t = {
          max_memo_characters : int64;
          tx_sig_limit : int64;
          tx_size_cost_per_byte : int64;
          sig_verify_cost_ed25519 : int64;
          sig_verify_cost_secp256k1 : int64;
          sig_verify_cost_mldsa65 : int64;
        }

        val make :
          ?max_memo_characters:int64 ->
          ?tx_sig_limit:int64 ->
          ?tx_size_cost_per_byte:int64 ->
          ?sig_verify_cost_ed25519:int64 ->
          ?sig_verify_cost_secp256k1:int64 ->
          ?sig_verify_cost_mldsa65:int64 ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?max_memo_characters:int64 ->
          ?tx_sig_limit:int64 ->
          ?tx_size_cost_per_byte:int64 ->
          ?sig_verify_cost_ed25519:int64 ->
          ?sig_verify_cost_secp256k1:int64 ->
          ?sig_verify_cost_mldsa65:int64 ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
    end = struct
      module rec BaseAccount : sig
        type t = {
          address : string;
          pub_key :
            Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
          account_number : int64;
          sequence : int64;
        }

        val make :
          ?address:string ->
          ?pub_key:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?account_number:int64 ->
          ?sequence:int64 ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?pub_key:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?account_number:int64 ->
          ?sequence:int64 ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = BaseAccount

        let name () = ".cosmos.auth.v1beta1.BaseAccount"

        type t = {
          address : string;
          pub_key :
            Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t option;
          account_number : int64;
          sequence : int64;
        }

        type make_t =
          ?address:string ->
          ?pub_key:Imported'modules.Google_protobuf_any.Google.Protobuf.Any.t ->
          ?account_number:int64 ->
          ?sequence:int64 ->
          unit ->
          t

        let make ?(address = {||}) ?pub_key ?(account_number = 0L)
            ?(sequence = 0L) () =
          { address; pub_key; account_number; sequence }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_address =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "address", "address"), string, {||}))
               in
               let merge_pub_key =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (2, "pub_key", "pubKey"),
                         message
                           (module Imported'modules.Google_protobuf_any.Google
                                   .Protobuf
                                   .Any) ))
               in
               let merge_account_number =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((3, "account_number", "accountNumber"), uint64, 0L))
               in
               let merge_sequence =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((4, "sequence", "sequence"), uint64, 0L))
               in
               fun t1 t2 ->
                 {
                   address = merge_address t1.address t2.address;
                   pub_key = merge_pub_key t1.pub_key t2.pub_key;
                   account_number =
                     merge_account_number t1.account_number t2.account_number;
                   sequence = merge_sequence t1.sequence t2.sequence;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "address", "address"), string, {||})
            ^:: basic_opt
                  ( (2, "pub_key", "pubKey"),
                    message
                      (module Imported'modules.Google_protobuf_any.Google
                              .Protobuf
                              .Any) )
            ^:: basic ((3, "account_number", "accountNumber"), uint64, 0L)
            ^:: basic ((4, "sequence", "sequence"), uint64, 0L)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { address; pub_key; account_number; sequence } ->
            serialize writer address pub_key account_number sequence

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor address pub_key account_number sequence =
            { address; pub_key; account_number; sequence }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { address; pub_key; account_number; sequence } ->
            serialize address pub_key account_number sequence

        let from_json_exn =
          let constructor address pub_key account_number sequence =
            { address; pub_key; account_number; sequence }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and ModuleAccount : sig
        type t = {
          base_account : BaseAccount.t option;
          name : string;
          permissions : string list;
        }

        val make :
          ?base_account:BaseAccount.t ->
          ?name:string ->
          ?permissions:string list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?base_account:BaseAccount.t ->
          ?name:string ->
          ?permissions:string list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = ModuleAccount

        let name () = ".cosmos.auth.v1beta1.ModuleAccount"

        type t = {
          base_account : BaseAccount.t option;
          name : string;
          permissions : string list;
        }

        type make_t =
          ?base_account:BaseAccount.t ->
          ?name:string ->
          ?permissions:string list ->
          unit ->
          t

        let make ?base_account ?(name = {||}) ?(permissions = []) () =
          { base_account; name; permissions }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_base_account =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic_opt
                       ( (1, "base_account", "baseAccount"),
                         message (module BaseAccount) ))
               in
               let merge_name =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((2, "name", "name"), string, {||}))
               in
               let merge_permissions =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ((3, "permissions", "permissions"), string, not_packed))
               in
               fun t1 t2 ->
                 {
                   base_account =
                     merge_base_account t1.base_account t2.base_account;
                   name = merge_name t1.name t2.name;
                   permissions = merge_permissions t1.permissions t2.permissions;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic_opt
              ((1, "base_account", "baseAccount"), message (module BaseAccount))
            ^:: basic ((2, "name", "name"), string, {||})
            ^:: repeated ((3, "permissions", "permissions"), string, not_packed)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { base_account; name; permissions } ->
            serialize writer base_account name permissions

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor base_account name permissions =
            { base_account; name; permissions }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { base_account; name; permissions } ->
            serialize base_account name permissions

        let from_json_exn =
          let constructor base_account name permissions =
            { base_account; name; permissions }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and ModuleCredential : sig
        type t = {
          module_name : string;
              (** {%html:
<p>module_name is the name of the module used for address derivation (passed into address.Module).</p>
%}
              *)
          derivation_keys : bytes list;
              (** {%html:
<p>derivation_keys is for deriving a module account address (passed into address.Module)
adding more keys creates sub-account addresses (passed into address.Derive)</p>
%}
              *)
        }

        val make :
          ?module_name:string -> ?derivation_keys:bytes list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?module_name:string -> ?derivation_keys:bytes list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = ModuleCredential

        let name () = ".cosmos.auth.v1beta1.ModuleCredential"

        type t = { module_name : string; derivation_keys : bytes list }

        type make_t =
          ?module_name:string -> ?derivation_keys:bytes list -> unit -> t

        let make ?(module_name = {||}) ?(derivation_keys = []) () =
          { module_name; derivation_keys }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_module_name =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "module_name", "moduleName"), string, {||}))
               in
               let merge_derivation_keys =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (2, "derivation_keys", "derivationKeys"),
                         bytes,
                         not_packed ))
               in
               fun t1 t2 ->
                 {
                   module_name = merge_module_name t1.module_name t2.module_name;
                   derivation_keys =
                     merge_derivation_keys t1.derivation_keys t2.derivation_keys;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "module_name", "moduleName"), string, {||})
            ^:: repeated
                  ((2, "derivation_keys", "derivationKeys"), bytes, not_packed)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { module_name; derivation_keys } ->
            serialize writer module_name derivation_keys

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor module_name derivation_keys =
            { module_name; derivation_keys }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { module_name; derivation_keys } ->
            serialize module_name derivation_keys

        let from_json_exn =
          let constructor module_name derivation_keys =
            { module_name; derivation_keys }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and Params : sig
        type t = {
          max_memo_characters : int64;
          tx_sig_limit : int64;
          tx_size_cost_per_byte : int64;
          sig_verify_cost_ed25519 : int64;
          sig_verify_cost_secp256k1 : int64;
          sig_verify_cost_mldsa65 : int64;
        }

        val make :
          ?max_memo_characters:int64 ->
          ?tx_sig_limit:int64 ->
          ?tx_size_cost_per_byte:int64 ->
          ?sig_verify_cost_ed25519:int64 ->
          ?sig_verify_cost_secp256k1:int64 ->
          ?sig_verify_cost_mldsa65:int64 ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?max_memo_characters:int64 ->
          ?tx_sig_limit:int64 ->
          ?tx_size_cost_per_byte:int64 ->
          ?sig_verify_cost_ed25519:int64 ->
          ?sig_verify_cost_secp256k1:int64 ->
          ?sig_verify_cost_mldsa65:int64 ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = Params

        let name () = ".cosmos.auth.v1beta1.Params"

        type t = {
          max_memo_characters : int64;
          tx_sig_limit : int64;
          tx_size_cost_per_byte : int64;
          sig_verify_cost_ed25519 : int64;
          sig_verify_cost_secp256k1 : int64;
          sig_verify_cost_mldsa65 : int64;
        }

        type make_t =
          ?max_memo_characters:int64 ->
          ?tx_sig_limit:int64 ->
          ?tx_size_cost_per_byte:int64 ->
          ?sig_verify_cost_ed25519:int64 ->
          ?sig_verify_cost_secp256k1:int64 ->
          ?sig_verify_cost_mldsa65:int64 ->
          unit ->
          t

        let make ?(max_memo_characters = 0L) ?(tx_sig_limit = 0L)
            ?(tx_size_cost_per_byte = 0L) ?(sig_verify_cost_ed25519 = 0L)
            ?(sig_verify_cost_secp256k1 = 0L) ?(sig_verify_cost_mldsa65 = 0L) ()
            =
          {
            max_memo_characters;
            tx_sig_limit;
            tx_size_cost_per_byte;
            sig_verify_cost_ed25519;
            sig_verify_cost_secp256k1;
            sig_verify_cost_mldsa65;
          }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_max_memo_characters =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (1, "max_memo_characters", "maxMemoCharacters"),
                         uint64,
                         0L ))
               in
               let merge_tx_sig_limit =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "tx_sig_limit", "txSigLimit"), uint64, 0L))
               in
               let merge_tx_size_cost_per_byte =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (3, "tx_size_cost_per_byte", "txSizeCostPerByte"),
                         uint64,
                         0L ))
               in
               let merge_sig_verify_cost_ed25519 =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (4, "sig_verify_cost_ed25519", "sigVerifyCostEd25519"),
                         uint64,
                         0L ))
               in
               let merge_sig_verify_cost_secp256k1 =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( ( 5,
                           "sig_verify_cost_secp256k1",
                           "sigVerifyCostSecp256k1" ),
                         uint64,
                         0L ))
               in
               let merge_sig_verify_cost_mldsa65 =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (6, "sig_verify_cost_mldsa65", "sigVerifyCostMldsa65"),
                         uint64,
                         0L ))
               in
               fun t1 t2 ->
                 {
                   max_memo_characters =
                     merge_max_memo_characters t1.max_memo_characters
                       t2.max_memo_characters;
                   tx_sig_limit =
                     merge_tx_sig_limit t1.tx_sig_limit t2.tx_sig_limit;
                   tx_size_cost_per_byte =
                     merge_tx_size_cost_per_byte t1.tx_size_cost_per_byte
                       t2.tx_size_cost_per_byte;
                   sig_verify_cost_ed25519 =
                     merge_sig_verify_cost_ed25519 t1.sig_verify_cost_ed25519
                       t2.sig_verify_cost_ed25519;
                   sig_verify_cost_secp256k1 =
                     merge_sig_verify_cost_secp256k1
                       t1.sig_verify_cost_secp256k1 t2.sig_verify_cost_secp256k1;
                   sig_verify_cost_mldsa65 =
                     merge_sig_verify_cost_mldsa65 t1.sig_verify_cost_mldsa65
                       t2.sig_verify_cost_mldsa65;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "max_memo_characters", "maxMemoCharacters"), uint64, 0L)
            ^:: basic ((2, "tx_sig_limit", "txSigLimit"), uint64, 0L)
            ^:: basic
                  ((3, "tx_size_cost_per_byte", "txSizeCostPerByte"), uint64, 0L)
            ^:: basic
                  ( (4, "sig_verify_cost_ed25519", "sigVerifyCostEd25519"),
                    uint64,
                    0L )
            ^:: basic
                  ( (5, "sig_verify_cost_secp256k1", "sigVerifyCostSecp256k1"),
                    uint64,
                    0L )
            ^:: basic
                  ( (6, "sig_verify_cost_mldsa65", "sigVerifyCostMldsa65"),
                    uint64,
                    0L )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer
            {
              max_memo_characters;
              tx_sig_limit;
              tx_size_cost_per_byte;
              sig_verify_cost_ed25519;
              sig_verify_cost_secp256k1;
              sig_verify_cost_mldsa65;
            }
          ->
            serialize writer max_memo_characters tx_sig_limit
              tx_size_cost_per_byte sig_verify_cost_ed25519
              sig_verify_cost_secp256k1 sig_verify_cost_mldsa65

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor max_memo_characters tx_sig_limit tx_size_cost_per_byte
              sig_verify_cost_ed25519 sig_verify_cost_secp256k1
              sig_verify_cost_mldsa65 =
            {
              max_memo_characters;
              tx_sig_limit;
              tx_size_cost_per_byte;
              sig_verify_cost_ed25519;
              sig_verify_cost_secp256k1;
              sig_verify_cost_mldsa65;
            }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun {
                max_memo_characters;
                tx_sig_limit;
                tx_size_cost_per_byte;
                sig_verify_cost_ed25519;
                sig_verify_cost_secp256k1;
                sig_verify_cost_mldsa65;
              }
          ->
            serialize max_memo_characters tx_sig_limit tx_size_cost_per_byte
              sig_verify_cost_ed25519 sig_verify_cost_secp256k1
              sig_verify_cost_mldsa65

        let from_json_exn =
          let constructor max_memo_characters tx_sig_limit tx_size_cost_per_byte
              sig_verify_cost_ed25519 sig_verify_cost_secp256k1
              sig_verify_cost_mldsa65 =
            {
              max_memo_characters;
              tx_sig_limit;
              tx_size_cost_per_byte;
              sig_verify_cost_ed25519;
              sig_verify_cost_secp256k1;
              sig_verify_cost_mldsa65;
            }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end
    end
  end
end

(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: tendermint/crypto/proof.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Gogoproto_gogo = Gogoproto_gogo
end

(**/**)

module rec Tendermint : sig
  module rec Crypto : sig
    module rec Proof : sig
      type t = {
        total : int64;
        index : int64;
        leaf_hash : bytes;
        aunts : bytes list;
      }

      val make :
        ?total:int64 ->
        ?index:int64 ->
        ?leaf_hash:bytes ->
        ?aunts:bytes list ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?total:int64 ->
        ?index:int64 ->
        ?leaf_hash:bytes ->
        ?aunts:bytes list ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and ValueOp : sig
      type t = {
        key : bytes;  (** {%html:
<p>Encoded in ProofOp.Key.</p>
%} *)
        proof : Proof.t option;
            (** {%html:
<p>To encode in ProofOp.Data</p>
%} *)
      }

      val make : ?key:bytes -> ?proof:Proof.t -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?key:bytes -> ?proof:Proof.t -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and DominoOp : sig
      type t = { key : string; input : string; output : string }

      val make : ?key:string -> ?input:string -> ?output:string -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?key:string -> ?input:string -> ?output:string -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>ProofOp defines an operation used for calculating Merkle root
The data could be arbitrary format, providing nessecary data
for example neighbouring node hash</p>
%}
    *)
    and ProofOp : sig
      type t = { type' : string; key : bytes; data : bytes }

      val make : ?type':string -> ?key:bytes -> ?data:bytes -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?type':string -> ?key:bytes -> ?data:bytes -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>ProofOps is Merkle proof defined by the list of ProofOps</p>
%}
    *)
    and ProofOps : sig
      type t = ProofOp.t list

      val make : ?ops:ProofOp.t list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?ops:ProofOp.t list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end
  end
end = struct
  module rec Crypto : sig
    module rec Proof : sig
      type t = {
        total : int64;
        index : int64;
        leaf_hash : bytes;
        aunts : bytes list;
      }

      val make :
        ?total:int64 ->
        ?index:int64 ->
        ?leaf_hash:bytes ->
        ?aunts:bytes list ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?total:int64 ->
        ?index:int64 ->
        ?leaf_hash:bytes ->
        ?aunts:bytes list ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and ValueOp : sig
      type t = {
        key : bytes;  (** {%html:
<p>Encoded in ProofOp.Key.</p>
%} *)
        proof : Proof.t option;
            (** {%html:
<p>To encode in ProofOp.Data</p>
%} *)
      }

      val make : ?key:bytes -> ?proof:Proof.t -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?key:bytes -> ?proof:Proof.t -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    and DominoOp : sig
      type t = { key : string; input : string; output : string }

      val make : ?key:string -> ?input:string -> ?output:string -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?key:string -> ?input:string -> ?output:string -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>ProofOp defines an operation used for calculating Merkle root
The data could be arbitrary format, providing nessecary data
for example neighbouring node hash</p>
%}
    *)
    and ProofOp : sig
      type t = { type' : string; key : bytes; data : bytes }

      val make : ?type':string -> ?key:bytes -> ?data:bytes -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?type':string -> ?key:bytes -> ?data:bytes -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end

    (** {%html:
<p>ProofOps is Merkle proof defined by the list of ProofOps</p>
%}
    *)
    and ProofOps : sig
      type t = ProofOp.t list

      val make : ?ops:ProofOp.t list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?ops:ProofOp.t list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end
  end = struct
    module rec Proof : sig
      type t = {
        total : int64;
        index : int64;
        leaf_hash : bytes;
        aunts : bytes list;
      }

      val make :
        ?total:int64 ->
        ?index:int64 ->
        ?leaf_hash:bytes ->
        ?aunts:bytes list ->
        unit ->
        t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t =
        ?total:int64 ->
        ?index:int64 ->
        ?leaf_hash:bytes ->
        ?aunts:bytes list ->
        unit ->
        t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = Proof

      let name () = ".tendermint.crypto.Proof"

      type t = {
        total : int64;
        index : int64;
        leaf_hash : bytes;
        aunts : bytes list;
      }

      type make_t =
        ?total:int64 ->
        ?index:int64 ->
        ?leaf_hash:bytes ->
        ?aunts:bytes list ->
        unit ->
        t

      let make ?(total = 0L) ?(index = 0L) ?(leaf_hash = Bytes.of_string {||})
          ?(aunts = []) () =
        { total; index; leaf_hash; aunts }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_total =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((1, "total", "total"), int64, 0L))
             in
             let merge_index =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((2, "index", "index"), int64, 0L))
             in
             let merge_leaf_hash =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic
                     ((3, "leaf_hash", "leafHash"), bytes, Bytes.of_string {||}))
             in
             let merge_aunts =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   repeated ((4, "aunts", "aunts"), bytes, not_packed))
             in
             fun t1 t2 ->
               {
                 total = merge_total t1.total t2.total;
                 index = merge_index t1.index t2.index;
                 leaf_hash = merge_leaf_hash t1.leaf_hash t2.leaf_hash;
                 aunts = merge_aunts t1.aunts t2.aunts;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "total", "total"), int64, 0L)
          ^:: basic ((2, "index", "index"), int64, 0L)
          ^:: basic ((3, "leaf_hash", "leafHash"), bytes, Bytes.of_string {||})
          ^:: repeated ((4, "aunts", "aunts"), bytes, not_packed)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { total; index; leaf_hash; aunts } ->
          serialize writer total index leaf_hash aunts

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor total index leaf_hash aunts =
          { total; index; leaf_hash; aunts }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { total; index; leaf_hash; aunts } ->
          serialize total index leaf_hash aunts

      let from_json_exn =
        let constructor total index leaf_hash aunts =
          { total; index; leaf_hash; aunts }
        in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and ValueOp : sig
      type t = {
        key : bytes;  (** {%html:
<p>Encoded in ProofOp.Key.</p>
%} *)
        proof : Proof.t option;
            (** {%html:
<p>To encode in ProofOp.Data</p>
%} *)
      }

      val make : ?key:bytes -> ?proof:Proof.t -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?key:bytes -> ?proof:Proof.t -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = ValueOp

      let name () = ".tendermint.crypto.ValueOp"

      type t = { key : bytes; proof : Proof.t option }
      type make_t = ?key:bytes -> ?proof:Proof.t -> unit -> t

      let make ?(key = Bytes.of_string {||}) ?proof () = { key; proof }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_key =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((1, "key", "key"), bytes, Bytes.of_string {||}))
             in
             let merge_proof =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic_opt ((2, "proof", "proof"), message (module Proof)))
             in
             fun t1 t2 ->
               {
                 key = merge_key t1.key t2.key;
                 proof = merge_proof t1.proof t2.proof;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "key", "key"), bytes, Bytes.of_string {||})
          ^:: basic_opt ((2, "proof", "proof"), message (module Proof))
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { key; proof } -> serialize writer key proof

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor key proof = { key; proof } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { key; proof } -> serialize key proof

      let from_json_exn =
        let constructor key proof = { key; proof } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and DominoOp : sig
      type t = { key : string; input : string; output : string }

      val make : ?key:string -> ?input:string -> ?output:string -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?key:string -> ?input:string -> ?output:string -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = DominoOp

      let name () = ".tendermint.crypto.DominoOp"

      type t = { key : string; input : string; output : string }
      type make_t = ?key:string -> ?input:string -> ?output:string -> unit -> t

      let make ?(key = {||}) ?(input = {||}) ?(output = {||}) () =
        { key; input; output }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_key =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((1, "key", "key"), string, {||}))
             in
             let merge_input =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((2, "input", "input"), string, {||}))
             in
             let merge_output =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((3, "output", "output"), string, {||}))
             in
             fun t1 t2 ->
               {
                 key = merge_key t1.key t2.key;
                 input = merge_input t1.input t2.input;
                 output = merge_output t1.output t2.output;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "key", "key"), string, {||})
          ^:: basic ((2, "input", "input"), string, {||})
          ^:: basic ((3, "output", "output"), string, {||})
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { key; input; output } -> serialize writer key input output

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor key input output = { key; input; output } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { key; input; output } -> serialize key input output

      let from_json_exn =
        let constructor key input output = { key; input; output } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and ProofOp : sig
      type t = { type' : string; key : bytes; data : bytes }

      val make : ?type':string -> ?key:bytes -> ?data:bytes -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?type':string -> ?key:bytes -> ?data:bytes -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = ProofOp

      let name () = ".tendermint.crypto.ProofOp"

      type t = { type' : string; key : bytes; data : bytes }
      type make_t = ?type':string -> ?key:bytes -> ?data:bytes -> unit -> t

      let make ?(type' = {||}) ?(key = Bytes.of_string {||})
          ?(data = Bytes.of_string {||}) () =
        { type'; key; data }

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_type' =
               Runtime'.Merge.merge
                 Runtime'.Spec.(basic ((1, "type", "type"), string, {||}))
             in
             let merge_key =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((2, "key", "key"), bytes, Bytes.of_string {||}))
             in
             let merge_data =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   basic ((3, "data", "data"), bytes, Bytes.of_string {||}))
             in
             fun t1 t2 ->
               {
                 type' = merge_type' t1.type' t2.type';
                 key = merge_key t1.key t2.key;
                 data = merge_data t1.data t2.data;
               })
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          basic ((1, "type", "type"), string, {||})
          ^:: basic ((2, "key", "key"), bytes, Bytes.of_string {||})
          ^:: basic ((3, "data", "data"), bytes, Bytes.of_string {||})
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer { type'; key; data } -> serialize writer type' key data

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor type' key data = { type'; key; data } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun { type'; key; data } -> serialize type' key data

      let from_json_exn =
        let constructor type' key data = { type'; key; data } in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end

    and ProofOps : sig
      type t = ProofOp.t list

      val make : ?ops:ProofOp.t list -> unit -> t
      (** Helper function to generate a message using default values *)

      val to_proto : t -> Runtime'.Writer.t
      (** Serialize the message to binary format *)

      val from_proto :
        Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from binary format *)

      val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
      (** Serialize to Json (compatible with Yojson.Basic.t) *)

      val from_json : Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
      (** Deserialize from Json (compatible with Yojson.Basic.t) *)

      val name : unit -> string
      (** Fully qualified protobuf name of this message *)

      (**/**)

      type make_t = ?ops:ProofOp.t list -> unit -> t

      val merge : t -> t -> t
      val to_proto' : Runtime'.Writer.t -> t -> unit
      val from_proto_exn : Runtime'.Reader.t -> t
      val from_json_exn : Runtime'.Json.t -> t

      (**/**)
    end = struct
      module This'_ = ProofOps

      let name () = ".tendermint.crypto.ProofOps"

      type t = ProofOp.t list
      type make_t = ?ops:ProofOp.t list -> unit -> t

      let make ?(ops = []) () = ops

      let merge =
        (* deferred by tools/lazify-merge.py; see that file *)
        let merge' =
          lazy
            (let merge_ops =
               Runtime'.Merge.merge
                 Runtime'.Spec.(
                   repeated
                     ((1, "ops", "ops"), message (module ProofOp), not_packed))
             in
             fun t1_ops t2_ops -> merge_ops t1_ops t2_ops)
        in
        fun t1' t2' -> (Lazy.force merge') t1' t2'

      let spec () =
        Runtime'.Spec.(
          repeated ((1, "ops", "ops"), message (module ProofOp), not_packed)
          ^:: nil)

      let to_proto' =
        let serialize =
          Runtime'.apply_lazy (fun () -> Runtime'.Serialize.serialize (spec ()))
        in
        fun writer ops -> serialize writer ops

      let to_proto t =
        let writer = Runtime'.Writer.init () in
        to_proto' writer t;
        writer

      let from_proto_exn =
        let constructor ops = ops in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize.deserialize (spec ()) constructor)

      let from_proto writer =
        Runtime'.Result.catch (fun () -> from_proto_exn writer)

      let to_json options =
        let serialize =
          Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
            options
        in
        fun ops -> serialize ops

      let from_json_exn =
        let constructor ops = ops in
        Runtime'.apply_lazy (fun () ->
            Runtime'.Deserialize_json.deserialize ~message_name:(name ())
              (spec ()) constructor)

      let from_json json = Runtime'.Result.catch (fun () -> from_json_exn json)
    end
  end
end

(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: cosmos/bank/v1beta1/bank.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Gogoproto_gogo = Gogoproto_gogo
  module Cosmos_proto_cosmos = Cosmos_proto_cosmos
  module Cosmos_base_v1beta1_coin = Cosmos_base_v1beta1_coin
  module Cosmos_msg_v1_msg = Cosmos_msg_v1_msg
  module Amino_amino = Amino_amino
end

(**/**)

module rec Cosmos : sig
  module rec Bank : sig
    module rec V1beta1 : sig
      (** {%html:
<p>Params defines the parameters for the bank module.</p>
%}
      *)
      module rec Params : sig
        type t = {
          send_enabled : SendEnabled.t list;
              [@ocaml.alert protobuf "Marked as deprecated in the .proto file"]
              (** {%html:
<p>Deprecated: Use of SendEnabled in params is deprecated.
For genesis, use the newly added send_enabled field in the genesis object.
Storage, lookup, and manipulation of this information is now in the keeper.</p>
<p>As of cosmos-sdk 0.47, this only exists for backwards compatibility of genesis files.</p>
%}
              *)
          default_send_enabled : bool;
        }

        val make :
          ?send_enabled:SendEnabled.t list ->
          ?default_send_enabled:bool ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?send_enabled:SendEnabled.t list ->
          ?default_send_enabled:bool ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>SendEnabled maps coin denom to a send_enabled status (whether a denom is
sendable).</p>
%}
      *)
      and SendEnabled : sig
        type t = { denom : string; enabled : bool }

        val make : ?denom:string -> ?enabled:bool -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?denom:string -> ?enabled:bool -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Input models transaction input.</p>
%} *)
      and Input : sig
        type t = {
          address : string;
          coins :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        val make :
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Output models transaction outputs.</p>
%} *)
      and Output : sig
        type t = {
          address : string;
          coins :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        val make :
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Supply represents a struct that passively keeps track of the total supply
amounts in the network.
This message is deprecated now that supply is indexed by denom.</p>
%}
      *)
      and Supply : sig
        type t =
          Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
          list

        val make :
          ?total:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?total:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
      [@@ocaml.alert protobuf "Marked as deprecated in the .proto file"]

      (** {%html:
<p>DenomUnit represents a struct that describes a given
denomination unit of the basic token.</p>
%}
      *)
      and DenomUnit : sig
        type t = {
          denom : string;
              (** {%html:
<p>denom represents the string name of the given denom unit (e.g uatom).</p>
%}
              *)
          exponent : int;
              (** {%html:
<p>exponent represents power of 10 exponent that one must
raise the base_denom to in order to equal the given DenomUnit's denom
1 denom = 10^exponent base_denom
(e.g. with a base_denom of uatom, one can create a DenomUnit of 'atom' with
exponent = 6, thus: 1 atom = 10^6 uatom).</p>
%}
              *)
          aliases : string list;
              (** {%html:
<p>aliases is a list of string aliases for the given denom</p>
%}
              *)
        }

        val make :
          ?denom:string -> ?exponent:int -> ?aliases:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?denom:string -> ?exponent:int -> ?aliases:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Metadata represents a struct that describes
a basic token.</p>
%}
      *)
      and Metadata : sig
        type t = {
          description : string;
          denom_units : DenomUnit.t list;
              (** {%html:
<p>denom_units represents the list of DenomUnit's for a given coin</p>
%}
              *)
          base : string;
              (** {%html:
<p>base represents the base denom (should be the DenomUnit with exponent = 0).</p>
%}
              *)
          display : string;
              (** {%html:
<p>display indicates the suggested denom that should be
displayed in clients.</p>
%}
              *)
          name : string;
              (** {%html:
<p>name defines the name of the token (eg: Cosmos Atom)</p>
%}
              *)
          symbol : string;
              (** {%html:
<p>symbol is the token symbol usually shown on exchanges (eg: ATOM). This can
be the same as the display.</p>
%}
              *)
          uri : string;
              (** {%html:
<p>URI to a document (on or off-chain) that contains additional information. Optional.</p>
%}
              *)
          uri_hash : string;
              (** {%html:
<p>URIHash is a sha256 hash of a document pointed by URI. It's used to verify that
the document didn't change. Optional.</p>
%}
              *)
        }

        val make :
          ?description:string ->
          ?denom_units:DenomUnit.t list ->
          ?base:string ->
          ?display:string ->
          ?name:string ->
          ?symbol:string ->
          ?uri:string ->
          ?uri_hash:string ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?description:string ->
          ?denom_units:DenomUnit.t list ->
          ?base:string ->
          ?display:string ->
          ?name:string ->
          ?symbol:string ->
          ?uri:string ->
          ?uri_hash:string ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
    end
  end
end = struct
  module rec Bank : sig
    module rec V1beta1 : sig
      (** {%html:
<p>Params defines the parameters for the bank module.</p>
%}
      *)
      module rec Params : sig
        type t = {
          send_enabled : SendEnabled.t list;
              [@ocaml.alert protobuf "Marked as deprecated in the .proto file"]
              (** {%html:
<p>Deprecated: Use of SendEnabled in params is deprecated.
For genesis, use the newly added send_enabled field in the genesis object.
Storage, lookup, and manipulation of this information is now in the keeper.</p>
<p>As of cosmos-sdk 0.47, this only exists for backwards compatibility of genesis files.</p>
%}
              *)
          default_send_enabled : bool;
        }

        val make :
          ?send_enabled:SendEnabled.t list ->
          ?default_send_enabled:bool ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?send_enabled:SendEnabled.t list ->
          ?default_send_enabled:bool ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>SendEnabled maps coin denom to a send_enabled status (whether a denom is
sendable).</p>
%}
      *)
      and SendEnabled : sig
        type t = { denom : string; enabled : bool }

        val make : ?denom:string -> ?enabled:bool -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?denom:string -> ?enabled:bool -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Input models transaction input.</p>
%} *)
      and Input : sig
        type t = {
          address : string;
          coins :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        val make :
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Output models transaction outputs.</p>
%} *)
      and Output : sig
        type t = {
          address : string;
          coins :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        val make :
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Supply represents a struct that passively keeps track of the total supply
amounts in the network.
This message is deprecated now that supply is indexed by denom.</p>
%}
      *)
      and Supply : sig
        type t =
          Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
          list

        val make :
          ?total:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?total:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
      [@@ocaml.alert protobuf "Marked as deprecated in the .proto file"]

      (** {%html:
<p>DenomUnit represents a struct that describes a given
denomination unit of the basic token.</p>
%}
      *)
      and DenomUnit : sig
        type t = {
          denom : string;
              (** {%html:
<p>denom represents the string name of the given denom unit (e.g uatom).</p>
%}
              *)
          exponent : int;
              (** {%html:
<p>exponent represents power of 10 exponent that one must
raise the base_denom to in order to equal the given DenomUnit's denom
1 denom = 10^exponent base_denom
(e.g. with a base_denom of uatom, one can create a DenomUnit of 'atom' with
exponent = 6, thus: 1 atom = 10^6 uatom).</p>
%}
              *)
          aliases : string list;
              (** {%html:
<p>aliases is a list of string aliases for the given denom</p>
%}
              *)
        }

        val make :
          ?denom:string -> ?exponent:int -> ?aliases:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?denom:string -> ?exponent:int -> ?aliases:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Metadata represents a struct that describes
a basic token.</p>
%}
      *)
      and Metadata : sig
        type t = {
          description : string;
          denom_units : DenomUnit.t list;
              (** {%html:
<p>denom_units represents the list of DenomUnit's for a given coin</p>
%}
              *)
          base : string;
              (** {%html:
<p>base represents the base denom (should be the DenomUnit with exponent = 0).</p>
%}
              *)
          display : string;
              (** {%html:
<p>display indicates the suggested denom that should be
displayed in clients.</p>
%}
              *)
          name : string;
              (** {%html:
<p>name defines the name of the token (eg: Cosmos Atom)</p>
%}
              *)
          symbol : string;
              (** {%html:
<p>symbol is the token symbol usually shown on exchanges (eg: ATOM). This can
be the same as the display.</p>
%}
              *)
          uri : string;
              (** {%html:
<p>URI to a document (on or off-chain) that contains additional information. Optional.</p>
%}
              *)
          uri_hash : string;
              (** {%html:
<p>URIHash is a sha256 hash of a document pointed by URI. It's used to verify that
the document didn't change. Optional.</p>
%}
              *)
        }

        val make :
          ?description:string ->
          ?denom_units:DenomUnit.t list ->
          ?base:string ->
          ?display:string ->
          ?name:string ->
          ?symbol:string ->
          ?uri:string ->
          ?uri_hash:string ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?description:string ->
          ?denom_units:DenomUnit.t list ->
          ?base:string ->
          ?display:string ->
          ?name:string ->
          ?symbol:string ->
          ?uri:string ->
          ?uri_hash:string ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
    end
  end = struct
    module rec V1beta1 : sig
      (** {%html:
<p>Params defines the parameters for the bank module.</p>
%}
      *)
      module rec Params : sig
        type t = {
          send_enabled : SendEnabled.t list;
              [@ocaml.alert protobuf "Marked as deprecated in the .proto file"]
              (** {%html:
<p>Deprecated: Use of SendEnabled in params is deprecated.
For genesis, use the newly added send_enabled field in the genesis object.
Storage, lookup, and manipulation of this information is now in the keeper.</p>
<p>As of cosmos-sdk 0.47, this only exists for backwards compatibility of genesis files.</p>
%}
              *)
          default_send_enabled : bool;
        }

        val make :
          ?send_enabled:SendEnabled.t list ->
          ?default_send_enabled:bool ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?send_enabled:SendEnabled.t list ->
          ?default_send_enabled:bool ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>SendEnabled maps coin denom to a send_enabled status (whether a denom is
sendable).</p>
%}
      *)
      and SendEnabled : sig
        type t = { denom : string; enabled : bool }

        val make : ?denom:string -> ?enabled:bool -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?denom:string -> ?enabled:bool -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Input models transaction input.</p>
%} *)
      and Input : sig
        type t = {
          address : string;
          coins :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        val make :
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Output models transaction outputs.</p>
%} *)
      and Output : sig
        type t = {
          address : string;
          coins :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        val make :
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Supply represents a struct that passively keeps track of the total supply
amounts in the network.
This message is deprecated now that supply is indexed by denom.</p>
%}
      *)
      and Supply : sig
        type t =
          Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
          list

        val make :
          ?total:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?total:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
      [@@ocaml.alert protobuf "Marked as deprecated in the .proto file"]

      (** {%html:
<p>DenomUnit represents a struct that describes a given
denomination unit of the basic token.</p>
%}
      *)
      and DenomUnit : sig
        type t = {
          denom : string;
              (** {%html:
<p>denom represents the string name of the given denom unit (e.g uatom).</p>
%}
              *)
          exponent : int;
              (** {%html:
<p>exponent represents power of 10 exponent that one must
raise the base_denom to in order to equal the given DenomUnit's denom
1 denom = 10^exponent base_denom
(e.g. with a base_denom of uatom, one can create a DenomUnit of 'atom' with
exponent = 6, thus: 1 atom = 10^6 uatom).</p>
%}
              *)
          aliases : string list;
              (** {%html:
<p>aliases is a list of string aliases for the given denom</p>
%}
              *)
        }

        val make :
          ?denom:string -> ?exponent:int -> ?aliases:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?denom:string -> ?exponent:int -> ?aliases:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end

      (** {%html:
<p>Metadata represents a struct that describes
a basic token.</p>
%}
      *)
      and Metadata : sig
        type t = {
          description : string;
          denom_units : DenomUnit.t list;
              (** {%html:
<p>denom_units represents the list of DenomUnit's for a given coin</p>
%}
              *)
          base : string;
              (** {%html:
<p>base represents the base denom (should be the DenomUnit with exponent = 0).</p>
%}
              *)
          display : string;
              (** {%html:
<p>display indicates the suggested denom that should be
displayed in clients.</p>
%}
              *)
          name : string;
              (** {%html:
<p>name defines the name of the token (eg: Cosmos Atom)</p>
%}
              *)
          symbol : string;
              (** {%html:
<p>symbol is the token symbol usually shown on exchanges (eg: ATOM). This can
be the same as the display.</p>
%}
              *)
          uri : string;
              (** {%html:
<p>URI to a document (on or off-chain) that contains additional information. Optional.</p>
%}
              *)
          uri_hash : string;
              (** {%html:
<p>URIHash is a sha256 hash of a document pointed by URI. It's used to verify that
the document didn't change. Optional.</p>
%}
              *)
        }

        val make :
          ?description:string ->
          ?denom_units:DenomUnit.t list ->
          ?base:string ->
          ?display:string ->
          ?name:string ->
          ?symbol:string ->
          ?uri:string ->
          ?uri_hash:string ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?description:string ->
          ?denom_units:DenomUnit.t list ->
          ?base:string ->
          ?display:string ->
          ?name:string ->
          ?symbol:string ->
          ?uri:string ->
          ?uri_hash:string ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end
    end = struct
      module rec Params : sig
        type t = {
          send_enabled : SendEnabled.t list;
              [@ocaml.alert protobuf "Marked as deprecated in the .proto file"]
              (** {%html:
<p>Deprecated: Use of SendEnabled in params is deprecated.
For genesis, use the newly added send_enabled field in the genesis object.
Storage, lookup, and manipulation of this information is now in the keeper.</p>
<p>As of cosmos-sdk 0.47, this only exists for backwards compatibility of genesis files.</p>
%}
              *)
          default_send_enabled : bool;
        }

        val make :
          ?send_enabled:SendEnabled.t list ->
          ?default_send_enabled:bool ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?send_enabled:SendEnabled.t list ->
          ?default_send_enabled:bool ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = Params

        let name () = ".cosmos.bank.v1beta1.Params"

        type t = {
          send_enabled : SendEnabled.t list;
          default_send_enabled : bool;
        }

        type make_t =
          ?send_enabled:SendEnabled.t list ->
          ?default_send_enabled:bool ->
          unit ->
          t

        let make ?(send_enabled = []) ?(default_send_enabled = false) () =
          { send_enabled; default_send_enabled }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_send_enabled =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (1, "send_enabled", "sendEnabled"),
                         message (module SendEnabled),
                         not_packed ))
               in
               let merge_default_send_enabled =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic
                       ( (2, "default_send_enabled", "defaultSendEnabled"),
                         bool,
                         false ))
               in
               fun t1 t2 ->
                 {
                   send_enabled =
                     merge_send_enabled t1.send_enabled t2.send_enabled;
                   default_send_enabled =
                     merge_default_send_enabled t1.default_send_enabled
                       t2.default_send_enabled;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            repeated
              ( (1, "send_enabled", "sendEnabled"),
                message (module SendEnabled),
                not_packed )
            ^:: basic
                  ( (2, "default_send_enabled", "defaultSendEnabled"),
                    bool,
                    false )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { send_enabled; default_send_enabled } ->
            serialize writer send_enabled default_send_enabled

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor send_enabled default_send_enabled =
            { send_enabled; default_send_enabled }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { send_enabled; default_send_enabled } ->
            serialize send_enabled default_send_enabled

        let from_json_exn =
          let constructor send_enabled default_send_enabled =
            { send_enabled; default_send_enabled }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and SendEnabled : sig
        type t = { denom : string; enabled : bool }

        val make : ?denom:string -> ?enabled:bool -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t = ?denom:string -> ?enabled:bool -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = SendEnabled

        let name () = ".cosmos.bank.v1beta1.SendEnabled"

        type t = { denom : string; enabled : bool }
        type make_t = ?denom:string -> ?enabled:bool -> unit -> t

        let make ?(denom = {||}) ?(enabled = false) () = { denom; enabled }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_denom =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "denom", "denom"), string, {||}))
               in
               let merge_enabled =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "enabled", "enabled"), bool, false))
               in
               fun t1 t2 ->
                 {
                   denom = merge_denom t1.denom t2.denom;
                   enabled = merge_enabled t1.enabled t2.enabled;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "denom", "denom"), string, {||})
            ^:: basic ((2, "enabled", "enabled"), bool, false)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { denom; enabled } -> serialize writer denom enabled

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor denom enabled = { denom; enabled } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { denom; enabled } -> serialize denom enabled

        let from_json_exn =
          let constructor denom enabled = { denom; enabled } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and Input : sig
        type t = {
          address : string;
          coins :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        val make :
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = Input

        let name () = ".cosmos.bank.v1beta1.Input"

        type t = {
          address : string;
          coins :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        type make_t =
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        let make ?(address = {||}) ?(coins = []) () = { address; coins }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_address =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "address", "address"), string, {||}))
               in
               let merge_coins =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (2, "coins", "coins"),
                         message
                           (module Imported'modules.Cosmos_base_v1beta1_coin
                                   .Cosmos
                                   .Base
                                   .V1beta1
                                   .Coin),
                         not_packed ))
               in
               fun t1 t2 ->
                 {
                   address = merge_address t1.address t2.address;
                   coins = merge_coins t1.coins t2.coins;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "address", "address"), string, {||})
            ^:: repeated
                  ( (2, "coins", "coins"),
                    message
                      (module Imported'modules.Cosmos_base_v1beta1_coin.Cosmos
                              .Base
                              .V1beta1
                              .Coin),
                    not_packed )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { address; coins } -> serialize writer address coins

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor address coins = { address; coins } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { address; coins } -> serialize address coins

        let from_json_exn =
          let constructor address coins = { address; coins } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and Output : sig
        type t = {
          address : string;
          coins :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        val make :
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = Output

        let name () = ".cosmos.bank.v1beta1.Output"

        type t = {
          address : string;
          coins :
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list;
        }

        type make_t =
          ?address:string ->
          ?coins:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        let make ?(address = {||}) ?(coins = []) () = { address; coins }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_address =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "address", "address"), string, {||}))
               in
               let merge_coins =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (2, "coins", "coins"),
                         message
                           (module Imported'modules.Cosmos_base_v1beta1_coin
                                   .Cosmos
                                   .Base
                                   .V1beta1
                                   .Coin),
                         not_packed ))
               in
               fun t1 t2 ->
                 {
                   address = merge_address t1.address t2.address;
                   coins = merge_coins t1.coins t2.coins;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "address", "address"), string, {||})
            ^:: repeated
                  ( (2, "coins", "coins"),
                    message
                      (module Imported'modules.Cosmos_base_v1beta1_coin.Cosmos
                              .Base
                              .V1beta1
                              .Coin),
                    not_packed )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { address; coins } -> serialize writer address coins

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor address coins = { address; coins } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { address; coins } -> serialize address coins

        let from_json_exn =
          let constructor address coins = { address; coins } in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and Supply : sig
        type t =
          Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
          list

        val make :
          ?total:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?total:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = Supply

        let name () = ".cosmos.bank.v1beta1.Supply"

        type t =
          Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
          list

        type make_t =
          ?total:
            Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base.V1beta1.Coin.t
            list ->
          unit ->
          t

        let make ?(total = []) () = total

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_total =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (1, "total", "total"),
                         message
                           (module Imported'modules.Cosmos_base_v1beta1_coin
                                   .Cosmos
                                   .Base
                                   .V1beta1
                                   .Coin),
                         not_packed ))
               in
               fun t1_total t2_total -> merge_total t1_total t2_total)
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            repeated
              ( (1, "total", "total"),
                message
                  (module Imported'modules.Cosmos_base_v1beta1_coin.Cosmos.Base
                          .V1beta1
                          .Coin),
                not_packed )
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer total -> serialize writer total

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor total = total in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun total -> serialize total

        let from_json_exn =
          let constructor total = total in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end
      [@@ocaml.alert protobuf "Marked as deprecated in the .proto file"]

      and DenomUnit : sig
        type t = {
          denom : string;
              (** {%html:
<p>denom represents the string name of the given denom unit (e.g uatom).</p>
%}
              *)
          exponent : int;
              (** {%html:
<p>exponent represents power of 10 exponent that one must
raise the base_denom to in order to equal the given DenomUnit's denom
1 denom = 10^exponent base_denom
(e.g. with a base_denom of uatom, one can create a DenomUnit of 'atom' with
exponent = 6, thus: 1 atom = 10^6 uatom).</p>
%}
              *)
          aliases : string list;
              (** {%html:
<p>aliases is a list of string aliases for the given denom</p>
%}
              *)
        }

        val make :
          ?denom:string -> ?exponent:int -> ?aliases:string list -> unit -> t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?denom:string -> ?exponent:int -> ?aliases:string list -> unit -> t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = DenomUnit

        let name () = ".cosmos.bank.v1beta1.DenomUnit"

        type t = { denom : string; exponent : int; aliases : string list }

        type make_t =
          ?denom:string -> ?exponent:int -> ?aliases:string list -> unit -> t

        let make ?(denom = {||}) ?(exponent = 0) ?(aliases = []) () =
          { denom; exponent; aliases }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_denom =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((1, "denom", "denom"), string, {||}))
               in
               let merge_exponent =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((2, "exponent", "exponent"), uint32_int, 0))
               in
               let merge_aliases =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated ((3, "aliases", "aliases"), string, not_packed))
               in
               fun t1 t2 ->
                 {
                   denom = merge_denom t1.denom t2.denom;
                   exponent = merge_exponent t1.exponent t2.exponent;
                   aliases = merge_aliases t1.aliases t2.aliases;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "denom", "denom"), string, {||})
            ^:: basic ((2, "exponent", "exponent"), uint32_int, 0)
            ^:: repeated ((3, "aliases", "aliases"), string, not_packed)
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer { denom; exponent; aliases } ->
            serialize writer denom exponent aliases

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor denom exponent aliases =
            { denom; exponent; aliases }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun { denom; exponent; aliases } -> serialize denom exponent aliases

        let from_json_exn =
          let constructor denom exponent aliases =
            { denom; exponent; aliases }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end

      and Metadata : sig
        type t = {
          description : string;
          denom_units : DenomUnit.t list;
              (** {%html:
<p>denom_units represents the list of DenomUnit's for a given coin</p>
%}
              *)
          base : string;
              (** {%html:
<p>base represents the base denom (should be the DenomUnit with exponent = 0).</p>
%}
              *)
          display : string;
              (** {%html:
<p>display indicates the suggested denom that should be
displayed in clients.</p>
%}
              *)
          name : string;
              (** {%html:
<p>name defines the name of the token (eg: Cosmos Atom)</p>
%}
              *)
          symbol : string;
              (** {%html:
<p>symbol is the token symbol usually shown on exchanges (eg: ATOM). This can
be the same as the display.</p>
%}
              *)
          uri : string;
              (** {%html:
<p>URI to a document (on or off-chain) that contains additional information. Optional.</p>
%}
              *)
          uri_hash : string;
              (** {%html:
<p>URIHash is a sha256 hash of a document pointed by URI. It's used to verify that
the document didn't change. Optional.</p>
%}
              *)
        }

        val make :
          ?description:string ->
          ?denom_units:DenomUnit.t list ->
          ?base:string ->
          ?display:string ->
          ?name:string ->
          ?symbol:string ->
          ?uri:string ->
          ?uri_hash:string ->
          unit ->
          t
        (** Helper function to generate a message using default values *)

        val to_proto : t -> Runtime'.Writer.t
        (** Serialize the message to binary format *)

        val from_proto :
          Runtime'.Reader.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from binary format *)

        val to_json : Runtime'.Json_options.t -> t -> Runtime'.Json.t
        (** Serialize to Json (compatible with Yojson.Basic.t) *)

        val from_json :
          Runtime'.Json.t -> (t, [> Runtime'.Result.error ]) result
        (** Deserialize from Json (compatible with Yojson.Basic.t) *)

        val name : unit -> string
        (** Fully qualified protobuf name of this message *)

        (**/**)

        type make_t =
          ?description:string ->
          ?denom_units:DenomUnit.t list ->
          ?base:string ->
          ?display:string ->
          ?name:string ->
          ?symbol:string ->
          ?uri:string ->
          ?uri_hash:string ->
          unit ->
          t

        val merge : t -> t -> t
        val to_proto' : Runtime'.Writer.t -> t -> unit
        val from_proto_exn : Runtime'.Reader.t -> t
        val from_json_exn : Runtime'.Json.t -> t

        (**/**)
      end = struct
        module This'_ = Metadata

        let name () = ".cosmos.bank.v1beta1.Metadata"

        type t = {
          description : string;
          denom_units : DenomUnit.t list;
          base : string;
          display : string;
          name : string;
          symbol : string;
          uri : string;
          uri_hash : string;
        }

        type make_t =
          ?description:string ->
          ?denom_units:DenomUnit.t list ->
          ?base:string ->
          ?display:string ->
          ?name:string ->
          ?symbol:string ->
          ?uri:string ->
          ?uri_hash:string ->
          unit ->
          t

        let make ?(description = {||}) ?(denom_units = []) ?(base = {||})
            ?(display = {||}) ?(name = {||}) ?(symbol = {||}) ?(uri = {||})
            ?(uri_hash = {||}) () =
          {
            description;
            denom_units;
            base;
            display;
            name;
            symbol;
            uri;
            uri_hash;
          }

        let merge =
          (* deferred by tools/lazify-merge.py; see that file *)
          let merge' =
            lazy
              (let merge_description =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((1, "description", "description"), string, {||}))
               in
               let merge_denom_units =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     repeated
                       ( (2, "denom_units", "denomUnits"),
                         message (module DenomUnit),
                         not_packed ))
               in
               let merge_base =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((3, "base", "base"), string, {||}))
               in
               let merge_display =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((4, "display", "display"), string, {||}))
               in
               let merge_name =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((5, "name", "name"), string, {||}))
               in
               let merge_symbol =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((6, "symbol", "symbol"), string, {||}))
               in
               let merge_uri =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(basic ((7, "uri", "uri"), string, {||}))
               in
               let merge_uri_hash =
                 Runtime'.Merge.merge
                   Runtime'.Spec.(
                     basic ((8, "uri_hash", "uriHash"), string, {||}))
               in
               fun t1 t2 ->
                 {
                   description = merge_description t1.description t2.description;
                   denom_units = merge_denom_units t1.denom_units t2.denom_units;
                   base = merge_base t1.base t2.base;
                   display = merge_display t1.display t2.display;
                   name = merge_name t1.name t2.name;
                   symbol = merge_symbol t1.symbol t2.symbol;
                   uri = merge_uri t1.uri t2.uri;
                   uri_hash = merge_uri_hash t1.uri_hash t2.uri_hash;
                 })
          in
          fun t1' t2' -> (Lazy.force merge') t1' t2'

        let spec () =
          Runtime'.Spec.(
            basic ((1, "description", "description"), string, {||})
            ^:: repeated
                  ( (2, "denom_units", "denomUnits"),
                    message (module DenomUnit),
                    not_packed )
            ^:: basic ((3, "base", "base"), string, {||})
            ^:: basic ((4, "display", "display"), string, {||})
            ^:: basic ((5, "name", "name"), string, {||})
            ^:: basic ((6, "symbol", "symbol"), string, {||})
            ^:: basic ((7, "uri", "uri"), string, {||})
            ^:: basic ((8, "uri_hash", "uriHash"), string, {||})
            ^:: nil)

        let to_proto' =
          let serialize =
            Runtime'.apply_lazy (fun () ->
                Runtime'.Serialize.serialize (spec ()))
          in
          fun writer
            {
              description;
              denom_units;
              base;
              display;
              name;
              symbol;
              uri;
              uri_hash;
            }
          ->
            serialize writer description denom_units base display name symbol
              uri uri_hash

        let to_proto t =
          let writer = Runtime'.Writer.init () in
          to_proto' writer t;
          writer

        let from_proto_exn =
          let constructor description denom_units base display name symbol uri
              uri_hash =
            {
              description;
              denom_units;
              base;
              display;
              name;
              symbol;
              uri;
              uri_hash;
            }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize.deserialize (spec ()) constructor)

        let from_proto writer =
          Runtime'.Result.catch (fun () -> from_proto_exn writer)

        let to_json options =
          let serialize =
            Runtime'.Serialize_json.serialize ~message_name:(name ()) (spec ())
              options
          in
          fun {
                description;
                denom_units;
                base;
                display;
                name;
                symbol;
                uri;
                uri_hash;
              }
          ->
            serialize description denom_units base display name symbol uri
              uri_hash

        let from_json_exn =
          let constructor description denom_units base display name symbol uri
              uri_hash =
            {
              description;
              denom_units;
              base;
              display;
              name;
              symbol;
              uri;
              uri_hash;
            }
          in
          Runtime'.apply_lazy (fun () ->
              Runtime'.Deserialize_json.deserialize ~message_name:(name ())
                (spec ()) constructor)

        let from_json json =
          Runtime'.Result.catch (fun () -> from_json_exn json)
      end
    end
  end
end

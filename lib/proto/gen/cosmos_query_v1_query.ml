(********************************************************)
(*           AUTOGENERATED FILE - DO NOT EDIT!          *)
(********************************************************)
(* Generated by: ocaml-protoc-plugin                    *)
(* https://github.com/andersfugmann/ocaml-protoc-plugin *)
(********************************************************)
(*
  Source: cosmos/query/v1/query.proto
  Syntax: proto3
  Parameters:
    debug=false
    annot=''
    opens=[]
    int64_as_int=false
    int32_as_int=true
    fixed_as_int=false
    singleton_record=false
    prefix_output_with_package=true
*)
[@@@ocaml.alert "-protobuf"] (* Disable deprecation warnings for protobuf*)

(**/**)

module Runtime' = Ocaml_protoc_plugin [@@warning "-33"]

module Imported'modules = struct
  module Google_protobuf_descriptor = Google_protobuf_descriptor
end

(**/**)

module rec Cosmos : sig
  module rec Query : sig
    module rec V1 : sig
      (** {%html:
<p>module_query_safe is set to true when the query is safe to be called from
within the state machine, for example from another module's Keeper, via
ADR-033 calls or from CosmWasm contracts.
Concretely, it means that the query is:</p>
<ol>
<li>deterministic: given a block height, returns the exact same response
upon multiple calls; and doesn't introduce any state-machine-breaking
changes across SDK patch version.
</li>
<li>consumes gas correctly.
</li>
</ol>
<p>If you are a module developer and want to add this annotation to one of
your own queries, please make sure that the corresponding query:</p>
<ol>
<li>is deterministic and won't introduce state-machine-breaking changes
without a coordinated upgrade path,
</li>
<li>has its gas tracked, to avoid the attack vector where no gas is
accounted for on potentially high-computation queries.
</li>
</ol>
<p>For queries that potentially consume a large amount of gas (for example
those with pagination, if the pagination field is incorrectly set), we
also recommend adding Protobuf comments to warn module developers
consuming these queries.</p>
<p>When set to true, the query can safely be called</p>
%}
      *)
      module rec Module_query_safe : sig
        type t = bool option

        val get_exn :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          bool option

        val get :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          (bool option, [> Runtime'.Result.error ]) result

        val set :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          bool option ->
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t
      end
    end
  end
end = struct
  module rec Query : sig
    module rec V1 : sig
      (** {%html:
<p>module_query_safe is set to true when the query is safe to be called from
within the state machine, for example from another module's Keeper, via
ADR-033 calls or from CosmWasm contracts.
Concretely, it means that the query is:</p>
<ol>
<li>deterministic: given a block height, returns the exact same response
upon multiple calls; and doesn't introduce any state-machine-breaking
changes across SDK patch version.
</li>
<li>consumes gas correctly.
</li>
</ol>
<p>If you are a module developer and want to add this annotation to one of
your own queries, please make sure that the corresponding query:</p>
<ol>
<li>is deterministic and won't introduce state-machine-breaking changes
without a coordinated upgrade path,
</li>
<li>has its gas tracked, to avoid the attack vector where no gas is
accounted for on potentially high-computation queries.
</li>
</ol>
<p>For queries that potentially consume a large amount of gas (for example
those with pagination, if the pagination field is incorrectly set), we
also recommend adding Protobuf comments to warn module developers
consuming these queries.</p>
<p>When set to true, the query can safely be called</p>
%}
      *)
      module rec Module_query_safe : sig
        type t = bool option

        val get_exn :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          bool option

        val get :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          (bool option, [> Runtime'.Result.error ]) result

        val set :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          bool option ->
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t
      end
    end
  end = struct
    module rec V1 : sig
      (** {%html:
<p>module_query_safe is set to true when the query is safe to be called from
within the state machine, for example from another module's Keeper, via
ADR-033 calls or from CosmWasm contracts.
Concretely, it means that the query is:</p>
<ol>
<li>deterministic: given a block height, returns the exact same response
upon multiple calls; and doesn't introduce any state-machine-breaking
changes across SDK patch version.
</li>
<li>consumes gas correctly.
</li>
</ol>
<p>If you are a module developer and want to add this annotation to one of
your own queries, please make sure that the corresponding query:</p>
<ol>
<li>is deterministic and won't introduce state-machine-breaking changes
without a coordinated upgrade path,
</li>
<li>has its gas tracked, to avoid the attack vector where no gas is
accounted for on potentially high-computation queries.
</li>
</ol>
<p>For queries that potentially consume a large amount of gas (for example
those with pagination, if the pagination field is incorrectly set), we
also recommend adding Protobuf comments to warn module developers
consuming these queries.</p>
<p>When set to true, the query can safely be called</p>
%}
      *)
      module rec Module_query_safe : sig
        type t = bool option

        val get_exn :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          bool option

        val get :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          (bool option, [> Runtime'.Result.error ]) result

        val set :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          bool option ->
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t
      end
    end = struct
      module rec Module_query_safe : sig
        type t = bool option

        val get_exn :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          bool option

        val get :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          (bool option, [> Runtime'.Result.error ]) result

        val set :
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t ->
          bool option ->
          Imported'modules.Google_protobuf_descriptor.Google.Protobuf
          .MethodOptions
          .t
      end = struct
        module This'_ = Module_query_safe
        module This = Module_query_safe

        type t = bool option

        let get_exn extendee =
          Runtime'.Extensions.get
            Runtime'.Spec.(
              basic_opt
                ((11110001, "module_query_safe", "moduleQuerySafe"), bool))
            extendee
              .Imported'modules.Google_protobuf_descriptor.Google.Protobuf
               .MethodOptions
               .extensions'

        let get extendee = Runtime'.Result.catch (fun () -> get_exn extendee)

        let set extendee t =
          let extensions' =
            Runtime'.Extensions.set
              Runtime'.Spec.(
                basic_opt
                  ((11110001, "module_query_safe", "moduleQuerySafe"), bool))
              extendee
                .Imported'modules.Google_protobuf_descriptor.Google.Protobuf
                 .MethodOptions
                 .extensions'
              t
          in
          {
            extendee with
            Imported'modules.Google_protobuf_descriptor.Google.Protobuf
            .MethodOptions
            .extensions';
          }
        [@@warning "-23"]
      end
    end
  end
end

(** Cosmos transaction construction, sign modes, intent and policy.

    Skeleton: the module boundary is fixed, the contents are G10 L2 work. *)

module Msg = Msg
module Body = Body
module Auth_info = Auth_info
module Sign_doc = Sign_doc
module Amino_json = Amino_json
module Intent = Intent
module Policy = Policy
